---
function: un_80307BA0
tu: src/melee/ty/toy.c
headline: permuter-false-positive + sda21
tags: [permuter-false-positive, sda21, float-literal, regalloc]
---
## un_80307BA0 (`src/melee/ty/toy.c`) — permuter-false-positive + sda21

- **Tags:** `permuter-false-positive`, `sda21`, `float-literal`, `regalloc`
- **Best fuzzy:** 99.9459%
- **Diagnosis:** Two issues remain after improving strict from 98.81% to 99.24% (fuzzy unchanged at 99.945%): (1) add r3, r29, r0 (target) vs add r3, r0, r29 (base) - operand swap in 'state + offset*4' computation for state->jobj[idx] = jobj store. mwcc puts pointer-first regardless of source order; tried (offset + state) and (offset + state + 4) but both commuted back. (2) lfs f0 references @866 anonymous label in target vs un_804DDCF8 in base for the 0.25F constant in HSD_JObjAddTranslationY. The named static was removed from src already; mwcc's sda2 emit now uses a name objdiff still maps to un_804DDCF8 because that's the target binary symbol name at that address. Plus 3 addi mismatches that show identical text (reloc-symbol pool noise). Permuter should NOT be dispatched: the permuter scorer treats sda21/reloc-symbol diffs as equivalent and would falsely report 100% while strict objdiff would still fail commit-match.
- **Tried:** 1) Removed static char un_804D5A64[]/un_804D5A6C[]/un_804DDCF8 declarations - eliminated 16 sda21-named-vs-anonymous string-pool diffs, took strict from 98.81% to 99.24%. KEPT this change. 2) Tried *(HSD_JObj**)((u8*)offset + (u32)state + 4) = jobj - commuted back, regressed register allocation. 3) Tried natural form state->jobj[state->x0E] = jobj - merged differently, regressed to 9 mismatches.
- **Likely fix:** The add r3, r0, r29 ordering may require source like *(idx[(u8*)state]) - i.e., array indexing with offset as base - or some bizarre pointer arithmetic that puts the integer term as the LHS of +. The remaining 4 reloc-symbol diffs (3 addi + 1 lfs) are permuter-false-positive class - they show fuzzy 99.945% because they fold post-link, but appear in strict diff. Possibly fixable by adjusting un_803FDD18 declaration or moving the file-scope statics.
