---
function: ftCo_800A4768
tu: src/melee/ft/chara/ftCommon/ftCo_0A01.c
headline: permuter-territory + r30-r31-swap
tags: [permuter-territory, r30-r31-swap, float-regalloc, frame-size, instruction-scheduling]
---
## ftCo_800A4768 (`src/melee/ft/chara/ftCommon/ftCo_0A01.c`) — permuter-territory + r30-r31-swap

- **Tags:** `permuter-territory`, `r30-r31-swap`, `float-regalloc`, `frame-size`, `instruction-scheduling`
- **Best fuzzy:** (unknown)
- **Diagnosis:** At 88.84% (69 mismatches), source structurally correct: iterates mpIsland_80458E88.next list, copies it->x14 (and later it->x8) to local Vec3, performs inlineD0-style blast-zone check (left/right via half_width, bottom/top via half_height), tracks closest island endpoint to fighter (only 'right side' for x14, 'left side' for x8), with sp14-5.0/sp14+5.0 offsets stored to out Vec3. Constants identified: f30=0.0f, f28=-1.0f initial best, f27=0.0(double) for cmp, f31=5.0(double) offset. Frame size 0x60 achieved via PAD_STACK(0xC). Remaining diffs are purely register allocation (r30 vs r31 swap throughout, f27/f28/f29 swap for sda2 float loads, f29 hoisting of v.y across stage calls).
- **Tried:** (1) Initial source with Vec3 v in outer scope, struct copy v=it->x14, inlineD0-style check inline twice; got 88.7%, frame 0x58. (2) Added PAD_STACK(0xC); got 88.84%, frame 0x60 matched. Stack layout cosmetically off (Vec3 at sp+0x10 vs target sp+0x14, both yield same frame size). Tried multiple PAD permutations (PAD before/after Vec3, two PADs); all stayed at ~88.7-88.84%.
- **Likely fix:** Permuter to find register-allocation shape that produces target's r30=island-iter / r31=&fp->x1A88 (currently swapped), float reg assignments (f27=0.0,f28=-1.0,f29=v.y,f31=5.0), and v.y hoist into f29 across the 4 Stage_GetBlastZoneXOffset calls. All 60+ remaining mismatches are downstream of these register choices.

