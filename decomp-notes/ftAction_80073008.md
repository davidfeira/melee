---
function: ftAction_80073008
tu: src/melee/ft/ftaction.c
headline: mwcc-aliasing + regalloc
tags: [mwcc-aliasing, regalloc]
---
## ftAction_80073008 (`src/melee/ft/ftaction.c`) — mwcc-aliasing + regalloc

- **Tags:** `mwcc-aliasing`, `regalloc`
- **Best fuzzy:** 68.6757%
- **Diagnosis:** 26 mismatches at 68.6% baseline. Target asm reads bitfields in reverse: lhz r5,0x2(r8) then lhz r6,0x0(r8), clrlwi from r6. Current source compiles to lhz r5,0x0 then lhz r7,0x2, clrlwi from r5 (opposite read order). Target also shows float-to-int round-trip for color_anim (lfd/fsubs/fctiwz/stfd/lwz pattern, ~6 extra instructions vs current direct lbz).
- **Tried:** (1) swap charge_frames/charge_rate args -> 56%; (2) f32 color_anim with explicit (u32) cast at call site -> 27%; (3) (u32)(s32)(f32)(u8) cast at field-read site -> 68.7% with stack frame growing 0x38->0x48 and DIFF_INSERTs. Reverted to baseline.
- **Likely fix:** Source likely needs intermediate float variable for color_anim with int->float->int dance, but the current attempts grow stack. May need a specific field-access pattern or different intermediate struct/var ordering. Permuter not viable: 26 mismatches exceeds 20-instr threshold; mismatches are insert/delete not just regalloc.
