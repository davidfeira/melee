---
function: ftAction_80073008
tu: src/melee/ft/ftaction.c
headline: frame-size + stack-offset
tags: [frame-size, stack-offset, float-regalloc, instruction-scheduling, mwcc-aliasing]
---
## ftAction_80073008 (`src/melee/ft/ftaction.c`) — frame-size + stack-offset

- **Tags:** `frame-size`, `stack-offset`, `float-regalloc`, `instruction-scheduling`, `mwcc-aliasing`
- **Best fuzzy:** 68.2162%
- **Diagnosis:** Call args are definitively swapped: target passes float(charge_frames) as f1 and (1/256)*float(charge_rate) as f2, not charge_rate/charge_frames as in baseline. color_anim needs f32 intermediate with (u32)(s32) cast. BUT frame size is the blocker: 3 f32 locals (no PAD_STACK) gives 0x30; PAD_STACK(8) still hits the 2-attempt cap; PAD_STACK(20) (baseline) gives 0x48. Need exactly 0x38 = 56-byte frame with 32 bytes of locals at 0x18-0x37 (4 FPU-trick 8-byte slots). Also lfs f2 (1/256 constant) must load early between the two lhz reads; in base it loads late.
- **Tried:** (1) f32 charge_rate/f32 charge_frames/f32 color_anim without PAD_STACK, swapped call args: 0x30 frame, 30 mismatches. (2) same with PAD_STACK(8): tool stopped as unproductive. (3) Upstream prior: swap charge_frames/rate = 56%; f32 color_anim with (u32) cast = 27%; (u32)(s32)(f32)(u8) cast = 68.7% but 0x48 frame.
- **Likely fix:** Need exactly 3 f32 locals (charge_rate, charge_frames, color_anim_f) declared in a specific order, with exactly 8 bytes of padding (maybe via u64 pad or 2 u32 pads) to reach 0x38 frame. Or find the declaration order that produces 4 stack slots without PAD_STACK. Reading order: charge_rate (half[2]) before charge_frames (half[0]). The lfs scheduling issue may self-resolve once frame size matches.

