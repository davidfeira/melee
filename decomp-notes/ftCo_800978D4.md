---
function: ftCo_800978D4
tu: src/melee/ft/chara/ftCommon/ftCo_DownBound.c
headline: upstream-regression + stack-offset
tags: [upstream-regression, stack-offset, regalloc]
---
## ftCo_800978D4 (`src/melee/ft/chara/ftCommon/ftCo_DownBound.c`) — upstream-regression + stack-offset

- **Tags:** `upstream-regression`, `stack-offset`, `regalloc`
- **Best fuzzy:** 99.2667%
- **Diagnosis:** Upstream (doldecomp/melee) has this function matched with identical source (pad[16] + same casts/calls), but local build produces 6 mismatches: param stored at sp+0xc instead of sp+0x18, and register allocation r6/r4 instead of r4/r9 for jobj_ptr/queue_head temporaries. Frame size (0x28) matches correctly. Source is byte-for-byte identical to upstream. Root cause likely a transitive header difference or MWCC optimizer state difference not yet identified.
- **Tried:** (1) pad[16] before fp — frame 0x28 correct, param at 0xc not 0x18, r4/r9 vs r6/r4 regalloc; (2) pad[28] — makes frame 0x38, worse; (3) pad after float declaration — same 6 mismatches as attempt 1
- **Likely fix:** Identify which transitive header inclusion differs between local and upstream state at time of matching — possibly a struct size change that causes MWCC to choose different local layout and register allocation. Could also be permuter-solvable if regalloc and stack-offset are independent expressions.

