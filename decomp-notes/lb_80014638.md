---
function: lb_80014638
tu: src/melee/lb/lb_00F9.c
headline: permuter-false-positive + tu-wide-data
tags: [permuter-false-positive, tu-wide-data, rodata-typing, regalloc]
---
## lb_80014638 (`src/melee/lb/lb_00F9.c`) — permuter-false-positive + tu-wide-data

- **Tags:** `permuter-false-positive`, `tu-wide-data`, `rodata-typing`, `regalloc`
- **Best fuzzy:** 99.359%
- **Diagnosis:** 10 mismatches: 3 sda21 named-global vs anonymous-pool (target uses lb_804D7C70=0.0f, lb_804D7C74=0.01f, lb_804D7C78=1.0f at sdata2 offsets 0xD0/0xD4/0xD8; base emits anonymous @295/@1406/@294 entries) + 6 cascading f2/f3 regalloc diffs in the muladd block (target loads 1.0f into f3 and keeps sp24.z in f2; base loads 1.0f into f2 clobbering sp24.z, then reloads sp30.x into f3). The 6 regalloc diffs are downstream of the literal-pool mismatch — mwcc picks different registers depending on whether 1.0f comes from a named sdata2 global vs a fresh function-local pool entry. fdivs ends up in f3 (target) vs f2 (base) and the muladds at sp18.y/sp18.x checks inherit the swap.
- **Tried:** Diff inspection only. Per prior tu-wide-data notes (mn_8022DDA8, itDosei_UnkMotion0_Anim, fn_801803FC), 'static const f32 lb_804D7C70 = 0.0f' would const-fold back to anonymous pool (verified by Opus subagent in mn_8022DDA8 case). Without the TU-wide sdata2 layout, mwcc cannot emit the named-global reference and thus cannot replicate the target register allocation. Skipped per Permuter Boundary ('Do not use permuter for false diffs caused by equivalent BSS/global base symbols').
- **Likely fix:** TU-wide sdata2 reconstruction in lb_00F9.c. Need lb_804D7C70 (0.0f), lb_804D7C74 (0.01f), lb_804D7C78 (1.0f) declared as named sdata2 globals at the proper offsets in the file's float-pool layout. Surrounding pool entries (lb_804D7C48..lb_804D7CA8 — 14 named sdata2 floats/doubles per symbols.txt) likely all need declaring together to preserve mwcc's pool ordering. Same blocker class as itDosei tu-wide-data and mn_8022DDA8 — single-function edits cannot bridge it. Once the named globals exist and are referenced via simple literal-folded constants, the 6 regalloc cascade should resolve simultaneously.
