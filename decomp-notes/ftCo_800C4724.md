---
function: ftCo_800C4724
tu: src/melee/ft/chara/ftCommon/ftCo_WarpStar.c
headline: permuter-false-positive + tu-wide-data
tags: [permuter-false-positive, tu-wide-data, float-literal]
---
## ftCo_800C4724 (`src/melee/ft/chara/ftCommon/ftCo_WarpStar.c`) — permuter-false-positive + tu-wide-data

- **Tags:** `permuter-false-positive`, `tu-wide-data`, `float-literal`
- **Best fuzzy:** 99.8442%
- **Diagnosis:** Argument-order bug fixed (swapped 2nd/3rd args of it_80294430 — m2c said arg2=x2C4.y, arg3=co_attrs.x164; source had them swapped). Fuzzy improved from 99.87% to 99.84% (objdiff 99.58%). Remaining 6 diff mismatches: 4 are sda21 float-literal pool naming (target uses named ftCo_804D8D48/4C, base uses anonymous @194/@195), 2 are instruction scheduling of the f1/f2 loads before the it_80294430 call (same offsets, equivalent semantically). The 0/1 float consts ARE declared at file scope (lines 38-39) and the matching siblings ftCo_800C4858/800C48AC reference them via the same literal pattern (pos.x=pos.y=pos.z=0, Item_8026ABD8(...,1)) and DO match. So the same literal usage in this function fails to pool to the named symbol. Likely TU-state-dependent: usage order or surrounding code in this function shifts mwcc's pool decisions.
- **Tried:** (1) swapped arg order on it_80294430 — fixed semantic bug, fuzzy improved. (2) hoisted args into local floats (a2,a3) — changed scheduling but introduced extra instruction shuffles, reverted. Cannot affect float-pool naming via source-level changes; that's TU-wide compiler-state issue.
- **Likely fix:** Permuter cannot fix sda21-named-vs-anonymous mismatches (false-positive class per CLAUDE.md). Possible remedy: explicit references to the named consts (e.g. fp->facing_dir = ftCo_804D8D48) but that distorts the source. Or accept current 99.84% fuzzy as the matchable best given TU layout.
