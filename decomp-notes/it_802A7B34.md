---
function: it_802A7B34
tu: src/melee/it/items/itlinkhookshot.c
headline: cross-tu-globals + permuter-false-positive
tags: [cross-tu-globals, permuter-false-positive, inlining]
---
## it_802A7B34 (`src/melee/it/items/itlinkhookshot.c`) — cross-tu-globals + permuter-false-positive

- **Tags:** `cross-tu-globals`, `permuter-false-positive`, `inlining`
- **Best fuzzy:** 99.458%
- **Diagnosis:** Two distinct issues blocking 100%: (1) target asm shows it_802A6944 is INLINED into it_802A7B34, but source calls it as a function. The 'if (it_802A6A78(...) != 0)' branch in target contains the full body of it_802A6944: PSMTXIdentity at sp+0x24, sets m[0][3]=m[1][3]=zero, m[2][3]=it_804D6D48, PSMTXConcat with item_link->jobj->mtx, PSMTXCopy to jobj->mtx, sets flags |= 0x03800000, calls HSD_JObjSetMtxDirtySub. This explains the regalloc swap (more locals -> different reg pressure) and the addi r3,r1,0x24 references. (2) target uses 'lfs f0, it_804DCDB8@sda21' (named global float), base produces 'lfs f0, @320@sda21' (anonymous sdata2 0.0f). it_804DCDB8 is in symbols.txt as a globally-scoped sdata2 f32 at 0x804DCDB8 but no TU defines it. Both inline_alt(used in it_802A2EE4 and here) and inline (used in it_802A6944) write 'm[0][3]=zero' where zero=0.0F local; produces anonymous label. Note it_802A2EE4 (uses inline_alt) MATCHES 100% with anonymous label, so the named symbol pattern is specific to this function pair. Also inlined it_802A6944's body uses 'zero=0.0F' which produces anonymous label too.
- **Tried:** (1) extern f32 it_804DCDB8 + manual-inlined inline_alt body using m[0][3]=it_804DCDB8 instead of zero local: pushed frame size up 8 bytes (stwu -0xb0 vs target -0xa8), 25->29 mismatches WORSE. (2) Hoisted local decls + added f32 pad[1]: completely changed regalloc, 25->32 mismatches WORSE.
- **Likely fix:** Out-of-scope TU-data work: define f32 it_804DCDB8 = 0.0f; in some TU's sdata2 (likely whichever TU originally introduced it). Then in this function, manually inline it_802A6944 body and use 'm[0][3] = it_804DCDB8' (extern) instead of zero=0.0F. The inlining alone won't match because zero=0.0F still produces @320@sda21 anonymous label. Mama Claude needs to coordinate the data-symbols definition + structural inline change in single commit. Note: it_802A6944 callers elsewhere may also start inlining unexpectedly if structural change leaks; verify in-TU before committing.
