---
function: un_80311960
tu: src/melee/ty/toy.c
headline: permuter-queued
tags: [permuter-queued]
---
## un_80311960 (`src/melee/ty/toy.c`) — permuter-queued

- **Tags:** `permuter-queued`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 93.74% match, 7 mismatches. Source pattern uses for(i<0x125) with toy struct casts; mwcc unrolls inner block to 0x12 ctr-based iterations and emits bounds check via cmpwi/bge before unrolled portion (target hoists bounds check after the unrolled loop). All 7 mismatches are pre-loop scheduling/hoisting of the bounds check and induction var setup. Permuter territory: extra cmpwi r7,0x125 + bge + DIFF_DELETE 'addi r5,r31,0' before the main loop. Sibling un_803067BC (84%) shares same loop-unroll pattern.

