---
function: lbMemory_80015320
tu: src/melee/lb/lbmemory.c
headline: permuter-queued + regalloc
tags: [permuter-queued, regalloc, string-pool, bss-anchor, permuter-false-positive, instruction-scheduling]
---
## lbMemory_80015320 (`src/melee/lb/lbmemory.c`) — permuter-queued + regalloc

- **Tags:** `permuter-queued`, `regalloc`, `string-pool`, `bss-anchor`, `permuter-false-positive`, `instruction-scheduling`
- **Best fuzzy:** 93.6602%
- **Diagnosis:** 93.66% fuzzy match. Function structure correct: cancelflag assert at line 0x188, OSAlarm DefragJob at &g_alloc.x6A0 with fields x28_src/x2C_dest/x30_size/x34_progress/x38_args/x3C_callback (24 bytes after 0x28-byte OSAlarm). Three-way branch on h: NULL calls x6E8(x6E0); same-addr fast path recurses on h->x0_next; otherwise either HSD_DevComRequest (lo addr) or OSCreateAlarm+OSSetAlarm with fn_80015184 (hi addr, inside OSDisableInterrupts critical section). Remaining ~40 mismatches are register-allocation swaps (r28↔r29, r30↔r28, r25↔r26 throughout), 2 instruction-scheduling inserts (mr r31,r0; addi r5,r30,0x0), 1 string-pool ordering (target wants !p->size at offset 0xA8 and !cancelflag at 0xB4 but compiler emits in source-encounter order producing reverse), and 1 BSS-anchor false-positive (...bss.0@l vs lbMemory_804318B0@l).
- **Tried:** (1) Initial implementation with nested if-else 93% match, 44 mismatches, mostly regalloc. (2) Restructured to early-return form: dropped to 55% — control-flow changed and compiler chose worse layout. Reverted to nested form.
- **Likely fix:** Permuter on regalloc + scheduling should land it. String-pool ordering may need source-level intervention (introduce !p->size literal earlier, or restructure asserts). BSS-anchor mismatch is the standard false-positive class that resolves at link time.

