---
function: hsd_80393D2C
tu: src/sysdolphin/baselib/particle.c
headline: bss-anchor + permuter-false-positive
tags: [bss-anchor, permuter-false-positive]
---
## hsd_80393D2C (`src/sysdolphin/baselib/particle.c`) — bss-anchor + permuter-false-positive

- **Tags:** `bss-anchor`, `permuter-false-positive`
- **Best fuzzy:** 94.3448%
- **Diagnosis:** BSS-anchor false positive: target uses lis/lbzu hsd_804CF7E8@ha+@l (folding addi into load-update) while base uses lis/addi bss.0@ha+@l then lbz 0x104(r4). All 7 mismatches are BSS symbol name + lbzu vs lbz+offset. Post-link bytes are equivalent. Source is identical to upstream/master.
- **Tried:** Confirmed source matches upstream. Diff is pure BSS-anchor addressing: target lbzu folds the addi into the load-update, so it has 29 instrs; base has addi+lbz making 30 instrs. No source change can fix this without breaking the BSS layout.
- **Likely fix:** BSS layout fix or linker-script change to make hsd_804CF7E8 the first BSS symbol so bss.0 == hsd_804CF7E8 (offset 0x0 instead of 0x104). Alternatively, tooling needs to recognize lbzu as equivalent to addi+lbz for scoring purposes.

