---
function: lbMthp8001F928
tu: src/melee/lb/lbmthp.c
headline: permuter-false-positive + cross-tu-globals
tags: [permuter-false-positive, cross-tu-globals, tu-wide-data]
---
## lbMthp8001F928 (`src/melee/lb/lbmthp.c`) — permuter-false-positive + cross-tu-globals

- **Tags:** `permuter-false-positive`, `cross-tu-globals`, `tu-wide-data`
- **Best fuzzy:** 99.8723%
- **Diagnosis:** All 17 mismatches are bss-anchor reloc-symbol class: target uses lbl_804335B8@ha/@l with small offsets (0x0, 0x6C, 0x6E, 0x20, 0x24, 0x44, 0x48, 0x68); base uses ...bss.0@ha/@l with offsets shifted by 0x1D8 (e.g. 0x1D8, 0x244, 0x246, 0x1F8, 0x1FC, 0x21C, 0x220, 0x240). 0x1D8 is exactly sizeof(struct lbl_804333E0_t) (Movieplayer). mwcc is coalescing Movieplayer (lbl_804333E0) and lbl_804335B8 into a single bss.0 section and addressing both via one anchor; target build emits lbl_804335B8 as its own labeled bss symbol. Post-link bytes are identical (same addresses) but report.json fuzzy treats the reloc symbols as different. Function body itself is correct. Same blocker affects sibling functions in lbmthp.c that touch lbl_804335B8 (lbMthp8001F890 99.58%, fn_8001F2A4 90.79%, lbMthp_8001F410 95.43%, etc.); functions that only touch Movieplayer match 100%.
- **Tried:** prep + diff inspection; no source edits attempted (CLAUDE.md forbids permuter for this class and forbids non-surgical fixes)
- **Likely fix:** TU-wide bss layout fix: force lbl_804335B8 into its own bss section so mwcc emits a separate symbol. Options: (a) split lbl_804335B8 declaration into a separate .c file or attribute that creates a distinct bss section; (b) add a sentinel pad/array between Movieplayer and lbl_804335B8 to break the coalescing (unlikely since contiguous); (c) check if lbmthp.s reveals separate .bss vs .sbss sections in target. Need cross-TU investigation; not a single-function fix.
