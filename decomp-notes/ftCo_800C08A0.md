---
function: ftCo_800C08A0
tu: src/melee/ft/chara/ftCommon/ftCo_Bury.c
headline: stack-offset + regalloc
tags: [stack-offset, regalloc, mwcc-aliasing]
---
## ftCo_800C08A0 (`src/melee/ft/chara/ftCommon/ftCo_Bury.c`) — stack-offset + regalloc

- **Tags:** `stack-offset`, `regalloc`, `mwcc-aliasing`
- **Best fuzzy:** 95.5362%
- **Diagnosis:** 7 mismatches: 4-byte stack shift (f at 0x150 vs 0x154 in base) plus 3 extra insns target emits 'li r31,0; mulli r3,r31,0x4c; add r31,r30,r31' to compute &fp->hurt_capsules[0] when base just emits 'addi r31,r30,0x11a0'. Suggests p_hurt is being assigned via array-index path that mwcc doesn't constant-fold here, even though index is literal 0. The dead 'li r31,0' is reused as r31 afterward as the result reg, so it's tied to the materialization sequence. The 4-byte stack shift correlates with one extra spilled local (likely r31's first value).
- **Tried:** (1) inlined &fp->hurt_capsules[0] at both call sites removing p_hurt local: regressed to 41 mismatches (changed regalloc completely). (2) p_hurt = fp->hurt_capsules (array decay): identical 7 mismatches, no change. Reverted both.
- **Likely fix:** Possibly a different access pattern for p_hurt that triggers mwcc to fold the index, e.g., union/cast or accessing through a different field path. Or an additional unused local that shifts stack by +4. Permuter false-positive risk: stack-offset 4-byte shifts can match post-link if base reserves equal stack but mwcc reorders. Mama could try p_hurt declared without explicit init then assigned, or via a member of another struct that aliases hurt_capsules[0].
