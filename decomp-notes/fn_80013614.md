---
function: fn_80013614
tu: src/melee/lb/lb_00F9.c
headline: regalloc + float-regalloc
tags: [regalloc, float-regalloc, permuter-false-positive]
---
## fn_80013614 (`src/melee/lb/lb_00F9.c`) — regalloc + float-regalloc

- **Tags:** `regalloc`, `float-regalloc`, `permuter-false-positive`
- **Best fuzzy:** 99.0663%
- **Diagnosis:** Float regalloc swap pattern: target asm assigns alpha=f27, xC=f28, x8=f29, x4=f30, x0=f31 (lowest-to-highest in call-arg order); current source produces the conventional declaration-order allocation alpha=f31, xC=f30, x4=f28, x0=f27 (first-declared->f31). Plus 2 BSS-anchor mismatches (lb_803BA1C0 vs ...bss.0) and 1 sda21 float symbol mismatch (lb_804D7C08@sda21 vs @295@sda21) -- both classic permuter-false-positive. Stack offsets in target use 0x70/0x40 (Mtx) vs base 0x74/0x44 -- structural stack layout drift.
- **Tried:** V1: reversed local declaration order (x0,x4,x8,xC,x10,x11,alpha,image) in both if/else branches -- mismatch count 45->37 but fuzzy regressed to 95.58% with DIFF_INSERT/DELETE noise (load-order changed but new wrong shape). V2: moved alpha to last declaration in if-branch only -- regressed to 67 mismatches/90.14% (frame size 0xf0 vs 0xf8, stmw instead of split stores). Both reverted.
- **Likely fix:** Allocation pattern target=alpha->f27 with x0->f31 implies mwcc allocated regs in CALL-ARG-ORDER (1st float arg x0 -> f31, last float arg alpha -> f27), not declaration order. The fact that alpha is loaded FIRST in target (because of the early if-check) but assigned f27 (lowest non-volatile) is unusual. May require a structural rewrite where alpha is computed/used differently, or where the call args are evaluated in a different shape (e.g., struct member auto-ptr access, or explicit temporaries). The 0x70/0x74 Mtx stack-offset diff suggests the target has one fewer scratch local on the stack -- maybe view_mtx was reused or one of view_mtx/view_mtx2/tex_obj is hoisted to function scope. Also note the BSS anchor and sda21 mismatches are guaranteed permuter-false-positive (won't be fixable by permuter; report.json fuzzy treats as real). Permuter would also struggle with the frame-size+regalloc pair given the cross-coupling.
