---
function: fn_80122B54
tu: src/melee/ft/chara/ftPopo/ftPp_SpecialS.c
headline: tu-data-osreport + cross-tu-globals
tags: [tu-data-osreport, cross-tu-globals, reloc-symbol-false-positive, regalloc]
---
## fn_80122B54 (`src/melee/ft/chara/ftPopo/ftPp_SpecialS.c`) — tu-data-osreport + cross-tu-globals

- **Tags:** `tu-data-osreport`, `cross-tu-globals`, `reloc-symbol-false-positive`, `regalloc`
- **Best fuzzy:** 95.102%
- **Diagnosis:** Fuzzy 95.10% / strict 94.80%, 13 mismatches on a 49-instr function. All mismatches are reloc-symbol/anchor false-positives plus correlated r30/r31 register-allocation swap. Target asm anchors 1.0f and 0.0f via named ftPp_Init_804D9890 (0.0f) and ftPp_Init_804D9894 (1.0f) sdata2 globals (defined in ftPp_Init.s data block, used cross-TU within ftPopo). Base TU resolves these as anonymous local literal pool entries @212@sda21 and @306@sda21 because no source declaration declares them as named static const f32. Other functions in same TU (ftPp_SpecialS_8011F68C and friends) reference ftPp_Init_804D9848@sda21 = -1.0f, also undeclared in source. Coupled with the literal-pool divergence is a r30/r31 swap on every register reference (target r31=gobj, r30=fp; base r30=gobj, r31=fp), driven by mwcc reordering register assignments due to different literal-pool layout. Function source itself (fp=gobj->user_data; ftCommon_8007D7FC(fp); Fighter_ChangeMotionState(...); ftPp_set_cbs inline; cmd_vars[3] check; ftPartSetRotX/atan2f branch; accessory4_cb=fn_80122D2C) is structurally and semantically correct; m2c output matches existing source.
- **Tried:** Diff inspection only; no source-shape attempts. Per Permuter Boundary, permuter is contraindicated for reloc-symbol/anchor false-positives, and prior notes for this pattern (Exception_ReportStackTrace, mpGetSpeed, it_80274DAC, fn_801803FC, lbMthp_8001F624, fn_801FBEB8, soundtest siblings) all confirm permuter cannot bridge data-pool anchor divergence.
- **Likely fix:** TU-wide multi-symbol refactor: declare ftPp_Init_804D9890 (0.0f), ftPp_Init_804D9894 (1.0f), ftPp_Init_804D9848 (-1.0f) and any other cross-TU sdata2 anchors as named static const f32 in ftPp_Init.c (the originating TU) so mwcc emits them as named symbols and stops generating @NNN local literals in this TU. Once the anchor is declared, all 13 mismatches in fn_80122B54 should resolve simultaneously (the r30/r31 swap is downstream of literal-pool layout, not an independent issue). Affects all ftPp_Init/ftPp_SpecialN/ftPp_SpecialS functions referencing 804D984x/804D989x. Same blocker family as it_80274DAC, mpGetSpeed, lbMthp_8001F624. Beyond single-function subagent scope.
