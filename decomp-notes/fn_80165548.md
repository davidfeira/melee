---
function: fn_80165548
tu: src/melee/gm/gm_1601.c
headline: permuter-queued + regalloc
tags: [permuter-queued, regalloc]
---
## fn_80165548 (`src/melee/gm/gm_1601.c`) — permuter-queued + regalloc

- **Tags:** `permuter-queued`, `regalloc`
- **Best fuzzy:** (unknown)
- **Diagnosis:** Source structurally matches at 96.13% (35 register-allocation mismatches). Outer i=0..3 over arg0->player_standings[i]: skip if slot_type==3 or team!=arg2; var_r10=i; inner k=0..3: skip if slot_type==3, team!=arg2, or i==k; if arg0->ps[i].is_small_loser > arg0->ps[k].is_small_loser then var_r10=-1 break. After inner: if var_r10>=0 then result=var_r10 and break outer. Else loop. is_teams!=1 path returns arg1 via shared result var. Header gm_1601.h: changed arg2 from u8 to s8 (already in working tree) — needs follow-up landing. Remaining diffs are pure regalloc cascade: r5/r6/r7 vs r6/r7/r8 indices in unrolled 4-block inner loop, plus extsb r7,r5 (arg2 sign-extension) base-only insert. No structural work remaining — pure permuter case. Decomp-permuter offline; queued.

