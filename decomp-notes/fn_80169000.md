---
function: fn_80169000
tu: src/melee/gm/gm_1601.c
headline: permuter-territory + regalloc
tags: [permuter-territory, regalloc, frame-size]
---
## fn_80169000 (`src/melee/gm/gm_1601.c`) — permuter-territory + regalloc

- **Tags:** `permuter-territory`, `regalloc`, `frame-size`
- **Best fuzzy:** (unknown)
- **Diagnosis:** Pointer-walk source shape gives 93.5% match (90 mismatches / 142 instr). Field is is_small_loser at offset 5 in MatchPlayerData (asm 0x5e from MatchEnd = 0x58+6, but actually arg0+0x5e accesses player_standings[0].is_small_loser since MatchPlayerData layout puts is_small_loser at 0x06 — verify if struct field offsets differ). Stack frame is 0x18 in compiled output but target uses 0x30 — extra 0x18 bytes between back-chain and locals suggests source has additional locals or reversed declaration order or different struct grouping. Register allocation cascades: target uses r5=count, r6=ranks_ptr, r7=index, r8=pp_walker, r9=vals_ptr; my output uses different mapping causing waterfall of arg-mismatches.
- **Tried:** (1) Array-indexed unrolled body — 84%. (2) Mixed pointer-walk with for(i=1;i<4) loop and post-increment vp/arg1 — 93.5%, but 90 mismatches mostly regalloc cascade + 0x30 vs 0x18 frame size.
- **Likely fix:** Permuter to find local declaration order that yields 0x30 frame and matching register assignments. Or investigate if MatchPlayerData layout has is_small_loser at different offset (asm offset 0x5e from arg0 (MatchEnd) = 0x06 inside MatchPlayerData, which is is_small_loser per current types.h).

