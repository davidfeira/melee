---
function: fn_80177B7C
tu: src/melee/gm/gmresultplayer.c
headline: stack-offset + permuter-false-positive
tags: [stack-offset, permuter-false-positive, sdata2-float]
---
## fn_80177B7C (`src/melee/gm/gmresultplayer.c`) — stack-offset + permuter-false-positive

- **Tags:** `stack-offset`, `permuter-false-positive`, `sdata2-float`
- **Best fuzzy:** 99.5705%
- **Diagnosis:** 99.57% fuzzy / 99.34% strict. 12 mismatches: 4x stack-offset (stick_y local at 0x1c target vs 0x14 base, with frame=0x38 in both), 7x sdata2 float anchor false-positives (lbl_804DA380/388/370 named globals vs @292/@293/@190 anonymous - same post-link bytes), and 1x DIFF_REPLACE 'mr r5,r31' vs 'addi r5,r31,0x0' at the fn_801748EC call (3rd arg slot).
- **Tried:** PAD_STACK(8) [baseline source], PAD_STACK(16) made it worse (frame grew to 0x40, base went to 30 mismatches), no PAD_STACK shrunk frame to 0x30 (also 30 mismatches), FORCE_PAD_STACK_8 (u64) gave identical 12-mismatch result to PAD_STACK(8).
- **Likely fix:** Stack-offset class: target source layout puts stick_y immediately below the int-to-double conversion temp (0x20-0x27) at 0x1c with no padding between, while every PAD_STACK variant tried places stick_y at 0x14 with the pad array between. May need rearranging the local declaration order so stick_y is declared LAST among the float/int locals, or introducing a different shape of dummy local that mwcc places below stick_y instead of above. The sdata2 anchor mismatches are pure permuter-false-positive territory (same float constants 0.0f, 8.0f, scroll_max double; named lbl_804DA380 etc reused across the TU vs anonymous @N pool here). The mr vs addi mismatch is the call to fn_801748EC(scroll_target, list, slot) - mwcc emitted addi r5,r31,0 to copy slot but target uses mr. Likely a downstream effect of some earlier register-pressure or scheduling decision.
