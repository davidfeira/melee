---
function: fn_80165FA4
tu: src/melee/gm/gm_1601.c
headline: regalloc + instruction-scheduling
tags: [regalloc, instruction-scheduling, mwcc-loop-opt]
---
## fn_80165FA4 (`src/melee/gm/gm_1601.c`) — regalloc + instruction-scheduling

- **Tags:** `regalloc`, `instruction-scheduling`, `mwcc-loop-opt`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 89.14% (35 mismatches) with for-loop + unrolled winners. The outer loop (i=0..4) over team_standings matches well — compiler strength-reduces arg0->team_standings[i] to r6=arg0+i*0xC. The winners section (team_winners[0..4] assignment) has structural mismatches: target uses stbx pattern (addi r0, n_winners_reg, 0x16; stbx val, r3, r0) while base generates add+stb; target loads team[j].is_big_loser via r5 walking pointer while base uses direct arg0+offset; loser field update generates clrrwi+or instead of rlwimi. Header prototype updated: UNK_RET -> s32 in gm_1601.h.
- **Tried:** Attempt 1: for-loop outer (i) + inner (j) with array indexing = 89.14% 35 mismatches. Attempt 2: do-while with MatchTeamData* pointer (cur/cmp) = 75.8% 106 mismatches (worse).
- **Likely fix:** The winners section needs a different n_winners expression: target initializes n_winners (r6) with li r6,0 between rlwimi and stb of loser, suggesting instruction reordering. The stbx vs add+stb difference may require expressing team_winners store as byte-offset from arg0 vs pointer addition. Try: s32 n_winners declared after the loser update, or use a u8* pointing to team_winners[0] for the store pattern.

