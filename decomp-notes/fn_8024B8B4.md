---
function: fn_8024B8B4
tu: src/melee/mn/mnsoundtest.c
headline: stack-offset + frame-size
tags: [stack-offset, frame-size, data-anchor, permuter-dispatched]
---
## fn_8024B8B4 (`src/melee/mn/mnsoundtest.c`) — stack-offset + frame-size

- **Tags:** `stack-offset`, `frame-size`, `data-anchor`, `permuter-dispatched`
- **Best fuzzy:** 99.8951%
- **Diagnosis:** 143-instr two-branch state-transition function reaches 99.65% (22 mismatches). Real diff is one stack-slot placement: target reserves 0x38 frame with 5 locals (sp10..sp1C plus sp20 at +0x20 above the others), but MWCC packs sp20 at +0xC regardless of declaration order tried (last-decl baseline, first-decl gives 4-slot 0x30 frame — worse). The other 14 mismatches are reloc-symbol false positives (lfs f1, 0xc(r31) and addi r4, r31, 0x18/0x30/0x3c — identical values, reloc against mnSoundTest_803EF0A8 vs vec_1/vec_2/vec_4/vec_5 aliases at same addresses; data-anchor class). Source uses vec_1.x, &vec_2, &vec_4, &vec_5 — could try referencing mnSoundTest_803EF0A8 directly via single anchor, but that requires #if-else swap currently used to keep fn_8024BAF0 matched.
- **Tried:** (1) original decl order [sp1C,sp18,sp14,sp10,sp20]: 99.65%, sp20 placed at +0xC instead of +0x20; (2) sp20-first [sp20,sp1C,...]: 99.56% — compiler merged into 4 slots/0x30 frame, regressed. Reverted to (1).
- **Likely fix:** Decomp-permuter offline per dispatch override. When available, dispatch should focus on stack-slot lifetime — getting sp20 above sp1C requires forcing distinct register allocation across branches; permuter randomization may discover. Alternative manual: replace vec_1.x/vec_2/vec_4/vec_5 with single mnSoundTest_803EF0A8 array reference (extern Vec3 mnSoundTest_803EF0A8[];) to collapse data-anchor false positives — would need to verify fn_8024BAF0 still matches with shared anchor.

