---
function: fn_80165AC0
tu: src/melee/gm/gm_1601.c
headline: permuter-territory + instruction-scheduling
tags: [permuter-territory, instruction-scheduling, bitfield-reuse]
---
## fn_80165AC0 (`src/melee/gm/gm_1601.c`) — permuter-territory + instruction-scheduling

- **Tags:** `permuter-territory`, `instruction-scheduling`, `bitfield-reuse`
- **Best fuzzy:** (unknown)
- **Diagnosis:** At 98.2 percent (3 mismatches): single 'li r6, 0x0' scheduled one instruction earlier in target plus narrower rlwimi mask (24,27 vs 0,27) requires no preceding rlwinm in target. Remaining diff is pure scheduling and rlwimi width optimization.
- **Tried:** Initial nested loop (80.4 pct), made max_loser s32 + n s32 -> 95 pct, dropped redundant '& 0xF0' on shifted value -> 98.2 pct. Tried '& 0xF' on max_loser update site (regressed mr-to-clrlwi). Tried 'arg0->loser & 0xF' simpler mask (clrlwi appeared instead of rlwinm). Tried (u8) max_loser cast - no change.
- **Likely fix:** Permuter should find an expression form that lets MWCC emit the narrow rlwimi mask 24,27 without preceding rlwinm; 'li r6, 0x0' scheduling is a trivial swap. Source-level structure looks correct; queue when permuter back online.

