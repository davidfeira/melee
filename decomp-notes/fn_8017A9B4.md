---
function: fn_8017A9B4
tu: src/melee/gm/gmresultplayer.c
headline: regalloc + instruction-scheduling
tags: [regalloc, instruction-scheduling, permuter-territory]
---
## fn_8017A9B4 (`src/melee/gm/gmresultplayer.c`) — regalloc + instruction-scheduling

- **Tags:** `regalloc`, `instruction-scheduling`, `permuter-territory`
- **Best fuzzy:** 68.8571%
- **Diagnosis:** After fixing dim arrays to use u16 halfword access (u16* cast on dim_w1/w2/h1/h2) and confirming is_big_loser is correct for both non-teams and teams path, the remaining 37 mismatches are pure register allocation: target uses r28-r31 (4 callee-saves, frame 0x20) while base uses r29-r31 only; target uses stwu for image_ptr zeroing while base uses stw+addi; target keeps &lbl_8046E1B0 in r29 as persistent base while base uses r5/r6; all offsets and instruction types are now correct.
- **Tried:** 1. Added explicit state pointer (lbl_8046E3AC_t* state) - made match_end computation explicit, got 67%; 2. Removed state pointer, used disp->state.xxx directly - got 69.63%; u16 cast approach for dim arrays generates correct lhz at offsets 0x24B0/0x24B8/0x24C0/0x24C8 from lbl_8046E1B0.
- **Likely fix:** Permuter should resolve stwu vs stw+addi and r28-r31 vs r29-r31 register allocation. The struct defines dim_w1/w2/h1/h2 as u32[2] but they must be accessed as u16 pairs via cast - lookup*2 is the byte offset into the packed u32 pairs (two u16 values packed per u32). Also: MatchTeamData.team_standings is at match_end+0x1B (unaligned) in binary but MWC adds padding to 0x1C; is_big_loser (offset 0x08) accessed via match_end+idx*0xC+0x24 is the correct target access (appears as is_big_loser through padded struct).

