---
function: un_803124BC
tu: src/melee/ty/tylist.c
headline: tu-wide-data + data-symbols-missing
tags: [tu-wide-data, data-symbols-missing]
---
## un_803124BC (`src/melee/ty/tylist.c`) — tu-wide-data + data-symbols-missing

- **Tags:** `tu-wide-data`, `data-symbols-missing`
- **Best fuzzy:** 99.2917%
- **Diagnosis:** The single 'real' mismatch (addi r3,r30,0x608 vs mr r3,r30) plus the chain of @ha/@l reloc-offset diffs all stem from one TU-wide layout issue: target's un_803FDD18 anchor needs ~0x608 bytes of preceding data before the first archive-string ('TyDatai.dat'), but the source emits the strings at offset 0 of .data. The 6 undecomped functions in tylist.c (un_80312904, un_8031305C, un_80313774, un_80313BD8, un_8031438C, un_8031457C) are responsible for the missing data. un_8031263C (sister function with same lbArchive_LoadSymbols call, same 8 string args) has the IDENTICAL anchor-offset diff confirming this is TU-wide rather than function-local. Same pattern as grIceMt_801F71E8 prior log-stuck: single-function attempts are doomed because the data definitions live in undecomped sibling functions.
- **Tried:** prep+diff baseline (99.29%/98.77%, 11 strict mismatches: 1 'real' addi-vs-mr, 10 reloc-offset diffs); inspected source — strings 'TyDatai.dat'..'tyDisplayModelUsTbl' are passed inline to lbArchive_LoadSymbols (mwcc emits these at .data+0 via deduped string pool); diff'd sibling un_8031263C — same anchor-offset shift confirming TU-wide; objdump -h shows target tylist.o has .data=0x190 vs source .data=0x94 (target carries data from undecomped functions); checked notes --blocker tu-wide-data, found grIceMt_801F71E8 with same 'declare named globals to force layout' pattern was attempted and failed without multi-function effort
- **Likely fix:** Decomp at least one of the data-bearing siblings (un_80313774 or un_80313BD8 are likely candidates given size) to populate the 0x608-byte gap, OR introduce a placeholder static byte array of size 0x608 before un_803124BC's strings — but that risks linker symbol clashes and only helps if the actual data layout is purely opaque bytes (likely not, given other code references un_803FE8D0/un_803FE8F0/un_803FE880 which sit further out in the same anchor).
