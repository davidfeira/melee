---
function: un_80307470
tu: src/melee/ty/toy.c
headline: permuter-territory + regalloc
tags: [permuter-territory, regalloc, frame-size, instruction-scheduling]
---
## un_80307470 (`src/melee/ty/toy.c`) — permuter-territory + regalloc

- **Tags:** `permuter-territory`, `regalloc`, `frame-size`, `instruction-scheduling`
- **Best fuzzy:** (unknown)
- **Diagnosis:** Decompiled from placeholder; structure matches at 88.6%. Remaining mismatches are register coloring (target uses r26 for arg0, r29 for un_803FDD18 base, r30 for joint; base picks r27/r31/r28) and a 0x18-byte stack frame discrepancy (target -0x40, base -0x28). One scheduling diff: target emits 'addi r27, r27, 0x188' after the HSD_ArchiveGetPublicAddress call (dead increment kept around for the error-path OSReport), base emits 'addi r3, r29, 0x188' lazily inside the error branch. The @713/@714 sda21 string-literal mismatches for 'toy.c'/'0' are an existing infra issue across the whole TU (un_803075E8 and un_803078E4 also show them). These are all permuter-class signals.
- **Tried:** (1) HSD_GObj** td with td[0]/td[0x50/4]/td[0x10/4]/td[0x30/4] indexing; works structurally. (2) Adding 6 unused s32 locals bumped frame to 0x40 but didn't fix stmw save count (target stmw r26=6 regs, base stmw r27=5 regs). Reverted unused locals as not worth source bloat.
- **Likely fix:** Permuter to find a local-variable shape (or specific cast pattern) that pushes mwcc to (a) save r26-r31 in stmw, (b) use r29 for the un_803FDD18 base pointer, and (c) emit the unconditional addi r27, r27, 0x188 in the success-path tail. The struct-typing of un_804D6ED8 may also matter — it's accessed at offsets 0x0 (HSD_GObj*), 0x10 (HSD_JObj*[]), 0x30 (HSD_JObj*), 0x50 (HSD_Archive*) and doesn't fit ToyData or ToyGlobalsS_ exactly; defining a dedicated struct may also help.

