---
function: un_80301964
tu: src/melee/if/soundtest.c
headline: tu-data-osreport + cross-tu-globals
tags: [tu-data-osreport, cross-tu-globals, paired-siblings, reloc-symbol-false-positive]
---
## un_80301964 (`src/melee/if/soundtest.c`) — tu-data-osreport + cross-tu-globals

- **Tags:** `tu-data-osreport`, `cross-tu-globals`, `paired-siblings`, `reloc-symbol-false-positive`
- **Best fuzzy:** 98.125%
- **Diagnosis:** Fuzzy 98.125%, strict 98.05%, 3 mismatches, all reloc-symbol false-positives. Target uses un_803FD274 with @ha/@l (full-address: lis r3,@ha; addi/stw via r3) and un_804D5990 with @sda21 (single 'li r4, un_804D5990@sda21'). Base inverts both: un_803FD274 via @sda21 ('stw r0, un_803FD274@sda21') and un_804D5990 via @ha/@l (lis r3,@ha; addi r4,r3,@l). Both symbols' SDA-range membership is inverted between target and base. un_803FD274 is declared at line 123 as 'extern void* un_803FD274;' (.data), un_804D5990 at line 169 as 'extern char un_804D5990[];' (.sdata-region but not declared as static within this TU). The function source itself (if-arg-eq-1, OSReport, lbSnap_8001E058, do/while loop, lbSnap_8001DE8C, GObj_Create, GObj_SetupGXLink, store, HSD_SObjLib_803A477C, store .x10/.x14/.x40 |= 2, store gobj) is semantically correct and matches m2c output exactly.
- **Tried:** Manual diff inspection only; no source-shape attempts. Per Permuter Boundary ('Do not use permuter for false diffs caused by equivalent BSS/global base symbols'), permuter is contraindicated. Per existing tu-data-osreport notes for sibling functions in same TU (un_802FF7DC at 72.76%, un_80300AF4 at 74.28%), the soundtest.c TU has known TU-wide BSS/.data/.sdata layout issues that cause mwcc to pick different anchor symbols and SDA-range membership; these cannot be bridged from inside a single function.
- **Likely fix:** TU-wide reconstruction of soundtest.c .data/.sdata layout. The two flipped anchors suggest un_803FD274 needs to be defined (not just extern'd) earlier in this TU so it lands inside the .data anchor pool, while un_804D5990 needs to be defined as a TU-local sdata extern/declaration so mwcc can reach it via @sda21. Pairs with un_802FF7DC (72.76%), un_80300AF4 (74.28%), and the as-yet-unmatched siblings un_80300B58 sharing the same TU-wide blocker. One unified data-pool restructure (declare un_803FD274/un_803FD28C/un_803FD29C/un_803FD2AC/un_803FD224/etc as static .data members with proper sizes/initializers, and un_804D5990 as a static sdata declaration anchored consistently) likely lifts all of these simultaneously. Beyond single-function scope.
