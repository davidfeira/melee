---
function: un_80312904
tu: src/melee/ty/tylist.c
headline: permuter-queued + r30-r31-swap
tags: [permuter-queued, r30-r31-swap, float-regalloc, instruction-scheduling]
---
## un_80312904 (`src/melee/ty/tylist.c`) — permuter-queued + r30-r31-swap

- **Tags:** `permuter-queued`, `r30-r31-swap`, `float-regalloc`, `instruction-scheduling`
- **Best fuzzy:** (unknown)
- **Diagnosis:** Manual decomp brings match from 0%/170 instr to 89.75%/60 mismatches. All remaining diff is pure register allocation (mwcc swaps r30<->r31 between state and arg0/row) and FPR allocation (f29/f30/f31 swaps for the three Vec3 components). Also subtle scheduling: one INSERT/DELETE around stw r30 save vs PAD_STACK accounting. No structural blockers — the source models the function correctly: TyListState* state at un_804A2AC0; row at arg0 with HSD_Text* fields at 0x18/0x1C/0x20, idx at 0x26, x28 s32, x30 f32; three null-checked jobj loads (row->jobj, then state->gobj->hsd_obj twice for f30=jobj->translate.x-DDE28, f29=-row->x30-jobj2->translate.y-DDE2C, f31=jobj3->translate.z); HSD_Text fills text_color from sdata lb_804D3760/lb_804D3764, set pos/font_size/kerning, call HSD_SisLib_803A6368 thrice; un_803082F8 must be declared s32-returning (locally extern in this TU) since it tail-calls un_803063D4 and source uses the leaked r3.
- **Tried:** (1) Initial decomp: 77.3%. Added PAD_STACK(0x78) for frame size: 83.5%. (2) Reordered jobj null-check after f30 compute (matches asm scheduling) plus local extern s32 un_803082F8 to use direct bl with leaked-r3 return: 89.75%.
- **Likely fix:** Permuter to flip r30<->r31 (state vs row register) and f29/f30/f31 for the three jobj-translate accesses. May also need to flip stack-save ordering. Sibling fn_8031438C in same TU is at 95.1% queued — same family of regalloc differences, suggests this whole TU needs a permuter pass once decomp-permuter is back online.

