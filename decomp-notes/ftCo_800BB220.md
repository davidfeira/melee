---
function: ftCo_800BB220
tu: src/melee/ft/ftcpuattack.c
headline: sdata2-anonymous-floats + frame-size
tags: [sdata2-anonymous-floats, frame-size, regalloc, permuter-blocked]
---
## ftCo_800BB220 (`src/melee/ft/ftcpuattack.c`) — sdata2-anonymous-floats + frame-size

- **Tags:** `sdata2-anonymous-floats`, `frame-size`, `regalloc`, `permuter-blocked`
- **Best fuzzy:** (unknown)
- **Diagnosis:** Function structurally correct (~77%, 236 mismatches, 338 instrs). Three persistent classes: (1) sdata2 float literals appear as @277/@279/@280@sda21 in my .o vs target's named ftCo_804D8A28/@2A30/@2A34@sda21 — these are TU-shared float pool entries (20.0f, 10.0f, 5.0f) referenced by other matched functions in ftcpuattack.c; until layout aligns, every lfs+stfd pair carries a relocation diff. (2) Stack frame is -0xE0 (mine) vs -0x128 (target) — target allocates 10 distinct Vec3 slots (3 endpoint Vec3s + 6 lbColl out Vec3s + dst), but mwcc reuses my block-scoped Vec3s across the three mutually-exclusive branches even when declared as separate locals inside if/else blocks. (3) Register allocation shifted by one: target binds r28=fp, r29=ip, r30=arg2, r31=&fp->x1A88 (cached prologue), but mine uses r29/r30/r31 only and spills temp_r31 to r25. Permuter not dispatched per directive (decomp-permuter offline, plus sdata2 anon floats are false-positive class).
- **Tried:** Two attempts: (a) flat function with all Vec3s declared at top — mwcc reused slots, frame=-0xE8. (b) block-local Vec3 declarations inside each if/else branch with names matching target stack offsets (sp94/spAC/spB8 for NESS-short, sp60/sp78/sp84 for NESS-long, sp2C/sp44/sp50 for default), plus split kind check into 'if (kind==Unk4||kind==Star) result=1; else result=ftCo_800A5980(ip)' to encourage the bge bridge in mwcc's compare chain. Frame grew to -0xE0 but mwcc still collapsed the per-branch Vec3 slots; bge bridge between the two beq's still missing.
- **Likely fix:** (1) For sda2 anonymous float symbols: add named static const f32 declarations at TU scope with the right values to force them into the TU sdata2 pool (compare matched siblings ftCo_800B6208 etc to see how 804D89F0/804D89F4 etc. are named). (2) For unique Vec3 slot allocation: try declaring all 9 branch Vec3s at function scope with explicit different types or volatile to defeat mwcc's lifetime-overlap reuse, OR break the 3 branches into 3 static helper functions. (3) For r28 binding: extend lifetime of temp_r31=&fp->x1A88 across one of the early function calls (move declaration above the ip->owner check or first ftLib_80086960 call) so mwcc preserves it in r31 from the prologue. (4) Once those align, residue should be permuter-territory regalloc/scheduling.

