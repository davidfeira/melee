---
function: fn_8001EBF0
tu: src/melee/lb/lbmthp.c
headline: regalloc + instruction-scheduling
tags: [regalloc, instruction-scheduling]
---
## fn_8001EBF0 (`src/melee/lb/lbmthp.c`) — regalloc + instruction-scheduling

- **Tags:** `regalloc`, `instruction-scheduling`
- **Best fuzzy:** 98.7692%
- **Diagnosis:** 11-instruction register allocation mismatch in the load/compute cluster for width/unk_100/height/unk_104. Target uses r7=width, r6=unk_100 (destructive clrrwi), r4=height, r5=unk_104. Base produces r6=width, r4=unk_100 (non-destructive clrrwi r5,r4,5), r7=height, r8=unk_104. Root cause: target computes &data->unk_9C (for THPDec arg) into r3 between the width load and unk_100 load, freeing r3 and shifting subsequent allocations by one register. Cannot be reproduced in source without permuter.
- **Tried:** 1) m2c-style single-expression with temp_r7/temp_r29 variables (71% - made worse). 2) Inline ALIGN_32 directly in load assignment (same 11 mismatches). Original upstream source is identical to local - pure register allocation difference.
- **Likely fix:** Permuter territory: pure regalloc/scheduler mismatch. The &data->unk_9C early computation into r3 (before unk_100 load) is the scheduling decision driving the register shift. A permuter run with expression-order and temp-lifetime mutations would likely resolve this.

