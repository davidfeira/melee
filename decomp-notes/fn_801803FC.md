---
function: fn_801803FC
tu: src/melee/gm/gmregclear.c
headline: tu-data-osreport + cross-tu-globals
tags: [tu-data-osreport, cross-tu-globals]
---
## fn_801803FC (`src/melee/gm/gmregclear.c`) — tu-data-osreport + cross-tu-globals

- **Tags:** `tu-data-osreport`, `cross-tu-globals`
- **Best fuzzy:** 99.9716%
- **Diagnosis:** 99.97% fuzzy / 99.62% strict, 10 mismatches, all reloc-symbol false-positives. (1) 6 mismatches anchor via lbl_803D8B88 (the declared u16[10] array at gmregclear.c:164) at offsets +0xb8/+0xe8/+0xf8 vs base ...data.0 + 0xcc/+0xfc/+0x10c. The 0x14 delta = sizeof(lbl_803D8B88) (10 u16 = 20 bytes), confirming base merges the array into the rodata blob and indexes from before it instead of treating lbl_803D8B88 as a separate anchor. (2) 4 mismatches: target lfs f1, lbl_804DA478@sda21 (0.0f) and lbl_804DA4E0@sda21 (1.0f) vs base @644/@265 local compiler-generated literal pool. Function source is already correct; m2c output matches existing structure exactly.
- **Tried:** Manual diff inspection only. No source-shape attempts since the function is structurally correct and all mismatches are TU-wide data-symbol anchor divergence (same pattern as Exception_ReportStackTrace, mpGetSpeed, it_80274DAC, it_80274740 per existing tu-data-osreport notes). Permuter Boundary forbids permuter on literal-pool/anchor divergence.
- **Likely fix:** TU-wide multi-symbol refactor: declare lbl_804DA478 (0.0f) and lbl_804DA4E0 (1.0f) as named sdata2 statics so mwcc stops generating @644/@265 local literals. Restructure the rodata pool around lbl_803D8B88 + the OSReport format strings so the merged ...data.0 blob anchors via lbl_803D8B88 rather than a 0x14-earlier symbol. Same blocker class as the other 4 tu-data-osreport entries; beyond single-function scope. Once TU-wide sdata2/rodata is reconstructed, this function will match for free.
