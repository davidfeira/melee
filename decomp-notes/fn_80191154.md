---
function: fn_80191154
tu: src/melee/gm/gm_18A5.c
headline: cross-tu-globals + permuter-false-positive
tags: [cross-tu-globals, permuter-false-positive]
---
## fn_80191154 (`src/melee/gm/gm_18A5.c`) — cross-tu-globals + permuter-false-positive

- **Tags:** `cross-tu-globals`, `permuter-false-positive`
- **Best fuzzy:** 99.8983% (9 mismatches — Opus subagent, no commit)
- **Diagnosis:** All 9 mismatches are BSS/sdata2 reloc-anchor naming. Target uses `lis r4, lbl_804799B8@ha; addi r30, r4, lbl_804799B8@l` then offsets `0xe`; base uses `...bss.0@ha/@l` (= some earlier anchor) then offsets `0x3c7a`. Plus 1 sdata2 anonymous-literal name swap (`@637` vs `@1164`) for the float in `fn_8019044C` arg. No instruction-level / regalloc / scheduling diffs at all — every diff is a reloc symbol name.
- **Function context:** The function does `xE_ptr = &lbl_804799B8.xE; fn_8019044C(jobj, (float)lbl_804799B8.xE); (*xE_ptr)++;` after some range-clamping branches. `lbl_804799B8` is `static struct Lbl804799B8_t` declared at line 1943, size 0x20.
- **Sonnet attempt (reverted):** Sonnet added `u8* xE_ptr2 = xE_ptr` duplicate-variable hack and used `xE_ptr2` for the increment. Pure cargo-cult — doesn't change codegen, doesn't address the BSS anchor issue. Reverted via Edit tool.
- **TU layout context:** `gm_18A5.c` BSS span 0x80473AB8–0x80479A58 (per splits.txt:1908). Target's `gm_80477738` (size 0x2280, scope:global) sits within this span at 0x80477738 between `gm_804771C4` and `lbl_804799B8`, but in source it's `extern MatchEnd gm_80477738` (line 6307) — defined-in-this-TU vs extern is irrelevant since linker addresses still align (build links). The blocker is purely which BSS symbol mwcc picks as anchor for this function's accesses.
- **Likely fix:** Same family as `fn_80186EFC`, `gm_80173754`, `lbColl_800077A0` — TU-wide BSS/sdata2 layout work. Single-function source-shape edits cannot redirect the anchor pick. Permuter is contraindicated per Permuter Boundary ("Do not use permuter for false diffs caused by equivalent BSS/global base symbols").
