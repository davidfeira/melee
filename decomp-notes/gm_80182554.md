---
function: gm_80182554
tu: src/melee/gm/gmregclear.c
headline: permuter-false-positive + bss-anchor
tags: [permuter-false-positive, bss-anchor, tu-wide-data]
---
## gm_80182554 (`src/melee/gm/gmregclear.c`) — permuter-false-positive + bss-anchor

- **Tags:** `permuter-false-positive`, `bss-anchor`, `tu-wide-data`
- **Best fuzzy:** 99.4444%
- **Diagnosis:** All 7 mismatches are BSS-anchor reloc differences: target emits lis/addi against lbl_80472ED8@ha/@l with member offsets 0x6BC..0x6C8; base resolves through anchor ...bss.0@ha/@l with offsets 0x9F8..0xA04 (delta 0x33C). Post-link bytes match (permuter scorer agrees) but report.json fuzzy treats the symbol pair as inequivalent. TU gmregclear.c already has 2 sibling functions logged stuck for the same structural blocker, indicating the BSS layout / split for lbl_80472ED8 needs adjustment at the symbols/data-split level, not the C source.
- **Tried:** Verified C source is semantically correct (5 plain field stores into lbl_80472ED8). diff shows only @ha/@l symbol-name mismatches against ...bss.0 anchor with constant 0x33C offset delta -- pure reloc-naming difference, no codegen room to maneuver in 9 instructions.
- **Likely fix:** TU-wide BSS anchor/split fix on lbl_80472ED8 (symbols.txt / splits.txt) so the linker resolves it as the named symbol rather than via the bss.0 anchor. Same fix should unblock the 2 sibling functions in gmregclear.c.
