---
function: ftCo_800C7434
tu: src/melee/ft/ft_0C31.c
headline: rodata-typing + permuter-false-positive
tags: [rodata-typing, permuter-false-positive, regalloc]
---
## ftCo_800C7434 (`src/melee/ft/ft_0C31.c`) — rodata-typing + permuter-false-positive

- **Tags:** `rodata-typing`, `permuter-false-positive`, `regalloc`
- **Best fuzzy:** 95.8333%
- **Diagnosis:** Target asm uses 'bne X; b Y' branch pair pattern (with extra 'b') instead of fall-through 'beq Y'. Float consts also reference named sda21 globals (ftCo_804D8E18, ftCo_804D8E1C) instead of anonymous literal pool entries (@206/@207). Other agent already inlined inlineC2 manually; reverting if/else (variant 2) made it worse (8 mismatches), switch+case (variant 1) made it worst (13). The named-vs-anonymous float globals suggest the TU originally had explicit named float globals declared at file scope, possibly forcing different rodata pool. Branch shape may follow from the float load/use ordering or unrelated code shape elsewhere in TU.
- **Tried:** variant1: switch(kind){case GKOOPS:inlineC2;default:inlineB2}; variant2: if(kind==GKOOPS){...changeMotion...}else{inlineB2}; both worsened mismatches
- **Likely fix:** Likely needs file-scope static const float definitions matching ftCo_804D8E18/1C values (probably 0.0f and 1.0f) to force named-symbol references, OR a different cross-TU layout where these named globals are referenced elsewhere. The bne;b pattern may resolve once the float constants land at named sda21 anchors. Permuter false-positive class: it'll score equivalent but report.json fuzzy won't match.
