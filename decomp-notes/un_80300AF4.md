---
function: un_80300AF4
tu: src/melee/if/soundtest.c
headline: tu-data-osreport + cross-tu-globals
tags: [tu-data-osreport, cross-tu-globals, paired-siblings]
---
## un_80300AF4 (`src/melee/if/soundtest.c`) — tu-data-osreport + cross-tu-globals

- **Tags:** `tu-data-osreport`, `cross-tu-globals`, `paired-siblings`
- **Best fuzzy:** 74.28%
- **Diagnosis:** Fuzzy 74.28%, strict 73.28%, 15 mismatches on a 25-instr function. 5 ARG_MISMATCH on un_803FA258 store offsets + prologue/epilogue divergence (r31 saved/restored, stack 0x18 vs 0x8). Target stores via un_803FA258@ha/@l directly: 'stw r0,0x8(r4)' '0x10(r4)' '0x28/0x2C/0x30(r4)'. Base hoists '...bss.0@l' into r31 and stores at 0x230/0x238/0x250/0x254/0x258(r31). 0x230-0x8 = 0x228 offset means the bss.0 anchor sits at un_803FA258-0x228 = 0x803FA030. Note the existing soundtest.c declarations have un_803FA128 with declared size 0x228 but next symbol un_803FA258 is only 0x130 later, so current static struct sizes overlap — the TU's BSS layout is wrong. Function source (if-arg-eq-1/store/call sequence) is already correct; divergence is purely mwcc's data-pool anchor choice driven by TU-wide BSS layout.
- **Tried:** Diff inspection only; no source-shape attempts. Permuter cannot bridge data-pool anchor symbol divergence per existing notes for it_80274DAC, mpGetSpeed, Exception_ReportStackTrace.
- **Likely fix:** TU-wide reconstruction of soundtest.c .bss/.data declarations into a unified pool symbol so mwcc picks ...bss.0 as the multi-store anchor and hoists base into r31. Sibling un_80300B58 at same fuzzy 74.3% has identical store pattern (x8/x10/x28/x2C/x30 on un_803FA258, only constants differ) — same TU-wide fix resolves both simultaneously. Multi-symbol cross-function refactor; beyond single-function scope.
