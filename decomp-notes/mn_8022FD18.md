---
function: mn_8022FD18
tu: src/melee/mn/mnmainrule.c
headline: permuter-plateau + permuter-dispatched
tags: [permuter-plateau, permuter-dispatched, regalloc, instruction-scheduling, frame-size, sdata2-named]
---
## mn_8022FD18 (`src/melee/mn/mnmainrule.c`) — permuter-plateau + permuter-dispatched

- **Tags:** `permuter-plateau`, `permuter-dispatched`, `regalloc`, `instruction-scheduling`, `frame-size`, `sdata2-named`
- **Best fuzzy:** (unknown)
- **Diagnosis:** Function at 89.42% / 54 mismatches. Logic decoded correctly: u8 arg0 selects between two paths. mn_804D6BD0->user_data cast as mn_8022FB88_t* (the slots[] struct). Reads three sdata2 constants: u32 mn_804DBE08=0x02030405, u16 mn_804DBE04=0x0708, u8 mn_804DBE0C=6. Stashes to stack sp+0xC u32, sp+0x10 u8 (trail), sp+0x14 u16; the 5-iter loop over &indices_b reads bytes 0..4 where byte 4 lives in trail u8 at sp+0x10. arg0!=0: SetFlagsAll slots[7,8] then ClearFlagsAll slots[2..6] then mn_8022FB88(((u8*)data)[3], data). arg0==0: ClearFlagsAll slots[7,8] then SetFlagsAll slots[2..6] then HSD_JObjReqAnimAll/AnimAll on slots[7]=(u8)(data[9]/10) and slots[8]=(u8)(data[9]%10). Remaining diff is regalloc + scheduling + frame size: target keeps mr r29,r31 as long-lived data alias used at mn_8022FB88 call site, base drops alias uses r31; loop iterators target r27+r28 vs base r29+r30; target frame 0x48 (locals 0x2C) vs base 0x40 (locals 0x24) — 8-byte diff tied to keeping trail u8 allocated at sp+0x10 (mwcc DCEs trail because loop only nominally points at &indices_b and compiler does not model that 5 iterations cross trail storage).
- **Tried:** 1) initial draft with separate u32+u8+u16 locals: pad_ DCE'd because compiler does not see read past indices_b end; got 87.74% / 52 mismatches. 2) restructured with struct {u32 b; u8 trail;} buf to force layout: trail still DCE'd since loop reads (u8*)&buf.b not &buf.trail; got 89.42% / 54 mismatches. Both versions reproduce regalloc/scheduling pattern of mn_8022FB88 sibling exactly.
- **Likely fix:** Permuter for register allocation (r27/r28 vs r29/r30 iterator choice, r29 alias for r31/data) and instruction scheduling (mr r29,r31 placement). Frame-size delta likely resolves once trail variable goes live — try: write *((u8*)&indices_b + 4) = mn_804DBE0C; (memory store mwcc must emit), or read trail back at loop-end so it is observed-live. Direct sibling mn_8022FB88 (4-byte indices no trail) hit similar plateau at 92.45% per decomp-note — same TU same pattern also queued for permuter.

