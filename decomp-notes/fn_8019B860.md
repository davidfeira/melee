---
function: fn_8019B860
tu: src/melee/gm/gm_18A5.c
headline: regalloc + permuter-plateau
tags: [regalloc, permuter-plateau]
---
## fn_8019B860 (`src/melee/gm/gm_18A5.c`) — regalloc + permuter-plateau

- **Tags:** `regalloc`, `permuter-plateau`
- **Best fuzzy:** 92%
- **Diagnosis:** 2-instruction scheduling diff: target hoists 'lbz r4, 0x2c(r31)' (load tm->x2C, u8) BEFORE the deref chain 'lwz r3, 0(r3); lwz r3, 0x10(r3)' for fn_8018E85C's first arg, base places it after. Both lbz instructions are identical, just position differs. Permuter already plateaued at score=60.
- **Tried:** (s32) cast on tm->x2C in both calls; hoisting lbl_804D6670->models[4] to a local DynamicModelDesc* temp before the call. Both produced identical diff to original (no scheduling change).
- **Likely fix:** Pure instruction-scheduling difference — permuter territory, but already plateaued. May need different temp lifetime: try splitting the chain differently or using a SceneDesc* local for lbl_804D6670 itself. Alternatively the function might need TmData* x2C field type adjusted, or fn_8018E85C signature widened (currently s32). Sister call site at line 7299 in same TU has identical pattern and matches — diff their containing fns to see what's different.
