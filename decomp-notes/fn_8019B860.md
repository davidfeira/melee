---
function: fn_8019B860
tu: src/melee/gm/gm_18A5.c
headline: instruction-scheduling + permuter-plateau
tags: [instruction-scheduling, permuter-plateau]
---
## fn_8019B860 (`src/melee/gm/gm_18A5.c`) — instruction-scheduling + permuter-plateau

- **Tags:** `instruction-scheduling`, `permuter-plateau`
- **Best fuzzy:** 92%
- **Diagnosis:** 2-instruction scheduling diff remains after 2 manual attempts: target wants 'lfs f1, lbl_804DA810@sda21' BEFORE 'lbz r4, 0x2c(r31)' in fn_8018E618 argument setup; our compiler always emits lbz before lfs. Both are independent loads so either order is valid. Attempts tried: (1) pointer cast ((u8*)tm)[0x2C] - worsened to 79%; (2) f32 local flt=lbl_804DA810 before call - same. Prior session also tried (s32) cast and hoisting lbl_804D6670->models[4] to DynamicModelDesc* local. Upstream has identical source and also does not 100% match in our build environment. Permuter previously dispatched and plateaued at score=60.
- **Tried:** Attempt 1: ((u8*)tm)[0x2C] pointer cast - broke regalloc to 79%. Attempt 2: f32 flt=lbl_804DA810 local in block scope - same 92%. Prior: (s32) cast on tm->x2C, hoisting model arg to DynamicModelDesc* local.
- **Likely fix:** This appears to be a genuine MWCC instruction scheduler non-determinism for independent SDA float load vs r31-relative byte load. No C-level expression change can force the ordering without changing the function shape. May be unsolvable without a different MWCC version or build flags. Could also be that this function genuinely requires a #pragma optimization hint.

