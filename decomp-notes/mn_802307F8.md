---
function: mn_802307F8
tu: src/melee/mn/mnmainrule.c
headline: regalloc + sdata2-float
tags: [regalloc, sdata2-float, permuter-false-positive, permuter-territory]
---
## mn_802307F8 (`src/melee/mn/mnmainrule.c`) — regalloc + sdata2-float

- **Tags:** `regalloc`, `sdata2-float`, `permuter-false-positive`, `permuter-territory`
- **Best fuzzy:** 85.8871%
- **Diagnosis:** Recovered from 85.4% to 95.81% (same as prior best) by adding a separate u8 disp local instead of mutating the index parameter. Remaining 11 mismatches: 5 in the else-branch address calc (mulli r4 vs r0, and operand order in the two add instructions — permuter territory) and 6 sdata2 float sda21 reloc-symbol mismatches (permuter-false-positive). The else-branch should emit mulli r4,r29,0x5 BEFORE the lis/addi base-address load, but mwcc always schedules mulli after lis/addi regardless of C source order.
- **Tried:** (1) Rewrote to use u8 disp local assigned in each branch instead of mutating index param — jumped from 85.4% to 95.81%. (2) Tried both 'mode*5+index' and 'index+mode*5' source orderings for the else branch — identical asm in both cases. Prior agent also tried inlined 'mn_803EC818[index + mode*5]' and 'u8* p = &mn_803EC818[index]; var = p[mode*5]' — all produce same mulli placement.
- **Likely fix:** Permuter on the 5-instruction else-branch regalloc cluster. May also try a s32 temp before the array indexing: 's32 off = index + mode*5; disp = mn_803EC818[off];' (untried in this session) to see if that forces mulli before lis/addi. The 6 sdata2-float mismatches are reloc-symbol-false-positive and will not affect post-link correctness.

