---
function: mn_802307F8
tu: src/melee/mn/mnmainrule.c
headline: regalloc + permuter-false-positive
tags: [regalloc, permuter-false-positive]
---
## mn_802307F8 (`src/melee/mn/mnmainrule.c`) — regalloc + permuter-false-positive

- **Tags:** `regalloc`, `permuter-false-positive`
- **Best fuzzy:** 96.2903%
- **Diagnosis:** mnmainrule.c TU. Real diff is regalloc on the 'mn_803EC818[index + mode*5]' address calc: target emits mulli into r4 first then 'base = mn_803EC818@l + mode*5; base + index', base wants 'base = mn_803EC818@l + index; base + mode*5'. The 6 lfs sda21 mismatches are permuter-false-positive (named global vs anchor literal class). Function reads u8 from mn_803EC818[]; current source (prior agent) writes 's32 off = mode*5; index = mn_803EC818[off+index]' assigning back into 'index' parameter (later cast to u8). Fuzzy 96.29%, strict 95.80%, 11 mismatches (3 real regalloc + 6 sda21 fp + 2 collateral).
- **Tried:** (1) inlined to 'mn_803EC818[index + mode*5]' with separate u8 var; (2) explicit 'u8* p = &mn_803EC818[index]; var = p[mode*5]'. Both produced identical asm to prior baseline (11 mismatches, no drop). mwcc strength-reduces mode*5 to a register early regardless of source shape; the sub-expression evaluation order in the address computation isn't being controlled by the C source.
- **Likely fix:** The lfs @sda21 anchor class makes this likely a TU-data layout issue (HSD_SisLib_803A5ACC's float literals living in mnmainrule rodata vs HSD_SisLib's). Address-calc regalloc may resolve once the float-literal class settles. Worth permuter dispatch on regalloc alone, but only after sda21 false-positives are ruled out by inspecting sibling functions in TU. Could also try: declare 'index' as parameter with const, or reorder mode*5 evaluation via an early temp 's32 idx = index + mode*5; var = mn_803EC818[idx];' (untried).
