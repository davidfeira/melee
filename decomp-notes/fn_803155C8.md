---
function: fn_803155C8
tu: src/melee/ty/tyfigupon.c
headline: permuter-false-positive + tu-wide-data
tags: [permuter-false-positive, tu-wide-data, regalloc]
---
## fn_803155C8 (`src/melee/ty/tyfigupon.c`) — permuter-false-positive + tu-wide-data

- **Tags:** `permuter-false-positive`, `tu-wide-data`, `regalloc`
- **Best fuzzy:** 99.4217%
- **Diagnosis:** 38 mismatches at fuzzy 99.42% / strict 99.12%: 24 permuter-false-positive (sda21 anchor mismatches: un_804DDEB0/EBC/EC4/ED0/EDC/EE0/EE4/EF4 named in target vs anonymous @N@sda21 in base; un_804DDEA8 lfd anchor; jumptable_803FEAF0 vs anonymous @497) + 10 regalloc (target keeps sc/r27 swapping with r28 in case-6 inv block at lbz/extsb/cmpwi/subi/mulli sequence around offset 3632-3692; comes from lifetime of ef4->x5D vs new_var) + 4 real (case-6 init triple at 3816-3824: target li r31,0; addi r28,r3,0; addi r27,r31,0 vs base addi r28,r3,0; li r31,0; li r27,0 -- target reuses r31's zero for r27 init via addi instead of li). Per agent protocol, sda21 anchor false-positives are not permuter territory; scorer treats them as equivalent. TU tyfigupon.c has 2 prior stuck siblings indicating TU-wide-data blocker.
- **Tried:** V1=for (i = total; i < 9; ...) chained init -- mwcc DCE'd the chain, no change in mismatch count (38 -> 38, identical diff). Reverted.
- **Likely fix:** Two parts: (a) tu-wide-data: declare named sdata2 globals un_804DDEB0/EBC/EC4/ED0/EDC/EE0/EE4/EF4 + un_804DDEA8 (lfd) + un_804DDEF4 with appropriate float values so mwcc anchors loads via named symbol instead of @N pool entry. Same pattern as it_80274DAC (it_2725.c) and Exception_ReportStackTrace. (b) regalloc swap at the case-6 init triple: try writing as 'i = total = 0' chained assignment as a separate statement BEFORE the for-loop (not inside for-init clause where DCE eats it), e.g. 'total = 0; i = total;' or 'i = total = 0;' as discrete statements. The target's addi r27, r31, 0 form suggests mwcc fwd-prop'd the zero from r31 (total) -- probably needs total decl in inner scope so its lifetime doesn't extend across the outer switch's other registers. Also: jumptable_803FEAF0 is a switch jumptable (likely the outer case 5/6/8 jumptable) -- should auto-name once layout is right.
