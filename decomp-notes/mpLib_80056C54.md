---
function: mpLib_80056C54
tu: src/melee/mp/mplib.c
headline: permuter-false-positive + tu-data-osreport
tags: [permuter-false-positive, tu-data-osreport, float-literal, frame-size]
---
## mpLib_80056C54 (`src/melee/mp/mplib.c`) — permuter-false-positive + tu-data-osreport

- **Tags:** `permuter-false-positive`, `tu-data-osreport`, `float-literal`, `frame-size`
- **Best fuzzy:** 99.934%
- **Diagnosis:** Diff at 99.744%/99.934% fuzzy is dominated by reloc-symbol/data-anchor mismatches: (1) OSReport debug-string lookups appear as 'addi r3, r31, 0x20d8' (target, anchored to mpLib_803BD3D8 + 0x20D8) vs 'addi r3, r31, 0x30' (base, anchored to a different lbl_X + 0x30) -- TU-wide data symbol naming difference, classic tu-data-osreport pattern. (2) sda21 float literals named mpLib_804D8050/8068/8070/8020 in target vs anonymous @415/@839/@840/@295 in base -- float-literal naming. (3) Frame size differs by 8 bytes (-0xd0 target vs -0xc8 base), cascading into all stmw/stfd/stwu offsets and stack-slot positions (sp0x34/0x44, sp0x30/0x3c) -- frame-size symptom downstream of (1)+(2). Post-link bytes are equivalent but report.json fuzzy treats them as different.
- **Tried:** prep + diff inspection only; no source-shape attempts since pattern is unambiguous reloc-symbol noise per Permuter Boundary rule 7.
- **Likely fix:** Requires either (a) consolidating mplib.c data/rodata symbol layout so OSReport strings and sdata2 float literals match the base, or (b) accepting as a known false-positive class. NOT permuter territory -- scorer would treat as 100% but report.json wouldn't agree.
