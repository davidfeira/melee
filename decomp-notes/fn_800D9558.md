---
function: fn_800D9558
tu: src/melee/ft/chara/ftCommon/ftCo_Attack100.c
headline: permuter-territory + regalloc
tags: [permuter-territory, regalloc, frame-size]
---
## fn_800D9558 (`src/melee/ft/chara/ftCommon/ftCo_Attack100.c`) — permuter-territory + regalloc

- **Tags:** `permuter-territory`, `regalloc`, `frame-size`
- **Best fuzzy:** (unknown)
- **Diagnosis:** Source-shape match at 96.98% (50 mismatches, 246 instr). Function is the Samus-grapple Catch_Anim helper, structurally analogous to ftCo_AirCatch_Anim's FTKIND_SAMUS branch but operating on mv.co.catch.x0 instead of mv.co.aircatch.x0; ftSs_DatAttrs.x9C/xA4/xA8 retyped from UNK_T to s32 (frame-counter timers compared against a float via int->double->float idiom). PAD_STACK(24) needed because target reserves an extra 24 bytes between stack-low and locals (likely the original C uses additional unused locals or struct alignment padding). Remaining mismatches are pure regalloc/scheduling: r29-vs-r3 split for tether_gobj, r24-vs-r25 for tether_ip, r23-vs-r25 for the loop counter, f29/f30 swap for the 0.5f and 4.0 constants, and a stack-slot ordering shift where Vec3 sp lands at 0x38 instead of 0x34 (24 bytes too low). All of these are classic permuter wins.
- **Tried:** (1) Wrote function from AirCatch analogue, retyped ftSs_DatAttrs fields s32, hoisted Vec3 pos/sp/vel to function scope. (2) Added PAD_STACK(24) which fixed the prolog frame size from 0x88 to 0xa0 and re-aligned the f29/f30 spill area. (3) Tried introducing f32 mtx[xyz] temps to mirror m2c's temp_f0 pattern but it scored worse (91.7%) so reverted. Source semantics confirmed by reading ftCo_AirCatch.c (FTKIND_SAMUS branch around line 198-252) and ItemLink->gobj->hsd_obj chain through ip->xDD4_itemVar.samusgrapple.x0.
- **Likely fix:** Permuter on local var declaration order, single-temp insertion patterns around the HSD_Randf calls, and small constant hoisting. The 24-byte frame surplus probably comes from one or two extra unused Vec3/Point3d locals in the original source (m2c reports sp40,sp3C,sp38,sp34,sp30,sp2C,sp28 — could be up to 4 Vec3s if the original had a redundant Vec3 zero); permuter could also try replacing PAD_STACK with named locals.

