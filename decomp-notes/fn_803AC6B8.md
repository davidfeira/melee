---
function: fn_803AC6B8
tu: src/sysdolphin/baselib/hsd_3AA7.c
headline: permuter-queued + regalloc
tags: [permuter-queued, regalloc, permuter-territory, header-required]
---
## fn_803AC6B8 (`src/sysdolphin/baselib/hsd_3AA7.c`) — permuter-queued + regalloc

- **Tags:** `permuter-queued`, `regalloc`, `permuter-territory`, `header-required`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 93.75% match (16 mismatches). Function sums per-file sector counts: handles file 0 specially (uses sector_size minus boundary offset), files 1..n-1 use simple ceil-div. Inlined fn_803AC634-style logic. Remaining diff is permuter territory: (a) compiler emits a dead bgt/li r5,0/b fallback before computing the i=0 result that my source's if-else chain doesn't reproduce; (b) ~13 regalloc swaps r5/r6/r8 in two near-identical inline sector blocks; (c) +1 ordering: target does 'addi r5,r5,1; addi r9,r5,0' vs base 'addi r9,r5,1' (compiler keeps result in r5 then copies). Required header change: hsd_3AA7.h prototype updated from UNK_RET/UNK_PARAMS to s32 fn_803AC6B8(struct hsd_803AC3E0_arg0_t*, s32).
- **Tried:** Two source-shape attempts: (1) flat if/else with shared sector_size/remaining/usable locals scoped to function -> 93.75%/16; (2) nested-block scoped locals to mirror m2c starter -> 88.36%/49 (regressed). Reverted to (1). Permuter offline so cannot dispatch.
- **Likely fix:** Permuter should crack regalloc swaps and find the dead-branch shape (likely 'if (>0) { if (==0) x=0; else compute; } total=x;' which leaves an unreachable but compiled fallback).

