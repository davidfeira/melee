---
function: fn_801AA854
tu: src/melee/gm/gmstaffroll.c
headline: permuter-false-positive + tu-wide-data
tags: [permuter-false-positive, tu-wide-data, rodata-typing]
---
## fn_801AA854 (`src/melee/gm/gmstaffroll.c`) — permuter-false-positive + tu-wide-data

- **Tags:** `permuter-false-positive`, `tu-wide-data`, `rodata-typing`
- **Best fuzzy:** 99.9487%
- **Diagnosis:** All 7 mismatches are reloc/arg only - no instruction-level diffs. (1) lfs f31 references named sdata2 float gm_804DAAD0 in target vs anonymous @214 in our build. The whole TU has many named gm_804DAAD0..gm_804DAB80 sdata2 floats in target but only gm_804DAAEC is declared in our source (as s32). (2) Six __assert string-anchor offsets all shift by uniform 0x1D8 bytes (target r31+0x1150 vs base r31+0xf78, etc). This is a TU-wide string pool layout difference - strings emitted earlier in this TU are smaller in our build by 0x1D8 bytes. Both classes are post-link byte-identical territory but report.json fuzzy treats them as diffs.
- **Tried:** prep + diff inspection. Verified all 7 mismatches are DIFF_ARG_MISMATCH, no opcode/regalloc differences. Confirmed sdata2 named-float pattern is consistent across many functions in this TU (gm_804DAAD0/D4/D8/DC/E0/E4/E8/EC/F0/F8/B00/B08/B0C/B10/B80 all in target asm but undeclared in src/melee/gm/gmstaffroll.c).
- **Likely fix:** Declare extern f32 gm_804DAAD0 (and probably the whole gm_804DAAD0..gm_804DAB80 sdata2 float pool) in gmstaffroll.c so 0.0f literal at line 188 routes to the named global. For the string-anchor delta, fix string layout by porting earlier functions in the TU that have shorter literal strings or differently-formatted assert/printf messages totaling 0x1D8 bytes more in target. Both fixes are TU-scope, not single-function scope.
