---
function: grPura_80213250
tu: src/melee/gr/grpura.c
headline: permuter-territory + regalloc
tags: [permuter-territory, regalloc, inlining]
---
## grPura_80213250 (`src/melee/gr/grpura.c`) — permuter-territory + regalloc

- **Tags:** `permuter-territory`, `regalloc`, `inlining`
- **Best fuzzy:** 97.1923%
- **Diagnosis:** [permuter-queued; permuter offline per Mama Claude] Restructured from 52.6% to 97.19% match in 2 attempts. Attempt 1: original source had wrong logic — reused 'jobj' across chunks 1/2/3, missed the (jobj->flags & (JOBJ_PTCL|JOBJ_SPLINE)) == 0 (union_type_dobj) gate, and missed the inner for-loop calling grPura_80213224(iter) plus the dobj->next subprocessing block. Rewrote to match three-chunk pattern (arg0->child, arg0->next, arg0 itself); chunk 3 has additional outer for-loop iterating dobj's siblings calling HSD_MObjCompileTev with redundant inner null check. Attempt 2: added '#pragma dont_inline on/reset' around grPura_80213250 to prevent mwcc -inline auto from inlining the 6-byte grPura_80213224 at the call sites (which would replace 'bl grPura_80213224' with 'bl HSD_MObjCompileTev'). Remaining 29 diffs are all DIFF_ARG_MISMATCH (register allocation: chunk 2 target uses r30 for jobj where mine ends up with different reg distribution) plus 2 DIFF_INSERTs. Pure permuter territory — semantics are right, only reg choices and minor scheduling. NB: tools/extract_asm_for_permuter.py was missing on master so prep/diff commands were broken; recovered using build/tools/dtk.exe disasm + objdiff-cli.exe diff directly. Func size: 520 (mine) vs 552 (target).

