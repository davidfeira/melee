---
function: ftCo_80090594
tu: src/melee/ft/chara/ftCommon/ftCo_DamageFall.c
headline: regalloc + permuter-plateau
tags: [regalloc, permuter-plateau, r30-r31-swap]
---
## ftCo_80090594 (`src/melee/ft/chara/ftCommon/ftCo_DamageFall.c`) — regalloc + permuter-plateau

- **Tags:** `regalloc`, `permuter-plateau`, `r30-r31-swap`
- **Best fuzzy:** 91.8254%
- **Diagnosis:** Regalloc mismatch: target assigns fp(r3)→r31 (highest callee-save), element(r4)→r28, ground_or_air(r7)→r29, p_ftCommonData→r30; but base assigns fp(r3)→r28 (lowest callee-save), element(r4)→r29, ground_or_air(r7)→r30, p_ftCommonData→r31. Frame size 0x40 matches only when calcShift helper is kept (not inlined). Both attempts worsened the baseline 91.8%: m2c nested-if structure (89.9%, 40 mismatches) and early-return with inline fcp (88.1%, 42 mismatches).
- **Tried:** Attempt 1: replaced with m2c nested-if structure using ftCommonData* temp_r30 at top and (u16)(s32) casts - gave 89.9% (40 mismatches). Attempt 2: early-return form with inline fcp local inside block - gave 88.1% (42 mismatches). Neither changed the fundamental regalloc ordering.
- **Likely fix:** The calcShift helper function (which reorders the args as hitlag_mul,msid,dmg vs original dmg,msid,hitlag_mul) is the key variable. But even with it, regalloc is still off. Need to investigate whether the argument type of element (enum_t vs s32 vs int) or ground_or_air type affects when the compiler decides to allocate callee-save regs. Also try placing fp use BEFORE element use in the earliest code path - e.g., check fp->motion_id BEFORE checking element - to make fp live longest.

