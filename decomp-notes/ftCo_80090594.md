---
function: ftCo_80090594
tu: src/melee/ft/chara/ftCommon/ftCo_DamageFall.c
headline: regalloc + permuter-plateau
tags: [regalloc, permuter-plateau]
---
## ftCo_80090594 (`src/melee/ft/chara/ftCommon/ftCo_DamageFall.c`) — regalloc + permuter-plateau

- **Tags:** `regalloc`, `permuter-plateau`
- **Best fuzzy:** 91.8254%
- **Diagnosis:** Permuter plateaued at 1045 after 50+ outputs. 26 mismatches, 22 regalloc + 4 real. Issue: base allocates fp->r28 (last saved reg, by ascending-arg-index order: r3->r28, r4->r29, r7->r30, p_ftCommonData->r31), target allocates fp->r31 (first saved reg, by first-use order). Frame size 0x40 matches in both. p_ftCommonData ends up in r31 (base) vs r30 (target).
- **Tried:** V1: reordered if/else branches (else-if GA_Air to !=GA_Air with normal stores in middle) - made worse (85%, 30 mismatches). V2: inlined calcShift - made worse (91.6%, 38 mismatches) - confirmed calcShift is NOT inlined in base (frame-size diff). Permuter outputs 1045-1395 are all variable-aliasing noise, none reach 100%.
- **Likely fix:** Need a way to coax mwcc into ascending-arg-index saved-reg allocation. Possibly an early local var that takes the arg in arg order, or an explicit copy block. Could also be that one of the arg types is wrong (e.g. enum_t vs int width affecting reg allocation). Worth checking whether element/dmg/msid are all 32-bit ints in base; or if a struct-by-value param shape changes things.
