---
function: grHeal_8021F180
tu: src/melee/gr/grheal.c
headline: permuter-queued + frame-size
tags: [permuter-queued, frame-size, mwcc-loop-opt, regalloc]
---
## grHeal_8021F180 (`src/melee/gr/grheal.c`) — permuter-queued + frame-size

- **Tags:** `permuter-queued`, `frame-size`, `mwcc-loop-opt`, `regalloc`
- **Best fuzzy:** 82.2934%
- **Diagnosis:** Reaches 81.88% (112 mismatches) with straightforward C: gp/gobj/sp94 vec, sp90[8] byte buf, sp84/sp88/sp8C jobjs, sp18[0x1A] jobjs, count counter. Two structural blockers: (1) stack frame is 0xc0 in target but 0xb0 in base - missing 0x10 of local storage; (2) target inlines an 8x-unrolled byte copy loop reading bytes 0x96..0x9D of gm_80473A18 into sp90 with a tail loop that re-reads gm_80473A18._94[1]. Base produces flat byte loop. (3) Reg alloc shifted (target r29/r30 for gobj/gp; base r30/r31).
- **Tried:** 1) Straight loop: count=0; while (count<_94[1]) sp90[count]=src[0x96+count]; count++; -> 81.88% / 112 mismatches. 2) Manual 8x unroll with do-while + pointer-stride srcptr/dstptr -> 66.80% (worse, mwcc emitted different prologue and reordered). Reverted to attempt 1.
- **Likely fix:** The 8x-unrolled byte copy is the suspect - likely needs pointer-stride form OR a manual chunk-loop form mwcc auto-unrolls. Could also need a struct/union shape on sp90 (asm shows sp94 Vec3 and sp90 byte buf overlap - Vec3 at 0x94, bytes at 0x90). Stack-frame size suggests an extra local (16 bytes) we are missing - perhaps the byte copy actually targets a 16-byte buffer.

