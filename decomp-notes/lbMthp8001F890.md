---
function: lbMthp8001F890
tu: src/melee/lb/lbmthp.c
headline: permuter-false-positive + bss-anchor
tags: [permuter-false-positive, bss-anchor]
---
## lbMthp8001F890 (`src/melee/lb/lbmthp.c`) — permuter-false-positive + bss-anchor

- **Tags:** `permuter-false-positive`, `bss-anchor`
- **Best fuzzy:** 99.579%
- **Diagnosis:** 19 mismatches all reloc-symbol-only: target uses lbl_804335B8@ha/@l with offsets 0x70-0x90, base uses ...bss.0@ha/@l with offsets 0x248-0x268 (delta 0x1D8). Same post-link address, different reloc form. Fuzzy reports 99.58%; permuter scorer would consider this a match (post-link bytes identical) so DO NOT dispatch. mwcc bundles lbl_804335B8 with preceding BSS objects (~0x1D8 of pad/objects ahead) into anonymous bss.0. Original ELF emits lbl_804335B8 as named .bss object.
- **Tried:** prep + diff + fuzzy diff inspection. No source change attempted: classification is BSS-layout/reloc-symbol class, not codegen. TU lbmthp.c already has 2 logged stuck siblings (lbMthp_8001F624, lbMthp_8001F4F8?) likely same blocker.
- **Likely fix:** Split lbl_804335B8 from its BSS neighbors so mwcc emits it in its own per-symbol BSS section. Approaches: (a) reorder declarations in lbmthp.static.h so lbl_804335B8 isn't clustered with the prior 0x1D8 of objects, (b) introduce alignment/size attribute to break clustering, (c) use #pragma section split. Score is already effectively a match; cosmetic only — defer unless TU-wide fix lands.
