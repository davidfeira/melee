---
function: fn_8003F294
tu: src/melee/pl/plbonuslib.c
headline: regalloc + mwcc-aliasing
tags: [regalloc, mwcc-aliasing]
---
## fn_8003F294 (`src/melee/pl/plbonuslib.c`) — regalloc + mwcc-aliasing

- **Tags:** `regalloc`, `mwcc-aliasing`
- **Best fuzzy:** (unknown)
- **Diagnosis:** mwcc caches pl_804D6470 base ptr in callee-saved r29 across multiple pl_8003906C calls, while goal reloads it into r4 (caller-saved) after each bl. Result: my code uses 4 saved regs (r28-r31, frame 0x20) vs goal 3 saved regs (r29-r31, frame 0x38 with extra stack space for double<->int conversion). Diff stuck at 78% with ~120 register-allocation mismatches that propagate through every pl_8003906C call site.
- **Tried:** (1) Initial impl with sqrtf intrinsic - inlined to frsqrte. (2) Switched to sqrtf__Ff direct call - matches sqrt block. (3) Used *(float*)&pl_804D6470->x8C to make x8C float - matches x8C usage. None of these stop mwcc caching pl_804D6470. Tried (table = pl_804D6470)->fields trick to force re-evaluation - didn't help.
- **Likely fix:** Either (a) struct field x8C (and possibly x88) should be float in types.h (struct currently has them as unsigned int, the goal treats x8C as float). (b) Source pattern that forces mwcc to NOT cache pl_804D6470 in callee-saved - perhaps using pl_80038914() accessor function instead of direct pl_804D6470 dereference, or volatile somewhere. (c) Goal has 0x38 frame with int->float conversion via stack (lis 0x4330 trick) which mine doesn't have - perhaps a spilled double argument is needed in source.
