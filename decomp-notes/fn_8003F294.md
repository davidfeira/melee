---
function: fn_8003F294
tu: src/melee/pl/plbonuslib.c
headline: permuter-queued + regalloc
tags: [permuter-queued, regalloc, mwcc-aliasing, sda21]
---
## fn_8003F294 (`src/melee/pl/plbonuslib.c`) — permuter-queued + regalloc

- **Tags:** `permuter-queued`, `regalloc`, `mwcc-aliasing`, `sda21`
- **Best fuzzy:** 77.7176%
- **Diagnosis:** mwcc allocates 4 callee-saved regs (r28-r31, frame 0x20) and caches pl_804D6470 base pointer in r29 across all 9 pl_8003906C calls, while target uses 3 callee-saved regs (r29-r31, frame 0x38) and reloads pl_804D6470 fresh from sda21 (lwz r4, pl_804D6470@sda21(r0)) after every bl. The compiler's CSE/regalloc decision to hoist the global pointer into a callee-saved reg cascades into a full register reassignment: target r30=arg0/r31=stalemove vs base r31=arg0/r30=stalemove. Stuck at 77.71% with 124 mismatches; structural mwcc allocation choice, not register-tweak territory.
- **Tried:** (1) Canonical impl matching m2c (Ptr2->GetEntity->GetActionStats then 9 pl_8003906C calls with direct pl_804D6470->xNN reads). (2) Reordered local-variable decls (no decl-init on first call). (3) Used (unsigned int*) not (u32*) since u32=unsigned long but signature wants unsigned int*. (4) *(float*)&pl_804D6470->x8C for lfs at 0x8C. (5) sqrtf__Ff direct call. Same 124 mismatches each time.
- **Likely fix:** Source pattern that forces mwcc to spill pl_804D6470 reload at each call site instead of caching. Candidates: (a) introduce a local that consumes a callee-saved reg forcing pl_804D6470 to caller-saved. (b) struct layout change to break CSE on the field reads. (c) the (table = pl_804D6470)->fields trick was tried before without success. Permuter is offline per orchestrator; queued for when it returns. 124 mismatches likely beyond simple randomization without source structure variation.

