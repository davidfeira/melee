---
function: ftCo_8009D81C
tu: src/melee/ft/ftdynamics.c
headline: regalloc + frame-size
tags: [regalloc, frame-size, instruction-scheduling]
---
## ftCo_8009D81C (`src/melee/ft/ftdynamics.c`) — regalloc + frame-size

- **Tags:** `regalloc`, `frame-size`, `instruction-scheduling`
- **Best fuzzy:** 92.9385%
- **Diagnosis:** Base (compiled) generates a BoneDynamicsDesc* desc pointer (add r7, r0, r31) and uses 0(r7)/8(r7) to access bone_id and dyn_desc.count. Target does not create this intermediate pointer; instead it uses direct offset loads (lwzx r3, r3, r31 and lwzx r5, r5, r0) from the ftDynamicBones base. Additionally, the target frame is 0x30 while base generates 0x28 (both saving r27-r31), suggesting extra stack spill. Register allocation throughout is shifted by 1 (r6 vs r5, r7 vs r6, etc.). Fixed: desc->bone_id=FtPart_TopN -> fp->dynamic_bone_sets[i].bone_id=0, which is correct.
- **Tried:** 1) Changed desc->bone_id=FtPart_TopN to fp->dynamic_bone_sets[i].bone_id=0 (correct fix, 23->22 mismatches). 2) Removed BoneDynamicsDesc* desc entirely, expanded all accesses inline - made things worse (22->23 mismatches, 91%). Reverted to attempt 1.
- **Likely fix:** Permuter needed: register allocation and instruction scheduling differ throughout. The structural fix (desc->bone_id -> fp->dynamic_bone_sets[i].bone_id=0) is correct; remaining diff is register assignment, ordering, and frame size which are permuter-territory.

