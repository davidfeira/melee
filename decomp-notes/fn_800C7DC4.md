---
function: fn_800C7DC4
tu: src/melee/ft/ft_0C31.c
headline: permuter-territory + regalloc
tags: [permuter-territory, regalloc, stack-offset, frame-size, varargs-cr1eq]
---
## fn_800C7DC4 (`src/melee/ft/ft_0C31.c`) — permuter-territory + regalloc

- **Tags:** `permuter-territory`, `regalloc`, `stack-offset`, `frame-size`, `varargs-cr1eq`
- **Best fuzzy:** 82.4884%
- **Diagnosis:** Function is structurally correct (DownReflect_Collide-shaped helper, mirrors ftCo_DamageIce_Collide). At 82.3% match, 87 mismatches all reg-alloc + stack-layout: target uses gobj=r31/fp=r30/coll_data=r29 and stack 0x60 with angle at 0x20, next_pos at 0x24-0x2C, vel at 0x30-0x38; mine has gobj=r26/fp=r31/coll_data=r30 and stack 0x58 with different local slot ordering. Body computes next_pos=cur_pos+offset, spawns efAsync x406 using atan2f(-normal->x, normal->y) with crclr cr1eq for varargs, calls Camera_80030E44(2,&next_pos), scales normal by xF0_ground_kb_vel and ftCommonData->x1BC into x8c_kb_vel, zeros self_vel, sets facing_dir from sign of x8c_kb_vel.x, ChangeMotionState(motion_state, 0x18040), then RightWallHug doubled-condition branch (matching DamageIce pattern) updates cur_pos.x or cur_pos.y using x68C_transNPos.z/y, stores kb_vel into dmg.x18A8, calls ftCo_80090574, ftCommon_8007EBAC(7,0), ftColl_8007B760(p_ftCommonData->x1B8). Header decl added to ft_0C31.h.
- **Tried:** Two source-shape attempts: declared locals in different orders (next_pos/vel/angle then angle/next_pos/vel) to nudge stack layout; introduced explicit CollData* coll_data local to keep coll_data pointer alive across uses (matched the addi r30,r31,0x6f0 instruction). Improved match from NOT_IMPLEMENTED baseline to 82.3%. Remaining mismatches are pure register-shift (everything offset by one register) and stack frame size (0x58 vs 0x60).
- **Likely fix:** Permuter for register pressure / stack layout. Possibly try declaring an explicit early-use variable to make MWCC pin gobj into r31 instead of r26, or add a 4-byte unused local to bump frame to 0x60. Permuter scrambles register allocation effectively for this class.

