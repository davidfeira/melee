---
function: fn_80169C54
tu: src/melee/gm/gm_1601.c
headline: permuter-territory + frame-size
tags: [permuter-territory, frame-size, regalloc, scheduler, mwcc-loop-opt]
---
## fn_80169C54 (`src/melee/gm/gm_1601.c`) — permuter-territory + frame-size

- **Tags:** `permuter-territory`, `frame-size`, `regalloc`, `scheduler`, `mwcc-loop-opt`
- **Best fuzzy:** (unknown)
- **Diagnosis:** At 75.8% (127 mismatches). Source-shape decompilation captures the algorithm correctly: outer 3-iter loop scanning gp->x0..x2 for value 4, then either copying gp->xC (if gp->xB) or filling colors[0..ncolors-1] via gm_80169238(4) using an unrolled-by-8 inner ramp, optionally appending arg1 if arg0==4, then for each of the 3 ports + arg0, calling Player_800325C8 with port-0 and port-1 then Player_80031DA8 over each color. Two structural mismatches remain: (a) MWCC stack-frame size differs (0x58 vs my 0x48) - target reserves an extra 0xC bytes of spill area below the s32 colors[7] array at offsets 0x18..0x30, suggesting register pressure forces extra spills the source-shape variant doesn't generate; (b) target lifts li r0,0x3 / mtctr r0 to BEFORE the colors[] init, then uses stw r4,0xN(r1) (with r4=-1) to fill the array, while my source generates stw r0,0xN with r0=-1 and computes the loop count later. Both are register/scheduling/spill choices the permuter would explore, not algorithmic mistakes.
- **Tried:** Two source-shape attempts: (1) idiomatic for-loop with array indexing (70.1%, 157 mismatches); (2) moving-pointer formulation matching m2c's var_r30 += 0x20 stride and explicit ctr-based outer loop framing (75.8%, 127 mismatches). Improvements in attempt 2 came from using s32* dst pointer-walk for the unrolled fill, and pre-decrementing count-of-iterations to mirror the bdnz semantics.
- **Likely fix:** Permuter pass focused on (a) reordering the colors[]=-1 inits before the gp = fn_8016AE60() call so MWCC retains r4 in scope, (b) splitting the 3-iter outer loop into a ctr-counted unrolled walk to encourage early li/mtctr emission, (c) trying alternative typings for gp pointer to bias spill choice. Auto-permute deferred per orchestrator override (decomp-permuter offline).

