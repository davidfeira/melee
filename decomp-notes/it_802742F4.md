---
function: it_802742F4
tu: src/melee/it/it_2725.c
headline: regalloc + sdata2-float
tags: [regalloc, sdata2-float, permuter-false-positive, instruction-scheduling]
---
## it_802742F4 (`src/melee/it/it_2725.c`) — regalloc + sdata2-float

- **Tags:** `regalloc`, `sdata2-float`, `permuter-false-positive`, `instruction-scheduling`
- **Best fuzzy:** 97.75%
- **Diagnosis:** 7 mismatches at 97.65%. 2 are sdata2 reloc-anchor false-positives (lfs it_804DC708@sda21 vs @293@sda21, lfd it_804DC720@sda21 vs @740@sda21 — the 0.0F and 0x4330... f64 conversion pool entries). 5 are 'real' regalloc/scheduling around 'item->xDC8_word.flags.xF |= 1 << ftLib_80086BE0(gobj)'. Target order: clrlwi r5,r3,24 / li r0,1 / extrwi r3,r4,4,15 / slw r0,r0,r5 / or r0,r3,r0. Base order: clrlwi r3,r3,24 / li r0,1 / slw r3,r0,r3 / extrwi r0,r4,4,15 / or r0,r0,r3. Difference is whether the slw 1<<count happens before or after the extrwi of the existing field, and which register holds the count/temp.
- **Tried:** (1) Introduced 'flag = ftLib_80086BE0(gobj); item->xDC8_word.flags.xF |= 1 << flag;' — grew stack frame from 0x38 to 0x30 wait actually expanded baseline frame by 8 bytes via flag local consumption (24 mismatches all stack-offset). (2) Rewrote |= as explicit 'x = x | (1<<...)' — produced bit-identical asm to baseline (still 97.65%, same 7 mismatches). The flag and word locals already declared are unused so don't take stack.
- **Likely fix:** Permuter-territory regalloc/scheduling. TU has 7 logged stuck siblings — likely structural blocker affecting whole TU. The 2 sdata2 reloc-anchor mismatches are CLAUDE.md rule 6 false-positives (do NOT dispatch permuter while these dominate). The remaining 5 instruction-pick mismatches likely resolve once the sdata2 pool naming is reconstructed for the TU. Wait on TU-wide TU pool/data anchor reconstruction (same family as other it_2725 stuck entries).
