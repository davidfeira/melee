---
function: fn_80179350
tu: src/melee/gm/gmresultplayer.c
headline: permuter-false-positive + regalloc
tags: [permuter-false-positive, regalloc, sdata2-float]
---
## fn_80179350 (`src/melee/gm/gmresultplayer.c`) — permuter-false-positive + regalloc

- **Tags:** `permuter-false-positive`, `regalloc`, `sdata2-float`
- **Best fuzzy:** 99.9068%
- **Diagnosis:** 6 mismatches: 3 are sdata2 float reloc-symbol false positives (target uses named lbl_804DA378/37C/380 for 10.0f/40.0f/0.0f, base uses anonymous @598/@599/@292 sdata2 pool). Post-link bytes match but report.json fuzzy still flags. The TU has many functions referencing same constants (e.g. fn_80177920 also at 99.97% with same class). Neighboring functions share the false positive — TU-wide pattern. The other 3 mismatches are r27 vs r29 in case 1 of the data->x1 switch: target loads jobj=arg0->hsd_obj into r29 then repurposes r30 for &lbl_8046DBE8. Source already has 'HSD_JObj* jobj = (HSD_JObj*) arg0->hsd_obj' but compiler picks r27.
- **Tried:** prep + diff inspection; reviewed neighboring fn_80177920 diff which has same false-positive class
- **Likely fix:** TU-wide refactor: declare static f32 lbl_804DA378=10.0f; static f32 lbl_804DA37C=40.0f; static f32 lbl_804DA380=0.0f; (and other named .sdata2 floats) in a new gmresultplayer.static.h, then replace 0.0f/10.0f/40.0f literals with those names — affects many functions in the TU. Regalloc r27→r29 may resolve via permuter once false positives are eliminated, OR may need source-shape reorder of case 1 jobj/d locals.
