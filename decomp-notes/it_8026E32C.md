---
function: it_8026E32C
tu: src/melee/it/it_266F.c
headline: regalloc + permuter-plateau
tags: [regalloc, permuter-plateau, paired-siblings]
---
## it_8026E32C (`src/melee/it/it_266F.c`) — regalloc + permuter-plateau

- **Tags:** `regalloc`, `permuter-plateau`, `paired-siblings`
- **Best fuzzy:** 97.3276%
- **Diagnosis:** 28 mismatches all pure regalloc - target uses r27/r28 for item_gobj/arg1 and r29/r30/r31 for item/coll/chk; base is reverse (r30/r31 for args, r27/r28/r29 for locals). Sibling it_8026E7E0 in same TU with nearly-identical shape MATCHES, so the regalloc shift is reachable - difference is E7E0 has no return/no chk2/no it_80276FC4 inner-call path. Permuter plateau at score=5 (chk |= variant) over 210min has not crossed strict-mismatch threshold. Cluster permuter cannot find the structural mutation needed.
- **Tried:** V1 (prior): 'chk |= it_802763E0(item_gobj)' compound assign (matches permuter score-5 candidate) - strict mismatch unchanged (28). V2 (prior): inline the it_8026E32C_inline wrapper directly to it_8026DD5C(item_gobj) - exploded to 58 mismatches at 52% match. V3: reorder local declarations (chk first, item/coll last) - no change, still 28 mismatches. V4: reuse new_var instead of separate chk2 (mirroring sibling it_8026E7E0 pattern) - no change, still 28 mismatches. mwcc regalloc not sensitive to declaration order or chk2/new_var consolidation in this shape.
- **Likely fix:** Compare to matching sibling it_8026E7E0 in same TU: differs by (a) it_8026E7E0 returns void vs E32C returns bool, (b) E7E0 has no it_80276FC4 inner call before the chk&1 block (no early use of new_var), (c) E7E0 calls helper directly not via wrapper. The 'return chk' at end of E32C may be extending chk's live range past the inner block, forcing chk to be a callee-save with longer live range than args - inverting the typical assignment. Try: storing chk into a stack-resident temp before the inner block and returning that temp, OR computing return value early. Permuter strict-mismatch scorer apparently can't reach this without random source-shape help.
