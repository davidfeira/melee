---
function: it_8026E32C
tu: src/melee/it/it_266F.c
headline: regalloc + permuter-plateau
tags: [regalloc, permuter-plateau, paired-siblings, upstream-regression]
---
## it_8026E32C (`src/melee/it/it_266F.c`) — regalloc + permuter-plateau

- **Tags:** `regalloc`, `permuter-plateau`, `paired-siblings`, `upstream-regression`
- **Best fuzzy:** 97.3276%
- **Diagnosis:** Pure regalloc swap (r27/r28 vs r30/r31 for params, r29-r31 vs r27-r29 for locals) persists across all source shapes. Key finding: removing chk2 from function scope (using '&&' short-circuit) DOES fix regalloc (28 mismatches → 0 regalloc mismatches), proving the regalloc is sensitive to having exactly 5 vs 6 callee-save candidates. With 5 variables (item_gobj, arg1, item, coll, chk/new_var), allocator correctly assigns r27-r31. Adding chk2 as 6th variable displaces the ordering. Upstream reports matched with identical source, suggesting build-environment regalloc divergence.
- **Tried:** V5: Removed chk2 variable entirely, used '&&' (it_8026DC24(gobj) && it_8026E32C_inline(gobj)) - fixes regalloc but generates short-circuit code missing 'b + li r3, 0' else pair (3 structural mismatches, 96.47%). V6: removed new_var alias, used chk directly in conditions, kept chk2 with if/else - 31 mismatches (worse). Both attempts confirm chk2 is the 6th callee-save candidate displacing regalloc.
- **Likely fix:** Need source form that generates explicit if/else (with li r3, 0 else branch) WITHOUT promoting chk2 to callee-save. Candidate: use '&&' but somehow get explicit else - impossible without named variable. Alternative: find source that makes chk and new_var allocate to same register in mwcc but still allows chk2 to stay in r3. Possibly a pragma or different variable scoping that hints chk2 as transient. Or: accept that this TU's build environment cannot reproduce target regalloc and mark as environment-dependent.

