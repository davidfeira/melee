---
function: grPura_802125F0
tu: src/melee/gr/grpura.c
headline: permuter-territory + regalloc
tags: [permuter-territory, regalloc, sdata2-named-floats, string-pool]
---
## grPura_802125F0 (`src/melee/gr/grpura.c`) — permuter-territory + regalloc

- **Tags:** `permuter-territory`, `regalloc`, `sdata2-named-floats`, `string-pool`
- **Best fuzzy:** 94.2857%
- **Diagnosis:** Down to 97.24% (108 mismatches) from 72.5% baseline. Body logic is fully understood and structurally correct: loop iterates 27 entries of {s32 idx, f32 mult, s32 check} starting at grPu_803E6800+0x2B0, calls grPura_80211E08(1) (made non-inline via #pragma dont_inline), updates gp->pura.xC4 via Ground_801C33C0, fetches gp->pura2.xC8 via Ground_801C3FA4, copies translate from xC8 to gobj->hsd_obj, propagates flag 0x10, then sets translate (0,0,0) on gobj->hsd_obj->child, then computes scale (clamp at 2.0, mult by entry->mult) and applies to both xC8 and gobj->hsd_obj. Remaining diff is dominated by register allocation rotations: target uses {r28=entry, r26=i, r29=gp, r27=jobj-temp}, base uses {r26=entry, r27=i, r28=gp, r29=jobj-temp}. Two structural issues remain: (1) target emits 'lfs f30, grPu_804DBA70@sda21' (named sda2 reference) but base emits 'lfs f30, @448@sda21' (anonymous float pool) because mwcc inlines the const f32 globals; volatile cast experiment hurt frame size (lost f29/f30 saves). (2) target's assert string 'grpura.c' is at grPu_803E6800+0x294 (offset 0x54 inside StageData struct?) but ours places it at +0x2D8. Both are file-layout issues outside the function body. decomp-permuter is offline so cannot dispatch.
- **Tried:** Wrote correct logic body, added #pragma dont_inline on grPura_80211E08 helper to prevent inlining (gained 25%), introduced grPura_AB0Entry typedef offset from grPu_803E6800. Tried *(volatile f32*) cast to force named sda2 references; broke frame-size optimizations.
- **Likely fix:** Structural fix needed for sdata2-named-floats: the source likely declares grPu_804DBA70/grPu_804DBA74 in a way that defeats const-folding (extern const decl + separate definition? or non-const f32?). Assert string offset suggests the StageData definition in this TU embeds the literal differently. Permuter on register rotations should close the regalloc gap if those two structural issues are resolved first.

