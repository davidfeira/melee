---
function: ftAction_80073118
tu: src/melee/ft/ftaction.c
headline: regalloc + upstream-regression
tags: [regalloc, upstream-regression]
---
## ftAction_80073118 (`src/melee/ft/ftaction.c`) — regalloc + upstream-regression

- **Tags:** `regalloc`, `upstream-regression`
- **Best fuzzy:** 84.377%
- **Diagnosis:** Target passes wind_fx_3.angle as int (r5) to ftCo_8009E714 arg2 and wind_fx_1.timer as float/256 (f1). Current code has them swapped (timer as int, angle as float). Two attempts to fix: (1) s32 angle + timer*1/256.0f inline, (2) swap angle/decay read order — both scored 80.95% (36 mismatches) vs baseline 84.37% (32 mismatches). Root cause: with s32 angle declared, compiler uses r0 for angle instead of r5, and timer stays in GPR preventing the target xoris+stw intermediate pattern. The target stores timer_xoris and x_xoris separately on the stack (interleaved across blocks 2-3) using a specific spill pattern that requires all s16 fields to be read as raw ints. Identical upstream match exists (same source code) — regression is due to fighter.h struct expansion adding void* x4..x1C fields to Fighter_804D64FC_t, which changes compiler's register pressure analysis for the TU even though this function does not use that struct.
- **Tried:** Attempt 1: s32 angle, decay read before angle, call with ftCo_8009E714(gobj,idx,angle,timer*(1/256.0f),x,y,mag,decay_amt) — 80.95% (36 mismatches). Attempt 2: same but reading angle before decay — 80.95% (36 mismatches).
- **Likely fix:** Need all s16 field reads to be s32 raw values, all float conversions deferred to call site, letting compiler use the natural xoris+stw spill pattern for all 6 values. Requires: s32 timer, s32 x_raw, s32 y_raw, s32 mag_raw, s32 decay_raw, s32 angle — all stored as raw ints, with timer*(1/256.0f), x_raw*(1/256.0f), etc. inline at the call. Register pressure will force the compiler to spill intermediates using the target pattern. The header change in fighter.h may need to be reverted or a compile flag toggled. Alternatively, this is pure permuter territory — the mismatches are all regalloc shifts.

