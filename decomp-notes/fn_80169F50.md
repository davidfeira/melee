---
function: fn_80169F50
tu: src/melee/gm/gm_1601.c
headline: permuter-queued + regalloc
tags: [permuter-queued, regalloc, permuter-territory]
---
## fn_80169F50 (`src/melee/gm/gm_1601.c`) — permuter-queued + regalloc

- **Tags:** `permuter-queued`, `regalloc`, `permuter-territory`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 94.02% (21 instr off) after gp pointer pattern. All remaining mismatches are pure register allocation: target preserves arg0 in r28 and uses r29 for loop counter; base preserves arg0 in r29 and uses r28 (loop1) then r30 (loop2) for loop counter. Sub-effects propagate into r3/r4 swap on early flag stores and the extsb sources. No structural issues — control flow, struct field access, condition order all match.
- **Tried:** Attempt 1 (87.35%): direct lbl_8046B488.field access. BSS-anchor false-positive fired (lbl_8046B488@ha vs ...bss.0+0x110@ha) plus regalloc drift. Attempt 2 (94.02%): introduced 'struct lbl_8046B488_t* gp = &lbl_8046B488;' pointer pattern matching sibling fn_80169444, plus 's8* p = &gp->x1' for the third comparison to materialize 'addi r3, r31, 0x1'. Cleared the BSS-anchor mismatch and produced asm with identical structure but shifted register choices.
- **Likely fix:** Permuter regalloc cycle. Specifically need r28=arg0, r29=loop_idx in loop1, r30=arg1 (post-extsb), r29=loop_idx in loop2. Decomp-permuter offline per override; queued for cluster dispatch.

