---
function: grPura_802120E0
tu: src/melee/gr/grpura.c
headline: permuter-queued + instruction-scheduling
tags: [permuter-queued, instruction-scheduling, sdata2-anonymous-floats]
---
## grPura_802120E0 (`src/melee/gr/grpura.c`) — permuter-queued + instruction-scheduling

- **Tags:** `permuter-queued`, `instruction-scheduling`, `sdata2-anonymous-floats`
- **Best fuzzy:** 51.4766%
- **Diagnosis:** Source shape correct (51% strict, 51% fuzzy, 57 mismatches). Remaining diff is mostly instruction scheduling: target hoists lfd f3 (grPu_804DBA60) and lfs f0 (grPu_804DBA5C) loads early before stack-store of next color, while base loads them just-in-time. Also sdata2-anonymous-float false-positive flavor: target uses named grPu_804DBA60/grPu_804DBA68 (signed/unsigned int->float magic doubles) while base emits anonymous @231/@230 sdata2 entries — these will resolve to same physical address post-link if our explicit f64 declarations land at BA60/BA68.
- **Tried:** (1) Initial decomp with cur/next as GXColor stack temps using *(s16*)&xC4 / xC6 for index, (s8)cast and component-wise blend. (2) Added PAD_STACK(8) to match 0x78 frame size — fixed frame, dropped mismatches 93->69. (3) Reordered local declarations (next before cur) so cur lands at sp+0x18, next at sp+0x1c — dropped 69->57. (4) Added explicit const f64 grPu_804DBA60/68 declarations between BA5C and BA70 — no effect because MWCC still creates anonymous int->float magic constants per-function rather than reusing named globals.
- **Likely fix:** Permuter for instruction scheduling/hoisting. Source shape now correct; named vs anonymous sdata2 mostly cosmetic but the load-position ordering is real scheduling work.

