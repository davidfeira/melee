---
function: fn_802192A4
tu: src/melee/gr/grpushon.c
headline: permuter-queued + r30-r31-swap
tags: [permuter-queued, r30-r31-swap, sdata2-named-global]
---
## fn_802192A4 (`src/melee/gr/grpushon.c`) — permuter-queued + r30-r31-swap

- **Tags:** `permuter-queued`, `r30-r31-swap`, `sdata2-named-global`
- **Best fuzzy:** 95.2661%
- **Diagnosis:** Reached 94.75% with unrolled loop using *p++ over Vec3 grPushOn_803E7CCC[4]. Pointer arithmetic with pre-increment (Vec3* p = arr - 1; p++; ...) correctly produces lfsu pattern. Two remaining structural classes: (1) target uses r3 as the array pointer (lfsu f0, base@l(r3); lfs 0x4(r3); lfs 0x8(r3)), but mwcc allocates r31 in our base because p is initialized BEFORE the bl ftLib_80086644 call and must therefore be saved across it; (2) target swaps r30/r31 args so out=r31 and gobj=r30, our base does the opposite.
- **Tried:** (a) explicit 4-step loop with *p++ — got 88.5% with linear pointer in r31. (b) pre-decrement-then-increment: Vec3* p = arr-1; p++; — got 94.75%, lfsu/0x4(r31)/0x8(r31) pattern correct but pointer in r31 not r3. Header signature widened to (s32 unused, HSD_GObj* gobj, s32* out) since callsite is active_cb(ground, gobj, &desc); r4=gobj, r5=desc/out confirmed by direct asm reading.
- **Likely fix:** Move 'Vec3* p = grPushOn_803E7CCC - 1;' initialization to AFTER the ftLib_80086644 call so the pointer doesn't need to be live across the bl. Also investigate whether function actually has 2 args (HSD_GObj*, s32*) but with a wrapper trampoline, OR whether mwcc's allocation prefers later-saved reg for first-passed arg under -O4,p with the Vec3 pos local clobbering ftLib's stack region. Permuter dispatch is appropriate after that pointer placement is fixed; remaining diff is pure regalloc. Override note: decomp-permuter offline at time of attempt; queued for cluster.

