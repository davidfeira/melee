---
function: grVenom_80205758
tu: src/melee/gr/grvenom.c
headline: permuter-territory + regalloc
tags: [permuter-territory, regalloc, float-regalloc, frame-size]
---
## grVenom_80205758 (`src/melee/gr/grvenom.c`) — permuter-territory + regalloc

- **Tags:** `permuter-territory`, `regalloc`, `float-regalloc`, `frame-size`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 94.31% with 68 reg-alloc cascades; target keeps gp/base/jobj/gobj in r28-r31 saved regs, my version gets gp=r29 and base reused via r3 — frame size 0x30 vs 0x38 because compiler didn't pick 5th saved nonvolatile. Float compare order swaps (fcmpo cr0,f1,f0 vs f0,f1) and an extra frsp/lfs in DC-loop are scheduling-only.
- **Tried:** manual shape pass with HSD_JObjSetTranslate inline + scale via *(f32*)((u8*)attr+0x34) idiom (matches sibling 80205AD4); tried (u32) entry[8] != 0U for cmplwi; removed bogus negation on grVe_804DB7F8 (already -π); reordered gp/base decl order — no movement past 94.31%.
- **Likely fix:** permuter shuffle for register allocation; main hand-fix would force gp into a saved register longer (e.g. add a no-op late use of gp after Ground_801C2FE0 — but unsupported here).

