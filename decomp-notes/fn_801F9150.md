---
function: fn_801F9150
tu: src/melee/gr/gricemt.c
headline: trailing-blr + goto-loop
tags: [trailing-blr, goto-loop, struct-typing]
---
## fn_801F9150 (`src/melee/gr/gricemt.c`) — trailing-blr + goto-loop

- **Tags:** `trailing-blr`, `goto-loop`, `struct-typing`
- **Best fuzzy:** 95.2381%
- **Diagnosis:** Function is loop-with-reset-tail pattern: load v1=xB0[xDE], v2=xB4[xDE], if either is -1 reset xDE=0 and re-loop, else xDE++ return v1. ALSO had to fix struct: xB0/xB4 are s16* not float (header was wrong). True signature is s32 fn(HSD_GObj* arg0, s32* arg1) (returns sign-extended s16, writes s32 through arg1). Reached 95.2%, 21/21 instruction body matches but mwcc emits an EXTRA trailing blr after final 'b loop'. Target asm has NO trailing blr (21 instr exactly). Tried while(1)/break, do-while, goto-based structure with reset label inside for(;;) — all generate identical body but include trailing blr.
- **Tried:** v1: simple while(1) with continue — 80% mismatch (reset block placed mid-function, not at end). v3/v4: goto-loop+reset-after-return pattern (matches target structurally) and for(;;) with internal goto label — both 95.2%, body identical to target but mwcc adds trailing blr after the unconditional b-to-loop. Could not find source shape that suppresses the trailing blr.
- **Likely fix:** Some way to convince mwcc that control flow cannot fall off the end. Maybe noreturn-style attribute on the loop, or restructuring so loop tail is conditional branch rather than unconditional. Possibly the original source uses a different idiom (e.g., recursive call, or a particular ordering of the two if-checks). Permuter unlikely to find since this is 1-instruction structural codegen quirk, not regalloc/scheduling. Header change required: IceMountainParams.xB0 and xB4 should be s16* (currently float).
