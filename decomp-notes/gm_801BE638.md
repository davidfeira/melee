---
function: gm_801BE638
tu: src/melee/gm/gm_1BA8.c
headline: permuter-false-positive + float-literal
tags: [permuter-false-positive, float-literal, cross-tu-globals]
---
## gm_801BE638 (`src/melee/gm/gm_1BA8.c`) — permuter-false-positive + float-literal

- **Tags:** `permuter-false-positive`, `float-literal`, `cross-tu-globals`
- **Best fuzzy:** 99.5935%
- **Diagnosis:** Strict objdiff = 100%; report.json fuzzy = 99.5935%. Asm shows 3 lfs from gm_804DAC78@sda21 (a 1.0F float symbol) at .L_801BE8E0/Player_GetP1Stock block/var_r0 block. Source has 1.0F literals there. Replacing with named extern const float gm_804DAC78 keeps strict 100% (verified after edit). Fuzzy result not re-verified due to repeated transient WSL /mnt/c filesystem I/O errors during full ninja rebuild (terminate called after throwing instance of std::filesystem::filesystem_error during MWCC compile of unrelated TUs). Edits left in place; awaiting working build env to confirm fuzzy improvement to 100%.
- **Tried:** Added extern const float gm_804DAC78 declaration; replaced all 3 gm_SetGameSpeed(1.0F) calls in gm_801BE638 with gm_SetGameSpeed(gm_804DAC78). Strict diff = 100% confirmed. Multiple fuzzy build attempts hit WSL FS I/O errors (extern/dolphin/include, src/MSL/string.h, etc.) -- not function-related, not reproducible to source.
- **Likely fix:** Verify with fresh fuzzy ninja in clean WSL session. If fuzzy still <100%, residual mismatches may be reloc-anchor naming for gmMainLib_804D3EE0 (BSS layout) -- requires TU-wide gm_1BA8.c BSS reorder. Otherwise this should reach 100% with current edits.
