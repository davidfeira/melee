---
function: fn_80186080
tu: src/melee/gm/gm_1832.c
tags: [struct-split, data-symbols-missing]
---
## fn_80186080 (`src/melee/gm/gm_1832.c`)

- **Tags:** `struct-split`, `data-symbols-missing`
- **Best fuzzy:** 99.987% (Opus subagent)
- **Structure improvement:** the unified `lbl_804735E8` struct should be split into two — `lbl_804735E8` (size 0xA4, fields x40..xE3) and `lbl_8047368C` (size 0x24, fields xE4..x103, dropping the obsolete `pad_104[0x54]`). Updating the 112 field references is mechanical. This fixes 3 strict mismatches but **fuzzy is unchanged** because objdiff already treated those as equivalent.
- **Remaining blocker (closes the 0.013% fuzzy gap):** inline literals `"SdIntro.dat"`, `"SIS_IntroData"`, `0.0f`, `640.0f`, `480.0f` need to become named static globals at exact addresses `0x803D945C`, `0x803D9468`, `0x804DA540`, `0x804DA570`, `0x804DA574`. Plus intermediate fillers `lbl_803D9420` (size 0x9 string), `lbl_803D942C` (size 0x9 string), `lbl_803D9438` (size 0x24, content unknown).
- **What's missing:** the contents of `lbl_803D9438` aren't in `config/GALE01/symbols.txt`. Reverse-engineering this requires inspecting the original game's data section directly (e.g., disassembling `.data` from `orig/GALE01/sys/main.dol` at the right offsets).
