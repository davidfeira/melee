---
function: hsd_80394950
tu: src/sysdolphin/baselib/particle.c
headline: reloc-symbol-false-positive + tu-data-osreport
tags: [reloc-symbol-false-positive, tu-data-osreport, regalloc, varargs-cr1eq]
---
## hsd_80394950 (`src/sysdolphin/baselib/particle.c`) — reloc-symbol-false-positive + tu-data-osreport

- **Tags:** `reloc-symbol-false-positive`, `tu-data-osreport`, `regalloc`, `varargs-cr1eq`
- **Best fuzzy:** 95.2742%
- **Diagnosis:** 17 mismatches: (1) 5 mismatches are data-anchor reloc false positives — target uses lbl_8040AB00@ha/@l with string offsets 0x784/0x7bc/0x7d4/0x80c but base compiles to ...data.0@ha/@l with offsets 0x528/0x554/0x56c/0x598 — same TU-wide data pool issue as Exception_ReportStackTrace. (2) 12 mismatches are r29/r30 register swap in loop 2 only (loop 1 matches correctly). (3) 3 of the 12 loop-2 mismatches involve varargs-cr1eq scheduling: crset cr1eq appears after lwz r5 and before lwz r6 in target but after lfs f2 in base, tied to the r29/r30 register pressure difference.
- **Tried:** (a) Swapped u8* p before s32 i in declarations — loop 1 broke (26 mismatches). (b) Scoped j/q variables for loop 2 in a block — regressed (per prior notes: 30 mismatches). Both loop 1 and loop 2 correct regalloc require same i/p declaration order — structural tension.
- **Likely fix:** TU-wide data pool reorganization needed so lbl_8040AB00 is the anchor symbol with the expected string offsets. Once data-anchor matches, register pressure for the OSReport varargs calls may shift and fix the r29/r30 allocation in loop 2 as a side effect. This is a multi-function cross-TU refactor beyond single-function scope.

