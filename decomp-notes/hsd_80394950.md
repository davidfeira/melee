---
function: hsd_80394950
tu: src/sysdolphin/baselib/particle.c
headline: tu-data-osreport + reloc-symbol-false-positive
tags: [tu-data-osreport, reloc-symbol-false-positive, regalloc]
---
## hsd_80394950 (`src/sysdolphin/baselib/particle.c`) — tu-data-osreport + reloc-symbol-false-positive

- **Tags:** `tu-data-osreport`, `reloc-symbol-false-positive`, `regalloc`
- **Best fuzzy:** 94.3871%
- **Diagnosis:** Best fuzzy 96.16% / strict 95.76%, 17 mismatches. (1) 5 ARG_MISMATCH on the OSReport string-pool anchor: target uses lbl_8040AB00@ha/@l with offsets 0x784/0x7bc/0x7d4/0x80c; base uses ...data.0@ha/@l with offsets 0x528/0x554/0x56c/0x598. Same anchor symbol issue documented for Exception_ReportStackTrace in this same TU (particle.c). (2) 12 mismatches in loop 2: target uses r29=index r30=pointer (matching loop 1); base uses r30=index r29=pointer (swap). Plus a 3-instruction scheduling reorder of addi r3,r31,0x80c relative to crset cr1eq and mr r4,r29. These regalloc/scheduling differences appear downstream of the data-pool anchor choice driving register pressure differently in loop 2.
- **Tried:** (a) Added separate var names j/q for loop 2 to break aliasing - regressed to 30 mismatches (more reg shuffling). (b) Swapped init order in loop 2 (p=ctx; i=0) - same 17 mismatches but worse fuzzy score from order metric. (c) Manual diff inspection confirmed loop 1 source already matches base bit-for-bit; only loop 2 + reloc anchors diverge.
- **Likely fix:** Same TU-wide data-pool refactor needed for Exception_ReportStackTrace: reorganize particle.c .data so OSReport format strings anchor via lbl_8040AB00 with offsets 0x784/0x7bc/0x7d4/0x80c rather than mwcc's automatic ...data.0 + 0x528-region anchor. Multi-symbol cross-function refactor; beyond single-function scope. Once the anchor symbol matches, the loop 2 register-allocation differences will likely fall out (downstream of data-pool register pressure).
