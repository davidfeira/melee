---
function: it_8027B288
tu: src/melee/it/it_2725.c
tags: [mwcc-aliasing, stack-offset]
---
## it_8027B288 (`src/melee/it/it_2725.c`)

- **Tags:** `mwcc-aliasing`, `stack-offset`
- **Best fuzzy:** 99.93% (Sonnet subagent, escalated)
- **Blocker:** mwcc `-O4,p` merges two `union Struct2070` stack slots (`spC` and `sp14`) because it can prove `sp14 == spC`. Target keeps them separate, ours alias them. 3 strict mismatches all stack-offset related.
- **Cross-cutting:** the same issue affects `ft_800895E0` in `src/melee/ft/ft_0892.c` (identical 2-mismatch subset).
- **Likely fix:** permuter with a source shape mwcc can't alias-optimize, OR an `asm volatile` inline hint that breaks aliasing.
