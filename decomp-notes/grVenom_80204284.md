---
function: grVenom_80204284
tu: src/melee/gr/grvenom.c
headline: permuter-false-positive + tu-wide-data
tags: [permuter-false-positive, tu-wide-data, stack-offset, regalloc]
---
## grVenom_80204284 (`src/melee/gr/grvenom.c`) — permuter-false-positive + tu-wide-data

- **Tags:** `permuter-false-positive`, `tu-wide-data`, `stack-offset`, `regalloc`
- **Best fuzzy:** 97.5769%
- **Diagnosis:** 32 mismatches at 97.58% fuzzy / 97.29% strict on 104-instr function. Mix: 15 stack-offset + 7 regalloc + 7 permuter-false-positive + 2 real + 1 frame-size. Two real diffs are register swap on the two GObj pointers: target wants mr r30,r3 for one path / source emits mr r31,r3 (and inverse missing/inserted on the other). Likely cascade from frame-size diff (PAD_STACK shape) propagating into stack-offset cluster across the local Vec3 + timer + nested helper pointer storage. TU has 2 logged stuck siblings (grVenom_802052E0 with permuter-false-positive cap at 99.92% from sdata2 layout; another sibling in same family) — same TU-wide data/literal-pool layout is bottlenecking this function too.
- **Tried:** compact-brief inspection only — no source-shape attempts. Per swarm protocol: compact-brief recommended log-stuck-immediately because TU has logged-stuck siblings indicating structural blocker beyond function-level mutation, and 7+ permuter-false-positive mismatches mean cluster permuter cannot land a real fix on this one.
- **Likely fix:** TU-wide sdata2/data anchor reconstruction (paired-siblings family): would need to declare named globals for the literal-pool entries the target uses (similar to grHr_804DBC30..70 fix recommended in grHomeRun_8021EA30 notes) so mwcc emits named @sda21 references matching target. The two real regalloc diffs (r30 vs r31 swap) likely self-resolve once frame layout stops shifting. Beyond single-function subagent scope; needs multi-function TU-wide refactor coordinated with grVenom_802052E0 and other siblings.
