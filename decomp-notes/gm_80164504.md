---
function: gm_80164504
tu: src/melee/gm/gm_1601.c
headline: regalloc + upstream-regression
tags: [regalloc, upstream-regression]
---
## gm_80164504 (`src/melee/gm/gm_1601.c`) — regalloc + upstream-regression

- **Tags:** `regalloc`, `upstream-regression`
- **Best fuzzy:** 99.5238%
- **Diagnosis:** Source is identical to upstream/master (which reports 100% match), but our TU has additional matched functions before gm_80164504 (fn_80160DE8, fn_80161154, fn_80161C90, fn_80162170, gm_8016260C as full implementations vs upstream stubs). This TU-context pollution causes MWCC to allocate r7 instead of r3 for loop counter i in the first for-loop, and r4 instead of r3 for the temp array pointer. Five instructions mismatching in first loop only (second loop already matches with r3). Function is semantically correct.
- **Tried:** Identical source to upstream; tried splitting shared loop variable i into separate i/j to change liveness — same result. Register allocation cannot be forced without external TU context matching.
- **Likely fix:** Match all functions before gm_80164504 in this TU to eliminate TU-context regalloc drift, OR use permuter once those siblings are aligned.

