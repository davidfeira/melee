---
function: it_802B2E7C
tu: src/melee/it/items/ityoshieggthrow.c
headline: regalloc + stack-offset
tags: [regalloc, stack-offset, paired-siblings]
---
## it_802B2E7C (`src/melee/it/items/ityoshieggthrow.c`) — regalloc + stack-offset

- **Tags:** `regalloc`, `stack-offset`, `paired-siblings`
- **Best fuzzy:** 91.0896%
- **Diagnosis:** 15 strict mismatches at fuzzy 91.1%. Real diffs: (1) regalloc swap r29<->r31 for 'ip' pointer chain (target keeps ip in r29; base loads into r3 then mr/addi to r31, plus uses r29 for attrs->x4 chain). (2) stack offset for f32 scale local: target 0x20(r1) vs base 0x24(r1), with same frame size 0x38 - extra 4-byte slot somewhere. (3) base prefers 'addi rN, rM, 0x0' while target prefers 'mr'.
- **Tried:** (a) PAD_STACK(0x14) -> stack frame grew to 0x40 (alignment), 26 mm worse. (b) Move attrs/jobj/scale decls into if-block (matching paired sibling it_2725_Logic43_Clanked shape) with PAD_STACK(0x14) -> 27 mm worse. Both rejected.
- **Likely fix:** Stack offset 0x20 vs 0x24 with same frame size suggests an additional 4-byte 'phantom' local in base. Could be a temp alias (e.g. extra Vec3* or local Item_GObj* re-fetched). Or use FORCE_PAD_STACK_4 in addition to PAD_STACK(0x10). Pair with sibling it_2725_Logic43_Clanked (same shape, also stuck plateau best=260). Both functions likely need same source-shape fix - solving one solves both. Real fix may require an inline GET_ITEM(gobj) re-read to force a 4-byte spill of original r3 'ip' value.
