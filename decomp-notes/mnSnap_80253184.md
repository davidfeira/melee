---
function: mnSnap_80253184
tu: src/melee/mn/mnsnap.c
headline: permuter-false-positive + regalloc
tags: [permuter-false-positive, regalloc, bss-anchor]
---
## mnSnap_80253184 (`src/melee/mn/mnsnap.c`) — permuter-false-positive + regalloc

- **Tags:** `permuter-false-positive`, `regalloc`, `bss-anchor`
- **Best fuzzy:** 92.6143%
- **Diagnosis:** Diff at 92.61% / 25 mismatches is dominated by a bss-anchor naming artifact: target uses 'addi rN, r3, mnSnap_804A0A10@l' (the struct symbol) while base uses 'addi rN, r3, ...bss.0@l' (the file's bss anchor). Both resolve to the same address post-link. Secondary difference is mwcc folding 'p57=&snap->card_result' and 'flags=&snap->thumb_loaded[0]' into 0x15c(r28)/0x160(r28) immediate-offset accesses, while base maintains those as persistent base-pointers (r27, r26) - typical of when fields are top-level statics rather than struct members. Splitting card_result/thumb_loaded out of mnSnap_State is a structural change that would impact many call sites in mnsnap.c.
- **Tried:** prep + diff inspection. Did NOT attempt source variants because (a) bss-anchor reloc-symbol mismatch is permuter-false-positive per CLAUDE.md rule, and (b) the regalloc/folding pattern would require splitting the struct, out of subagent scope.
- **Likely fix:** Either (1) accept as link-equivalent and re-classify (post-link bytes match - check report.json strict vs fuzzy difference), or (2) split card_result/thumb_loaded[4] out of mnSnap_State into separate file-scope statics and audit all 5+ functions in mnsnap.c that touch them (mnSnap_8025329C, mnSnap_80253640, etc).
