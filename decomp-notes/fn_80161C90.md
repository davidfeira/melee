---
function: fn_80161C90
tu: src/melee/gm/gm_1601.c
headline: permuter-queued + regalloc
tags: [permuter-queued, regalloc, r30-r31-swap, frame-size, struct-typing]
---
## fn_80161C90 (`src/melee/gm/gm_1601.c`) — permuter-queued + regalloc

- **Tags:** `permuter-queued`, `regalloc`, `r30-r31-swap`, `frame-size`, `struct-typing`
- **Best fuzzy:** (unknown)
- **Diagnosis:** Function reaches 90.9% match (133 mismatch insns) with synthetic stats struct cast on arg2. Remaining diff is dominated by register allocation cascade: target picks r29=p, r30=arg0; base picks r29=arg0, r30=p. This produces matching arg2 r31 layout but flips r29/r30 throughout the loads. Also frame size differs (target stwu -0x38, base stwu -0x30; 8 extra bytes target, possibly an unused spill slot). Logic structure is correct: saturating-add idiom (sum > -1U ? -1U : sum) reproduces the dead cmplw branches; sd_count uses signed cmpw against 0xFFFF; total_player_count caps at 0xFFFF (NOT 0xFFFFFFFF) via cmplwi. Header fix required: changed src/melee/gm/gm_1601.h fn_80161154 from UNK_RET/UNK_PARAMS to s32 fn_80161154(MatchEnd*).
- **Tried:** (1) Naive write with separate u32* aliases for each arg2 offset → 83% (compiler hoisted addr arithmetic into pre-loop). (2) Cast arg2 to (FighterData*)((u8*)arg2 - 0x34) → 90.6% (subi r31, r5, 0x34 instead of mr r31, r5). (3) Synthetic local stats struct cast on arg2 (kept r31=arg2 directly) → 90.9%. Remaining is register-rename + scheduling.
- **Likely fix:** Permuter run targeting r29/r30 swap and frame-size. The 3-call gmMainLib_8015EDBC ternary expansion (expand 'sum = ...' inline so both ternary branches reload x10) might shake regalloc loose. Source-shape attempts exhausted at 2-cap; routing to permuter on Mama Claude resume.

