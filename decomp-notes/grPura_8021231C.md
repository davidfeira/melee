---
function: grPura_8021231C
tu: src/melee/gr/grpura.c
headline: permuter-blocked + frame-size
tags: [permuter-blocked, frame-size]
---
## grPura_8021231C (`src/melee/gr/grpura.c`) — permuter-blocked + frame-size

- **Tags:** `permuter-blocked`, `frame-size`
- **Best fuzzy:** 89.8389%
- **Diagnosis:** At 99.84% (29 mismatches). Switched from NULL ptr locals (which triggered HSD_ASSERT __assert calls and used 0x28 stack) to stack-allocated Vec3 vec + Quaternion quat (eliminated asserts, jumped from 89.84% to 99.84%). Remaining diff is purely stack-offset shift: target stack frame is -0x50, current is -0x48 (8 bytes smaller). Target layout: quat@0x14 (16b), gap@0x24-0x2f (12b unused), vec@0x30 (12b), saved regs@0x44+. Current: quat@0x1c, vec@0x2c, saved@0x3c. Tried adding unused 'Vec3 scale' as third local but mwcc DCE'd it (no effect). Likely needs an additional 8-12 byte local that survives DCE, or a different variable type (e.g. double, or struct returning 16 bytes by value). Suitable for permuter — try adding stack-pinned dummy locals or alternative call shapes. NOTE: decomp-permuter offline at time of attempt.

