---
function: ftCo_8009C744
tu: src/melee/ft/chara/ftCommon/ftCo_Shouldered.c
headline: mwcc-aliasing + float-literal
tags: [mwcc-aliasing, float-literal, regalloc, paired-siblings]
---
## ftCo_8009C744 (`src/melee/ft/chara/ftCommon/ftCo_Shouldered.c`) — mwcc-aliasing + float-literal

- **Tags:** `mwcc-aliasing`, `float-literal`, `regalloc`, `paired-siblings`
- **Best fuzzy:** 96.2542%
- **Diagnosis:** V2 attempt confirmed prior diagnosis. The 6 mismatches split into two classes: (1) 4-instr regalloc cluster around vic_gobj/hit lifetime — target preserves vic_gobj in r30 across ftCo_800DC920 (mr r30,r3 then later addi r3,r30,0 for ftColl_80078710), base loads it fresh; (2) 1-instr sdata2 mismatch where target lfs uses ftCo_804D8750@sda21 directly but base has anonymous @190@sda21. Source-shape changes inside ftCo_8009C744 alone cannot suppress the @190 because it's emitted from elsewhere in the TU (likely ftCo_Shouldered_Anim's int 0->float promotion in ftCo_8008E908(gobj1, 0) call).
- **Tried:** V1 (prior session): 0.0F->ftCo_804D8750 in our func only. V1b (this session): changed BOTH our func AND Anim's call to use ftCo_804D8750 — zero change in mismatch count or asm output, @190 still emitted in OUR function's lfs. V2: split locals into separate decl + assignment forms — zero change. ftCo_Shouldered_Anim itself is also broken (92.7%, 50 mismatches) and any source-shape change there could regress it.
- **Likely fix:** Investigate WHERE @190 is actually defined in the TU's sdata2 — could be coming from a third sibling function in ftCo_Shouldered.c, or from an inlined helper. Run 'powerpc-eabi-objdump -d -r build-linux/GALE01/src/melee/ft/chara/ftCommon/ftCo_Shouldered.o | grep -B5 @190' to find emitter. The regalloc piece (4 instrs) becomes permuter-friendly once @190 is gone.
