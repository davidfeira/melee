---
function: ftCo_800A2C80
tu: src/melee/ft/chara/ftCommon/ftCo_0A01.c
headline: permuter-territory + frame-size
tags: [permuter-territory, frame-size, regalloc, r30-r31-swap, float-regalloc, stack-offset]
---
## ftCo_800A2C80 (`src/melee/ft/chara/ftCommon/ftCo_0A01.c`) — permuter-territory + frame-size

- **Tags:** `permuter-territory`, `frame-size`, `regalloc`, `r30-r31-swap`, `float-regalloc`, `stack-offset`
- **Best fuzzy:** (unknown)
- **Diagnosis:** At 84% (156 mismatches, 301 instr). Source-shape correct: control flow matches m2c, all field accesses validated (Fighter pos_delta=xC8, coll_data.cur_pos+ecb.bottom for ray start, x1A88.xFA_b6/b5 bitfields, x1A88.half_width/half_height for blast-zone bounds, INISHIE1/FOURSIDE stage-id constants). Remaining diff is dominated by structural register allocation: target uses r28 for &fp->x1A88 throughout AND r30 for a per-block reload, plus reuses r31 to hold floor.index after the ground branch (clobbering fp); my mwcc gives r29 instead of r28 and won't re-use r31 since later code references fp. Stack frame is 0x18 bytes smaller than target (frame=0x70 vs 0x88) because target spills sp3C.x/y into f27/f26 (callee-saved FPRs) for reuse across 4 Stage_GetBlastZone* calls per blast-zone block, while my mwcc loads them inline each time. All struct copy patterns (Vec3 sp24=fp->pos_delta producing word-loads then float-reads) reproduced.
- **Tried:** (1) Vec3 struct copy for pos_delta -> matched the int store/float read pattern. (2) Reassigning data=&fp->x1A88 inside each blast-zone branch and using a fresh local pointer per block to try to coax mwcc into the r30 reload pattern. (3) Various permutations of data-pointer aliasing (data->xFA_b6 vs fp->x1A88.xFA_b6).
- **Likely fix:** Permuter to shuffle register allocation between r28-r31, FPR allocation between f26-f31, and force sp3C.x/y to live in callee-saved FPRs across the Stage_GetBlastZone* call sequence. Frame size will follow once FPR usage matches.

