---
function: fn_80313BD8
tu: src/melee/ty/tylist.c
headline: permuter-queued + regalloc
tags: [permuter-queued, regalloc, r30-r31-swap]
---
## fn_80313BD8 (`src/melee/ty/tylist.c`) — permuter-queued + regalloc

- **Tags:** `permuter-queued`, `regalloc`, `r30-r31-swap`
- **Best fuzzy:** (unknown)
- **Diagnosis:** At 94.4%, 125 mismatches almost entirely register-allocation churn (r28<->r30, r3<->r4 swaps cascading through the function). Only 2 structural diffs in the prologue, both also register-allocation: target derives r28=state+0x2AC directly from r31, base derives r30 from r3 after fully resolving address. No control-flow, no missed loads/stores, no semantic differences identified.
- **Tried:** Initial decomp from m2c skeleton; signed div fix (>>1 -> /2); branch-direction inversion in pad_29D parity branch (5- vs 4- ordering).
- **Likely fix:** Permuter (offline at log time, queued) — pure regalloc problem. May need order-of-locals tweak or inline first use of g pointer to delay register binding until after un_GetTrophyTotal().

