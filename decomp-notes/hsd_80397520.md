---
function: hsd_80397520
tu: src/sysdolphin/baselib/particle.c
headline: regalloc + permuter-territory
tags: [regalloc, permuter-territory]
---
## hsd_80397520 (`src/sysdolphin/baselib/particle.c`) — regalloc + permuter-territory

- **Tags:** `regalloc`, `permuter-territory`
- **Best fuzzy:** 96.8889%
- **Diagnosis:** 6 mismatches: 4 ARG_MISMATCH (lwz r31/r30 directly vs lwz r0 + later mr) + 2 DIFF_REPLACE (beq vs mr r31/r30,r0). Target loads node->child and child1->child straight into non-volatile r31/r30 with cmplwi/beq right after; mwcc consistently emits 'lwz r0; cmplwi r0; beq; mr rN, r0' instead. Pure regalloc/scheduling on null-test idiom.
- **Tried:** V1: collapse decl into condition '(child1 = node->child) != NULL' -> 89% (worse, 32+ mismatches, regalloc explosion). V2: wrap whole body in 'if (node != NULL) { ... }' instead of early-return -> 89% (same regalloc explosion).
- **Likely fix:** Pure permuter territory. Original null-test idiom is correct shape; the early-return form is needed (V2 confirmed). Dispatch --auto-permute --cluster. Could also try inline asm hint to force lwz directly into non-volatile, but unusual for sysdolphin code.
