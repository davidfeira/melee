---
function: fn_8031438C
tu: src/melee/ty/tylist.c
headline: permuter-dispatched + instruction-scheduling
tags: [permuter-dispatched, instruction-scheduling, permuter-territory]
---
## fn_8031438C (`src/melee/ty/tylist.c`) — permuter-dispatched + instruction-scheduling

- **Tags:** `permuter-dispatched`, `instruction-scheduling`, `permuter-territory`
- **Best fuzzy:** (unknown)
- **Diagnosis:** At 95% / 5 mismatches. Structural shape correct: TyListState* state cast from un_804A2AC0, entry overlay at +0x2AC, archive cast from un_804D6ED8 (jobjs[3] at +0x18, x0 root at +0x0), HSD_JObj* chain to TObj.aobj for HSD_AObjSetRate, do/while loop with i*4 indexed access. Added TyListState::x290 (HSD_Text*) replacing pad_290[8] in types.h. Remaining diffs are scheduler/regalloc only: (1) target loop preamble uses slwi+add (i*4 indexed) vs base addi r29,r28,0x0 (constant-folded); (2) lfs un_804DDE40 hoisted differently around state->x290 reload; (3) one extra reload of state->x290.
- **Tried:** Multiple expression forms for jobj indexing (p+=4 vs (u8*)archive+i*4); reordered local declarations; PAD_STACK(0x18); named float externs; added extern char un_804D5A88[3] to force sda21; updated TyListState x290 field for repeat-reload pattern.
- **Likely fix:** Permuter should resolve via expression-order/scheduling permutations. Header change (types.h x290 field) required to land match. Decomp-permuter is offline; queued for cluster dispatch when restored.

