---
function: lbMthp_8001F624
tu: src/melee/lb/lbmthp.c
headline: tu-data-osreport + cross-tu-globals
tags: [tu-data-osreport, cross-tu-globals, reloc-symbol-false-positive, paired-siblings]
---
## lbMthp_8001F624 (`src/melee/lb/lbmthp.c`) — tu-data-osreport + cross-tu-globals

- **Tags:** `tu-data-osreport`, `cross-tu-globals`, `reloc-symbol-false-positive`, `paired-siblings`
- **Best fuzzy:** 89.36% (8 mismatches — Opus subagent, no commit)
- **Diagnosis:** All 8 mismatches are reloc-symbol/anchor false-positives. Target asm anchors via `str_arr_803BADB0@ha/@l + 0x238/0x23c/0x23e` (i.e. one named string-array symbol with offsets into the trailing `lbl_803BAFE8` struct), while base emits `lbl_803BAFE8@ha/@l + 0/4/6` directly. Per `lbmthp.o` objdump: target `.data` layout (per asm) is `str_arr_803BADB0(0x64) | str_Warning_frame_offsets(0x27) | gap(1) | str_arr_803BAE3C(0x1AC) | lbl_803BAFE8(0x18)` ending at 0x250. Base `.data` is 0x238 with all OSReport/assert literals as anonymous `@35..@230` symbols and NO `lbl_803BAFE8` definition (the C declares it `extern` in `lbmthp.static.h`). Because base lacks `lbl_803BAFE8` as an in-TU named symbol, mwcc emits a direct `lbl_803BAFE8@ha` reloc instead of anchoring on a nearby named string symbol.
- **Sibling:** `lbMthp8001F890` has the same shape (anchors via `lbl_804335B8.x88` to call `HSD_SObjLib_803A477C` then `lbl_804335B8.x90->x40 |= 0x10`), and is the originally-flagged BSS-anchor false-positive sibling per dispatch context. Same blocker family.
- **Tried:** Manual diagnosis only. Per Permuter Boundary ("Do not use permuter for false diffs caused by equivalent BSS/global base symbols"), permuter is contraindicated. No source-shape attempts.
- **Likely fix:** TU-wide data layout work — define `lbl_803BAFE8` as a real `static struct lbl_803BAFE8_t lbl_803BAFE8 = { 0, 0x280, 0x1E0, 6, 0, 0, 0 };` in `lbmthp.c` (m2c suggests this initializer), AND define the OSReport/assert literals as named arrays `static const char str_arr_803BADB0[] = ...; static const char str_arr_803BAE3C[] = ...;` so they emit as named symbols at `.data` offsets 0..0x238 with `lbl_803BAFE8` landing at 0x238. Then mwcc would anchor accesses to `lbl_803BAFE8` via `str_arr_803BADB0@ha + 0x238` (since `str_arr_803BADB0` is the section-start named anchor). Affects fn_8001E910, fn_8001ECF4, fn_8001F13C (all touch `str_arr_803BADB0`-region literals), fn_8001EB14, fn_8001F410, lbMthp_8001F624. Same family as `it_80274DAC`, `mpGetSpeed`, `it_80274740`, `fn_801803FC`, `un_80317A60`. Multi-function cross-cutting refactor, beyond single-function subagent scope.
