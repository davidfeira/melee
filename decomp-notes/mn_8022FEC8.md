---
function: mn_8022FEC8
tu: src/melee/mn/mnmainrule.c
headline: branch-pattern + regalloc
tags: [branch-pattern, regalloc, frame-size, struct-typing, permuter-territory]
---
## mn_8022FEC8 (`src/melee/mn/mnmainrule.c`) — branch-pattern + regalloc

- **Tags:** `branch-pattern`, `regalloc`, `frame-size`, `struct-typing`, `permuter-territory`
- **Best fuzzy:** (unknown)
- **Diagnosis:** Reached 81.3%/113-mismatch with switch(mode) {case 1; case 3; case 0,2,4} on (s32)mode; data ptr from gobj->user_data; mn_8022FB88_t extended with parent_a/parent_b at +0xA8/+0xAC (HSD_JObj*, ->child accessed); case 4 path uses single mn_803EC600_t aggregate with sub-arrays at +0x134 (AnimLoopSettings[5] = mn_803EC734), +0x170 (AnimLoopSettings[9] = mn_803EC770), +0x1DC (u8[0x3C] = mn_803EC7DC) so mwcc emits ONE @ha + multiple @l. Three structural blockers remain: (1) target hoists addi r0,&mn_803EC600 AND addi r4,r31 (data ptr) BEFORE switch dispatch, suggesting source spelling makes both case-1 (mn_8022FB88(value,data) needs r4=data) and case-4 (needs &mn_803EC600 base) common deps the optimizer can extract; (2) target uses r30 for jobj/r31 for data, base swaps; (3) target stack frame -0x58 with stmw at 0x44, base only -0x40 — 6 extra spill slots. Current source generates correct switch dispatch (cmpwi 3, beq case3, bge halves) only when cases listed in source order 1,3,0/2/4 — reordering 0/2/4 first regresses to ~24%. Sig: void(HSD_GObj*, HSD_JObj*, u8, u8). Header sig change required (.h).
- **Tried:** (1) Initial decompilation with separate extern AnimLoopSettings mn_803EC734[5], mn_803EC770[9], u8 mn_803EC7DC[]: 79.3%, mwcc emits 3 separate @ha loads (target only loads mn_803EC600 once). (2) Coalesced into struct mn_803EC600_t aggregate with sub-arrays at +0x134/+0x170/+0x1DC: 81.3%, single @ha load matches target shape but case-bodies still differ structurally (regalloc + frame size + dead-code shape for NULL var_r8 path).
- **Likely fix:** Permuter territory for the regalloc/spill differences once switch-dispatch matches. May also need to express the case-4 NULL-then-conditionally-assign loop pointer pattern differently — possibly using a single common 'ptr' variable initialized at function entry then overwritten in each case to push the compiler toward the early-hoisted addi r4,r31 pattern. Alternative: case-4's redundant 'mode==0||mode==2||mode==4' check might need to be kept as an explicit if/else even though it's tautological at that point.

