---
function: it_802799E4
tu: src/melee/it/it_2725.c
headline: sdata2-float + reloc-symbol-false-positive
tags: [sdata2-float, reloc-symbol-false-positive, permuter-false-positive, regalloc, tu-wide-data, sdata2-anonymous-floats]
---
## it_802799E4 (`src/melee/it/it_2725.c`) — sdata2-float + reloc-symbol-false-positive

- **Tags:** `sdata2-float`, `reloc-symbol-false-positive`, `permuter-false-positive`, `regalloc`, `tu-wide-data`, `sdata2-anonymous-floats`
- **Best fuzzy:** 99.8507%
- **Diagnosis:** 5 mismatches on 67-instr function: 3 are sdata2-float reloc-false-positives (it_804DC7B0@sda21 vs @2219@sda21 for F32_MAX; it_804DC7AC@sda21 vs @293@sda21 for 0.0f) and 2 are correlated regalloc (lbz r28 vs lbz r0 / extrwi r28,r28 vs extrwi r28,r0). Fuzzy=99.85%, strict=99.63%. The r28/r0 swap is downstream of the sdata2 layout: when F32_MAX and 0.0f are compiled as anonymous pool entries (base) the compiler loads via r0 scratch; when compiled as named statics (target) the compiler loads directly into r28. The function source is semantically and structurally correct.
- **Tried:** Attempt 1: introduced u8 raw intermediate (raw = *(u8*)cmd->u; opcode = (raw>>2)&0x3F) -- frame grew by 8 bytes causing 14 mismatches. Attempt 2: split opcode assignment across two statements (opcode = *(u8*)cmd->u; opcode = (opcode>>2)&0x3F) -- triggered full regalloc cascade (r29/r30 swap, opcode moved to r30), 24 mismatches. Both reverted.
- **Likely fix:** TU-wide reconstruction of missing sdata2 statics: at minimum need to declare static const f32 it_804DC7AC = 0.0f and static const f32 it_804DC7B0 = F32_MAX in correct order relative to adjacent named statics (it_804DC7A8, etc.). Target sdata2 has 0x128 bytes vs base 0xA4 bytes - many statics from it_804DC770 to it_804DC7B0 are missing. Multi-symbol cross-function TU-wide refactor required; fixing all missing sdata2 entries from that range would likely fix all 5 mismatches simultaneously (3 reloc-false-positive + 2 regalloc downstream).

