---
function: it_802799E4
tu: src/melee/it/it_2725.c
headline: tu-data-osreport + cross-tu-globals
tags: [tu-data-osreport, cross-tu-globals, paired-siblings, reloc-symbol-false-positive]
---
## it_802799E4 (`src/melee/it/it_2725.c`) — tu-data-osreport + cross-tu-globals

- **Tags:** `tu-data-osreport`, `cross-tu-globals`, `paired-siblings`, `reloc-symbol-false-positive`
- **Best fuzzy:** 99.8507%
- **Diagnosis:** Fuzzy 99.85% / strict 99.63%, 5 mismatches on a 67-instr function. All mismatches reduce to TU-wide sdata2 anchor divergence: (1) 3 ARG_MISMATCH on it_804DC7B0@sda21 (F32_MAX) and it_804DC7AC@sda21 (0.0f) vs base @2545@sda21 / @293@sda21 anonymous compiler-generated literal pool entries. (2) 2 ARG_MISMATCH on lbz r28 vs r0 then extrwi r28, r28 vs extrwi r28, r0 at the opcode load before Command_Execute - this is a correlated register-alloc swap downstream of the literal-pool layout (mwcc moves the opcode lbz forward into r0 in base because the float-literal materialization shape differs). Function source itself is structurally and semantically correct: simple cmd-loop dispatcher reading opcodes from cmd->u, calling Command_Execute, dispatching it_803F22A8[opcode-10] table. m2c output matches the existing source.
- **Tried:** Manual diff inspection only; no source-shape attempts. Per Permuter Boundary rules ('Do not use permuter for false diffs caused by equivalent BSS/global base symbols'), permuter is contraindicated for reloc-symbol/anchor false-positives. Same blocker family as sibling it_80274DAC and it_80274740 in same TU; their notes confirm permuter cannot bridge literal-pool/anchor divergence (it_80274DAC plateaued at score=5 after 70min cluster idle).
- **Likely fix:** TU-wide reconstruction of the commented-out sdata2 statics block at it_2725.c:4716+ (the '/// static f32 it_804DC7AC = 0.0f;' / '/// static f32 it_804DC7B0 = F32_MAX;' comments at lines 4747-4748). Declaring these as real named static const f32 entries (in correct order with surrounding it_804DC708/77C/780/788/790/etc) should make mwcc anchor via the named symbols instead of generating @NNN local literals. Once declared, all 5 mismatches likely resolve simultaneously since the r28/r0 register swap is downstream of literal-pool layout (mwcc reschedules the lbz when literal materialization changes shape). Pairs with it_80274DAC, it_80274740 - same TU, same fix. Multi-symbol cross-function refactor, beyond single-function scope.
