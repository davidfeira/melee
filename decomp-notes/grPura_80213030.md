---
function: grPura_80213030
tu: src/melee/gr/grpura.c
headline: regalloc + permuter-resistant
tags: [regalloc, permuter-resistant]
---
## grPura_80213030 (`src/melee/gr/grpura.c`) — regalloc + permuter-resistant

- **Tags:** `regalloc`, `permuter-resistant`
- **Best fuzzy:** 95.5556%
- **Diagnosis:** 2 instr off (95.56%): compiler emits 'addi r0,r3,@l; mr r31,r0' (route-via-r0) where target has 'addi r31,r3,@l' directly. Upstream/master has void signature + UNUSED char _[8] which is semantically identical to our source but upstream claims matched. Tried void signature: regressed frame offsets (spC 0xc->0x8, 5 mismatches). Both arg0 and void variants still produce r0-intermediate addi pattern.
- **Tried:** 1) void signature + updated call site/decl: spC frame offset changed 0xc->0x8, 5 mismatches. 2) Prior: s8* alias rewrite and var_r30/var_r31 swap regressed to 7-8 mismatches. Upstream-identical source still compiles differently in our build.
- **Likely fix:** Investigate why upstream build gets different codegen for identical source. Possible difference in includes, compiler flags, or TU-level context. Check grpura.h/.static.h for forward-decl differences, or whether a surrounding function's frame/inlining affects regalloc of this one.

