---
function: grPura_80213030
tu: src/melee/gr/grpura.c
headline: regalloc + permuter-false-positive
tags: [regalloc, permuter-false-positive]
---
## grPura_80213030 (`src/melee/gr/grpura.c`) — regalloc + permuter-false-positive

- **Tags:** `regalloc`, `permuter-false-positive`
- **Best fuzzy:** 92.9167%
- **Diagnosis:** 2 instr off (95.56%): base emits 'addi r0, r3, @l ; mr r31, r0' (extra mov-via-r0) where target emits 'addi r31, r3, @l' directly. Permuter outputs scoring 100 introduce a dead 's8* new_var = (s8*)var_r31' alias and reroute body field accesses through it (without incrementing it). Porting that variant locally REGRESSES to a third live local (r29 added, frame grows from 0x28 to 0x30) - the permuter scorer is matching on stale instruction patterns that don't translate to a real source-level rewrite. Looks like permuter false-positive territory; the real target compiler made a regalloc choice we cannot trigger via simple decl/order/alias reshuffling.
- **Tried:** 1) added 's8* new_var = (s8*)var_r31' alias and rerouted body M2C_FIELD reads through new_var (per output-100-1 / output-100-2): regressed - frame grew 0x28->0x30, r29 added, base now diverges in 8+ insns. 2) swapped declaration order of var_r30 / var_r31: regressed - r30/r31 roles swapped, now diverges in 7 insns.
- **Likely fix:** Possibly cluster permuter with different randomization seed, or accept as permuter-territory regalloc that needs a more creative source-shape (e.g. function inlining, splitting init from decl across statements, manipulating initializer expression). The two surviving 100-score outputs are scorer-only matches (semantically dead alias) that don't actually compile to matching bytes once ported.
