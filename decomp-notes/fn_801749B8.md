---
function: fn_801749B8
tu: src/melee/gm/gmresult.c
headline: regalloc + upstream-regression
tags: [regalloc, upstream-regression]
---
## fn_801749B8 (`src/melee/gm/gmresult.c`) — regalloc + upstream-regression

- **Tags:** `regalloc`, `upstream-regression`
- **Best fuzzy:** 98.7143%
- **Diagnosis:** At 99.28% (5 mismatches) with data-ptr restructure: remaining diff is r0/r4 swap in the GXSetScissor argument computation (lwz r4/r0 0x34(r1) for spC.y, then andi./rlwinm cascade). Upstream has identical C code and is matched; local diverges due to changed function signatures in gm_1601.h affecting compile-time regalloc. The data-ptr local fix (ResultsData* data = &lbl_8046DBE8) resolved 4 mismatches by eliminating extra r30 callee-save + addi r30,r31,0x1c pattern.
- **Tried:** 1) Direct lbl_8046DBE8.cobj access: 9 mismatches (92%), extra r30 save/restore + addi r30,r31,0x1c. 2) ResultsData* data local ptr: 5 mismatches (99.28%), no r30. 3) bottom/top split vars: 9 mismatches (98.71%), f0/f1 swap. Attempt 2 reverted to best state from attempt 1.
- **Likely fix:** Permuter (off per task instructions). The 5 remaining mismatches are pure register coloring: compiler picks r0 vs r4 for the spC.y integer temp. The ResultsData* data form is correct semantic structure.

