---
function: un_80305058
tu: src/melee/ty/toy.c
headline: frame-size + mwcc-branch-inversion
tags: [frame-size, mwcc-branch-inversion, regalloc]
---
## un_80305058 (`src/melee/ty/toy.c`) — frame-size + mwcc-branch-inversion

- **Tags:** `frame-size`, `mwcc-branch-inversion`, `regalloc`
- **Best fuzzy:** 93.6484%
- **Diagnosis:** 93.2% match (82 mismatches). Two structural blockers: (1) Frame is 0x998 vs target 0x9b0 (diff=0x18=24 bytes). Root cause: target precomputes tableBase=&un_804A284C[5] into a dedicated register (r27) before the loop, generating 'addi r27, r3, 0xa' at offset 48; this variable uses one more saved register slot, which also shifts the local array positions from sp+0x18/0x4ac to sp+0x2c/0x4c0. Source needs explicit 'u16* tableBase = &un_804A284C[5]' declared before loop. (2) arg1==0x63 branch generates 'cmpwi+beq loop_bottom' (1 instruction, branch-inversion optimization) instead of target's 'cmpwi+bne+cmpwi arg2+beq loop_bottom+b loop_bottom' (4 instructions). MWCC at -O4,p optimizes away the empty 'if (arg2 != 0) {}' body. Adding explicit goto-labels made things worse (moved lis instruction). The 3-instruction arg2 check sequence might need a different source structure to force the compiler to generate it. Remaining 75+ mismatches are register allocation.
- **Tried:** (1) while loop version: same result. (2) do-while loop version: same result. (3) Tried explicit 'goto next_iter' for arg1==99 case: made things worse (92.4%, lis instruction moved). (4) Tried 'if (arg2 != 0) {}' empty body: compiler optimizes it away into single beq.
- **Likely fix:** Add 'u16* tableBase = &un_804A284C[5];' local variable before loop to fix frame size and DIFF_REPLACE issues. For the arg1==99 empty-body issue: try using tableBase in a no-op way inside the empty block, or find the exact source pattern that MWCC GC/1.2.5n generates the redundant arg2 check from. After fixing structural issues, dispatch to permuter for remaining register allocation mismatches.

