---
function: gm_80166CCC
tu: src/melee/gm/gm_1601.c
headline: regalloc + instruction-scheduling
tags: [regalloc, instruction-scheduling, mwcc-loop-opt, permuter-territory]
---
## gm_80166CCC (`src/melee/gm/gm_1601.c`) — regalloc + instruction-scheduling

- **Tags:** `regalloc`, `instruction-scheduling`, `mwcc-loop-opt`, `permuter-territory`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 86.10% (121 mismatches) after 2 attempts. Function merges MatchEnd round data (arg0=prev, arg1=curr): copies result (7/8), accumulates player/team is_big_loser from arg0+arg1 (n_winners/n_team_winners sections), counts active players/teams into r5/r6, does CTR=2 loop updating arg0 player data from arg1, CTR=5 loop updating arg0 team data. Core issues: (1) n_winners unrolled section uses MatchEnd* r8=arg1+0xA8, r9=arg0+0xA8 for player field access at offsets 0x5D/0x5E; compiler setup order is addi r8 first then slot check from r4 then addi r9; (2) n_team_winners: MatchEnd* r8=arg1+0x0C, access team[0] fields at 0x24/0x25/0x26 from that base; (3) count-teams section accesses 6 items not 5 (6th at arg1+0x62 overlaps player_standings[0].self_destructs - possibly struct bug); (4) CTR=2 loop processes 3 players per iteration over 6 players total; (5) CTR=5 loop advances arg0/arg1 by sizeof(MatchTeamData)=0xC each iteration.
- **Tried:** Attempt 1: MatchPlayerData* r8/r9 starting at player_standings[1] (wrong - gave 0x5/0x6 offsets instead of 0x5D/0x5E), 86.70% 148 mismatches. Attempt 2: MatchEnd* r8/r9 with sequential if+advance pattern, 86.10% 121 mismatches; r8/r9 setup order still wrong, slot_type checks using wrong base register combinations.
- **Likely fix:** Permuter should resolve register allocation and instruction scheduling. Key structural insight: use MatchEnd* for all sliding pointers. n_winners: init r8=arg1+0xA8 before slot check, check arg1->player_standings[1].slot_type using arg1 directly, then setup r9=arg0+0xA8 after. Then sequential: advance both, check arg1->player_standings[k] directly. Count-teams: use MatchEnd* r5t=arg1+0x0C, check r5t->team_standings[1].active sliding. The 6th team count check at +0x62 may need explicit struct reinterpretation or force by doing one extra iteration.

