---
function: lb_800115F4
tu: src/melee/lb/lb_00F9.c
headline: tu-sdata2-globals + regalloc
tags: [tu-sdata2-globals, regalloc, float-literal]
---
## lb_800115F4 (`src/melee/lb/lb_00F9.c`) — tu-sdata2-globals + regalloc

- **Tags:** `tu-sdata2-globals`, `regalloc`, `float-literal`
- **Best fuzzy:** 99.2254%
- **Diagnosis:** 99.0% (13 mismatch). One mismatch is the float literal pool: target uses named global lb_804D7BE8 (= 0.1 as double in .sdata2 at 0x804D7BE8) for the > 0.1 comparison, but base emits an anonymous @687 literal. The lb_00F9 TU has many unused named .sdata2 globals (lb_804D7BA0=1.0f, lb_804D7BA8=0.5, lb_804D7BB0=3.0, lb_804D7BB8=1.0, lb_804D7BC0=0.05, lb_804D7BC8=4503601774854144, lb_804D7BE0=0.1f, lb_804D7BE8=0.1d, lb_804D7BF0=pi/2, lb_804D7BF4=1e-5, etc, all .double/.float) not yet declared in source. Base TU sdata2 = 0xC8 bytes vs target 0xE0; adding lb_804D7BE8 alone would shift literal pool ordering across all functions in TU. The other 12 mismatches are a persistent r3<->r5 register allocation swap in the inline node-removal block (prev / head pointers in the while-loop scanner) — likely also influenced by literal pool layout shifting how the inliner spills. Same inline pattern appears in lb_80011850 per @todo comments.
- **Tried:** Swapping declaration order of var_r3/prev (no change). The reg-alloc swap is mechanical and persists.
- **Likely fix:** TU-level fix: declare all named lb_804D7B[A0..C80] globals as 'const f32 lb_804D7BXX = ...' / 'const f64 lb_804D7BXX = ...' in lb_00F9.c in the right order matching the asm sdata2 layout. Reference lb_804D7BE8 directly in the > 0.1 comparison (e.g. via 'extern const double lb_804D7BE8;'). This will likely also resolve the r3<->r5 swap as a side effect of correct literal pool ordering. Pattern parallels lbcollision.c which already declares lbColl_804D7A* constants at top of file.
