---
function: fn_80215B84
tu: src/melee/gr/grgreens.c
headline: permuter-territory + regalloc
tags: [permuter-territory, regalloc, mwcc-loop-opt, goto-loop]
---
## fn_80215B84 (`src/melee/gr/grgreens.c`) — permuter-territory + regalloc

- **Tags:** `permuter-territory`, `regalloc`, `mwcc-loop-opt`, `goto-loop`
- **Best fuzzy:** 82.3826%
- **Diagnosis:** Block search across 5x6 grGreens_BlockVars grid for one matching item_gobj, then HSD_ASSERT(1465, found) and call grGreens_802159B8(gp, j, i, arg3). Plateau at 89.3% (61 mismatches). Mwcc unrolls the inner j-loop into 6 explicit checks per outer iter; structural shape correct (115 instr matched). Remaining diff: scratch register churn (target uses r0 throughout; my base uses r6/r8), saved register ordering (target r28=arg3,r29=found_i,r30=found_j,r31=gp; mine r28=gp,r29=arg3,r30=found_j,r31=found_i), and target keeps r4=gp live across the loop reloading 0xCC(r4) each iter while my base spills gp to r28 and reloads 0xCC(r28). Also target maintains a single byte-offset accumulator (var_r6 += 0xC0 per outer iter) vs base recomputing slwi r4,r6,5. ioff-accumulator rewrite (attempt 2) regressed to 82.3%/72 mismatches because mwcc reordered scratch reg use further.
- **Tried:** (1) Nested for(i)/for(j) with 'goto done' on match using gp->gv.greens.x8_blocks[i*6+j] -> 89.3%/61. (2) Hand-unrolled inner with byte-offset accumulator ioff += 0xC0 using ((u8*)x8_blocks + ioff + j*0x20) cast -> 82.3%/72 (regressed). Reverted to attempt 1.
- **Likely fix:** Permuter territory: register allocation + scheduling. Decomp-permuter is offline per orchestrator override; queued for cluster dispatch when back online. Manual exploration of struct member access vs byte arithmetic, or different temp variable lifetimes, may shake loose r28/r31 ordering but hasn't worked in 2 attempts. Possibly arg3 is consumed in source via an inline early, biasing mwcc to spill it to r28 first.

