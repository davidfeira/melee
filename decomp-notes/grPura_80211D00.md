---
function: grPura_80211D00
tu: src/melee/gr/grpura.c
headline: frame-size + sdata2-float
tags: [frame-size, sdata2-float, permuter-false-positive, reloc-symbol-false-positive]
---
## grPura_80211D00 (`src/melee/gr/grpura.c`) — frame-size + sdata2-float

- **Tags:** `frame-size`, `sdata2-float`, `permuter-false-positive`, `reloc-symbol-false-positive`
- **Best fuzzy:** 99.9444%
- **Diagnosis:** After 2 attempts: (1) removed const from grPu_804DBA58 + switched to inline form for cam bounds calls — fixed f0/f2 regalloc and symbol refs, giving 99.57% strict / 99.94% fuzzy. (2) removed const from ALL grPu_804DBA5C..7C globals — sdata2 layout unchanged, still 7 mismatches. Remaining 7 mismatches: 4 are sdata2-float false positives (both sides show 'lfs f0, grPu_804DBA58@sda21' identically but raw encoding differs — same symbol, different sda21 addend/offset pre-link); 3 are real frame-size mismatches (stwu -0x20 vs -0x18, lwz 0x24 vs 0x1c, addi 0x20 vs 0x18). The 4 sdata2-float false positives block objdiff 100% but fuzzy treats them as equal. The 3 frame mismatches: target needs -0x20 frame but base generates -0x18. Vec3 cam_offset (12 bytes) + 8-byte overhead = 20->24 bytes = -0x18. Target has -0x20 (32 bytes = 8 + 24 bytes locals), implying 12 more bytes of locals beyond Vec3. No extra locals appear in the target asm (no stfs/lfs to stack slots beyond cam_offset at r1+0x8-0x13). Unknown source pattern that produces -0x20 without visible extra locals in asm.
- **Tried:** (1) Remove const from grPu_804DBA58 only: fixes symbol refs and f0/f2 regalloc with inline form (confirmed by grkinokoroute pattern match). (2) Remove const from ALL grPu_804DBA*: no change in sdata2 encoding mismatches, still 7 mismatches. (3) Add f32 fVar1 declaration (unused): MWCC allocated it at r1+0x8 pushing cam_offset to r1+0xc — wrong offset. Frame stayed -0x18. (4) Prior attempts (per notes): PAD_STACK(8) moved cam_offset to wrong offset. fVar1 as intermediate with const removed: f0/f2 swap persisted.
- **Likely fix:** The 4 sdata2-float false positives require matching the exact sda21 section layout — likely a TU-wide change ensuring ALL sdata2 variables compile to same offsets as target. The 3 frame mismatches need source pattern producing -0x20 frame with cam_offset at r1+0x8: needs 12 additional bytes of locals after Vec3. Possible: TWO additional f32 locals (Vec3=12 + f32+f32=8 = 20 bytes -> 28 -> 32 = -0x20) declared AFTER Vec3 and actually used. Or one local that's 12 bytes (another Vec3). The inline form must be kept for correct regalloc.

