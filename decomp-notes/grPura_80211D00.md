---
function: grPura_80211D00
tu: src/melee/gr/grpura.c
headline: cross-tu-globals + reloc-symbol-false-positive
tags: [cross-tu-globals, reloc-symbol-false-positive, regalloc, stack-offset, float-literal]
---
## grPura_80211D00 (`src/melee/gr/grpura.c`) — cross-tu-globals + reloc-symbol-false-positive

- **Tags:** `cross-tu-globals`, `reloc-symbol-false-positive`, `regalloc`, `stack-offset`, `float-literal`
- **Best fuzzy:** 97.7222%
- **Diagnosis:** 19 mismatches at 97.35%/97.72% on 54-instr function. Diff is 4 repetitions of pattern 'lfs cam_offset.coord; lfs grPu_804DBA58 (multiplier); fsubs; fmuls' — 4 sites for top/bottom/left/right ground bound calls. Each site has 4 mismatches: (a) reloc-symbol on the grPu_804DBA58 load — target uses named 'grPu_804DBA58@sda21', base uses anonymous '@161@sda21'; (b,c,d) f0/f2 register swap for the resulting fsubs/fmuls. Plus stwu -0x20 (target) vs -0x18 (base) frame-size delta of 8 bytes. The anonymous-pool diff is caused by 'const f32 grPu_804DBA58 = 0.8;' — mwcc constant-folds the const-qualified scalar global at use-site, emitting 0.8 to the @161 literal pool instead of referencing the named symbol. symbols.txt declares grPu_804DBA58..7C as scope:global type:object data:float in sdata2, suggesting they should be regular non-const globals to suppress folding. Removing 'const' fixed all 8 reloc-symbol mismatches (named symbol reference now matches), but mismatch count stayed at 19 because the f0/f2 regalloc swap persists and PAD_STACK(8) variant fixed the frame-size but pushed cam_offset to 0x10/0x14 instead of target's 0x8/0xc. Mwcc allocates locals top-down in declaration order, so adding char[8] above OR below cam_offset doesn't yield target layout (cam_offset low + 8 bytes pad somewhere).
- **Tried:** (1) Drop 'const' qualifier from grPu_804DBA58..7C declarations: changed 8 instructions from anonymous '@161@sda21' to named 'grPu_804DBA58@sda21' (REAL progress), but f0/f2 regalloc swap kept mismatch count at 19. (2) PAD_STACK(8) at top of function (with const removed): frame became 0x20 matching target, but cam_offset moved to r1+0x10/0x14 instead of target's r1+0x8/0xc — wrong direction. (3) Reorder decls (f32 fVar1 first, Vec3 cam_offset second): made it worse (97.35% but cam_offset offset shifted wrong way again).
- **Likely fix:** Two-step fix needed: (a) Drop 'const' from grPu_804DBA58 (and likely all grPu_804DBA5C/70/74/78/7C for consistency with symbols.txt scope:global declaration) to restore named-symbol references — confirmed working. (b) For the cam_offset stack layout (target r1+0x8 with frame 0x20), need to find a way to push cam_offset DOWN and reserve 8 bytes ABOVE it. Possible: declare an additional unused 8-byte local (f64 or u8[8]) AFTER cam_offset in declaration order so mwcc allocates it at higher offset, pushing cam_offset to lower offset. Or use 'register' hint on cam_offset. Pure regalloc f0/f2 swap is permuter-territory but only after fixing the @161 false-positive (since strict objdiff counts symbol diffs). Permuter dispatch likely won't help while const remains because permuter scorer treats @161 vs grPu_804DBA58 as equivalent and won't optimize for that. Suggest: drop const + permuter dispatch with PERM_ADD_VARIABLE for the cam_offset placement issue.
