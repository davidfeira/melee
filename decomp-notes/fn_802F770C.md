---
function: fn_802F770C
tu: src/melee/if/if_2F72.c
headline: mwcc-const-fold + instruction-scheduling
tags: [mwcc-const-fold, instruction-scheduling]
---
## fn_802F770C (`src/melee/if/if_2F72.c`) — mwcc-const-fold + instruction-scheduling

- **Tags:** `mwcc-const-fold`, `instruction-scheduling`
- **Best fuzzy:** 93.1356%
- **Diagnosis:** MWCC refuses to emit walking-pointer addi r4,r4,0x8 updates; collapses ptr+=2 into base-relative offsets (lwz 0xc/0x14/0x1c/0x24/0x2c) instead of updating r4 and using 0x4/0xc. Target requires addi r4,r4,0x8 before slot-1 lwz and after slot-2..5 lwz, forming a true walking-pointer pattern. 9 mismatches = 4 missing addis + 5 wrong offsets, all same structural cause.
- **Tried:** Attempt 1: removed temp variable for slot-1, using ptr[1] inline after ptr+=2. Attempt 2: tried comma-operator (ptr+=2,ptr[1])==gobj. Both attempts: MWCC folds ptr[1]-after-ptr+=2 into base[3] = lwz 0xc(r4) without addi, ignoring pointer update because it can express all offsets directly from original base.
- **Likely fix:** Needs a source form that forces MWCC to track r4 as a live walking pointer. Possible approaches: (1) use a struct Pair{void*a,*b} with ++pair for slot-1 then pair[1].b pre-save for slots 2-5; (2) use volatile intermediate for r4 update; (3) find the exact MWCC optimization level or pragma that prevents offset-folding. Alternatively this may be permuter-territory once permuter is re-enabled.

