---
function: fn_80235F80
tu: src/melee/mn/mnstagesw.c
headline: permuter-territory + regalloc
tags: [permuter-territory, regalloc, mwcc-loop-opt]
---
## fn_80235F80 (`src/melee/mn/mnstagesw.c`) — permuter-territory + regalloc

- **Tags:** `permuter-territory`, `regalloc`, `mwcc-loop-opt`
- **Best fuzzy:** (unknown)
- **Diagnosis:** At 95.12% / 22 mismatches after structural rewrite. Source shape is correct: stack frame matches (0x50), control flow matches (early-return on 0x20 button, nested 0x200 with hovered<0x1D check, 0x100 path, fallback 0xF d-pad). Remaining mismatches are permuter territory: (1) count loop in confirmed!=0 branch — target uses mtctr+bdnz with r3/r4 reg pair sharing r30=0 init via 'mr r3, r30'; mwcc base produces subic./bne on r5/r6 with separate li r6,0. (2) 'extsb. r0, r0' vs target 'cmplwi r0, 0x0' on confirmed_selection u8 zero-test. (3) regalloc r28/r30 swap on confirmed_selection address pointer. (4) regalloc r29/r30 swap and r3/r4 swap in inlined sync loop at .L_802360A0. The inlined sync loop (copy of mnStageSw_8023593C body) was kept inline to match — the named static function exists separately with #pragma dont_inline.
- **Tried:** (1) Initial draft with arr2[(u8)i] indexing — 88.9%. (2) Added u8 pad[16] for frame size, kept lbzx access — 88.1% (frame still wrong). (3) Rewrote count loop as do-while with explicit ctr countdown + ternary 'cond=0/1; if(cond)' pattern, frame pad[40] — 93.1%. (4) Changed second sync loop to use 'p = arr + (u8)j; *p' pattern matching mnStageSw_8023593C — 94.6%. (5) Inverted gm_801A4310 condition — regressed to 93.5%, reverted. Final: 95.1%.
- **Likely fix:** Permuter cluster run targeting regalloc + loop induction variables. The structural source is fully correct; only register choices, mtctr vs subic. count loop pattern, and u8 sign-extension on zero compare remain. Try permuter passes that swap struct member access patterns (mn_804A04F0.confirmed_selection vs cached pointer) and reorder local variable declarations to influence register allocation.

