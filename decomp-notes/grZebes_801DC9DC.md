---
function: grZebes_801DC9DC
tu: src/melee/gr/grzebes.c
headline: cross-function-rodata + literal-pool
tags: [cross-function-rodata, literal-pool, tu-wide-data]
---
## grZebes_801DC9DC (`src/melee/gr/grzebes.c`) — cross-function-rodata + literal-pool

- **Tags:** `cross-function-rodata`, `literal-pool`, `tu-wide-data`
- **Best fuzzy:** 99.8775%
- **Diagnosis:** All 12 mismatches are +0x30 offsets on loads from r7 (anchor grZe_803B7FF0). Root cause: our base.o emits an extra 0x30-byte rodata literal @595 at offset 0x54 (between grZe_803B7FF0/802C/8038 and grZe_803B8044), pushing grZe_803B8044 from rodata 0x54 (target) to 0x84 (base). The literal @595 contains 12 floats {-320,0,0,0,-80,1,20,30,90,90,75,0} which match the local-array initializer 'grZe_ColorEntry table[3]' in grZebes_801DA0C4 (line 1035-1039). In the DOL/target build that initializer was inlined (stack stores) without emitting a rodata pool; in our build mwcc emits it as a const pool literal that ends up before grZe_803B8044. The 803B8044 target asm and our generated bytes for it match — only its position in rodata differs because of the leading @595.
- **Tried:** prep --m2c --similar 3 --examples 2 (no useful sibling), diff (12 DIFF_ARG_MISMATCH all +0x30), inspected target rodata layout vs our base.o symbol table to identify the @595 literal source.
- **Likely fix:** Fix is in grZebes_801DA0C4 (NOT in grZebes_801DC9DC). Either rewrite the local array as direct stack stores (per-element assignment), or change the type/struct so mwcc inlines the initializer instead of emitting a const pool. Possibly the original src declared 'grZe_ColorEntry table[3]' with non-const auto initializer in a way that produces stack-store init. Note: this is cross-function — fixing grZebes_801DC9DC alone is impossible because the rodata layout is determined by all functions in the TU.
