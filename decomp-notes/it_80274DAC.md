---
function: it_80274DAC
tu: src/melee/it/it_2725.c
headline: tu-data-osreport + cross-tu-globals
tags: [tu-data-osreport, cross-tu-globals]
---
## it_80274DAC (`src/melee/it/it_2725.c`) — tu-data-osreport + cross-tu-globals

- **Tags:** `tu-data-osreport`, `cross-tu-globals`
- **Best fuzzy:** 99.75%
- **Diagnosis:** Down to 2 mismatches from 3 with score=5 permuter candidate (temp_r4 = item / paired-store fusion via 'unk_y = (unk_x = 0.0F)'). Best fuzzy: 99.75%, strict: 99.5%. Remaining mismatches: (1) literal pool 0.0f anchor target=it_804DC708@sda21 base=@293@sda21; (2) register r3 vs r4 on lfs f0,0x4c — likely downstream of the literal pool fix.
- **Tried:** score=5 permuter source-shape applied (paired store + temp_r4=item assignment in one of three writes); merged variant without inner block regressed to 3 mismatches; single-var variant regressed to 3 mismatches
- **Likely fix:** TU-wide reconstruction of sdata2 statics. it_2725.c:4716 onward has it_804DC708/70C/710/714/718/720/728/72C/730/734/738/etc all commented as '///'. Asm at it_2725.s:17089 shows it_804DC708 is the global sdata2:0x0 anchor referenced from many functions. Need to uncomment/declare all sdata2 statics in correct order so MWCC stops generating local @NNN literals and the anchor symbol matches. Multi-symbol cross-function refactor, beyond single-function scope. Cluster permuter previously plateaued at score=5 after 70min idle — confirms register/scheduling-only nudge can't bridge the literal pool divergence.
