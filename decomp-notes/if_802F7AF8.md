---
function: if_802F7AF8
tu: src/melee/if/if_2F72.c
headline: regalloc + permuter-territory
tags: [regalloc, permuter-territory]
---
## if_802F7AF8 (`src/melee/if/if_2F72.c`) — regalloc + permuter-territory

- **Tags:** `regalloc`, `permuter-territory`
- **Best fuzzy:** 94.4681%
- **Diagnosis:** 12 mismatches (10 regalloc + 2 real instr-position swap of clrlwi r4, r27, 24). Target keeps slot in r27 alive THROUGH the entry pointer arithmetic so clrlwi r4 happens just before bl fn_802F77F8 (offset 2208); base hoists clrlwi r4 EARLY (right after the rlwinm shift) so r27 becomes free and gets reused as the entry pointer. Target uses r28 for entry, base uses r27. Cascade: target uses r27 as base1 after bl (addi r27, r30, 0x4), base uses r28; target uses r29 for second-iter entry pointer, base uses r30 (overwriting base register). Same input, just different register-allocation choices — no instruction count delta, no frame size delta (both 0x28).
- **Tried:** V1: hoist *++entry into 'gobj' local before fn_802F77F8 call. WORSE: 16 mismatches, mr r3,r0 inserted, addi r31,r3,0x0 reordered. V2: inline base[idx+1]= for first iter, drop result temp. WORSE: 15 mismatches, store offset becomes 0x4 vs 0x0, base register r30 used directly.
- **Likely fix:** Pure register-allocation toss-up. Permuter --cluster with PERM_RANDOMIZE_TYPE / PERM_TEMP_FOR_EXPR / PERM_INT_TYPE on slot/slot2/idx, possibly PERM_ADD_VARIABLE to nudge mwcc into keeping slot live until just before the bl. The base/target asm differs only in WHICH gpr holds slot, entry, base1 — same algorithm. clrlwi position swap is the load-bearing change; everything else cascades from it.
