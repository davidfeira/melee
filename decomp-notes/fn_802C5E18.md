---
function: fn_802C5E18
tu: src/melee/it/items/itmewtwoshadowball.c
headline: dead-loads + dead-cmpw
tags: [dead-loads, dead-cmpw, mwcc-aliasing]
---
## fn_802C5E18 (`src/melee/it/items/itmewtwoshadowball.c`) — dead-loads + dead-cmpw

- **Tags:** `dead-loads`, `dead-cmpw`, `mwcc-aliasing`
- **Best fuzzy:** 70.2941%
- **Diagnosis:** 17-instr leaf with 4 unexplained dead instructions: lwz x18 to r3, lwz x1C to r0, cmpw r3 r0 (signed compare, result discarded, no branch), then x20 increment proceeds with addi r3,r4,0x1 (forced into r4 because r3 still claimed by x18). 6 mismatches at 70.29%.
- **Tried:** (prior) bare 'x18==x1C;', ternary identical arms, empty-body if. (this agent) (A) bool unused = x18==x1C; mwcc dropped both load/cmp. (B) if(x18==x1C){body}else{body} duplicated -- emits branch+full body twice, drops to 17%.
- **Likely fix:** Pattern unknown. Not HSD_ASSERT (would call __assert+frame). Not a struct side-effect. Possibly a custom debug macro that compiles to dead loads+cmpw in release. Worth grepping for similar load,load,cmpw,no-branch fingerprints. Permuter false-positive territory: cmpw without consumer is the kind of thing permuter scorer treats equivalent to no-op but report.json fuzzy does not. DO NOT dispatch permuter.
