---
function: fn_802C5E18
tu: src/melee/it/items/itmewtwoshadowball.c
headline: dead-cmpw + dead-loads
tags: [dead-cmpw, dead-loads, mwcc-aliasing, permuter-territory]
---
## fn_802C5E18 (`src/melee/it/items/itmewtwoshadowball.c`) — dead-cmpw + dead-loads

- **Tags:** `dead-cmpw`, `dead-loads`, `mwcc-aliasing`, `permuter-territory`
- **Best fuzzy:** 94.1176%
- **Diagnosis:** 94.1% match (1 mismatch). Current code has 'bool _unused = x18==x1C; if (_unused) { return; }' which generates the needed cmpw+loads but also adds beqlr (conditional return to LR). Target has cmpw r3,r0 dead (no branch). Need x18/x1C loads + cmpw with zero conditional branch. MWCC drops the comparison entirely when not used in a branch (void-cast, bare assign), keeps it only when used in if/branch. beqlr = MWCC tail optimization of 'if (cmp_result) return' in leaf function. No known C idiom produces dead cmpw without branch in this context.
- **Tried:** (1) bool _unused=x18==x1C; (void)_unused; -- MWCC drops everything, 70.29%. (2) bool _unused=x18==x1C; if(_unused){return;} -- produces beqlr, 94.1%. Attempts by prior agents: bare 'x18==x1C;', ternary identical arms, empty-body if, if(x18==x1C){body}else{body}.
- **Likely fix:** Permuter should find the idiom -- likely a MWCC-specific expression form that forces the comparison to be computed (keeping cmpw) but whose branch is statically eliminated (no beqlr). Possibly: ternary with same result both arms, or a dead-path assignment that MWCC knows is unreachable. Could also be mwcc-branch-inversion where the beqlr is replaced by a bne to a label that falls through immediately.

