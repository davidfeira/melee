---
function: grOldYoshi_8020F31C
tu: src/melee/gr/groldyoshi.c
headline: tu-data-osreport + cross-tu-globals
tags: [tu-data-osreport, cross-tu-globals, literal-pool]
---
## grOldYoshi_8020F31C (`src/melee/gr/groldyoshi.c`) — tu-data-osreport + cross-tu-globals

- **Tags:** `tu-data-osreport`, `cross-tu-globals`, `literal-pool`
- **Best fuzzy:** 79.7414%
- **Diagnosis:** 79.74% fuzzy (26 strict mismatches). Target uses named .sdata2 globals 'grOy_804DB9A8' (0.0f) and 'grOy_804DB9C0' (0.5f) as the fcmp/fmuls operands; base mwcc generates anonymous '@234'/'@415' literal-pool entries instead. Both resolve to same byte offsets in linked binary, but objdiff penalizes the symbol-name mismatch. Additionally, base sdata2 has 16 mystery bytes at the start (0x00=0.5(double), 0x08=3.0(double)) that target lacks (target sdata2=0x20, base=0x30); these unreferenced doubles are not produced by any visible source construct (rand_range, randi_between, HSD_Randf usage all checked). Beyond the symbol naming, the asm scheduling itself differs: target interleaves the abs(fVar1) computation with the multiplication chain (fmuls f7,f11,f7; fmuls f8,f9,f8; fcmpo f10,f0; fmuls f0,f8,f11; fmadds f0,f11,f0,f7; bge; fneg f10,f10) while base computes abs(fVar1) before the multiplications. Target uses register f7 for both abs(param1) and (fVar3*fVar2); base separates them as f9 and f0.
- **Tried:** (1) Reordered '+': '(fVar3 * fVar2) + fVar3 * (-param2 * 0.5f * fVar3)' instead of original lhs-first form -> dropped to 79.48%. (2) Removed dead static inline 'randi_between' (truly never called) -> sdata2 byte content unchanged, no effect. (3) Added named float globals 'f32 grOy_804DB9A8 = 0.0f' etc. as 'const'/non-const/'extern const' and rewrote function body to use them -> 'const' got constant-folded back to @234 literal (no improvement); without const dropped to 73.5% because mwcc hoists the global load into a single fmr (no reload per use) while target reloads from sda21 each time. The function only matches if mwcc TREATS '0.0f'/'0.5f' as anonymous literal-pool entries while LINK-time symbol resolution provides them as named extern globals.
- **Likely fix:** Likely cross-TU. The named sdata2 globals (grOy_804DB9A8/AC/B0/B8/C0/C4) probably need to be defined in a separate TU/asm file as ACTUAL globals while staying UNDEFINED (extern) in groldyoshi.c. Source-level use of '0.0f'/'0.5f' must remain so mwcc emits @nnn anonymous pool entries that the linker then merges with the named externs. Pattern is similar to gm_801A9B30_OnEnter (named-symbol vs section-anchor reloc) but inverted - here we need the linker to use the named symbol because dtk's asm extraction renamed the @nnn target to grOy_804DB9A8. The mystery 0.5(double)/3.0(double) at sdata2 offset 0x00/0x08 in base is also unexplained - they appear in base.o but not target.o; could be a header-include-order pollution or compiler artifact from -O4,p, source not yet identified. Could potentially be cracked manually by inspecting other gr/ TUs' source for the float-globals declaration pattern (grOy_8020F31C is the only F31C-style helper here).
