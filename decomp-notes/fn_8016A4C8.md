---
function: fn_8016A4C8
tu: src/melee/gm/gm_1601.c
headline: permuter-queued + bss-anchor
tags: [permuter-queued, bss-anchor, sdata2-named, regalloc, permuter-false-positive]
---
## fn_8016A4C8 (`src/melee/gm/gm_1601.c`) — permuter-queued + bss-anchor

- **Tags:** `permuter-queued`, `bss-anchor`, `sdata2-named`, `regalloc`, `permuter-false-positive`
- **Best fuzzy:** (unknown)
- **Diagnosis:** Function structurally matches at 86.66% with 103 mismatches. Bulk of mismatches are bss-anchor false positives (target uses lbl_8046B488@ha/@l directly; base uses bss.0@ha/@l + 0x110 offset producing identical post-link bytes). Also sdata2-named float (target: lbl_804DA28C@sda21; base: @470@sda21) for the 0.0f compare constant. Remaining real differences are pure register allocation: target keeps r25=fn_8016A488 across loop, base reassigns r25 to &lbl_8046B488+0x110 and r24 to fn_8016A488. Body shape, loops, struct field accesses, callback dispatch via M2C_FIELD(..., 0x1BC), and bitfield tests all line up.
- **Tried:** Attempt 1: straightforward translation of m2c output with double-test (cond ? 1 : 0; if != 0) for unk_10_b1 gate, lbl_8046B488.x1A6[i] indexing -> 83.27%. Attempt 2: pointer-walk style 's8* p = (s8*)&lbl_8046B488; p[0x1A6] = ...; p++' plus PAD_STACK(0xD0) to match -0x118 frame -> 86.66%, frame size now -0x120 (close to target -0x118). Could not eliminate bss-anchor / sdata2 reloc differences via source-shape changes.
- **Likely fix:** Permuter pass to converge regalloc (r24/r25 swap, r26 vs r3 mr) and possibly tighten frame size by 8 bytes (PAD_STACK(0xCC) maybe). bss-anchor and sdata2-named reloc differences are the documented permuter-false-positive class so the permuter scorer will treat them as already-equivalent.

