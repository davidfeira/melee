---
function: pl_80037C60
tu: src/melee/pl/pltrick.c
headline: stack-offset + frame-size
tags: [stack-offset, frame-size]
---
## pl_80037C60 (`src/melee/pl/pltrick.c`) — stack-offset + frame-size

- **Tags:** `stack-offset`, `frame-size`
- **Best fuzzy:** 99.9802%
- **Diagnosis:** 2 mismatches at 99.98% fuzzy. Target stw r4, 0xc(r1) and lwz r0, 0xc(r1) for the volatile s32 prev2070_word slot; base uses 0x1c(r1). Frame size 0x28 in both, r31 saved at 0x24, but target unifies the param-save location for r4 (prev2070_int) WITH the volatile slot at sp+0xC. Base allocates two distinct slots: param-save at 0x14 and volatile at 0x1C. Source stores r4(prev2070_int) into volatile prev2070_word; mwcc target collapses these into one stack slot at the bottom of the locals region, ours doesn't. Existing @todo comment notes 'volatile hack' is misaligned.
- **Tried:** V1: moved volatile s32 prev2070_word declaration AFTER union prev_union and union ev (last) — got worse, 11 mismatches at 99.89%, unions shifted. V2: declare-and-init combined 'volatile s32 prev2070_word = prev2070_int;' — no change, baseline 2 mismatches.
- **Likely fix:** permuter — pure stack-allocation/scheduling decision. The volatile prevents normal CSE/forwarding; only mwcc-internal shuffles can pick which slot the volatile lands in. May benefit from PERM_INT_TYPE/PERM_RANDOMIZE_TYPE on the volatile temp, or addition of an inline that exposes the param-save unification. Permuter cluster recommended.
