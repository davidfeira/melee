---
function: fn_80251FE4
tu: src/melee/mn/mninfo.c
headline: permuter-queued + permuter-territory
tags: [permuter-queued, permuter-territory, regalloc, frame-size]
---
## fn_80251FE4 (`src/melee/mn/mninfo.c`) — permuter-queued + permuter-territory

- **Tags:** `permuter-queued`, `permuter-territory`, `regalloc`, `frame-size`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 93.6% match (46 mismatches) after 2 source-shape attempts. Remaining diff is dominated by register allocation: target uses r28=dw,r27=dr in Up branch and r27=dw,r26=dr in Down; mine produces r29=dw,r27=dr and r30=dw,r29=dr. Also a 24-byte stack-frame difference (target 0x40 vs base 0x28) — target reserves extra locals for which I see no purpose in the asm. Loop counters and temp lifetimes differ across branches. Permuter territory.
- **Tried:** (1) Initial source produced 88.5%/55 mm; (2) Cached gobj=mnInfo_804D6C78 in a local before the post-free-loop, eliminating reloads in the call sequence -> 93.6%/46 mm. Used inlines.h helpers (sfxBack, sfxMove, Menu_DecrementAnimTimer, MenuInput_*) to match sibling style.
- **Likely fix:** Permuter to settle register allocation. The structural shape (free-text loop, sequential mnInfo_80251D58/F04 calls inside a 0..3 loop with mnInfo_804A0968+scroll_idx walker) appears correct. Possible source-side investigation: why target reserves 0x18 bytes of locals — maybe a struct-by-value PAD_STACK or unused local that bumps frame allocation.

