---
function: ftKb_SpecialN_800F16D0
tu: src/melee/ft/chara/ftKirby/ftKb_Init.c
headline: struct-typing + regalloc
tags: [struct-typing, regalloc, frame-size, permuter-blocked]
---
## ftKb_SpecialN_800F16D0 (`src/melee/ft/chara/ftKirby/ftKb_Init.c`) — struct-typing + regalloc

- **Tags:** `struct-typing`, `regalloc`, `frame-size`, `permuter-blocked`
- **Best fuzzy:** 62.979%
- **Diagnosis:** 143-instr switch dispatcher on FighterKind, 32-entry jtbl ftKb_Init_803CA14C dispatching to internal labels that call it_8026B3F8 with various ft_80459B88 hat slots and ItemKind constants 0x82-0x9D. Reached 94.09% (36 mismatches) with hoisted base pointer + case order matching target source order. Three mismatch classes remain: (1) JUMP TABLE SYMBOL — target asm references named symbol ftKb_Init_803CA14C@ha/@l for jtbl base, mwcc emits anonymous @908. The explicit jtbl_t ftKb_Init_803CA14C placeholder at line 2620 references undefined static fns ftKb_SpecialN_800F1708..18F8 (those are local switch labels, not real functions); compiler emits its own @908 alongside, no clean way to direct mwcc's switch jtbl reloc to the named symbol. (2) BASE-POINTER REGISTER — target keeps &ft_80459B88 in caller-saved r5 (one lis/addi pair before switch, dead after first case-body load). Hoisting into local 'struct ft_80459B88_t* g' as we do produces base in callee-saved r31 because g is live across all cases (saved once, used across multiple bl). Frame thus differs: target stwu -0x20 / stw r31,0x1C(r1); base stwu -0x18 / stw r31,0x14(r1). (3) Single bgt branch: target has 'bgt .L_800F18F8' BEFORE jtbl lookup; base places equivalent branch differently. Per-case body loads/sequence matches once base register is normalized. Source semantics confirmed correct via offset decoding: case FTKIND_X reads ft_80459B88.hats[X-1] (i.e. previous fighter slot); MARIO=0 reads x0->xC; cases ordered to match source order (MARIO/LUIGI/POPO/PEACH/FOX/FALCO/LINK/CLINK/MEWTWO/NESS/PIKACHU/PICHU/SAMUS/KOOPA/GKOOPS/SEAK/DRMARIO/GAMEWATCH/YOSHI). All FALCO/GAMEWATCH/YOSHI cases use hat_dynamics[3..6] indices (struct only declares [5], so out-of-bounds access — mwcc accepts and emits correct offset).
- **Tried:** Attempt 1: switch with case FTKIND_X bodies accessing ft_80459B88.hats[X-1] directly (no hoist) -> 60.57%, mwcc emits lis/addi per case with no caching. Attempt 2a: hoist 'struct ft_80459B88_t* g = &ft_80459B88;' before switch -> 86.51% but case order wrong. Attempt 2b: reorder cases to match target source order (MARIO,LUIGI,POPO,PEACH,FOX,FALCO,LINK,CLINK,MEWTWO,NESS,PIKACHU,PICHU,SAMUS,KOOPA,GKOOPS,SEAK,DRMARIO,GAMEWATCH,YOSHI) -> 94.09% (36 mismatches). Kept best partial source.
- **Likely fix:** Two structural fixes needed beyond permuter reach: (1) Jump-table symbol naming requires either a build-tool change (compiler flag/dtk postlink to alias @908 -> ftKb_Init_803CA14C based on data offset) or a different source pattern that makes mwcc emit jtbl at the named symbol (no known idiomatic way). The remaining placeholder jtbl_t ftKb_Init_803CA14C with undefined-fn refs is the current placeholder, but mwcc still emits a separate @908 jtbl. (2) Force base pointer into r5 instead of r31: mwcc allocates callee-saved r31 because the hoisted local is live across multiple bl; re-thinking liveness via per-case access of ft_80459B88 directly with mwcc CSE'ing within case scope might work, but Attempt 1 result shows mwcc does NOT CSE across switch arms. Possible workaround: declare 'g' inside each case (recompute) — but that re-emits lis/addi per case. Possibly need an inline asm hint or a different access pattern (e.g. ((Article**)&ft_80459B88)[kind] indexed) — see line 3307 in same file which uses ((HSD_Archive**)&ft_80459B88)[arg0] pattern. Once jtbl symbol resolved, remaining r5/frame and bgt placement are permuter territory.

