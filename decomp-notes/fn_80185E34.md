---
function: fn_80185E34
paired_with: [fn_80186080]
tu: src/melee/gm/gm_1832.c
headline: paired by struct-split + data-symbols
tags: [struct-split, data-symbols-missing, paired-siblings]
---
## fn_80185E34 + fn_80186080 (`src/melee/gm/gm_1832.c`) — paired by struct-split + data-symbols

- **Tags:** `struct-split`, `data-symbols-missing`, `paired-siblings`
- **Best fuzzy:** 99.93% / 99.987% (Opus subagents, escalated, no commits)
- **Shared blocker (gm_1832 TU-wide):**
  - Struct `lbl_804735E8` should split at offset 0xA4 → `lbl_804735E8` (0xA4 bytes) + `lbl_8047368C` (0x24 bytes, drop `pad_104[0x54]`). Mechanical fix; **fuzzy unchanged** because objdiff already treats these as equivalent.
  - Literal-pool floats `lbl_804DA540`, `lbl_804DA55C`, `lbl_804DA570`, `lbl_804DA574` need named static globals at exact addresses. Plus filler symbols `lbl_803D9420`, `lbl_803D942C`, `lbl_803D9438` (last has 0x24 bytes of unknown content).
- **Likely fix:** disassemble `.data` from `orig/GALE01/sys/main.dol` at the right offsets to recover the filler contents, then declare the named float globals in the file. Multi-symbol RE work; closes both functions (and probably more in this TU).
