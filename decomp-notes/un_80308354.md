---
function: un_80308354
tu: src/melee/ty/toy.c
headline: regalloc + permuter-blocked
tags: [regalloc, permuter-blocked, varargs-cr1eq, sdata2-named-global]
---
## un_80308354 (`src/melee/ty/toy.c`) — regalloc + permuter-blocked

- **Tags:** `regalloc`, `permuter-blocked`, `varargs-cr1eq`, `sdata2-named-global`
- **Best fuzzy:** 68.0606%
- **Diagnosis:** mwcc spills 'target' (s16) to r31 across OSReport call regardless of __attribute__((noreturn)) on __assert. Target asm keeps target in r4 (volatile) throughout, frame=-0x8, no callee-saved regs. Best achieved: 80.9% (18 mismatches) with do{}while(i!=0x125) + OSReport(fmt,target) + string literals for assert. Permuter blocked: crclr cr1eq relocation in variadic OSReport call causes import.py 'unsupported relocation against cr1eq' exit 1 when assembling target.s. String literals for assert args give @818@sda21 vs un_804D5A48@sda21 (reloc-symbol false positive). Loop form: target uses mtctr/bdnz + explicit up-counter i; base generates cmpwi/bne or subic./bne.
- **Tried:** Attempt 1: do{}while(i!=0x125) + OSReport(fmt,target) + string literals -> 80.9%/18 mismatches. r31 spill remains. Attempt 2: do{}while(--ctr) + explicit i up-counter + OSReport(fmt,target) + string literals -> 78.5%/17 mismatches (adds extra subic. reg). All variants: mwcc always chooses r31 for target. Tried countdown for(ctr=0x125;ctr!=0;ctr--) -> 78.5%, wrong loop form. Reordering variable declarations did not change regalloc.
- **Likely fix:** (a) Patch vendor/decomp-permuter/import.py to skip/strip 'crclr cr1eq' lines before assembling target, then dispatch permuter for regalloc shuffle. (b) Find a source shape where mwcc keeps target in volatile register - e.g., computing target inline at call sites rather than as a named local. (c) Investigate whether a pragma or compiler flag affects the spill decision for noreturn calls.

