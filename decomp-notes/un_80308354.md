---
function: un_80308354
tu: src/melee/ty/toy.c
headline: regalloc + permuter-blocked
tags: [regalloc, permuter-blocked]
---
## un_80308354 (`src/melee/ty/toy.c`) — regalloc + permuter-blocked

- **Tags:** `regalloc`, `permuter-blocked`
- **Best fuzzy:** 83.7879%
- **Diagnosis:** Manual progress 68%->83.8% (17 strict mismatches remaining). Target asm: tight loop with mtctr/bdnz, target value held in r4 throughout (no r31 spill, frame -0x8). Base allocates r31 to preserve 'target' across post-loop OSReport+__assert calls (frame -0x18) and emits explicit subic./bne loop. Target also has unreachable 'b' after bl __assert that base elides. Function is structurally correct; pure register-allocation/loop-form differences.
- **Tried:** Variant 1: do{}while(--ctr) explicit down-counter -> 81.7%. Variant 2: do{}while(i!=0x125) -> 83.8%. Switched __assert(un_804D5A48,..,un_804D5A50) to string literals __assert('toy.c',0xC2A,'0') which fixed @ha/@l vs @sda21 mismatch. cr1eq relocation in target's OSReport call breaks permuter's import.py: 'Error: unsupported relocation against cr1eq' assembling target.s line 66, so --auto-permute --cluster cannot dispatch (silently fails after import.py exit 1).
- **Likely fix:** Either (a) patch vendor/decomp-permuter/import.py to strip/skip crclr cr1eq lines before assembling, then re-dispatch to permuter for the regalloc shuffle; or (b) find a source shape where mwcc keeps target in a volatile register - perhaps factoring the loop into a helper or reordering so 'target' is computed-on-demand for the assert path. mwcc not recognizing __assert as effectively-noreturn for liveness analysis is the root cause of the r31 spill.
