---
function: mn_80230198
tu: src/melee/mn/mnmainrule.c
headline: regalloc + frame-size
tags: [regalloc, frame-size, instruction-scheduling]
---
## mn_80230198 (`src/melee/mn/mnmainrule.c`) — regalloc + frame-size

- **Tags:** `regalloc`, `frame-size`, `instruction-scheduling`
- **Best fuzzy:** 91.0909%
- **Diagnosis:** At 91.09% fuzzy / 90.18% strict with 12 mismatches. Two arrays mn_803EC770 (r5) and mn_803EC734 (r3) loop correctly, but (1) stack frame is 0x20 vs target 0x28 -- extra 8-byte spill area persists across all source variants; (2) array init scheduling: base emits extra mr/addi sequence (6 insns) vs target's interleaved 4-insn lis+addi pairs. Tried: swapping b/a init order (a first), swapping a/b roles so b=770 checked first in loop (got loop body r5/r3 right but init sequence still mismatches). All remaining mismatches are scheduling and register allocation.
- **Tried:** 1) Swapped init order (a=mn_803EC770 first, b=mn_803EC734 second) - removed redundant pre-loop found=a; 2) Swapped a/b roles (b=mn_803EC770, a=mn_803EC734, check b first in loop) -- this fixed loop body r3/r5 to match target but init sequence and frame still wrong. Final state: b=mn_803EC770, a=mn_803EC734, loop checks b(r5=770) first then a(r3=734).
- **Likely fix:** Permuter needed for regalloc/scheduling. Frame-size blocker (0x28 vs 0x20) may require extra local variable that forces 8-byte stack spill, but unclear what type/usage could achieve this without changing semantics.

