---
function: it_8027870C
tu: src/melee/it/it_2725.c
headline: tu-data-osreport + tu-wide-data
tags: [tu-data-osreport, tu-wide-data]
---
## it_8027870C (`src/melee/it/it_2725.c`) — tu-data-osreport + tu-wide-data

- **Tags:** `tu-data-osreport`, `tu-wide-data`
- **Best fuzzy:** 74.5714%
- **Diagnosis:** At 70.28% match. Function source is correct but mwcc bundles file-scope global char[] strings (it_803F1ED8/EE4/EF0) into '.data.0' anchor pattern with r31 base + offsets, instead of the target's per-string lis/addi @ha/@l pattern. 18 mismatches: 4 prologue/epilogue diffs (stwu -0x18 vs -0x8, stw r31, lwz r31, lwz r0 0x1c vs 0xc, addi r1 0x18 vs 0x8) + 14 data-reference diffs (every it_803F1ED8/EE4/EF0 reference splits into anchor+offset form vs direct symbol form). Same TU as 8 other stuck siblings under tu-data-osreport (it_80274DAC etc). Also added struct Item_r13_Data* it_804D6D20 declaration, uncommented Article**/ItemCommonData* sda statics, and added it_804D6D20 extern to it_2725.h.
- **Tried:** (1) Direct call with string literals 'ItCo.dat'/'ItCo.usd'/'itPublicData' (70.28%); (2) static char[] inside function (70.28%); (3) global char[] at file-scope (70.28%, exact symbol names but bundled into .data.0 anchor)
- **Likely fix:** TU-wide reconstruction: declare every preceding data symbol in the TU in the correct order so mwcc's data anchor heuristic matches. The function consumes 3 string globals at 0x803F1ED8/EE4/EF0 — currently mwcc emits them via .data.0 anchor because the surrounding declared symbols force that compaction strategy. Beyond single-function scope; same blocker pattern as it_80274DAC, mpGetSpeed, etc.
