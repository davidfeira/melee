---
function: mnSnap_80253F60
tu: src/melee/mn/mnsnap.c
headline: regalloc + r30-r31-swap
tags: [regalloc, r30-r31-swap, permuter-resistant]
---
## mnSnap_80253F60 (`src/melee/mn/mnsnap.c`) — regalloc + r30-r31-swap

- **Tags:** `regalloc`, `r30-r31-swap`, `permuter-resistant`
- **Best fuzzy:** 96.2222%
- **Diagnosis:** Pure r30/r31 swap: target wants r31=byte_off(4), r30=snap_ptr, but mwcc assigns r31=snap_ptr, r30=byte_off. 11 mismatches, all register argument mismatches. Three attempts total across two sessions: (1) swap declaration order (no effect), (2) direct global access for active_slot (no effect - compiler still uses BSS anchor in r31), (3) eliminate snap variable using walk as base (regressed to 88.75%). No structural source shape found to flip r30/r31.
- **Tried:** (1) swap declaration order; (2) direct global access for active_slot; (3) eliminate snap var using walk as base
- **Likely fix:** Permuter-only or adopt upstream solution. mwcc assigns snap_ptr to r31 regardless of declaration order due to higher usage count.

