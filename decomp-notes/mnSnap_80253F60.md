---
function: mnSnap_80253F60
tu: src/melee/mn/mnsnap.c
headline: regalloc + r30-r31-swap
tags: [regalloc, r30-r31-swap, permuter-territory]
---
## mnSnap_80253F60 (`src/melee/mn/mnsnap.c`) — regalloc + r30-r31-swap

- **Tags:** `regalloc`, `r30-r31-swap`, `permuter-territory`
- **Best fuzzy:** 96.2222%
- **Diagnosis:** 11 mismatches, all r30/r31 swap. Target wants byte_off->r31 and snap->r30, but mwcc consistently assigns the more-used local (snap, used 4x: walk cast, active_slot read, byte_off offset compute) to r31. Tried (1) swapping declaration order (no change - mwcc orders by usage not declaration), (2) hoisting jp = (HSD_JObj**)((u32)snap + byte_off + 0x98) into loop (regressed to 64% / 24 mismatches - changes frame layout).
- **Tried:** (1) swap decl order; (2) cache jp pointer in loop body
- **Likely fix:** Pure r30/r31 swap, no semantic shape variant found. Dispatch with --cluster --auto-permute. Alternative: rewrite jobj access via walk-pointer indexing so snap appears less.
