---
function: fn_80160DE8
tu: src/melee/gm/gm_1601.c
headline: permuter-territory + frame-size
tags: [permuter-territory, frame-size, regalloc, stack-offset]
---
## fn_80160DE8 (`src/melee/gm/gm_1601.c`) — permuter-territory + frame-size

- **Tags:** `permuter-territory`, `frame-size`, `regalloc`, `stack-offset`
- **Best fuzzy:** (unknown)
- **Diagnosis:** At 97.9%, 17 mismatches all stack-frame and register-allocation. Source structure semantically correct (mirrors gm_80160B40 pattern). Base uses 0x58 stack vs target 0x50 (extra 8 bytes for one local). Base uses r30 directly for tmp_text where target uses r29 first then mr r30. Also: base's r31 base register loads as ...rodata.0@ha rather than lbl_803B75F8@ha (linker-equivalent anchor swap, may resolve once layout finalizes).
- **Tried:** Attempt 1 (95.6%): straightforward translation mirroring gm_80160B40. Attempt 2 (97.9%): introduced var_f0 temp before var_f31 assignment to force lfs f0 then fmr f31, f0 pattern; added text/tmp_text alias chain to encourage r29->r30 reg flow.
- **Likely fix:** Permuter for register allocation + frame size. Manual: try removing one local to shrink frame by 8 bytes, or restructure to remove the f32 var_f0 temp via different control flow. The lbl_803B75F8@ha vs rodata.0@ha may auto-resolve.

