---
function: fn_8024F840
tu: src/melee/mn/mndatadel.c
headline: struct-split + struct-typing
tags: [struct-split, struct-typing, permuter-blocked, frame-size]
---
## fn_8024F840 (`src/melee/mn/mndatadel.c`) — struct-split + struct-typing

- **Tags:** `struct-split`, `struct-typing`, `permuter-blocked`, `frame-size`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 90.59% (82 mismatches). Reached after writing the function body — control flow, calls, arg counts, and constants all match. Two stubborn structural diff classes remain. (1) WarnCmnData (per static.h) is only modeled at 3 bytes (x0/visible/cursor_idx) but real layout includes a 6-byte 'slot_used' array at offset 3 and HSD_Text* at offset 0xC. The asm pattern 'add r4,r28,r0; lbz r0,0x3(r4)' for the data->x0+3 read maps cleanly to data->slot_used[data->x0]; my workaround '((u8*)data + data->x0)[3]' folds to lbzx. (2) Stack frame is 0x40 vs target 0x60 even with PAD_STACK(16); needs another 0x20 bytes — probably driven by the slot_used+text struct fields holding callee-saved temporaries differently, or PAD_STACK(48). (3) Numerous regalloc churn (r5/r6 mnDataDel_804D6C68 anchor; r0/r27 caching of data->x0 across calls).
- **Tried:** (a) Wrote semantically-correct fn body matching the m2c sketch and asm control flow; (b) Added PAD_STACK(16) — moved frame from 0x30 to 0x40 (target 0x60); (c) Tried '((u8*)data + data->x0)[3]' to coerce add+lbz vs lbzx — mwcc still folded to lbzx.
- **Likely fix:** Either: (i) widen WarnCmnData header to include 'u8 slot_used[6]' at +3 and 'HSD_Text* text' at +0xC, then rewrite this fn (and probably siblings 8024EA6C/8024FBA4/etc.) to use member access — this also matches the user_data[i+3] writes seen in mnDataDel_8024EA6C; (ii) bump PAD_STACK to PAD_STACK(48) which may absorb missing callee-saved spills; (iii) hand to permuter for residual reg/sched diffs once (i)+(ii) close the structural gap. Best to coordinate widening with sibling fns since they share the same layout. decomp-permuter offline so dispatch deferred.

