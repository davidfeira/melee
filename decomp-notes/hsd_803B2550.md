---
function: hsd_803B2550
tu: src/sysdolphin/baselib/hsd_3AA7.c
headline: regalloc + frame-size
tags: [regalloc, frame-size]
---
## hsd_803B2550 (`src/sysdolphin/baselib/hsd_3AA7.c`) — regalloc + frame-size

- **Tags:** `regalloc`, `frame-size`
- **Best fuzzy:** 78.7808%
- **Diagnosis:** MWCC assigns wrong saved registers (r26-r31) relative to target. Target uses: r26=arg1(filename), r27=chan_then_CARDClose_retry, r28=CARDOpen_retry_then_arg0[4], r29=base, r30=arg0, r31=arg2. Both attempts generated different register assignments despite structural logic being correct. PAD_STACK(8) fixes the frame size (0x38 vs 0x30). Remaining 38 mismatches after PAD_STACK are all register allocation cascades.
- **Tried:** Attempt 1 (86.45%, 36 mismatches): basic struct with u8* base, no explicit filename/chan vars. Attempt 2 (78.49%, 38 mismatches): added filename=(char*)arg1, chan=arg0[1] vars + PAD_STACK(8). Second attempt fixed frame size but made regalloc worse. Target pattern: CARDOpen retry uses r28, second loop uses r3(scratch) for count and r0(temp) for loaded value then mr r28,r0, CARDClose retry reuses r27 (was chan). Source uses same retries var for CARDOpen counter and final stored value.
- **Likely fix:** Permuter with correct source structure: need retries var to be reused for both CARDOpen counter and arg0[4] stored value (r28 throughout), chan var reused for CARDClose counter (r27), and second loop using a scratch tmp for count. PAD_STACK(8) required for correct frame size. Alternatively try: declaring vars in order base, arg1, arg2, chan, retries so compiler assigns r29=base, r26=arg1, r31=arg2, r27=chan, r28=retries.

