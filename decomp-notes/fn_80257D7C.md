---
function: fn_80257D7C
tu: src/melee/mn/mnsnap.c
headline: regalloc
tags: [regalloc]
---
## fn_80257D7C (`src/melee/mn/mnsnap.c`) — regalloc

- **Tags:** `regalloc`
- **Best fuzzy:** 99.2453%
- **Diagnosis:** 12 mismatches, all r29/r30 swap in two consecutive for-loops (thumb_labels[4] then count_texts[2]). Target picks r29 for struct base ptr + r30 for loop counter; base picks opposite. mwcc allocates regs by some lifetime/first-use heuristic that two manual variants couldn't perturb.
- **Tried:** 1) Removed snap local var (replaced snap->cursor_gobj with mnSnap_804A0A10.cursor_gobj); same 12 mismatches. 2) Reordered locals (i before snap); same 12 mismatches. Permuter previously plateaued at score=75 (output-75-1).
- **Likely fix:** Pure permuter case but plateaued. Possibly needs a different randomization seed or a wider source-shape mutation set. Could try inlining the for-loops with explicit unrolled accesses, or splitting the back-handling block into a helper to alter live ranges.
