---
function: fn_8016989C
tu: src/melee/gm/gm_1601.c
headline: frame-size + mwcc-aliasing
tags: [frame-size, mwcc-aliasing, prototype-experiment]
---
## fn_8016989C (`src/melee/gm/gm_1601.c`) — frame-size + mwcc-aliasing

- **Tags:** `frame-size`, `mwcc-aliasing`, `prototype-experiment`
- **Best fuzzy:** 99.8%
- **Diagnosis:** 99.8% match. Body asm matches target perfectly; only the prologue/epilogue frame size differs: target uses a 0x30 frame with saved r26-r31 at 0x18, base uses a 0x38 frame with saved r26-r31 at 0x20. This appears to be MWCC outgoing-argument-area sizing around the five-argument call to fn_801695BC, not source-level control flow.
- **Tried:** Existing attempts: explicit pointer increment and do-while loop both worsened regalloc. Codex cleanup-trial attempts: block-scope old-style declaration 's32 fn_801695BC();' inside fn_8016989C compiled but left the same 5 frame mismatches; old-style definition syntax for fn_8016989C itself also compiled but left the same 5 frame mismatches. Both test edits were reverted.
- **Likely fix:** Do not spend more source-shape attempts here. The remaining path is a compiler/prototype ABI quirk: investigate call-site visibility, header/prototype history, or a TU-level declaration arrangement that makes MWCC reserve only the 0x10 outgoing area while preserving the direct fn_801695BC call.
