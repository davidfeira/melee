---
function: fn_8016989C
tu: src/melee/gm/gm_1601.c
headline: frame-size + upstream-regression
tags: [frame-size, upstream-regression]
---
## fn_8016989C (`src/melee/gm/gm_1601.c`) — frame-size + upstream-regression

- **Tags:** `frame-size`, `upstream-regression`
- **Best fuzzy:** 99.8%
- **Diagnosis:** 99.8% match (5 frame-prologue/epilogue mismatches: stwu -0x30 vs -0x38, stmw/lmw 0x18 vs 0x20, lwz 0x34 vs 0x3c, addi 0x30 vs 0x38). Root cause identified: our local TU has additional matched functions (fn_80160DE8, fn_80161154, fn_80161C90) implemented before fn_8016989C that upstream did not have when they matched this function. The extra TU content changes MWCC optimizer/frame-allocation state, causing 0x20 outgoing-arg area instead of target's 0x18. Source code is functionally identical to upstream.
- **Tried:** for-loop (matches upstream exactly), do-while loop, old-style prototype experiments, block-scope s32 fn_801695BC() declaration inside fn_8016989C. All leave same 5 frame mismatches. Reset and confirmed current source matches upstream verbatim.
- **Likely fix:** This will self-resolve once earlier stub functions (fn_80160DE8, fn_80161154, fn_80161C90, etc.) are matched in upstream and pulled in. The frame-size difference is a TU-order MWCC side-effect, not a source bug. Do not attempt further source-shape changes.

