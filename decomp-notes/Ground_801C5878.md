---
function: Ground_801C5878
tu: src/melee/gr/ground.c
headline: regalloc + cross-tu-globals
tags: [regalloc, cross-tu-globals, paired-siblings]
---
## Ground_801C5878 (`src/melee/gr/ground.c`) — regalloc + cross-tu-globals

- **Tags:** `regalloc`, `cross-tu-globals`, `paired-siblings`
- **Best fuzzy:** 75.8846%
- **Diagnosis:** Target has 2 callee-saved regs (r30=temp, r31=stage_info ptr); ptr loaded into r31 BEFORE tyDisplay_8031C2EC and held across both calls in if-true branch. Else-branch independently reloads stage_info into r3 (caller-save). Frame 0x18 in both. Permuter best=480 by inserting duplicate 'stage_info.x6E4[0]=temp' before un_8031C454 (semantically wrong, +1 stw vs target). Variant1 (StageInfo* si at fn-top, used in both branches) -> 8 mismatches: hoisted ptr to r31 spans whole fn but else uses r31 instead of reloading r3 like target. Variant2 (si inside if-block) -> 13 mismatches: extra local pushes frame to 0x20.
- **Tried:** V1 fn-top StageInfo*si used in both branches (76.5%/8 mm); V2 si inside if-only with stage_info direct in else (75.8%/13 mm, frame grew to 0x20). Earlier prior notes match. Permuter 480-1 duplicate-write candidate is +1 instr (won't reach 0).
- **Likely fix:** Target's regalloc may require a specific source pattern that mwcc only emits when stage_info usage is explicitly visible early in if-true block but the else-branch is somehow forced to reload. Possibly a no-op-style read of stage_info before tyDisplay_8031C2EC (e.g., predicate using stage_info field). Or this is the rare case where target was compiled with slightly different flags/version — check if ground.c TU has historical -opt difference. Permuter cannot synthesize an early stage_info reference without adding instructions.
