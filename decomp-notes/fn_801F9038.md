---
function: fn_801F9038
tu: src/melee/gr/gricemt.c
headline: permuter-queued + regalloc
tags: [permuter-queued, regalloc]
---
## fn_801F9038 (`src/melee/gr/gricemt.c`) — permuter-queued + regalloc

- **Tags:** `permuter-queued`, `regalloc`
- **Best fuzzy:** 99.5%
- **Diagnosis:** 99.14%, 9 mismatches. Source structurally complete (xF4 decrement loop unrolls correctly, 0x20 stack frame matches via PAD_STACK(8)). Remaining diff is pure register allocation in the row-rejection comparison block: target uses r0/r3/r4 = idx*12 / xC4 / row->id with cmpw r3,r4 ordering; base assigns r3/r0/r3 with cmpw r0,r3. Final 'lwz r3,0x0(r5)' return load also flagged (same text, likely reloc cosmetic). Classic regalloc-only mismatch — permuter territory. Decomp-permuter offline so cannot --auto-permute; queueing via tag.

