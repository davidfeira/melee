---
function: grGreens_80213524
tu: src/melee/gr/grgreens.c
headline: data-anchor + permuter-false-positive
tags: [data-anchor, permuter-false-positive, varargs-cr1eq, fuzzy-vs-strict]
---
## grGreens_80213524 (`src/melee/gr/grgreens.c`) — data-anchor + permuter-false-positive

- **Tags:** `data-anchor`, `permuter-false-positive`, `varargs-cr1eq`, `fuzzy-vs-strict`
- **Best fuzzy:** 99.9655%
- **Diagnosis:** OSReport call in the else-branch uses __FILE__ and a format string whose .rodata addresses are computed as offsets from grGr_callbacks (r31). Target has offsets 0xcc and 0xf0; recompiled object has 0x18c and 0x1b0 (delta 0xC0 = 192 bytes). This is a data-anchor section-layout difference — the string pool lands at a different position relative to grGr_callbacks in the recompiled TU.
- **Tried:** Verified source is semantically correct (OSReport with __FILE__, literal 281, and id). No source change can fix a section layout delta. Confirmed fuzzy=99.97% but objdiff strict=99.79%; commit-match will refuse.
- **Likely fix:** Fix requires aligning .rodata layout in grgreens.c to match the original — likely by adjusting the order or presence of other string literals in the TU, or by splitting/merging string pool entries. A tu-wide-data audit of all OSReport strings in this TU may help.

