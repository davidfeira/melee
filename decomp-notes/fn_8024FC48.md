---
function: fn_8024FC48
tu: src/melee/mn/mndatadel.c
headline: regalloc + permuter-blocked
tags: [regalloc, permuter-blocked, varargs-cr1eq]
---
## fn_8024FC48 (`src/melee/mn/mndatadel.c`) — regalloc + permuter-blocked

- **Tags:** `regalloc`, `permuter-blocked`, `varargs-cr1eq`
- **Best fuzzy:** 98.339% (strict 97.694%, 20 mismatches on 62-instr function)
- **Diagnosis:** Function is structurally correct; m2c output matches existing source. Adding `PAD_STACK(8);` at end of locals (matching sibling fn_8024FBA4 style) brought match from 98.258%/25 mismatches to 98.339%/20 mismatches by aligning frame size to 0x30 and stmw at 0x1c. All 20 remaining mismatches are pure register-allocation shift: target uses gobj→r30, data→r29, user_data→r31, i→r27, loop_ptr→r28 with jobj at sp+0x10; base produces gobj→r27, data→r30, user_data→r31, i→r28, loop_ptr→r29 with jobj at sp+0xC. Same mwcc instruction sequence, all DIFF_ARG_MISMATCH (no INSERT/DELETE). Calls variadic `lb_80011E24` which emits `crclr cr1eq`.
- **Tried:** (1) Inlining `((u8*) gobj->user_data)[0]` instead of `user_data[0]` for the comparison — no effect (mwcc still hoists user_data load). (2) PAD_STACK(8) at end of locals — fixed frame size, dropped 5 mismatches. (3) `u8 _pad[8]` declared before jobj — placed below jobj (jobj at sp+0xC, target wants 0x10). (4) `u8 _pad[8]` after jobj — placed below jobj too (jobj at sp+0x14, target wants 0x10). Settled on canonical PAD_STACK(8) at end matching sibling fn_8024FBA4 shape.
- **Permuter blocker:** `import.py` exits 1 on `Error: unsupported relocation against cr1eq` from binutils when assembling target.s — the variadic `lb_80011E24` call emits `crclr cr1eq` (FPR-args bitmask) which binutils 2.x rejects as a relocation. `permute fn_8024FC48` confirms the blocker:
  - `target.s:82: Error: unsupported relocation against cr1eq`
  - `import.py failed (exit 1)`
  - Cluster dispatch via `--auto-permute --cluster` silently no-ops after import.py fails — no pidfile, no scratch dir, doesn't appear in `budget`.
- **Likely fix:** Same blocker family as fn_801652D8, un_80308354, grShrineRoute_8020AF38, grFlatzone_80216F48 (all `regalloc` + `permuter-blocked` + `varargs-cr1eq`). Either (a) patch `vendor/decomp-permuter/import.py` to translate `crclr cr1eq` → `crclr 6` (or strip the line) before assembly, then re-dispatch — pure regalloc shuffle, low iteration count expected to land; or (b) discover a source-shape trick that flips the priority order of gobj vs data live ranges (gobj currently gets lowest-priority callee-saved slot r27 because mwcc decides data has higher priority; would need gobj's effective live-range × frequency to dominate data's). Tried (b) variants did not move the allocator.
