---
function: if_802F7C30
tu: src/melee/if/if_2F72.c
headline: regalloc + upstream-regression
tags: [regalloc, upstream-regression, mwcc-aliasing]
---
## if_802F7C30 (`src/melee/if/if_2F72.c`) — regalloc + upstream-regression

- **Tags:** `regalloc`, `upstream-regression`, `mwcc-aliasing`
- **Best fuzzy:** 97.3148%
- **Diagnosis:** Source is bit-for-bit identical to upstream/master. 97.31% match with 6-instr pure r29/r30 swap in case -1 branch only. TU is 486 lines; upstream matched when other TU siblings were in a different state. mwcc 1.2.5n -O4,p assigns idx-offset to r30 and entry to r29 in case -1; target wants offset in r29 and entry in r30. Attempt to use u8 idx8 separate variable and swap idx/entry declaration order both left 97.31% unchanged.
- **Tried:** (1) u8 idx8 = (u8)slot separate var in case -1 only — identical 97.31%; (2) swapped void** entry / s32 idx declaration order — identical 97.31%. Both prior-agent attempts also identical 97.31% (see existing notes).
- **Likely fix:** TU-size dependent mwcc CSE: same root as sibling if_802F7E7C (notes: TU was 37 lines when upstream matched). Fix requires either fixing other unmatched functions in the TU first (if_802F7AF8 has 12 mismatches, if_802F7D08 has 21) to change TU context, or finding a source shape that breaks the asymmetric regalloc — which 4 total attempts have not found. May be inherently non-fixable at current TU state.

