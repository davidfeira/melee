---
function: fn_802A3110
tu: src/melee/it/items/itlinkhookshot.c
headline: regalloc + permuter-false-positive
tags: [regalloc, permuter-false-positive, tu-wide-data]
---
## fn_802A3110 (`src/melee/it/items/itlinkhookshot.c`) — regalloc + permuter-false-positive

- **Tags:** `regalloc`, `permuter-false-positive`, `tu-wide-data`
- **Best fuzzy:** 98.9474%
- **Diagnosis:** 98.88% strict / 98.95% fuzzy, 15 mismatches: 14 regalloc cascade + 1 sda21 false-positive (it_804DCDB8@sda21 vs @320@sda21 for the -attr->x0 negation, an anonymous local pool entry). Regalloc divergence is a consistent reg-pair swap r27<->r29, r28<->r29, r29<->r28, r4<->r5: target keeps fp in r27/attr in r29; base wants fp in r29/attr in r27. The cascade propagates through fn_802A5320 args (r3=item_link, r5=attr, r6=fp), the case-1 attr->x0 load, and the trailing it_802A7168 fp->x34_scale.y. Sibling it_802A3254 in the same TU has identical decl shape (Item->fp/attr/item_link fetched in same order from same offsets) and matches 100%; the only structural diff is fn_802A3110 has the case-1 'item_link->vel.x *= -attr->x0' which loads attr->x0 mid-function and forces an extra long-lived register for attr. TU has 3 stuck siblings already, suggesting TU-wide regalloc/literal-pool issue.
- **Tried:** V1: swapped local decl order (attr before fp): made it WORSE structurally, dropped to 13 mismatches but inverted load order vs canonical (mwcc reordered per source). V2: hoisted fp->x34_scale.y to a local before final call: no change (same 15 mismatches). Per Permuter Boundary, sda21 anonymous-vs-named pool entries are a permuter-false-positive class.
- **Likely fix:** Two avenues: (a) The named float it_804DCDB8 (which canonical anchors as @sda21 for the negation literal) probably needs to be declared as a TU-local 'const f32 it_804DCDB8 = X.Xf;' near the top of itlinkhookshot.c, matching the sdata2 pool layout. The 0x320 offset suggests it sits past existing named floats in the pool; declaring it may shift the regalloc since the reloc-symbol class affects scheduling. (b) Alternative: the 'item_link->vel.x *= -attr->x0' negate-and-multiply may need a different idiom (e.g. 'item_link->vel.x = item_link->vel.x * -attr->x0' or temp via 'f32 nx0 = -attr->x0') so mwcc schedules the attr->x0 load differently. Sibling it_802A3254 lacks this case-1 statement and matches at 100%, suggesting the multiplication is the structural blocker. Pure regalloc remainder may yield to permuter on cluster, but the sda21 false-positive will block 100% even if regalloc resolves - so this is likely TU-wide-data class until the named float is declared.
