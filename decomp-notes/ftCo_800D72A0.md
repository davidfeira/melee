---
function: ftCo_800D72A0
tu: src/melee/ft/chara/ftCommon/ftCo_Attack100.c
headline: regalloc + permuter-resistant
tags: [regalloc, permuter-resistant, permuter-plateau]
---
## ftCo_800D72A0 (`src/melee/ft/chara/ftCommon/ftCo_Attack100.c`) — regalloc + permuter-resistant

- **Tags:** `regalloc`, `permuter-resistant`, `permuter-plateau`
- **Best fuzzy:** 97.037%
- **Diagnosis:** 97% plateau. 13 pure regalloc mismatches. Target uses r7 for fp->x2D0 pointer (loaded first instruction), r5 for fields, r6 for motion_id throughout both blocks. Base consistently uses r4 for x2D0 and r3 for motion_id in 2nd block (clobbering fp). Tried: (1) inlined fp->x2D0->field reads (97.04%, 13 mismatches), (2) single local x2D0 + per-block msid locals (97.22%, 13 mismatches), (3) single local x2D0 + named x2C/x30 locals (13 mismatches). All 3 yield 13 mismatches with different register arrangements but none match target. Prior permuter run ~95k iterations no match. Upstream has function as matched but with same source code - may be TU context difference.
- **Tried:** inlined field accesses; local for x2D0 pointer; separate named locals for x2C and x30 fields; per-block msid locals
- **Likely fix:** Unknown. The r7 allocation for the first local in a no-preamble leaf function is unusual. May require a specific MWCC optimization quirk or additional register pressure that none of the standard source shapes produce. Extended permuter run (>200k iterations) might find it, or it may require a different TU-wide approach.

