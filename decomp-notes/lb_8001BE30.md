---
function: lb_8001BE30
paired_with: [lb_8001BF04, lb_8001BB48]
tu: src/melee/lb/lbcardnew.c
headline: paired stack frame size
tags: [stack-offset, frame-size, paired-siblings]
---
## lb_8001BE30 + lb_8001BF04 + lb_8001BB48 (`src/melee/lb/lbcardnew.c`) — paired stack frame size

- **Tags:** `stack-offset`, `frame-size`, `paired-siblings`
- **Best fuzzy:** 99.91% / 99.91% / 96.15% (Opus subagent — cluster permuter dispatched on BE30)
- **Diagnosis:** Stack frame size delta — target wants `0x40`, base produces `0x48` (8 bytes excess from mwcc temp/spill behavior, not source shape). All three siblings share the same `setup_task(N, M) + assignments` pattern. BB48 also has register-naming diffs on top.
- **Tried (BE30):** `u8 _[8]` pad and hoisting `task` local — both pushed frame to `0x50` (wrong direction). Reverted.
- **Likely fix:** if permuter finds the right shape for BE30, it should port directly to BF04 (identical structure). BB48 is more complex but shares the stack-size delta. `lb_8001BA44` is a thin wrapper, unrelated.
