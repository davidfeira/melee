---
function: gm_8016AC44
tu: src/melee/gm/gm_1601.c
headline: permuter-blocked + permuter-territory
tags: [permuter-blocked, permuter-territory, regalloc, r30-r31-swap, bss-anchor]
---
## gm_8016AC44 (`src/melee/gm/gm_1601.c`) — permuter-blocked + permuter-territory

- **Tags:** `permuter-blocked`, `permuter-territory`, `regalloc`, `r30-r31-swap`, `bss-anchor`
- **Best fuzzy:** 68.136%
- **Diagnosis:** At 88.2% (70 mismatches) with manual decomp. Function searches lbl_8046B668.arr2[] for first -2 sentinel (27 entries), then scans backward looking for arr2[i]==costume_id && arr1[i]==ckind to clear. Remaining diff is dominated by r3 vs r4 register swaps in the search/scan loops and one extra r29 register save (caused by the local 'ptr' pointer var that prevents bss-anchor false-positive). Without the local ptr, mwcc emits bss.0+offset instead of lbl_8046B668@ha, dropping match to 63%. Permuter offline -- queued for cluster dispatch when available.
- **Tried:** (1) Inlined globals (no ptr local) -> 63% with bss.0 anchor false-positive. (2) Local 'ptr' var to keep symbol named -> 88% but introduces r29 register save and r3<->r4 swaps in loops. (3) m2c-style 'x = (cond) ? 1 : 0; if (x==1)' pattern -> regressed; mwcc collapses to single bne which already matches.
- **Likely fix:** Permuter territory: regalloc swap of r3/r4 in both loops (search loop has r3=found,r4=i in target but r4=found,r3=i in base; scan loop has r5=ptr,r3=i in target but r3=ptr,r5=i in base). Also needs to drop the r29 local while keeping symbol named (likely via PERM_VAR or struct typing change). Header change required: gm_1601.h line 257 changed UNK_RET to int.

