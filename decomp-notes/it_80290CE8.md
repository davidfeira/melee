---
function: it_80290CE8
tu: src/melee/it/items/itflipper.c
headline: regalloc + float-regalloc
tags: [regalloc, float-regalloc, mwcc-aliasing, permuter-territory]
---
## it_80290CE8 (`src/melee/it/items/itflipper.c`) — regalloc + float-regalloc

- **Tags:** `regalloc`, `float-regalloc`, `mwcc-aliasing`, `permuter-territory`
- **Best fuzzy:** 96.1864%
- **Diagnosis:** 6 mismatches at 96.19% in two pure regalloc clusters: (1) r0-vs-r29 cluster: correct binary loads xCF4_fighterGObjUnk to r0 then cmplwi r0, mr r29 r0, addi r3 r29 (4 insns); our base loads directly to r29 (3 insns: lwz r29, cmplwi r29, mr r3 r29). (2) f31 cluster: correct binary does fmuls f0 f0 f1 then fmr f31 f0 (2 insns); our base does fmuls f31 f0 f1 directly (1 insn). Both differences are MWCC register allocator choosing different scratch-vs-persistent allocation patterns. The correct binary is LESS efficient than what our code produces -- the compiler used r0 as scratch for NULL check then saved to r29 (fighter), and used f0 as intermediate for the multiply then copied to f31 (speed). Our code compiles more efficiently to the direct forms.
- **Tried:** Attempt 1 (this session): moved HSD_GObj* fighter declaration from inside if-block to outside (before *pos=ip->pos) -- caused r29/r30 swap, 20 mismatches 81.9%. Reverted. Prior V1/V2 (per existing notes): expanding inline body directly into it_80290CE8 also failed (V1: 14 mismatches; V2: frame diverged). The r0-to-r29 pattern with addi r3,r29,0x0 form requires MWCC to not commit r29 for fighter until AFTER the NULL check, which cannot be achieved by moving declarations without causing other regressions.
- **Likely fix:** Permuter is the right tool -- the r0-vs-r29 and f0-vs-f31 patterns are scheduling/regalloc choices that require exhaustive search over expression orderings and temp lifetimes. No structural C change has been found that produces the r0-intermediate pattern without breaking the frame or swapping r29/r30.

