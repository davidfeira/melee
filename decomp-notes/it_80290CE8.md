---
function: it_80290CE8
tu: src/melee/it/items/itflipper.c
headline: regalloc + mwcc-aliasing
tags: [regalloc, mwcc-aliasing, permuter-territory]
---
## it_80290CE8 (`src/melee/it/items/itflipper.c`) — regalloc + mwcc-aliasing

- **Tags:** `regalloc`, `mwcc-aliasing`, `permuter-territory`
- **Best fuzzy:** 96.1864%
- **Diagnosis:** 6 mismatches at 96.19%. Two clusters: (1) target loads ip->xCF4_fighterGObjUnk into r0 then mr r29,r0 + addi r3,r29 inside the if-block (4 mismatches: lwz/cmplwi/mr/addi); base loads directly to r29 and uses mr r3,r29. (2) target does fmuls f0,f0,f1 then fmr f31,f0 (2 mismatches); base does fmuls f31,f0,f1 directly. The f31 pattern indicates target pre-loaded f31 with the 10.0f speed literal early so it could be the fallback if the if-block isn't entered, then conditionally overwrites f31 via f0->f31 inside the block. Base CSEs the speed parameter f1 into f31 directly. The r0->r29 round-trip suggests target compiled the function with the body inlined-with-literal so 10.0f is a constant in the local scope, not a parameter.
- **Tried:** V1: expanded it_80290CE8_inline body directly into it_80290CE8 with f32 speed=10.0f local and outer Item* ip - went to 97.2%/14 mismatches (improved load-to-r0 pattern but introduced r29/r30 swap from second ip declaration). V2: same expansion with single Item* ip declared before speed - went to 95.0%/37 mismatches (frame size diverged 0x58 vs 0x50, lost PAD_STACK shape). Reverted to original 96.19%/6.
- **Likely fix:** Need source shape that (a) makes 10.0f appear as a literal in scope (not f1 parameter) so f31 gets pre-loaded with it, and (b) preserves the original frame layout 0x58 with PAD_STACK(8) so frame doesn't diverge. Possibly: declare the body with Item* ip; computed once, but needs careful var-decl ordering to avoid r29/r30 swap. Or: forced register reservation via static-inline boundary. Permuter --cluster might find the right local var ordering since this is regalloc + literal-pool layout territory.
