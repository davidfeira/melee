---
function: it_802775F0
tu: src/melee/it/it_2725.c
headline: float-literal + permuter-false-positive
tags: [float-literal, permuter-false-positive]
---
## it_802775F0 (`src/melee/it/it_2725.c`) — float-literal + permuter-false-positive

- **Tags:** `float-literal`, `permuter-false-positive`
- **Best fuzzy:** 99.9718%
- **Diagnosis:** (layered on prior note) 99.97% fuzzy with 8 mismatches; 6 are sdata2 reloc-symbol mismatches (anonymous @N@sda21 vs named it_804DC73C/758/760/768@sda21) — post-link bytes match. 2 are stack offset mismatches (volatile float y at 0x18 in target vs 0x14 in our build), coupled to the same literal-pool ordering: mwcc emits anonymous slots for 0.0f / 0.5 / 3.0 / 0.85f literals because no named decl forces those exact symbol names. TU it_2725.c owns .sdata2 0x804DC708..0x804DC830, but most named entries between 0x708 and 0x76C are commented out (///) — anonymous literals fill them in. The volatile-y stack offset divergence likely follows from a different intermediate sdata2 layout. Prior diagnosis was correct: needs named decl strategy. Note 0.5/3.0 come from sqrtf_accurate inline in MSL/math_ppc.h which is shared and cannot be edited. Would need a TU-local sqrtf wrapper using named externs, or accept as permuter-false-positive.
- **Tried:** re-ran diff to verify mismatches are dominated by sdata2 reloc-symbol class (6/8); confirmed prior float-literal note still applies; confirmed 0.5/3.0 originate from shared sqrtf_accurate inline
- **Likely fix:** needs structural change to TU sdata2 layout: uncomment named float decls in correct order across 0x804DC70C..0x804DC76C range AND introduce TU-local sqrtf wrapper that references it_804DC758/760 by name. Risk: layout shift breaks other functions. Alternatively accept as permuter-false-positive since post-link bytes match.
