---
function: fn_801F8E58
tu: src/melee/gr/gricemt.c
headline: permuter-queued + data-anchor
tags: [permuter-queued, data-anchor, string-pool, cross-function-rodata, regalloc]
---
## fn_801F8E58 (`src/melee/gr/gricemt.c`) — permuter-queued + data-anchor

- **Tags:** `permuter-queued`, `data-anchor`, `string-pool`, `cross-function-rodata`, `regalloc`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 73.9% / 87 mismatches. Source shape semantically correct (12-slot scan over (s16*)&gp->gv.icemt.xDC, decrement loop unrolled 2x6, then s16 lo/hi randi range write to *out_delay). Most remaining mismatches are propagated register-allocation swaps (r26<->r27, r28<->r29, r30<->r31) downstream of TWO structural blockers identical to sibling grIceMt_801F686C: (1) assert string literals 'gricemt.c'/'max' emitted as anonymous @289 instead of being pooled under the explicit grIm_803E46F8 symbol — same data-anchor / string-pool blocker that prevents mwcc from collapsing two base-register loads into one offset-from-grIm_803E4068, bumping live-reg count and shifting allocation; (2) the resulting extra base-reg load forces the search-loop counter and candidate-buffer pointer to occupy different physical registers than target. Updated header to 2-arg signature s32 fn_801F8E58(Ground_GObj*, s32* out_delay); call site in grIceMt_801F7728 now casts to HSD_GObjEvent matching grIceMt_801FA364's punned dispatch. Real fix is data-side (define grIm_803E4544 and other gricemt.c-owned anchors so mwcc can prove fixed offset between grIm_803E4068 and the pooled assert strings) — not source-shape.

