---
function: it_8027C56C
tu: src/melee/it/it_2725.c
headline: permuter-false-positive + cross-tu-globals
tags: [permuter-false-positive, cross-tu-globals, tu-wide-data]
---
## it_8027C56C (`src/melee/it/it_2725.c`) — permuter-false-positive + cross-tu-globals

- **Tags:** `permuter-false-positive`, `cross-tu-globals`, `tu-wide-data`
- **Best fuzzy:** 99.9783%
- **Diagnosis:** All 25 mismatches are reloc-symbol-only diffs (post-link bytes match, scorer ignores). Three classes: (1) data anchor: target uses 'lis/addi it_803F5428@ha/@l' with 'addi r5, r29, 0x18' while base uses '...data.0@ha/@l' with 'addi r5, r29, 0x3c24' — same final pointer, different anchor+offset split. (2) sdata named globals: target 'it_804D51E0/@sda21', 'it_804D51E8/@sda21' vs base anonymous '@406/@sda21', '@407/@sda21'. (3) sdata2 float literals: target 'it_804DC7D0/D4/0808@sda21' vs base anonymous '@293/@328/@593@sda21'. Function body and instruction stream match; only relocation symbol naming differs. report.json fuzzy: 99.07%; objdiff scorer would treat as match.
- **Tried:** prep + diff inspection — confirmed every mismatch is a DIFF_ARG_MISMATCH on a reloc symbol; no instruction-shape diffs.
- **Likely fix:** The base TU (it_2725.c) needs the named globals/floats it_804D51E0, it_804D51E8 to be declared/used such that mwcc emits them with their named symbols rather than as anonymous data pool entries (@406/@407). Likely needs the consts at it_804DC7D0/D4/0808 to be referenced as named extern floats (or moved into a shared TU header) so they emit with their full-symbol relocs instead of anonymous @N pool entries. Same applies to the data anchor at 0x803F5428 — likely a missing extern array declaration causing mwcc to anchor it to the local data section anonymously.
