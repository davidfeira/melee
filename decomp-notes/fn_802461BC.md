---
function: fn_802461BC
tu: src/melee/mn/mndiagram3.c
headline: permuter-territory + frame-size
tags: [permuter-territory, frame-size, inlining, regalloc]
---
## fn_802461BC (`src/melee/mn/mndiagram3.c`) — permuter-territory + frame-size

- **Tags:** `permuter-territory`, `frame-size`, `inlining`, `regalloc`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 737-instr fresh TU. Structural source matches all branches (& 0x20 quit, & 0xC0 forward/init, & 0xC00 toggle name mode, & 1 up/decrement, & 2 down/increment) using static inline helpers (clearRows, rebuildRows, movePopup) that mirror sibling matched mnDiagram3_8024714C and mnDiagram2 patterns. 88.4% match @ 358 mismatches. Stack frame 0x98 vs target 0x140 (168 bytes short) — likely needs each inlined branch to own its int->float scratch slots and Vec3 spills rather than coalescing through the helper. Remaining diffs are register-allocation shuffles (r25-r31 cycles) and minor address/scheduling reorders. Permuter cluster recommended once decomp-permuter back online (queued).
- **Tried:** (1) Initial structural source with helper inlines (clearRows, rebuildRows for HSD_SisLib_803A5ACC label loop, movePopup for popup_jobj position-update with HSD_JObjGetTranslation*/HSD_JObjSetMtxDirty inline expansion, asserts at 0x3EE/0x3E1/0x3A4/0x3B3/0x3C2/0x3FB/0x234). 87.2%. (2) Hoisted base = (char*)&mnDiagram3_803EEC10 to fn-level local, threaded through rebuildRows(sp, base) to keep r30 live across function. 88.4%, no structural regression.
- **Likely fix:** Permuter cluster pass should resolve regalloc/scheduling. Structural fixes still needed before that: (a) inline helper bodies directly into each branch (3 rebuildRows sites, 2 movePopup sites, 4 clearRows sites) so each gets its own stack scratch — should bring frame to 0x140. (b) The store-order at top (((s32*)&mn_804A04F0.buttons)[1] before [0]) may need different syntax — see how mndiagram2.c line 292-293 does it: result variable first, then zero. (c) HSD_GObjPLink_80390228 ordering in & 0xC0 branch suggests HSD_GObjPLink + clear loop runs before HSD_GObjPLink(gobj).

