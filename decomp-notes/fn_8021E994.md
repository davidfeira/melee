---
function: fn_8021E994
tu: src/melee/gr/grhomerun.c
headline: regalloc + permuter-queued
tags: [regalloc, permuter-queued]
---
## fn_8021E994 (`src/melee/gr/grhomerun.c`) — regalloc + permuter-queued

- **Tags:** `regalloc`, `permuter-queued`
- **Best fuzzy:** 96.1539%
- **Diagnosis:** At 96.15% (9 mismatches): r30/r31 swap between 'kind' (CollData.x34_flags.b1234) and 'gp' (GET_GROUND result from gobj->user_data), plus 'bne; b' vs 'beq' control-flow shape difference on the gp NULL check. Two structural variants tried this session (sequential early-returns, deeply-nested if) -- both produce identical 96.15% diff. Function signature confirmed as mpLib_Callback (Ground*, s32, CollData*, s32, mpLib_GroundEnum, f32) -- registered via mpJointSetCb1 in grHomeRun_8021CB20 family. Cast-to-anonymous-bitfield-struct on (u8*)gp+0xE8 produces the correct rlwimi r0,r3,7,24,24 pattern for the b0 bit-set. Source committed to file (no longer a placeholder stub) -- partial improvement preserved. Permuter dispatched previously; awaiting decomp-permuter re-online.

