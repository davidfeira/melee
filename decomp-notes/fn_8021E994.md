---
function: fn_8021E994
tu: src/melee/gr/grhomerun.c
headline: r30-r31-swap + regalloc
tags: [r30-r31-swap, regalloc, branch-pattern, permuter-dispatched]
---
## fn_8021E994 (`src/melee/gr/grhomerun.c`) — r30-r31-swap + regalloc

- **Tags:** `r30-r31-swap`, `regalloc`, `branch-pattern`, `permuter-dispatched`
- **Best fuzzy:** (unknown)
- **Diagnosis:** At 96.15% (9 mismatches): r30/r31 swap between kind (bitfield from CollData.x34_flags) and gp (Ground* from gobj->user_data), plus bne+b vs beq branch shape on the gp NULL check. Tried nested-if and declaration reorder -- both produce identical 96.15%. All remaining mismatches are regalloc/branch-pattern; permuter dispatched to cluster.
- **Tried:** 1) Original nested-if with kind declared first. 2) Declared gobj before kind with split decl/init. Neither changed register assignment.
- **Likely fix:** Permuter should resolve r30/r31 swap via expression reordering. branch-pattern (bne+b vs beq) may need a specific if-goto structural variant that permuter can find.

