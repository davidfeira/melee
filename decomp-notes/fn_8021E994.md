---
function: fn_8021E994
tu: src/melee/gr/grhomerun.c
headline: regalloc + permuter-dispatched
tags: [regalloc, permuter-dispatched]
---
## fn_8021E994 (`src/melee/gr/grhomerun.c`) — regalloc + permuter-dispatched

- **Tags:** `regalloc`, `permuter-dispatched`
- **Best fuzzy:** (unknown)
- **Diagnosis:** At 96.15% (9 mismatches) all permuter-territory: register allocation r30/r31 swap between 'kind' and 'gp' locals (8 mismatches), plus one 'bne; b' vs 'beq' control-flow shape difference. Permuter dispatched to cluster.
- **Tried:** (1) Cast-to-bitfield struct for gp->E8 bit-set works (li,rlwimi pattern matches). (2) Reordered local declarations (kind first vs last) — does not change MWCC's register choice. (3) Tried various early-return shapes (single nested if, three sequential early returns, etc) — same 9 mismatches each time. (4) Restructured condition order — no change.
- **Likely fix:** permuter to flip register allocation; OR mama Claude can try inverting condition senses (e.g. 'if (gp != NULL) ...' to 'if (gp == NULL) goto end' with goto, or via sentinel value).
