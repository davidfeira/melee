---
function: hsd_803B5D70
tu: src/sysdolphin/baselib/hsd_3B34.c
headline: permuter-dispatched + regalloc
tags: [permuter-dispatched, regalloc, r30-r31-swap]
---
## hsd_803B5D70 (`src/sysdolphin/baselib/hsd_3B34.c`) — permuter-dispatched + regalloc

- **Tags:** `permuter-dispatched`, `regalloc`, `r30-r31-swap`
- **Best fuzzy:** 92.5%
- **Diagnosis:** 92.5% match (14 mismatches). JPEG huffman decoder. Source structure mirrors m2c output: if/else picks codes/counts/values pointers based on (arg0,arg1), then huffman-decode loop with hsd_803B5C4C(1) bit reader. Two permuter-class issues remain: (1) r27 vs r29 swap on values pointer (var_r27); (2) target emits 'addi r0, r5, K; mr r6/r3, r0' pattern in the arg0!=0 branches while mwcc hoists my (u16*)(base + (arg1==0 ? 0x8C : 0x3BC)) ternaries to 'li r0, K; add r6, r5, r0'. Both attempts (offset-ternary vs pointer-ternary) hit the same hoisting. Likely needs explicit per-branch if/else writing to typed pointer temp, or needs permuter to shuffle expression order. Externs added: lbl_80431090[0x5A8], lbl_80431678/80431690/8043169C/804316B4. Data still lives in hsd_3B34.s asm. Permuter offline (override); queued for re-attempt when permuter back online.

