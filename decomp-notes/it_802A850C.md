---
function: it_802A850C
tu: src/melee/it/items/itlinkarrow.c
headline: permuter-false-positive + rodata-typing
tags: [permuter-false-positive, rodata-typing, regalloc]
---
## it_802A850C (`src/melee/it/items/itlinkarrow.c`) — permuter-false-positive + rodata-typing

- **Tags:** `permuter-false-positive`, `rodata-typing`, `regalloc`
- **Best fuzzy:** 99.9476%
- **Diagnosis:** 7 mismatches: 4 are sdata2 named-vs-unnamed float pool (target uses it_804DCE24/30/20 named globals at .sdata2 offsets 0x4/0x10/0x0; base uses @249/@374/@241 unnamed pool entries at offsets 0x3c/0x48/0x38, which mwcc duplicates because OTHER functions in the same TU still reference 0.0f/0.02f/M_PI_2 as literals, polluting the literal pool). Even though it_802A850C source already uses named globals, mwcc dedups to the unnamed pool entry created by sibling functions. 3 are register-allocation: target loads ip->xDD4_itemVar.linkarrow.xE0 (fighter pointer) into r4 across the if-test, base picks r3. The fighter local var is alive across HSD_MtxGetRotation/it_8026B3A8 calls then reloaded into r3 anyway.
- **Tried:** (1) replaced named-globals with literals 0.0f/0.02f/M_PI_2 — moved mismatch class but still 7 diffs (because target wants named, base produced unnamed). (2) reverted to named globals (current state), confirmed 7 diffs unchanged with named source.
- **Likely fix:** File-wide refactor: replace ALL 0.0f/0.02f/1.5707963267948966 literal references in itlinkarrow.c (lines 133, 159, 165, 188-189, 571, 606, 661-662, 750-752, 838, 861, 865, 868) with named-global references it_804DCE24/it_804DCE20/it_804DCE30. This eliminates the unnamed sdata2 pool entries (@241, @249, @374) so mwcc has no choice but to use the named globals. After float fix is in place, re-evaluate r3/r4 — may still need permuter, or may be fixed once register pressure changes.
