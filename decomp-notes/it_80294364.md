---
function: it_80294364
tu: src/melee/it/items/itwstar.c
headline: regalloc + permuter-territory
tags: [regalloc, permuter-territory, stack-offset]
---
## it_80294364 (`src/melee/it/items/itwstar.c`) — regalloc + permuter-territory

- **Tags:** `regalloc`, `permuter-territory`, `stack-offset`
- **Best fuzzy:** 98.3922%
- **Diagnosis:** Permuter cluster plateaued at score 90 (98.39% fuzzy with 13 mismatches). All remaining mismatches are pure regalloc swaps (r4<->r5<->r6 for p/total/count) plus stack offset diff: target r1+0x14 vs base r1+0x10 for candidates[8] base address. Frame size matches (-0x40). Saved regs match (r29-r31 at 0x34-0x3c). Both have 32 bytes for candidates and 4 bytes wasted padding, but at different ends of the locals region. Target wastes 4 bytes BELOW candidates (r1+0x10..0x13), our build wastes 4 bytes ABOVE (r1+0x30..0x33). Variant 1 (i=(count=0) chained) improved from 97.31% to 98.39%, removed cmpw/cmpwi peephole, dropped mismatches 14->13. Variant 2 (count=0 reorder before/after total= load) made no improvement.
- **Tried:** V1: 'i = (count = 0)' chained assignment with p=candidates first, count decl removal -> 98.39%, 13 mismatches (improved over prior 97.31%/14). V2: count=0 reordered before/after 'total = attr->x24_count' load -> no change vs original 97.31%. Permuter cluster ran with default randomizers; best output-90-1 found exactly the chained-assignment trick. Output-205-1 and others injected semantically-broken changes (total used as scratch for picked cast) so they're not viable.
- **Likely fix:** Genuine compiler-internal frame layout decision. To shift candidates from r1+0x10 to r1+0x14 needs an extra local variable that gets pre-allocated to the bottom of the locals region. Try permuter --cluster with PERM_INT_TYPE on i,count,total, PERM_RANDOMIZE_TYPE, AND PERM_ADD_VARIABLE (or manual: declare an unused 's32 _pad' early in the locals to push candidates up by 4). Could also try declaring 'last' as 's8' to see if narrower type changes alloc. Apply Variant 1 chained assignment as baseline before next permuter run since it removed the cmpw peephole.
