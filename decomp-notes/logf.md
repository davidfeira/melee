---
function: logf
tu: src/MSL/math.c
headline: tu-wide-data + permuter-false-positive
tags: [tu-wide-data, permuter-false-positive, mwcc-aliasing, float-literal, cross-tu-globals]
---
## logf (`src/MSL/math.c`) — tu-wide-data + permuter-false-positive

- **Tags:** `tu-wide-data`, `permuter-false-positive`, `mwcc-aliasing`, `float-literal`, `cross-tu-globals`
- **Best fuzzy:** 99.8022%
- **Diagnosis:** (2nd pass, 2026-04-26) Confirmed prior diagnosis. Match still 99.31%. The 27 mismatches break down: (a) frame size 0x28(target) vs 0x40(base) — 24 byte diff = 6 stack-spilled float intermediates (C0,C1,H,new_var,poly,h2_poly); (b) 6 sdata2 symbol-name mismatches: MSL_Math_804DE1B0/B4 vs __logf_C0_bits/__logf_C1_bits (Taylor coeffs), MSL_Math_804DE1C0 vs @40 (LN2 float), MSL_Math_804DE1C8 vs @42 (int-to-double magic 0x4330000080000000), __float_nan vs float_nan, __float_huge vs float_inf; (c) all stack offset shifts from frame size diff. CRITICAL DISCOVERY: actual ROM bit pattern at MSL_Math_804DE1B4 is **0x3EAAAA36** (=1/3 ≈ 0.33333334), but current src/MSL/math_data.c has __logf_C1_bits=0x3EAA9F44 (=0.333329856) — these differ by ~16 ULP. This is a **real bit-level mismatch** that contributes to fuzzy report (not just naming). Splits.txt assigns sdata2 0x804DE1B0..0x804DE1D0 to MSL/math.c (NOT math_data.c) — current __logf_C*_bits placement in math_data.c lands them in wrong section/address.
- **Tried:** (1st-pass attempts, see prior note) Plus this pass: (a) inlined the 6 float intermediates into a single expression (C0,C1,H,new_var,poly,h2_poly removed) — frame dropped to 0x30 (saved 16/24 bytes) but mwcc reordered instructions, breaking match to 80.5%. (b) Replaced coef[2] array with two separate u32 vars — mwcc deduplicated/elided stack stores, dropped match to 74.5%. Reverted both, back to 99.31% baseline. The 6-float-temps structure mwcc gets RIGHT instruction-order with is the original; collapsing locals is the wrong knob.
- **Likely fix:** ALL changes must land in math.c (and removal from math_data.c) — sdata2 ownership per splits.txt. Coordinated rewrite: (1) In math.c, define: 'static const u32 MSL_Math_804DE1B0 = 0xBF000030;' and 'static const u32 MSL_Math_804DE1B4 = 0x3EAAAA36;' [note CORRECTED bit value]. Use these directly instead of __logf_C*_bits externs. Verify mwcc emits them at sdata2 0x804DE1B0/B4. (2) In math.c, rename 'float float_nan = 0.0/0.0;' to 'float __float_nan = ...;' (init that produces 0x7FFFFFFF, possibly via union/u32 init). Rename 'float float_inf = 1e100*1e100;' to 'float __float_huge = ...' (must produce 0x7F800000 = +inf). May need to use u32 initialization with type-pun to control exact bit values — the target's __float_nan is 0x7FFFFFFF specifically (qNaN with all mantissa bits set), not the 0x7FC00000 that 0.0/0.0 typically produces. (3) Define 'static const float MSL_Math_804DE1C0 = 0.6931472f;' (LN2 named float). Replace 'LN2 * E' macro with 'MSL_Math_804DE1C0 * E'. (4) MSL_Math_804DE1C8 is auto-generated by mwcc when emitting (double)int conversion — should appear automatically at sdata2 0x804DE1C8 once other sdata2 entries are present and properly ordered. (5) DELETE __logf_C0_bits/__logf_C1_bits definitions from math_data.c. Verify math_data.c only owns rodata, no sdata2 contributions per splits.txt. (6) After all renames, expect frame to remain 0x40 (the spill issue is orthogonal); if it stays at 0x40 with named symbols, that's expected — fewer test mismatches. Frame reduction to 0x28 likely requires separate restructuring tied to mwcc's optimization of named const float vs runtime-initialized float globals (target may inline LN2 differently when it's 'const float' vs 'float'). NOT permuter territory: the diff is reloc-symbol layout + values, requires multi-file source surgery.
