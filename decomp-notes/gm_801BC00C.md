---
function: gm_801BC00C
tu: src/melee/gm/gm_1BA8.c
headline: regalloc + permuter-territory
tags: [regalloc, permuter-territory, frame-size]
---
## gm_801BC00C (`src/melee/gm/gm_1BA8.c`) — regalloc + permuter-territory

- **Tags:** `regalloc`, `permuter-territory`, `frame-size`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 287-instr scene-init dispatcher in gm_1BA8.c reaches 82.72% match (169 mismatches) with structurally correct switch decomp. All four switches use correct cases (4..48 first, 0x23/0x2B if-else, 9..48 timer block, 9..48 + case 35 unfurl block). Final HSD_GObj_SetupProc(GObj_Create(0xF,0x11,0), gm_803DF94C[unk_535]->x0, 0x15) tail call matches. Remaining mismatches are entirely register allocation drift: target preserves r26-r31 (6 saved regs); base preserves r27-r31 (5). Target hoists 'addi r28, r3, gm_803DF94C@l' into prologue (extending its live range across whole function) which forces an extra callee-save. Base computes that pointer lazily near the tail call, so it only needs r28 briefly. Effects cascade: target uses r29 for unk_535 / r30 for tbl, base uses r30/r29 (swapped). Stack frame target=0x60 vs base=0x50 (close, helped by PAD_STACK(0x40)).
- **Tried:** (1) Initial source-shape with if/else for 0x23/0x2B; switch range checks expressed as outer if((u32)(unk_535-9)<=0x27). 78.2% match. (2) Replaced if/else for 0x23/0x2B with switch (matched binary-search compare-tree the compiler emits). Removed redundant outer if-checks. Added PAD_STACK(0x40) to bump stack closer to target 0x60. 82.7% match.
- **Likely fix:** Permuter to explore register-pinning permutations, especially the gm_803DF94C@ha early-load pattern. Or hand-shape: introduce an early use of gm_803DF94C[unk_535] (e.g. cache it in a local) so the compiler hoists the @ha load into prologue and allocates r28 long-lived. Header change required: src/melee/gm/gm_1601.h proto for gm_8016A9E8 changed from UNK_RET/UNK_PARAMS to void(u8) - needed for u8 arg to compile.

