---
function: gm_801B1C24
tu: src/melee/gm/gm_1B03.c
headline: dead-cmpw + dead-loads
tags: [dead-cmpw, dead-loads, r30-r31-swap, regalloc]
---
## gm_801B1C24 (`src/melee/gm/gm_1B03.c`) — dead-cmpw + dead-loads

- **Tags:** `dead-cmpw`, `dead-loads`, `r30-r31-swap`, `regalloc`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 94.0% match (22 mismatches). Else-branch generates cmplwi (unsigned) but target uses cmpw r0,r4 (signed reg-reg compare); target also has dead li r5,0x1 and li r5,0x2 between compare and branch; stb base pointer differs (r29+0x24 base + 0x6c offset vs r29+0x8c + 0x4 offset, same effective address). Second accumulation loop has r30/r31 swap (counter vs low-accum register). All gm_804D68C0 comparisons need to produce cmpw not cmplwi, likely requires s32 local copy or signed cast.
- **Tried:** (1) Initial full implementation with PAD_STACK(16) and dont_inline pragma on gm_801B07E8 to fix frame size and inlining. (2) Added VsModeData* new_var alias before vs_data (permuter-found) to fix main register allocation - improved from 78.7% to 94.0%.
- **Likely fix:** Declare a local s32 (or int) copy of gm_804D68C0 before the else-branch to force signed reg-reg comparisons instead of cmplwi. The dead li r5 values may require an intermediate variable that holds the slot value (slot_val=1,2,3) before the conditional stb. The r30/r31 swap in the accum loop needs the permuter to find the right declaration order for the two u32 halves of accum.

