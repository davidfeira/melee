---
function: hsd_803B2374
tu: src/sysdolphin/baselib/hsd_3AA7.c
headline: permuter-territory + mwcc-loop-opt
tags: [permuter-territory, mwcc-loop-opt, instruction-scheduling]
---
## hsd_803B2374 (`src/sysdolphin/baselib/hsd_3AA7.c`) — permuter-territory + mwcc-loop-opt

- **Tags:** `permuter-territory`, `mwcc-loop-opt`, `instruction-scheduling`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 92.39% match (7 mismatches). Manual source: clears hsd_804D7990/7994, memset hsd_804D2348[0x300], zeroes hsd_804D7980/7984, sets hsd_804D799C=2, loops 128x to zero hsd_804D1148[i][0], then hsd_804D7988=0. Target compiled with outer ctr=2 / inner fully-unrolled 64 stws (bdnz with 64 stw r31 at offsets 0,0x24..0x8DC, then addi r3,r3,0x900); my single 128-iter for-loop produces equivalent semantics but with li r3,0/cmpwi 0x80/bge structure. Also target reorders stw r31,7984 and stw r0,799C to BEFORE the loop (filling delay slots around mtctr setup); base puts them after memset call. 7 mismatches = 3 store reorder + 4 loop-structure. Tried explicit nested for(i=0;i<128;i+=64) for(j<64) — regressed to 10% (mwcc did not unroll inner; introduced mulli/add scaffolding). Likely needs explicit 64-store unroll macro or permuter to discover unroll equivalent. Decomp-permuter offline; queued.

