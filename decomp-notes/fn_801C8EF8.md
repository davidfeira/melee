---
function: fn_801C8EF8
tu: src/melee/gr/grmaterial.c
headline: permuter-territory + frame-size
tags: [permuter-territory, frame-size, regalloc, permuter-queued]
---
## fn_801C8EF8 (`src/melee/gr/grmaterial.c`) — permuter-territory + frame-size

- **Tags:** `permuter-territory`, `frame-size`, `regalloc`, `permuter-queued`
- **Best fuzzy:** (unknown)
- **Diagnosis:** Manual implementation reaches 94.06% (117 mismatches). Function structure, control flow, struct field accesses, and template offsets are all semantically correct. Remaining diffs cascade from: (1) target uses 9 callee-save regs (r23..r31) vs base's 8 (r24..r31); (2) target stack frame 0x150 vs base 0x130 — target reserves a ~0x78-byte gap (sp+0x4C..0x9F) likely from an unused HSD_TExp local mirroring ftMaterial's layout; (3) GXColor compiler temps for HSD_SetMaterialColor land at sp+0x118-0x120 (high) in target vs sp+0x18-0x20 (low) in base — mwcc declaration-order/layout heuristic difference. All mismatches are register allocation + stack-offset cascades from these layout choices.
- **Tried:** initial m2c-derived implementation (92.76%); deferred gp/overlay assignments to just-before-condition; inlined grMaterial_GetOverlay() at use sites to encourage gp-relative addressing; added PAD_STACK(0x74) — bumped frame to 0x130 but target needs 0x150
- **Likely fix:** permuter run on register allocation + local declaration order. Try declaring an unused HSD_TExp local to add ~0x78 stack bytes, or PAD_STACK(0x20) to bridge remaining gap. Also try forcing 9-register usage by introducing additional persistent local.

