---
function: gm_8016A164
tu: src/melee/gm/gm_1601.c
headline: permuter-false-positive + cross-tu-globals
tags: [permuter-false-positive, cross-tu-globals, bss-anchor]
---
## gm_8016A164 (`src/melee/gm/gm_1601.c`) — permuter-false-positive + cross-tu-globals

- **Tags:** `permuter-false-positive`, `cross-tu-globals`, `bss-anchor`
- **Best fuzzy:** 99.8125%
- **Diagnosis:** 8 mismatches all reloc-symbol on lbl_8046B488: target uses named global lbl_8046B488 (offset 0x8/0x7/0x10), base uses ...bss.0+0x118/0x117/0x120. Post-link bytes identical (0x110 offset delta cancels). Same TU as 2 prior stuck siblings (gm_1601.c) — structural BSS layout issue across this TU, not a per-function bug.
- **Tried:** Inspected diff; verified false-positive class (BSS anchor named-vs-anonymous, consistent offset delta 0x110). Did not dispatch permuter (scorer treats as equivalent, report.json fuzzy does not).
- **Likely fix:** TU-wide fix: declare/define lbl_8046B488 (and likely sibling BSS symbols) such that the compiler emits a named global at the right TU position so it becomes the @ha anchor instead of an anonymous ...bss.0 base. Probably needs a static or extern declaration ordering change in gm_1601.c or its header. Same fix should clear the other 2 sibling stucks in this TU.
