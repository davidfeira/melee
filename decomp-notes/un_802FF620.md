---
function: un_802FF620
tu: src/melee/if/ifcoget.c
headline: regalloc + mwcc-aliasing
tags: [regalloc, mwcc-aliasing]
---
## un_802FF620 (`src/melee/if/ifcoget.c`) — regalloc + mwcc-aliasing

- **Tags:** `regalloc`, `mwcc-aliasing`
- **Best fuzzy:** 91.2188%
- **Diagnosis:** Target keeps TWO base-pointer registers for un_804A1F58 (r30=base@l, r29=copy of r30) plus loop counter in r28. Source has single 'base' local. Inside loop, base reads x8.x4 via lwz r3, 0xc(r29) = direct offset into element 0 (curiously, suggesting base[i].x8.x4 actually uses base+i*0x14+0xc but with i held in r28). The +1 instr in target prelude vs base (extra addi/mr) is the structural diff; downstream blt displacement +0x10 follows from it.
- **Tried:** V1 walking-pointer p++ alongside outer base => 12 mismatches (worse). V2 inline un_804A1F58[i] (no base local) => 14 mismatches (worse). Single-base-local is closest to base.
- **Likely fix:** MWCC pattern looks like compiler keeping a duplicate r29=r30 register. Possibly volatile-ish or struct-deref idiom would force; or permuter could find via PERM_VAR/PERM_GENERAL on base local. Suggest cluster permuter dispatch — only 6 mismatches and shape looks reg-allocation flavored.
