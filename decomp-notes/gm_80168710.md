---
function: gm_80168710
tu: src/melee/gm/gm_1601.c
headline: permuter-queued + regalloc
tags: [permuter-queued, regalloc, mwcc-loop-opt]
---
## gm_80168710 (`src/melee/gm/gm_1601.c`) — permuter-queued + regalloc

- **Tags:** `permuter-queued`, `regalloc`, `mwcc-loop-opt`
- **Best fuzzy:** 89.8932%
- **Diagnosis:** At 89.89% / 25 mismatches with two-loop natural source. Both halves are 4-iteration loops over player_standings[i] (slot_type==0 filter). First loop finds max is_big_loser, second collects matching indices into cand[count] then picks via HSD_Randi(count). Compiler unrolls the second loop but converts cand[count]=i into a moving-pointer autoincrement (stb r0, 0x0(r5); addi r5, r5, 0x1) whereas target keeps r4=&cand[0] pinned and uses indexed stbx r5, r4, r0 with separate count tracker. Sliding-pointer for player_standings also lands on r4 in base vs r6 in target. Remaining diffs are pure regalloc/store-pattern — permuter territory.
- **Tried:** (1) Natural two-loop source with cand[count]=i: 89.89%/25. (2) Fully-unrolled explicit if-blocks per player: regressed to 89.53%/33 (lost the sliding-pointer pattern, used constant offsets from r29 instead).
- **Likely fix:** Permuter should land it via reordering of cand index/value lifetimes or temp-variable shape changes to prevent autoincrement coalescing of cand[count]=i. Override note: decomp-permuter offline, queued for cluster dispatch when available.

