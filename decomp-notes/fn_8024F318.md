---
function: fn_8024F318
tu: src/melee/mn/mndatadel.c
headline: permuter-territory + frame-size
tags: [permuter-territory, frame-size, r30-r31-swap, sdata2-anonymous-floats, instruction-scheduling]
---
## fn_8024F318 (`src/melee/mn/mndatadel.c`) — permuter-territory + frame-size

- **Tags:** `permuter-territory`, `frame-size`, `r30-r31-swap`, `sdata2-anonymous-floats`, `instruction-scheduling`
- **Best fuzzy:** (unknown)
- **Diagnosis:** Structural decomp at 94.6% match (83 mismatches of ~330 instr). Source-shape correct: input dispatcher with switch on data->x0. Remaining diffs: (1) Stack frame too small (0x38 vs 0xb0) - mwcc collapses 4 jobj locals across non-overlapping switch cases despite being declared as 4 separate vars (jobj0/2/3/4), (2) Register allocation: target uses r29=mdata/r30=gobj/r31=data, mine uses r29=gobj/r30=data/r31=mdata - mdata is materialized lazily because not used until late, (3) slwi/add/lwz vs slwi/addi/lwzx scheduling in case bodies, (4) Anonymous sdata2 floats (250/5/0.05) numbered @205-207 vs target's mnDataDel_804DC1C8/CC/D0 - likely sdata2-float false-positive class but not verifiable without working fuzzy build (mnsoundtest.o build broken by another agent).
- **Tried:** Source decompiled from m2c+asm. Tried reordering local declarations to force mdata into r29; declaring 4 distinct HSD_JObj* locals (jobj0/jobj2/jobj3/jobj4) for stack-slot expansion - mwcc still merged the slots.
- **Likely fix:** Permuter for regalloc swaps and scheduling. For stack-frame issue: may need to take address of jobj locals before the switch (volatile or external write) to keep them all live, OR declare them inside each case block with different identifiers. For named sdata2 floats: verify post-link match via fuzzy mode once mnsoundtest.o build is fixed - this may already be a sdata2-float false positive.

