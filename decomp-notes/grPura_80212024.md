---
function: grPura_80212024
tu: src/melee/gr/grpura.c
headline: tu-data-osreport + tu-wide-data
tags: [tu-data-osreport, tu-wide-data]
---
## grPura_80212024 (`src/melee/gr/grpura.c`) — tu-data-osreport + tu-wide-data

- **Tags:** `tu-data-osreport`, `tu-wide-data`
- **Best fuzzy:** 99.9111%
- **Diagnosis:** At 99.91% with 4 mismatches, all the same root cause: array offset 0x270 vs 0x2a0 to grPu_803E6AA0. Field-type casts (s16) for pura.xC4 and pura.xC8 fixed extsh/sth/lha/stw mismatches. Adding 'char grPu_803E6A30[] = "/GrPu.dat";' as a separate symbol matched the target's separate string symbol. Remaining 0x30 byte gap is mwcc placing OSReport format string '%s:%d: couldn t get gobj(id=%d)\n' and 'grpura.c' (referenced from grPura_80211E08) inline within grPu_803E6A3C symbol space (target size 0x64) vs our compile placing them in separate stringBase rodata. To match, the format strings would need to be declared as named globals between StageData and grPu_803E6AA0 and referenced from grPura_80211E08, but that risks breaking grPura_80211E08 (currently 99.97%). Cross-TU/cross-function string literal layout — structural at TU level.

