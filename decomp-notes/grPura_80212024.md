---
function: grPura_80212024
tu: src/melee/gr/grpura.c
headline: struct-typing + data-anchor
tags: [struct-typing, data-anchor]
---
## grPura_80212024 (`src/melee/gr/grpura.c`) — struct-typing + data-anchor

- **Tags:** `struct-typing`, `data-anchor`
- **Best fuzzy:** 99.9111%
- **Diagnosis:** Two structural blockers: (1) grPura_GroundVars.xC4 is declared u16 but target emits lha/extsh (signed halfword), needs s16; xC8 field stores as halfword (sth) but declared as HSD_JObj* (pointer, 4 bytes). (2) grPu_803E6AA0 is at grPu_803E6800+0x270 in our TU but target expects +0x2A0 (48 bytes missing). Layout gap is likely due to missing data between grPu_803E6A3C and grPu_803E6AA0 in the source.
- **Tried:** Attempt 1: replaced grPu_803E6AA0 refs with (GXColor*)((char*)grPu_803E6800+0x2A0) cast — compiler pre-baked offset into r31, wrong register allocation. Attempt 2: matched upstream exactly (removed s16 casts, used direct field access, kept grPu_803E6AA0) — 93.6%, still offset mismatch and lhz/lha/stw/sth field type mismatches.
- **Likely fix:** Fix grPura_GroundVars.xC4 to s16 (not u16) in types.h, fix xC8 type to s16. Also identify and add the 48 missing bytes of data between grPu_803E6A3C and grPu_803E6AA0 in grpura.c (could be additional StageData fields or a padding struct). Header changes required.

