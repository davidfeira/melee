---
function: un_803067BC
tu: src/melee/ty/toy.c
headline: permuter-queued
tags: [permuter-queued]
---
## un_803067BC (`src/melee/ty/toy.c`) — permuter-queued

- **Tags:** `permuter-queued`
- **Best fuzzy:** (unknown)
- **Diagnosis:** Match at 84.04% (40 diffs, 73 instr). Source shape correct: arg1==0 forward loop copies un_804D6E64[arg0+i*3] into un_804D6EDC[i] for *gmMainLib_8015CC90() iterations; arg1!=0 path copies same source pattern backward into un_804D6EDC[count..0] with 8-way unroll + mtctr/bdnz cleanup. Remaining diffs are pure permuter territory: (1) register allocation r29<->r31 swap in arg0/base/src holders, (2) my do{...}while(--n) generates subic./bne, target uses mtctr/bdnz on the unrolled outer loop and the cleanup loop. Original m2c was structurally accurate. Permuter offline so queued.

