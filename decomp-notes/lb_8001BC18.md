---
function: lb_8001BC18
tu: src/melee/lb/lbcardnew.c
headline: permuter-false-positive + tu-wide-data
tags: [permuter-false-positive, tu-wide-data, paired-siblings]
---
## lb_8001BC18 (`src/melee/lb/lbcardnew.c`) — permuter-false-positive + tu-wide-data

- **Tags:** `permuter-false-positive`, `tu-wide-data`, `paired-siblings`
- **Best fuzzy:** 96.6197%
- **Diagnosis:** 71-instr wrapper around lb_8001BB48; mwcc inlines BB48 into BC18 so this function inherits BB48's full diff. 26 mismatches: 18 regalloc (r26-r31 register-name shift one slot, identical ordering), 3 frame-size (stwu -0x60 vs -0x68 — 8 bytes excess on base), 1 stack-offset (lmw r26 0x48 vs 0x50), 3 'real' which decompose to: 1 reloc-symbol false-positive (addi r31, r11, lb_80432A68@l vs ...bss.0@l — same address, post-link bytes match), 2 instruction-order swap (li r5,0x20 / addi r3,r3,0x10 swapped — this is the setup_task(7,0x10) call followed by the immediately-following sibling setup_task arg). All structurally inherited from lb_8001BB48 (same TU sibling, also stuck at 96.15%) — same blocker pattern noted for lb_8001BB48/BE30/BF04 'paired stack frame size' (target wants 0x40, base produces 0x48 — 8 bytes excess from mwcc temp/spill behavior). Permuter-false-positive on the bss.0@l reloc-symbol class.
- **Tried:** No source variants attempted. Function source is a thin wrapper (1 call + 1 do-while loop) so all diff is inherited from the inlined sibling lb_8001BB48 — fixing this requires fixing BB48 first.
- **Likely fix:** Wrapper diff resolves automatically once lb_8001BB48 matches. Watch the lb_8001BB48 cluster permuter (and related BE30/BF04 paired-siblings) — same fix lands BC18 for free. Don't dispatch a separate permuter for BC18 since it would compete with the BB48 work and any 'fix' here has to come from the inlined-sibling shape anyway.
