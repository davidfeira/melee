---
function: mn_8022A440
tu: src/melee/mn/mnmain.c
headline: permuter-false-positive + sdata2-float
tags: [permuter-false-positive, sdata2-float, regalloc]
---
## mn_8022A440 (`src/melee/mn/mnmain.c`) — permuter-false-positive + sdata2-float

- **Tags:** `permuter-false-positive`, `sdata2-float`, `regalloc`
- **Best fuzzy:** 99.15%
- **Diagnosis:** 4 mismatches at 99.25% strict / 99.15% fuzzy. 3 are reloc-anchor false-positives: (1) lfs f1, mn_804DBDA8@sda21 vs @267@sda21 (anonymous sdata2 float anchor for 0.0F passed to HSD_JObjReqAnimAll); (2) lfd f1, mn_804DBD88@sda21 vs @377@sda21 (sdata2 double for the 0x4330... f64 conversion of selection*2); (3) DIFF_ARG_MISMATCH on identical 'lfs f0, 0x504(r30)' (reloc anchor for mn_803EAE68 base — the post-link bytes match). The 1 remaining real mismatch is a single 'mr r3, r29' (target) vs 'addi r3, r29, 0x0' (base) instruction-pick — likely the call site immediately before bl HSD_JObjAnim at 0x8022A558 (last r29-arg call after the chain of 8 mn_8022F3D8 calls that all use addi r3,r29,0). Per CLAUDE.md rule 6, do NOT dispatch permuter on diffs dominated by sdata2/data-anchor false-positives — its scorer treats those as equivalent but report.json fuzzy does not.
- **Tried:** Two prior source-shape attempts visible in working-tree diff (other agent): (a) swapped 'r29 = sp24[1]; flow = &mn_804A04F0;' to 'flow = &mn_804A04F0; r29 = sp24[1];', (b) reformulated 'f = mn_8022F298(sp24[0]) - mn_803EB360[1].start_frame; HSD_JObjReqAnimAll(sp24[0], f + mn_803EB360[0].start_frame)' into the natural form 'f = mn_8022F298(sp24[0]); HSD_JObjReqAnimAll(sp24[0], mn_803EB360[0].start_frame + (f - mn_803EB360[1].start_frame))'. Best is current 99.25% strict.
- **Likely fix:** Permuter-false-positive class. The 3 reloc-anchor mismatches need named sdata2 float/double pool entries pre-defined in correct order (or a sibling TU that pre-anchors them). The single mr/addi may resolve once the sdata2 pool layout is correct (instruction scheduling can shift). If it doesn't, it's a single permuter-territory regalloc diff — but per CLAUDE.md rule 6, not worth dispatching while reloc-anchor noise dominates the scorer. Likely waits on TU-wide pool reconstruction (same family as ftCo_800BE6AC, Camera_8002CB0C in notes).
