---
function: fn_80174284
tu: src/melee/gm/gmresult.c
headline: regalloc + permuter-plateau
tags: [regalloc, permuter-plateau]
---
## fn_80174284 (`src/melee/gm/gmresult.c`) — regalloc + permuter-plateau

- **Tags:** `regalloc`, `permuter-plateau`
- **Best fuzzy:** 98.6667%
- **Diagnosis:** 98.66%, 10 mismatches all DIFF_ARG_MISMATCH register naming. Target uses r28=do_call, r29=count, r30=i, r31=buf-strength-reduced. Base uses r31=do_call, r29=count, r28=i, r30=buf. Only count register assignment matches; do_call/i/buf are permuted across r28/r30/r31. Previous cluster permuter ran 619min and plateaued at score=40 (best mutation was useless inline assignment (new_var = tmp)[slot].x0[i]).
- **Tried:** Variant 1: explicit u8* buf = tmp[slot].x0; before loop -> 96.5%, 20 mismatches (extra stack slot, worse). Variant 2: swap count=0 / do_call=... init order -> 94.2%, 12 mismatches (worse). Reverted both. Original source already best.
- **Likely fix:** Likely needs cross-TU change: register allocator picks differ based on inlined call surrounding fn_80174284 (call site is line 1554 inside another function in same TU). Could be that another function in gmresult.c needs to land first to change register-allocation pressure. Or: this is a candidate for permuter-false-positive re-classification if base.s register choice depends on something the .o-only diff sees but the linker fixes up. Worth checking if the function's caller (line 1554) is also nonmatching and whether matching that caller changes regalloc here.
