---
function: fn_80174284
tu: src/melee/gm/gmresult.c
headline: regalloc + permuter-plateau
tags: [regalloc, permuter-plateau, upstream-regression]
---
## fn_80174284 (`src/melee/gm/gmresult.c`) — regalloc + permuter-plateau

- **Tags:** `regalloc`, `permuter-plateau`, `upstream-regression`
- **Best fuzzy:** 98.6667%
- **Diagnosis:** Source is byte-identical to upstream/master (doldecomp/melee). Local build gives 98.67% with 10 DIFF_ARG_MISMATCH instructions: r28/r30/r31 permuted across do_call, i, buf locals (only count in r29 matches). Upstream upstream-check reports 'state=matched' but our toolchain produces different register allocation for the same source. This is a toolchain-environment divergence — the function is structurally complete and correct.
- **Tried:** (1) explicit u8* buf = tmp[slot].x0 before loop -> 96.5%, 20 mismatches (extra stack slot, worse). (2) swap count=0/do_call=... init order -> 94.2%, 12 mismatches (worse). Both reverted. (3) 619-min cluster permuter run plateaued at score=40. (4) Verified source is identical to upstream/master — no source change can fix this.
- **Likely fix:** Toolchain mismatch between our build environment and upstream. No source change available. Either accept as-is (upstream considers it matched) or investigate build flags/compiler version difference that causes different regalloc for the same source.

