---
function: fn_800DAD18
tu: src/melee/ft/chara/ftCommon/ftCo_Attack100.c
headline: frame-size + stack-offset
tags: [frame-size, stack-offset, instruction-scheduling, regalloc]
---
## fn_800DAD18 (`src/melee/ft/chara/ftCommon/ftCo_Attack100.c`) — frame-size + stack-offset

- **Tags:** `frame-size`, `stack-offset`, `instruction-scheduling`, `regalloc`
- **Best fuzzy:** 69.5769%
- **Diagnosis:** Frame is 0x48 in compiled base but target needs 0x50. Direct gobj->user_data access (no GET_FIGHTER) and two Vec3 locals (sp2C, sp2C) with PAD_STACK(0x18) gives 0x48 frame. Stack offsets for sp20/sp2C are consistently 4 bytes too high (sp20 at 0x24 vs target 0x20). Unknown local variable causing 4-byte stack spill. Additionally, the compiler schedules dx/dz delta computations lazily (at cur_pos update sites) rather than hoisting them before the fcmpo, which differs from target's instruction scheduling.
- **Tried:** Attempt 1: removed Vec3 tmp, switched GET_FIGHTER to gobj->user_data, changed PAD_STACK to 0x18, removed bool var_r3 in favor of bool — still 0x48 frame. Attempt 2: changed to s32 var_r3, added f32 temp_f5 explicit temp for dy, same PAD_STACK(0x18) — still 0x48 frame, 28 mismatches.
- **Likely fix:** Try PAD_STACK(0x20) instead of 0x18 to get the 0x50 frame. Also try removing s32 var_r3 as a named variable (use only r3) — the 4-byte frame discrepancy is likely one of {s32 var_r3, f32 temp_f5} being spilled to stack. Once frame is correct (maybe 10-12 mismatches), remaining register allocation (f5 vs f3 for dy) and instruction scheduling (dx/dz hoisting) can be handled by permuter.

