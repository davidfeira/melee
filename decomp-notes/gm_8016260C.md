---
function: gm_8016260C
tu: src/melee/gm/gm_1601.c
headline: permuter-queued + regalloc
tags: [permuter-queued, regalloc, permuter-territory]
---
## gm_8016260C (`src/melee/gm/gm_1601.c`) — permuter-queued + regalloc

- **Tags:** `permuter-queued`, `regalloc`, `permuter-territory`
- **Best fuzzy:** 96.79%
- **Diagnosis:** At 97.9% (30 mismatches across 6 saturating-add sites). Structural shape (control flow, branches, struct field accesses, switch dispatch) all match. Only diff is register allocation in the saturating-add pattern: target uses 'lwz r5; li r4,-1; addi r0,r5,1; mr r4,r0; stw r4' but base uses 'lwz r4; li r0,-1; addi r4,r4,1; mr r0,r4; stw r0'. Same instructions, off-by-one register rotation, 5 instructions x 6 sites.
- **Tried:** Two source shapes attempted: (1) inline 'p = ...; *p = MAX(-1U, *p + 1)' style; (2) m2c-style with explicit 'temp_r3 = ...; var_r4 = MAX(-1U, *temp_r3 + 1); *temp_r3 = var_r4' temps. Both produce identical 97.9% with the same r4/r0 vs r5/r4 register allocator divergence at every saturating-add site.
- **Likely fix:** Permuter (offline per dispatch override). Pure regalloc rotation, 30 instr exceeds 15-instr auto-permute threshold but classifies cleanly as register-allocation noise. Sibling gm_8016247C uses the same 'MAX(-1U, *p + arg)' pattern and matches; the difference here is +1 (immediate addi) vs +arg0 (register add) which forces a different live-range shape. Try when permuter is back online; alternatively explore source shapes that force the load result to remain live as a separate temp.

