---
function: ftCo_800B77E8
tu: src/melee/ft/ftcpuattack.c
headline: struct-typing + header-required
tags: [struct-typing, header-required, permuter-blocked]
---
## ftCo_800B77E8 (`src/melee/ft/ftcpuattack.c`) — struct-typing + header-required

- **Tags:** `struct-typing`, `header-required`, `permuter-blocked`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 1053-instruction switch-of-32-cases CPU AI per-character attack queueing function. Structure: a guard prefix (level==7 or ftCo_800A1C44() succeeds && xEC<8: append cmd 0x34), then switch on Fighter.kind (jtbl_803C613C, 32 entries, 0x80 bytes). Cases for FTKIND_MARIO/FOX/FALCO/CAPTAIN/GANON/PIKACHU/PICHU/KOOPA/GKOOPS/YOSHI/LUIGI/PEACH/KIRBY each enqueue 1-3 commands based on opponent reachability (mpCheckAll vs ftCo_800A0FB0 ground-projection at character-specific X-offsets like 22/62/102/58/57/27/53/16/60/32 ft). Several cases gated on ground_or_air==GA_Ground vs special !(unk1B82&0x4) air handling. Kirby case branches on fv.gw.x2238_panicCharge in {3,0xD,0x10}. Each enqueue is the idiom (&fp->x1A88+xEC*4)->xCC=cmd; xEC++ — i.e., array fp->x1A88.xCC[8] of u32 where xEC is the count, capped at 8.
- **Tried:** Did NOT attempt source rewrite. Same xCC[..xEC] queue-array struct-typing blocker as already-logged ftCo_800B5AB0 in same TU. The current Fighter_x1A88_t in src/melee/ft/types.h has u8 xC8; u8 pad_xC9[0xEC-0xC9]; u8 xEC; with no typed array at +0xCC. Cannot write a structural translation without first adding a u32 xCC[8] field (or equivalent array overlay) to Fighter_x1A88_t. Header changes are outside permuter-attempter scope per agent rules; mama Claude lands header changes.
- **Likely fix:** Two-step: (1) Mama Claude lands a Fighter_x1A88_t header change adding 'u32 xCC[8]' (or struct-split with a queue subfield) at offset 0xCC, replacing the pad_xC9[0xEC-0xC9] coverage there; verify all existing matchers in TU still build. (2) THEN rewrite this function with explicit kind switch (likely 13 case-arms covering 32 enums via fall-through cases for FOX/FALCO and CAPTAIN/GANON and PIKACHU/PICHU pairs), each arm using the helper pattern: target=fp->x1A88.x44; if target && mpCheckAll(...,fp->cur_pos.x,fp->cur_pos.y,target->cur_pos.x,target->cur_pos.y) hit=1; else if facing_dir>0 forward-trace via ftCo_800A0FB0 with +offset; else reverse-trace with -offset; if hit && xEC<8: fp->x1A88.xCC[fp->x1A88.xEC++]=cmd_const; return. y-offset literals are 5.0 and -1000.0 (lfd from sdata2 doubles). Pipe to permuter cluster only after structural source reaches >=85%. Until header lands this is permuter-blocked.

