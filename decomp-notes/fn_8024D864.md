---
function: fn_8024D864
tu: src/melee/mn/mnevent.c
headline: permuter-territory + regalloc
tags: [permuter-territory, regalloc, r30-r31-swap]
---
## fn_8024D864 (`src/melee/mn/mnevent.c`) — permuter-territory + regalloc

- **Tags:** `permuter-territory`, `regalloc`, `r30-r31-swap`
- **Best fuzzy:** (unknown)
- **Diagnosis:** Function structurally matches at 12.8%/784 mismatches but every mismatch is a propagating register-allocation difference (target uses r31=0/r30=inputs/r28=&mn_804A04F0; base uses r28=0/r31=inputs/r29=&mn_804A04F0). Branch logic, struct accesses, inlines (Menu_GetAllInputs/sfx*/HSD_JObjGetTranslationY/HSD_JObjSetTranslateY/HSD_JObjSetMtxDirty/Menu_DecrementAnimTimer), assert lines (jobj.h:0x234,0x3B3,0x3EE), variadic lb_80011E24, u64 button bit tests, scoped-locals stack pattern (12 distinct slots) all match. mn_804A04F0.buttons stored as low=r_inputs at 0xC and high=r_zero at 0x8 confirms u64 assignment via Menu_GetAllInputs(). Permuter offline (override) — queued.
- **Tried:** Two source shapes: (1) flat locals declared at top, (2) per-branch scoped locals. Both produce same regalloc divergence; frame size 0xE8 vs target 0xD8 (extra slots from explicit tree local in 2 inner branches; target keeps tree in r29).
- **Likely fix:** Permuter only. Cannot affect mwcc-1.3.2 register allocator from source-level rewrites at this scale (~600 instr fn).

