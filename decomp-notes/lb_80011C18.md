---
function: lb_80011C18
tu: src/melee/lb/lb_00F9.c
headline: regalloc + permuter-territory
tags: [regalloc, permuter-territory]
---
## lb_80011C18 (`src/melee/lb/lb_00F9.c`) — regalloc + permuter-territory

- **Tags:** `regalloc`, `permuter-territory`
- **Best fuzzy:** 99.6947%
- **Diagnosis:** 7 mismatch r28<->r30 swap in jobj->next dobj-flag block. Target: r28=dobj->next, r30=dobj->next->next. Base swaps to r30=dobj->next, r28=dobj->next->next. The first jobj->child block has same pattern but matches; only second block (cur=jobj->next) diverges, suggesting the second block shares spill state with the third (jobj outer flag) block. The third block uses explicit 'HSD_DObj* dobj' local; first two inline cur->u.dobj. All three asm blocks compile to identical shape (lwz dobj into r29, check, lwz dobj->next into r28). Source semantics correct.
- **Tried:** V1: split shared 'cur' local into separate 'child'+'sibling' locals (no change, same 7 mismatches). V2: removed dobj local in third block, used jobj->u.dobj inline (regressed to 12 mismatches at 98.7%, reverted).
- **Likely fix:** Permuter (regalloc territory). Either the shared 'cur' lifetime across both blocks needs shape change mwcc finds equivalent, or some restructuring of the third block's dobj local that changes spill ordering. Dispatching to cluster.
