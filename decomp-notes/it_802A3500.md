---
function: it_802A3500
tu: src/melee/it/items/itlinkhookshot.c
headline: stack-offset + regalloc
tags: [stack-offset, regalloc, permuter-false-positive, tu-wide-data]
---
## it_802A3500 (`src/melee/it/items/itlinkhookshot.c`) — stack-offset + regalloc

- **Tags:** `stack-offset`, `regalloc`, `permuter-false-positive`, `tu-wide-data`
- **Best fuzzy:** 98.831%
- **Diagnosis:** 27 mismatches at 98.76%/98.83% on 71-instr fn. Stack layout differs: target places Mtx m at r1+0x18 (low) and Vec3 pos at r1+0x4c (high, after m); base places Vec3 pos at r1+0x1c (low) and Mtx m at r1+0x28. Offset delta of 0x10 cascades through all references: addi r3,r1,0x18 vs 0x28; addi r4,r1,0x4c vs 0x1c; stfs at 0x24/0x34/0x44 (target inside m, translation column) vs 0x34/0x44/0x54 (base inside m at +0x10). Plus regalloc r28<->r29<->r30 swap and one named-vs-anon sdata2 float (it_804DCDB8 vs @320, false-positive). 5 of 27 are reloc-symbol false-positives.
- **Tried:** V1: removed 'f32 pad[3]' from locals -> went worse (98.69%, 32 mismatches). V2: reordered locals (pad,pos,m instead of m,pos,pad) -> identical 27 mismatches as baseline (no effect).
- **Likely fix:** mwcc seems to allocate Vec3 below Mtx regardless of declaration order; the pad[3] is a manual workaround that doesn't reproduce target layout. Target was likely compiled from source with different aggregate (e.g. struct that puts m and pos together at specific offsets, or extra/different padding scheme). TU itlinkhookshot.c has 2 logged stuck siblings - same TU-wide blocker. Need TU-wide structural look at how Mtx+Vec3 stack locals are placed across these item functions; potentially a struct wrapping Mtx+Vec3+pad as a single aggregate, OR an extra leading f32 local that mwcc places at high offset to push pos up. Permuter would not help: the 5 reloc-symbol mismatches are false-positives (scorer treats equal post-link bytes as match) and the stack-offset cascade requires structural change.
