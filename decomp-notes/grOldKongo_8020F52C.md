---
function: grOldKongo_8020F52C
tu: src/melee/gr/groldkongo.c
headline: tu-data-osreport + cross-tu-globals
tags: [tu-data-osreport, cross-tu-globals]
---
## grOldKongo_8020F52C (`src/melee/gr/groldkongo.c`) — tu-data-osreport + cross-tu-globals

- **Tags:** `tu-data-osreport`, `cross-tu-globals`
- **Best fuzzy:** 94.8814%
- **Diagnosis:** 5 mismatches on 59-instr StageCallbacks dispatcher (essentially identical pattern to grRCruise_801FF2C8). Target asm: r31 = grOk_803E6580 (a 12-byte struct at .data+0x0 holding `0x3, 0x10001, 0x30002` — likely stage-id metadata) sitting BEFORE the StageCallbacks array grOk_803E658C at .data+0xC. r29 = r31 + arg0*0x14 + 0xC indexes into the array. OSReport string args resolve as `addi r3, r31, 0x9C` (format string `%s:%d: couldn t get gobj(id=%d)\n` embedded INSIDE grOk_803E65E8, a 0x58-byte cross-TU struct at .data+0x68 containing pointers to grOk_803E658C, grOk_803E65DC `/GrOk.dat`, grOldKongo_8020F46C, …, grOk_803E6580 itself, plus the inlined format string at struct-offset 0x34) and `addi r4, r31, 0xC0` (`groldkongo.c` at grOk_803E6640). Base produces 0x50/0x74 offsets via the StageCallbacks anchor `grOk_803E658C` directly, with `addi r29, r29, 0` (no pre-add) since base computes `&callbacks[arg0].callback0` at use-sites. Function source already uses the inline `Ground_SetupStageCallbacks` (matches sibling grBattle_80219D84/grTLink_802219D0 pattern). 5-mismatch shape: 2 ARG_MISMATCH on the OSReport offsets, 1 INSERT (`addi r29, r29, 0xc` pre-shift), 1 DELETE/INSERT pair on `addi r3, r28, 0x0` scheduling adjacent to the pre-shift.
- **Tried:** Diff inspection + groldkongo.s data-section trace (lines 1518-1620). Did not attempt source rewrite — same TU-data refactor scope as grRCruise_801FF2C8.
- **Likely fix:** Define `grOk_803E6580` (12-byte struct: `{ s32 = 3, s32 = 0x10001, s32 = 0x30002 }`), `grOk_803E65DC = "/GrOk.dat"`, and `grOk_803E65E8` (0x58-byte struct containing `{ 0x1E, &grOk_803E658C, &grOk_803E65DC, &grOldKongo_8020F46C, &grOldKongo_8020F468, &grOldKongo_8020F4E4, &grOldKongo_8020F4E8, &grOldKongo_8020F524, &grOldKongo_80210780, &grOldKongo_80210788, 0x1, &grOk_803E6580, 0x2, char[0x28] = "%s:%d: couldn t get gobj(id=%d)\n", 0x0 }`) so the format string lives inside the struct at offset 0x34. Then OSReport in grOldKongo_8020F52C should reference the format string and `groldkongo.c` via fields of these structs (likely via `grOk_803E65E8.fmtstr` and a separate `static const char[] grOk_803E6640 = "groldkongo.c"`). Also affects other OSReport sites in the TU at 8020F92C (grOk_803E6640) and 8020FC60 (grOk_803E6650 `!(jobj->flags & JOBJ_USE_QUATERNION)`) and 802105FC/80210754 (grOk_803E6678 `gp->u.taru.keep`). Same blocker family as grRCruise_801FF2C8, fn_80122B54, lbMthp_8001F624. Beyond single-function subagent scope; needs TU-wide data refactor.
