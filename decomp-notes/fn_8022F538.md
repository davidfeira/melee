---
function: fn_8022F538
tu: src/melee/mn/mnmainrule.c
headline: permuter-queued + regalloc
tags: [permuter-queued, regalloc, frame-size, r30-r31-swap, branch-direction]
---
## fn_8022F538 (`src/melee/mn/mnmainrule.c`) — permuter-queued + regalloc

- **Tags:** `permuter-queued`, `regalloc`, `frame-size`, `r30-r31-swap`, `branch-direction`
- **Best fuzzy:** (unknown)
- **Diagnosis:** At 95.33% (80 mismatches). Source structure is semantically correct: A/Start/Back input branches do gameRules copy from mn_804D6BD0->user_data, Up/Down loops with 0x1B/sel==4 skip, switch-on-hovered_selection followed by Left/Right confirmed_selection adjust. Remaining diff is dominated by regalloc: target reserves 4 callee-saved (r28-r31) with r31 caching panel pointer across whole function and r29 caching gobj; mine uses 3 callee-saved (frame 0x20 vs target 0x50) so panel reloads on every use. Plus 1 branch-direction inversion in Start handler (target uses 'if (gm_801A4310() != 1) goto B; A; B:' pattern with cmpwi-signed; mine emits cmplwi+bne). The lbzx vs add+lbz for panel[sel].unk2 is a subexpression of the same regalloc issue (when panel is in r31-saved, mwcc emits add+lbz; when temporary, mwcc emits lbzx).
- **Tried:** (1) Initial decomp with struct mn_8022F538_panel_t accessing unk2/unk3/unk4/unk5/unk6/unk9, switch-based hovered_selection 5/6/1/default, ((u8*) panel)[sel+2] indexing: 90.56%. (2) Refactored to share rules-copy via static inline helper, cached (u8) hovered_selection in local before gm_801A4310 call inside up/down loops, used (s32) cast to provoke cmpwi for sel==0/sel==6 tests, used explicit done=0/1 control flag for loop continue: 95.33%.
- **Likely fix:** Permuter to find regalloc that pins panel pointer in r31 across whole function (forcing 4-callee-saved frame). May need source to dereference panel field early and store result in another local that mwcc keeps in r31 to free up the pointer, or rewrite with PANEL-as-array indexing pattern that uses 'add r4, panel, sel' in a way mwcc can hoist. Also try inverting Start handler to 'if (!=1) {non1-path; return} ; ==1-path' shape.

