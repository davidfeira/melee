---
function: mnInfo_80251D58
tu: src/melee/mn/mninfo.c
headline: permuter-plateau + frame-size
tags: [permuter-plateau, frame-size, mwcc-aliasing, stack-offset]
---
## mnInfo_80251D58 (`src/melee/mn/mninfo.c`) — permuter-plateau + frame-size

- **Tags:** `permuter-plateau`, `frame-size`, `mwcc-aliasing`, `stack-offset`
- **Best fuzzy:** (unknown)
- **Diagnosis:** Plateau ~90%. Two structural blockers: (1) target uses lwzu r0, 0x8(r30) for &data->left_column[idx] then re-computes data+idx*4 separately for the SisLib_803A5CC4 call, suggesting source aliases r30 as a struct pointer (data+idx*4) accessed via constant +8 offset. mwcc instead emits lwzx (indexed) when source is data->left_column[idx]. (2) Stack frame target=0x60, base=0x58: target places dt at 0x18 (lowest), char buffers 0x20-0x37, and i2f-conversion scratch at 0x40/0x44 above buffers; base packs them tighter putting scratch at 0x38/0x3C touching dt. Buffer/dt ordering differs from any natural decl order I tried.
- **Tried:** (1) m2c-style with MnInfoData* data + data->left_column[idx]. (2) Inlined ((MnInfoData*)gobj->user_data)->left_column[idx] to avoid extra register; no improvement on lwzu. Buffers as char[4]; dt declared last; format strings via (char*)mnInfo_803EFC08 + 0x1C / + 0x28 (matches target asm's single base-load reuse via r31).
- **Likely fix:** Need source shape that materializes a MnInfoData* pointer offset by idx*4 (not via array indexing) so mwcc emits lwzu/constant-offset. E.g., MnInfoData* slot = (MnInfoData*)((u8*)data + idx*4); if (slot->left_column[0] != NULL) HSD_SisLib_803A5CC4(((MnInfoData*)((u8*)data + idx*4))->left_column[0]); Then bumping frame to 0x60 may need an unused 8-byte local declared early or a change in dt declaration order. Permuter dispatch deferred (offline).

