---
function: fn_80169A84
tu: src/melee/gm/gm_1601.c
headline: permuter-blocked + branch-pattern
tags: [permuter-blocked, branch-pattern, regalloc]
---
## fn_80169A84 (`src/melee/gm/gm_1601.c`) — permuter-blocked + branch-pattern

- **Tags:** `permuter-blocked`, `branch-pattern`, `regalloc`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 71% match (79 mismatches). Function dispatches on u8 mode with three paths: mode==1 (build randomized character pool, shuffle, cull, fill output via Player_800325C8), mode==0 (set output to -1 until terminator), other (no-op). Two structural blockers remain: (1) Target prologue has cmpwi r0,1; beq; bge; cmpwi r0,0; bge; b sequence — looks like double cmpwi suggesting if/else-if cascade with comparisons against literal 1 then 0, but my single 'else if mode==0' only emits one cmp. (2) Register allocation differs (r25/r27/r28 swapped) in shuffle phase — likely permuter territory once branch shape matches.
- **Tried:** (1) void return type — header declares 'long' so caller does 'return fn_80169A84(...)'; reverted to long. (2) added '(s32) mode' casts to switch from cmplwi to cmpwi — improved 41 pct to 71 pct. (3) added pragma dont_inline on/pop matching sibling fn_80169900 — prevented gm_80164840 inlining as gmMainLib_8015ED8C.
- **Likely fix:** Try restructuring branch as 'if (mode == 1) {...} else if (mode >= 1) return; else if (mode >= 0) {mode0 path;} else return;' to emit both cmpwi compares the target shows. Then permuter for register swaps. Permuter offline at time of log.

