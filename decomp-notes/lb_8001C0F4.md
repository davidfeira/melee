---
function: lb_8001C0F4
tu: src/melee/lb/lbcardnew.c
headline: stack-offset + frame-size
tags: [stack-offset, frame-size, paired-siblings]
---
## lb_8001C0F4 (`src/melee/lb/lbcardnew.c`) — stack-offset + frame-size

- **Tags:** `stack-offset`, `frame-size`, `paired-siblings`
- **Best fuzzy:** 99.9587%
- **Diagnosis:** Pure frame-size diff at 99.96%/5-mismatches: target wants stwu -0x40 (frame 64B, stmw r25 at 0x24), base produces stwu -0x68 (frame 104B, stmw r25 at 0x4c). The 5 mismatches are all and only stwu/stmw/lmw/lwz/addi epilogue-prologue offsets - body is byte-identical (121 instructions match). r25-r31 callee-saves, no spills. The 0x28 (40B) excess is FAR larger than siblings lb_8001BE30/BF04/BB48 (8B excess). All 8 permuter outputs at output-5-* are score=5 (no improvement over current); permuter cannot shrink frame, just shuffles cosmetic noise. m2c output matches our source exactly.
- **Tried:** (1) Inspected all output-5-* permuter results - none touch stwu/stmw, permuter scorer treats frame size as floor. (2) Checked sibling notes for lb_8001BE30/BF04/BB48 - those explicitly tested u8 _[8] pad and hoisting task local; both pushed frame the wrong direction (0x48 to 0x50). Did not retry the same dead-ends here.
- **Likely fix:** 0x28 excess suggests 10 spilled/unused stack slots that don't exist in target. Possibly the original C source assigned setup_task() returns to distinct task variables (task1, task2, task3) without reuse, OR used struct CardTask** patterns. Or PAD_STACK at a specific source line that lets MWCC place it before saves (per HSD_CObj note: 'an originally-referenced unused local that escapes DCE'). Cluster permuter alone cannot solve this - need structural source rewrite. Recommend mama Claude triage: try (a) splitting the 3 repeated blocks into separate inline functions, (b) hoisting all 'task->x8 = 0' to use a single tail-write through a shared pointer, (c) replacing inline setup_task with explicit lb_80019C38() + field stores to remove inline scratch slots.
