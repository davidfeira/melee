---
function: fn_80316170
tu: src/melee/ty/tyfigupon.c
headline: stack-offset + frame-size
tags: [stack-offset, frame-size, regalloc]
---
## fn_80316170 (`src/melee/ty/tyfigupon.c`) — stack-offset + frame-size

- **Tags:** `stack-offset`, `frame-size`, `regalloc`
- **Best fuzzy:** 99.363%
- **Diagnosis:** 99.36% match, 26 mismatches: 8-byte excess frame size (target 0x30, base 0x38) plus r29/r30 regswap on 'ud' local. Source-level changes (PAD_STACK, inlining y/aa8) either grow the frame the wrong direction or destroy the structural match. The function only declares 3 locals (aa8, y, ud); regalloc puts ud in r30 because aa8/y dominate earlier registers, but target wants ud in r29 — implies a 4th live across-call value pushes ud lower. Frame is already minimal at 0x38 with 4 GPR + 1 FPR saves; cutting 8 bytes requires eliminating one save or changing alignment, neither of which a source-level rewrite obviously achieves.
- **Tried:** (1) PAD_STACK(8) — wrong direction, frame went 0x38→0x40. (2) Inlined y and aa8 to remove stack-allocated locals — match dropped to 92%, structural diff appeared (lwz offset and lis added).
- **Likely fix:** Likely needs an additional callee-save-region temp that mwcc spills cleanly: e.g. a function-call return value held across a later call. May also be permuter territory once the structural angle is right (regalloc + scheduling). Manual avenue: try splitting the early-exit branch into a helper, or reordering so HSD_GObjGetUserData runs first and aa8/y later; the goal is to make ud's live range start before aa8's so ud claims r29.
