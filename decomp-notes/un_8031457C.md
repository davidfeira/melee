---
function: un_8031457C
tu: src/melee/ty/tylist.c
headline: mwcc-aliasing + frame-size
tags: [mwcc-aliasing, frame-size, stack-offset, regalloc, instruction-scheduling, permuter-queued]
---
## un_8031457C (`src/melee/ty/tylist.c`) — mwcc-aliasing + frame-size

- **Tags:** `mwcc-aliasing`, `frame-size`, `stack-offset`, `regalloc`, `instruction-scheduling`, `permuter-queued`
- **Best fuzzy:** 91.589%
- **Diagnosis:** 96.24% match, 41 mismatches. Structural blocker: mwcc folds stack slots for HSD_RectS16 viewport and Scissor scissor (both 8-byte structs with non-overlapping lifetimes), reusing the same stack memory for both. Target keeps them at separate offsets (viewport=0x30, scissor=0x28), causing frame-size delta of 0x10 (target 0x48 vs ours 0x38) which propagates to ~16 stack-offset mismatches across eye/interest Vec3 stores too. Also ~25 reg-alloc/scheduling mismatches around the second HSD_GObjObject_80390A70 call where target keeps cobj in r30 across calls vs ours uses r0 as transit.
- **Tried:** (1) Direct un_804A2D6C global access with TyListGobjEntry struct, (2) declaration order swap (scissor before viewport), (3) considered struct-wrap to force separate slots but reverted as the asm copy pattern lhz/sth viewport->scissor field-by-field implies plain field assignments not memcpy/struct copy. lb_8001372C in lb_00F9.c uses interleaved viewport/scissor field assignments to defeat folding but our target shows sequential viewport-then-scissor copy which our source matches structurally.
- **Likely fix:** Permuter to find layout perturbation, OR find the original source idiom that prevents mwcc from coalescing the two equivalent-sized stack structs while still emitting a sequential field-copy. May need a holding variable or struct trick. Permuter offline; queued.

