---
function: fn_801A94BC
tu: src/melee/gm/gm_1A4C.c
headline: regalloc + permuter-territory
tags: [regalloc, permuter-territory]
---
## fn_801A94BC (`src/melee/gm/gm_1A4C.c`) — regalloc + permuter-territory

- **Tags:** `regalloc`, `permuter-territory`
- **Best fuzzy:** 99.3548%
- **Diagnosis:** 1-instruction diff: target uses 'addi r30, r31, 0x0' (i = 0 via copy-prop from var_r31=0) for the FIRST loop; mwcc base emits 'li r30, 0x0'. The SECOND loop already emits 'addi r30, r31, 0' correctly. Asymmetric copy-prop choice between the two structurally-identical loops.
- **Tried:** for (i = var_r31; ...) didn't change emit (mwcc folds var_r31 constant). Reverted.
- **Likely fix:** Permuter dispatch — clean register/copy-prop noise. Cluster build infra was broken at time of report (build.ninja in Windows-path mode); could not actually verify post-edit diff or dispatch permuter.
