---
function: fn_803ACF30
tu: src/sysdolphin/baselib/hsd_3AA7.c
headline: mwcc-aliasing + regalloc
tags: [mwcc-aliasing, regalloc]
---
## fn_803ACF30 (`src/sysdolphin/baselib/hsd_3AA7.c`) — mwcc-aliasing + regalloc

- **Tags:** `mwcc-aliasing`, `regalloc`
- **Best fuzzy:** 93.8889%
- **Diagnosis:** mwcc reassociates loop-cond addition: source has ((x24+x8)+0x2F)/x8 but mwcc emits x24 + (x8+0x2F)/x8 -- it folds the 0x2F into x8 first regardless of explicit parenthesization. Target asm: lwz r4=x8; lwz r0=x24; add r3,r0,r4; addi r0,r3,0x2f; divwu. Base asm: lwz r4=x8; lwz r3=x24; addi r0,r4,0x2f; add r0,r3,r0; divwu. 4 mismatches, 93.89%, score=10 in permuter speak. Permuter ran 201 min and got down to score 10 (4 mismatches) but never 100% -- the redundant-trick mutations (e.g. buf[expr*0]=11) weren't enough.
- **Tried:** paren grouping ((a+b)+c); comma-operator with sector_size/total temps (worse: more stack stores); reverted to canonical.
- **Likely fix:** needs either (a) a struct-field/typedef change that makes mwcc treat x24 differently (e.g. signed vs unsigned, or volatile read), (b) an inline asm or PAD trick around the loop cond, or (c) different surrounding-function pressure in the TU. Worth trying: declare i as s32 instead of u32, or reorder x24 access via temp computed before the loop and re-read inside.
