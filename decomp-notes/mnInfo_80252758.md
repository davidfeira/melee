---
function: mnInfo_80252758
tu: src/melee/mn/mninfo.c
headline: string-pool + tu-wide-data
tags: [string-pool, tu-wide-data, cross-function-rodata, permuter-queued]
---
## mnInfo_80252758 (`src/melee/mn/mninfo.c`) — string-pool + tu-wide-data

- **Tags:** `string-pool`, `tu-wide-data`, `cross-function-rodata`, `permuter-queued`
- **Best fuzzy:** 80.3269%
- **Diagnosis:** mnInfo_803EFC08 is laid out in the .o as a single 0xE0-byte global containing AnimLoopSettings { 0.0f, 199.0f, 0.0f } at offset 0x00, four u32s (0x505,0x506,0x507,0x508) at 0x0C, then 8 string literals at offsets 0x1C..0xB8 (format strings, OSReport msg, __FILE__, __VAR__, 4 model section names). My function references 5 of those strings at named-global+offset. Current source has 'static AnimLoopSettings mnInfo_803EFC08[0x12]' which is 0xD8 bytes (not 0xE0) and no embedded strings — strings are emitted as separate string literals, landing in a side data.0 pool. Diff shows base addressing strings via .data.0@l with offsets 0xd8/0xf0/0x10c/0x12c, target via mnInfo_803EFC08@l with offsets 0x34/0x4C/0x58/0x64/0x7C/0x98/0xB8. Match=78.25% (61/279 mismatches), but every mismatch is downstream of the data symbol mismatch — string references propagate frame layout, register allocation (r5 vs r3 base reg for global addressing), and instruction scheduling differences.
- **Tried:** (1) Wrote function with HSD_ASSERTREPORT macro and standard string literals; reached 78.25%. (2) Did not attempt restructuring mnInfo_803EFC08 because it would also break the existing matched mnInfo_802522B8 and fn_802523D8 calls of mn_8022ED6C(jobj, mnInfo_803EFC08) which depend on the AnimLoopSettings* type.
- **Likely fix:** Need to restructure mnInfo_803EFC08 as a single struct/union or static char[0xE0] containing both the AnimLoopSettings prefix AND the 8 inline string literals, then have ALL string references in the file (including unmatched mnInfo_80251D58 which uses %s.%s.%s/%s:%s:%s and fn_80251FE4) reference the global by offset/byte-cast. Existing matched callers mn_8022ED6C(jobj, mnInfo_803EFC08) become a cast. This is a TU-wide refactor that requires all unmatched siblings (mnInfo_80251AFC, mnInfo_80251D58, fn_80251FE4, fn_80252548) to be at least partially decompiled in the same pass, since their string references determine the precise byte content at offsets 0x0C-0x1B (the four u32s 0x505-0x508 are likely sis indices used by one of those functions).

