---
function: gm_801B0DD0
tu: src/melee/gm/gm_1B03.c
headline: permuter-queued + regalloc
tags: [permuter-queued, regalloc, frame-size]
---
## gm_801B0DD0 (`src/melee/gm/gm_1B03.c`) — permuter-queued + regalloc

- **Tags:** `permuter-queued`, `regalloc`, `frame-size`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 83-instr scene init for soundtest results screen. Source structure semantically correct: SoundTestResults struct (8B prefix bitfield x0_0/x0_1 + u8 x1..x4 + 3B pad + MatchEnd at 0x8, total 0x2284) matches gm_80485454 buffer size. Calls gm_80166A98 with 10 args (4 char_kinds, 4 stocks-1, color, last stocks), then accumulates u64 of lbAudioAx_80026E84 results across 4 player_standings, calls 0x14/0x8002702C/gm_80168FC4/gm_801701A0. Manual passes: 77.5% -> 87.9% by switching x1..x4 from s8 to u8 (target stb without extsb) and hoisting accum=0 decl above other locals (forces r30/r31 zero-init in prologue). Remaining 36 mismatches at 87.9%: stack frame is 0x30 in target vs 0x28 in base (8-byte padding diff, no extra spills), and un_803FA258 base ptr lives in r7 in target vs r6 in base — register-allocation/scheduling driven by interleaving of un_803FA258 deref with &data->match_end computation. Pure regalloc/scheduling at this point; permuter territory but decomp-permuter offline so queued.

