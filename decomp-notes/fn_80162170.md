---
function: fn_80162170
tu: src/melee/gm/gm_1601.c
headline: permuter-queued + regalloc
tags: [permuter-queued, regalloc, instruction-scheduling]
---
## fn_80162170 (`src/melee/gm/gm_1601.c`) — permuter-queued + regalloc

- **Tags:** `permuter-queued`, `regalloc`, `instruction-scheduling`
- **Best fuzzy:** (unknown)
- **Diagnosis:** At 90% match (55 diffs), source is structurally correct: 4-i loop with 4-j unrolled inner loop updating nd->vs_kos[pdata_j->x4] saturated to 0xFFFF, plus per-fighter play_time aggregation. Remaining diffs are pure register allocation: target uses r24-r31 sequentially, base shifts by one (r25-r31+r24) because the divide-by-60 magic constant lis 0x8889 is hoisted later in base than in target. Also one extra 'mr r3, r0' because base loads x4 via r0 then moves to r3, while target loads directly into r3.
- **Tried:** (1) Direct translation mirroring fn_80162068 sibling style. (2) Cached pdata_i->x4 into a local u8 nt to encourage single-load and direct-into-r3 codegen — no change in match%.
- **Likely fix:** permuter regalloc/scheduling pass should resolve. Specifically need the prologue to set up the 0x88888889 magic constant earlier so it lands in r31 rather than r24, shifting all other register allocations down.

