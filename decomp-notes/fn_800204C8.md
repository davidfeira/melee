---
function: fn_800204C8
tu: src/melee/lb/lbbgflash.c
headline: mwcc-branch-inversion + permuter-blocked
tags: [mwcc-branch-inversion, permuter-blocked]
---
## fn_800204C8 (`src/melee/lb/lbbgflash.c`) — mwcc-branch-inversion + permuter-blocked

- **Tags:** `mwcc-branch-inversion`, `permuter-blocked`
- **Best fuzzy:** 96.9595%
- **Diagnosis:** 96.89% (5 mismatches), pure branch-inversion. mwcc inverts 'if (mode >= 0) goto case_0_1_2; return;' into 'if (mode < 0) return; fall-through case_0_1_2;' (saves 1 instr). Same inversion on 'if ((s32) data->x30 == 0) { loop }; return;' becomes 'if (!=0) return; loop;'. Target asm keeps un-inverted form: 'bge case; b end' and 'beq loop; b end'. Both are semantically equivalent; mwcc just chose to invert and fall-through. Strict 96.89% / fuzzy 96.96%, 5 instr off (above auto-permute threshold of 15? No actually 5 is below). DELETE-style mismatches (target has 2 more instructions) makes this 'real' diffs not regalloc — permuter cannot add the redundant 'b' instructions via source mutation since mwcc will always optimize them out. Tried: (1) inline case_0_1_2 and case_3_4 bodies into the if-blocks (no gotos) — regressed massively to 32%. (2) flatten 'if (mode >= 0) goto case_0_1_2; return; case_0_1_2:' into 'if (mode < 0) return; <body>;' — identical asm (96.89%). mwcc collapses both forms identically.
- **Tried:** (1) inline case bodies into if-blocks (no goto labels): 32.16%/52 mm — much worse. (2) replace 'if (mode>=0) goto X' + 'return; X: body;' with 'if (mode<0) return; body;': 96.89%/5 mm — identical to baseline.
- **Likely fix:** Permuter unlikely to help — mwcc consistently picks the inverted/fall-through form for these if-then-empty-else patterns. The 2 DELETE mismatches (target has extra 'b' + extra branch instr) cannot be reintroduced by source-shape changes since mwcc would optimize them. Likely needs an upstream change: caller TU layout that forces the function to be longer/aligned so mwcc emits the un-inverted form, OR an unrelated edit elsewhere in the function (like adding a side-effect call) that defeats the inversion peephole. Could potentially try cluster permuter with PERM_RANDOMIZE_TYPE / PERM_INT_TYPE, but high false-positive risk: any 'fix' it finds would likely be a register-renaming coincidence, not a real shape match. Defer until more lbbgflash.c neighbors are decompiled and TU layout context is clearer.
