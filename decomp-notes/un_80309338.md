---
function: un_80309338
tu: src/melee/ty/toy.c
headline: bss-anchor + fuzzy-vs-strict
tags: [bss-anchor, fuzzy-vs-strict, instruction-scheduling]
---
## un_80309338 (`src/melee/ty/toy.c`) — bss-anchor + fuzzy-vs-strict

- **Tags:** `bss-anchor`, `fuzzy-vs-strict`, `instruction-scheduling`
- **Best fuzzy:** 99.9216%
- **Diagnosis:** 5 of 9 objdiff mismatches are bss-anchor false-positives (un_803B88D4 symbol identical text both sides). Remaining 4 are real: lfs load order swapped — target loads y(0x4) before x(0x0) but base loads x before y. Source assigns dy=y-y first then dx=x-x which should produce y-first loads, but MWCC schedules x-loads first. Fuzzy=99.92%, strict=99.43%.
- **Tried:** 1) Changed expression tree to dz*dz+(dx*dx+dy*dy) matching m2c output — increased mismatches to 11. 2) Swapped dx/dy declaration order (dx first) — no change. Both attempts consumed, permuter disabled per task instructions.
- **Likely fix:** This is permuter territory: load order between dy/dx is an instruction scheduling decision. The expression dy*dy+dx*dx+dz*dz with dy assigned first should be correct semantically; need permuter to find the variant that produces y-first load scheduling.

