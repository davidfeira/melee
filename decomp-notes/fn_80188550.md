---
function: fn_80188550
tu: src/melee/gm/gm_1832.c
headline: bss-anchor + regalloc
tags: [bss-anchor, regalloc, instruction-scheduling]
---
## fn_80188550 (`src/melee/gm/gm_1832.c`) — bss-anchor + regalloc

- **Tags:** `bss-anchor`, `regalloc`, `instruction-scheduling`
- **Best fuzzy:** 95.4426%
- **Diagnosis:** 95.27% / 18 mismatches: 2 are BSS-anchor false positives (lbl_80473700@ha/l vs bss.0+0x1a4, post-link identical), 1 is instruction scheduling (remaining computed before ptr in base, after in target), 15 are register allocation (count in r30 target vs r28 base; to_remove in r28 target vs r25 base; j in r31 target vs r26 base)
- **Tried:** Renamed current→count and reordered i declaration inside the true branch — made it worse (90.5%), reverted. Original 95.27% restored.
- **Likely fix:** Permuter needed for regalloc/scheduling. BSS-anchor mismatches are false positives and will not affect post-link match. Structural change to compress variable lifetimes may shift count from r30→r28 but unclear how without permuter.

