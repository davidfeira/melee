---
function: fn_80236998
tu: src/melee/mn/mnstagesw.c
headline: struct-typing + frame-size
tags: [struct-typing, frame-size, regalloc]
---
## fn_80236998 (`src/melee/mn/mnstagesw.c`) — struct-typing + frame-size

- **Tags:** `struct-typing`, `frame-size`, `regalloc`
- **Best fuzzy:** (unknown)
- **Diagnosis:** MWCC -O4 generates address CSE (addi r24, r31, 0x1f) for repeated (MnStageSw_Data*)inner->state casts, causes 9 saved regs (r23-r31) instead of 8 (r24-r31) and f30/f31 FPR saves, yielding 0x88 frame instead of 0x40. Root cause: inner is typed HSD_GObj* but original source likely uses a typed pointer to an extended struct with state byte at offset 0x1F, eliminating the pointer-cast CSE chain.
- **Tried:** (1) Raw u8* index access at 0x1F; (2) MnStageSw_Data typedef struct cast pattern with inner as HSD_GObj*; both produced wrong frame size and extra FPR saves at 0% match.
- **Likely fix:** Extend HSD_GObj definition (in gobj.h or a static header) with a union or struct variant that includes a state byte at offset 0x1F, OR define inner as a typed pointer to a struct embedding HSD_GObj fields at offset 0, so inner->state can be accessed without repeated cast; also investigate why f30/f31 are saved - likely a float value held across call.

