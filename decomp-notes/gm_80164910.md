---
function: gm_80164910
tu: src/melee/gm/gm_1601.c
headline: regalloc + permuter-territory
tags: [regalloc, permuter-territory]
---
## gm_80164910 (`src/melee/gm/gm_1601.c`) — regalloc + permuter-territory

- **Tags:** `regalloc`, `permuter-territory`
- **Best fuzzy:** 98.8095%
- **Diagnosis:** 98.81%, 11 mismatches: pure cyclic register allocation shift. Base generates lbz r4,0x2ac(r4) (temp addr reused as lbz dest), target has lbz r5,0x2ac(r4) (separate dest reg). This cascades to r4/r5/r3/r6 vs r5/r6/r4/r3 for internal_id/struct_base/loop_ptr/i. Tried: reordering variable declarations (u16*/u8/s32/u8/u8 -> u16*/u8/u8/s32/u8 and reverse); explicit ptr walk loop. Neither changed register allocation.
- **Tried:** (1) Reordered local variable declarations (u8 unlock_idx and notify_val before s32 i and u8 internal_id); (2) Explicit const struct ptr variable with i++,ptr++ for loop — degraded to 91.49%/13 mismatches. Source shape mirrors gm_80164504. The only difference is the lbz destination register (r4 self-reload vs r5 fresh reg), suggesting the compiler needs r4 to remain live past the load, which requires a use of r4 after the add and before the lbz that is hard to engineer manually.
- **Likely fix:** Permuter random scheduling/regalloc shuffles. The 11 mismatches are all ARG_MISMATCH (no structural diff), making this a clean permuter target. Dispatch with --cluster.

