---
function: ftCo_8009CB40
tu: src/melee/ft/ftdynamics.c
headline: regalloc + string-pool
tags: [regalloc, string-pool, mwcc-loop-opt]
---
## ftCo_8009CB40 (`src/melee/ft/ftdynamics.c`) — regalloc + string-pool

- **Tags:** `regalloc`, `string-pool`, `mwcc-loop-opt`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 91.6% match. Three structural blockers: (1) register allocation shifted by one (stmw r20 vs r21 frame) despite adding temp_jobj local; (2) MWCC generates 'add r0, bone_id, var_r29' instead of strength-reducing to a stride variable r31 for fp->parts array index; (3) HSD_JObjSetTranslate assert 'translate' string resolves to @181 (sdata2) in base instead of ftDynamics_803C5720 (non-sdata2 @ha/@l) as in target.
- **Tried:** Attempt 1: natural C with u8 var_r22/temp_r21 and fp->parts[bone_id+var_r29].flags_b7 access. Attempt 2: added explicit temp_jobj local for jobj caching, changed ternary to explicit if/else for branch direction. Neither fixes the stride variable strength reduction or string placement.
- **Likely fix:** Need to find a C formulation that causes MWCC to (a) hoist the stride variable r31 = bone_id*16 + j*16 out of the array index computation, and (b) causes 'translate' string to land outside sdata2 range. Possibly needs explicit byte-offset variable and/or different function ordering. The string issue may require the function to appear after ftCo_8009D074 in the TU, or an explicit string reference before the function.

