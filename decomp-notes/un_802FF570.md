---
function: un_802FF570
tu: src/melee/if/ifcoget.c
headline: regalloc + struct-split
tags: [regalloc, struct-split, mwcc-aliasing]
---
## un_802FF570 (`src/melee/if/ifcoget.c`) — regalloc + struct-split

- **Tags:** `regalloc`, `struct-split`, `mwcc-aliasing`
- **Best fuzzy:** 70.4091%
- **Diagnosis:** Truth uses pointer-pattern (addi r4,r3,8 ; lwz r4,4(r4)) for un_804A1F58[i].x8.x4 access; ours uses direct-offset (lwz r4,0xc(r3)). Adding pointer alias variable HSD_Text* t cached the load but didn't change codegen pattern. Using struct un_804A1F58_x8_t* p =&[i].x8 then p->x4 was much worse (55%, allocates extra reg r5 as base). The truth is unrolled to 6 iters with peculiar offset accumulation: iter 0 uses 0x18/0x4, iters 2-5 reach offsets 0x2c/0x20 before bumping r3 by 0x14. Possibly truth source uses different control structure (e.g. duff-style unrolled), or the struct layout is different (TODO comment notes sizeof should be 0x80 not 0x8C, suggesting 6-element array or different elem size).
- **Tried:** cached t = un_804A1F58[i].x8.x4 (no change, 70.0% / 20 mismatches); used un_804A1F58_x8_t* p = &[i].x8 with p->x4 (worse: 55% / 47 mismatches, mwcc allocates extra r5).
- **Likely fix:** Investigate true struct layout: comment says size should be 0x80, current is 7*0x14=0x8C. Maybe x4 is split or there's a hidden member. Or try fully manual 6-iteration unrolled C source. Could also be a manual sister loop in same TU calling un_804A1F58 elements directly with offsets.
