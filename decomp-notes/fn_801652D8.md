---
function: fn_801652D8
tu: src/melee/gm/gm_1601.c
headline: regalloc + reloc-symbol-false-positive
tags: [regalloc, reloc-symbol-false-positive]
---
## fn_801652D8 (`src/melee/gm/gm_1601.c`) — regalloc + reloc-symbol-false-positive

- **Tags:** `regalloc`, `reloc-symbol-false-positive`
- **Best fuzzy:** 95.4545%
- **Diagnosis:** 95.45% fuzzy / 2 strict mismatches; pure mwcc instruction-scheduling diff. After the second sprintf prep, target schedules 'addi r5, r3, 0x0' (move return value from gm_801A4BB8 into the 4th sprintf arg slot) BEFORE 'lis r4, lbl_803D559C@ha' (load \ce0e0ff%5d format string), but base puts 'lis r4, @1137@ha' BEFORE 'addi r5, r3, 0x0'. Source structure is semantically correct; both sprintf calls and all data flow match. The relocation symbols also differ in name (target 'lbl_803D5590'/'lbl_803D559C' globals vs base auto-pool '@1136'/'@1137'), but that mismatch is fuzzy-tolerated and only the second one's position swap shows as strict diff. Both strings exist at the correct .data addresses regardless.
- **Tried:** (1) Inlining gm_801A4BB8() call into sprintf arg dropped match to ~89% by adding an extra register save. (2) Reverted to baseline. Permuter cannot be invoked: import.py fails on 'unsupported relocation against cr1eq' from binutils on the 'crclr cr1eq' opcodes preceding both sprintf calls (variadic call FPR-args bitmask); affects every PowerPC variadic call function.
- **Likely fix:** Either (a) patch decomp-permuter's binutils path / asm sanitizer to translate 'crclr cr1eq' -> 'crclr 6' before assembly so import.py succeeds, then run permuter on this 2-insn scheduling near-miss; or (b) source-level: try a tmpfile-style hold of the gm_801A4BB8 return so mwcc commits the r3->r5 move before the next string-load (e.g., introducing a volatile-temp sequence point). Same crclr-cr1eq import blocker likely affects all variadic-call near-misses across the codebase.
