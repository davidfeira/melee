---
function: grBigBlueRoute_8020B9D4
tu: src/melee/gr/grbigblueroute.c
headline: tu-data-osreport + cross-tu-globals
tags: [tu-data-osreport, cross-tu-globals, regalloc]
---
## grBigBlueRoute_8020B9D4 (`src/melee/gr/grbigblueroute.c`) — tu-data-osreport + cross-tu-globals

- **Tags:** `tu-data-osreport`, `cross-tu-globals`, `regalloc`
- **Best fuzzy:** 89.4828% (28 mismatches on 58-instr StageCallbacks dispatcher)
- **Diagnosis:** Same family as grOldKongo_8020F52C / grRCruise_801FF2C8 / grKongo_801D5340 (already matched, see commit 35ca3ce00). The whole TU's `.data` segment (0x803E5E78..0x803E6278, splits.txt confirms) is currently fed from `build-linux/GALE01/asm/melee/gr/grbigblueroute.s`, with the .c file declaring just `extern StageCallbacks grBb_Route_803E5E78[];`. Target asm: r31 = grBb_Route_803E5E78 (the StageCallbacks array, .data:0x0, size 0x2F8 = 38 entries of 0x14 each), r29 = r31 + arg0*0x14 indexes into the array, OSReport string args resolve as `addi r3, r31, 0x338` (format string `%s:%d: couldn t get gobj(id=%d)\n` embedded INSIDE grBb_Route_803E617C, a 0x58-byte cross-TU struct at .data:0x304 containing `{0x22, &grBb_Route_803E5E78, &grBb_Route_803E6170 "/GrNBr.dat", &grBigBlueRoute_8020B89C, &grBigBlueRoute_8020B864, &grBigBlueRoute_8020B920, &grBigBlueRoute_8020B95C, &grBigBlueRoute_8020B9CC, &grBigBlueRoute_8020DF78, &grBigBlueRoute_8020DF80, 0x1, 0x0, 0x0, char[0x24] = "%s:%d: couldn t get gobj(id=%d)\n"}`, format string at struct-offset 0x34) and `addi r4, r31, 0x35c` (`grbigblueroute.c` at grBb_Route_803E61D4). Base uses `__FILE__` literal + raw format string → mwcc emits separate `lis/addi` for `@184/@185`, can't fold via the array-base SDA pool optimization, which cascades into r28→r29, r29→r30, r30→r31 register pressure shifts (most of the 28 mismatches are the same regalloc swap repeated). grBb_Route_803E617C is referenced by ground.c:113,136 — moving it out of the .s file changes the cross-TU symbol provider; verify the linker still picks the .c definition. grBb_Route_803E6200 (.data:0x388, 0x3C bytes) is already extern-declared and used by grBigBlueRoute_8020C140; grBb_Route_803E623C (.data:0x3C4, 0x3C bytes "gp->u.car.car_info\\0\\0!(jobj->flags & JOBJ_USE_QUATERNION)") is the HSD_ASSERT message source for grBigBlueRoute_8020BC68. Sdata symbols at 0x804D4850 ("jobj.h") and 0x804D4858 ("translate") and 0x804D4868 ("reb0_jobj") also part of the TU — these are referenced via @sda21 from currently-matched assert sites and should stay if/when data is moved to source.
- **Tried:** Diff inspection + grbigblueroute.s data-section trace (lines 2904-3176). m2c output makes the pattern explicit: `OSReport(grBb_Route_803E5E78 + 0x338, grBb_Route_803E5E78 + 0x35C, 0x117, arg0, ...)` — the offsets reveal the inline-format-string-in-wrapper-struct pattern. Did not attempt source rewrite — same TU-data refactor scope as grOldKongo_8020F52C.
- **Likely fix:** Move the entire TU `.data` segment into source: define `grBb_Route_803E5E78[38]` (StageCallbacks array, with the 4 non-NULL entries at indices 0, 5, 20, ~28 corresponding to grBigBlueRoute_8020BABC/BB68/C140/BC68 callback families plus a few flag entries — read full table from grbigblueroute.s lines 2910-3099), `grBb_Route_803E6170 = "/GrNBr.dat"`, `grBb_Route_803E617C` (0x58-byte wrapper struct with the inline format string at field offset 0x34, see grKongo `grKg_803E1800` template at commit 35ca3ce00), `grBb_Route_803E61D4 = "grbigblueroute.c"`, `grBb_Route_803E6200` (0x3C-byte u16 array of fzerocar params), `grBb_Route_803E623C` (assert message blob). Then OSReport call becomes `OSReport(grBb_Route_803E617C.fmt, grBb_Route_803E61D4, 0x117, gobj_id)` — mwcc folds these into r31-relative addi via the data-pool optimization triggered by the array's lis/addi. Also unblocks the rest of the TU (currently 55.46% fuzzy at TU level — many other functions in this TU likely depend on the same data definitions). Beyond single-function subagent scope; needs TU-wide data refactor identical in shape to the grKongo commit 35ca3ce00 just larger (38 entries vs 12). Sibling grBigBlueRoute_8020DD64 (98.6%, in-progress) is unrelated — pure float-literal externs.
