---
function: fn_80216DE4
tu: src/melee/gr/grgreens.c
headline: regalloc + permuter-false-positive
tags: [regalloc, permuter-false-positive]
---
## fn_80216DE4 (`src/melee/gr/grgreens.c`) — regalloc + permuter-false-positive

- **Tags:** `regalloc`, `permuter-false-positive`
- **Best fuzzy:** 81.6875%
- **Diagnosis:** Target hoists 'mulli r7, r9, 0xc0' (i*192=i*6*32 byte stride) outside inner j loop and tracks both j (in r10) and j*0x20 byte-offset (in r5) separately, incrementing r5 by 0x20 per iter. Base shape with getBlock(gp,i,j) (inline returning &gp->gv.greens.x8_blocks[i*6+j]) produces 'mulli r7, r9, 0x6' followed by 'slwi r5, r7, 5' inside the loop — base computes (i*6+j)*32 fused via shift, target distributes the multiply to i*192+j*32. 11 mismatches, 81.69%. Permuter cluster plateaued at 560 (only added dead noise like new_var = gp; i=0; j+=0; i++; i--), no real progress.
- **Tried:** V1: cache &gp->gv.greens.x8_blocks[i*6+j] into local block ptr — identical 11 mismatches (compiler still emits same mulli 6 + slwi 5 pattern). V2: cast x8_blocks to (BlockVars(*)[6]) outside loops and use blocks[i][j] — got 'mulli r5, r6, 0xc0' (correct stride!) but the lwz r8,0xcc(r3) base load got hoisted outside the i loop instead of staying inside; degraded to 27 mismatches / 63.6%. The 2D-cast forces i*0xc0 but also lifts the base pointer load.
- **Likely fix:** Need a shape that (a) forces stride-by-0x20 in inner loop AND (b) keeps the lwz of x8_blocks inside the i loop body. Possibly write the byte-offset arithmetic explicitly while reloading gp->gv.greens.x8_blocks per i iteration, e.g. an outer-loop reload with a separate byte-stride increment in inner. Or this may simply need permuter with a custom PERM macro; the cluster's randomization didn't find this shape. Could also be a TU-wide compile flag/optimization issue worth comparing to neighbor functions in grgreens.c that do similar nested block iteration (grGreens_80215358 at line 510 has same i*6+j pattern).
