---
function: hsd_803AC558
tu: src/sysdolphin/baselib/hsd_3AA7.c
headline: permuter-blocked + bitfield-reuse
tags: [permuter-blocked, bitfield-reuse, instruction-scheduling]
---
## hsd_803AC558 (`src/sysdolphin/baselib/hsd_3AA7.c`) — permuter-blocked + bitfield-reuse

- **Tags:** `permuter-blocked`, `bitfield-reuse`, `instruction-scheduling`
- **Best fuzzy:** 76.6%
- **Diagnosis:** Reached 76.6% (46 mismatches, 55 instr) with loop-unrolled 3-iter form. Each iter reads 4 bytes: byte0=file_idx, byte1=flags|size_hi (top 2 bits flags, low 6 bits size_hi), byte2=size_mid, byte3=size_lo. mwcc base emits a fused 'rlwimi. r6, r0, 8, 10, 23' (14-bit insert covering bytes 1-2 with CR0 set) plus 'srawi r0, r5, 6; clrlwi r8, r0, 24' for the top-2-bit flags extract. Target uses 'rlwimi r4, r5, 8, 18, 23' + 'slwi r4, r4, 8' + 'or.' (3 ops) and 'extrwi r8, r5, 2, 24' (1 op for flags). The fused rlwimi is impossible to coax with simple expressions; would need different bit layout / type. Tried unrolled 3x (worse, 64.4%). Source shape correct semantically; permuter territory for instruction fusion / register allocation. Note: decomp-permuter offline.

