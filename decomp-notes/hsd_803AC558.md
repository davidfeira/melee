---
function: hsd_803AC558
tu: src/sysdolphin/baselib/hsd_3AA7.c
headline: permuter-blocked + mwcc-loop-opt
tags: [permuter-blocked, mwcc-loop-opt, instruction-scheduling, regalloc]
---
## hsd_803AC558 (`src/sysdolphin/baselib/hsd_3AA7.c`) — permuter-blocked + mwcc-loop-opt

- **Tags:** `permuter-blocked`, `mwcc-loop-opt`, `instruction-scheduling`, `regalloc`
- **Best fuzzy:** 76.6%
- **Diagnosis:** Target is fully unrolled (3 iters, no loop). Each iter uses rlwimi rA, rS, 8, 18, 23 (6-bit insert SH=8) + slwi + or. to build 24-bit size from 3 bytes; extrwi extracts 2-bit flags. Loop form produces rlwimi. (CR-updating, 14-bit field MB=10) instead; unrolled form produces clrlslwi+or or rlwimi with SH=16 depending on expression. Could not coax SH=8 non-CR rlwimi + separate or. in 2 attempts. Requires unroll + specific 2-step accumulation: size=data[2]; size|=(byte1&0x3F)<<8; size=(size<<8)|data[3]. Pointer arithmetic: iter1 uses addi r9,r4,1 then accesses r9+2; iters 2-3 use r9 advancing by 4.
- **Tried:** (1) Fully unrolled with size=data[2];size|=(byte1&0x3F)<<8 two-step: generated clrlslwi+or not rlwimi (55 mismatches). (2) Direct ((byte1&0x3F)<<16)|((data[2])<<8)|data[3]: generated rlwimi SH=16 not SH=8 (52 mismatches). Baseline loop form remains best at 76.6% (46 mismatches).
- **Likely fix:** Need to find C expression that MWCC compiles to rlwimi rA,rS,8,18,23: try inline asm or a loop pragma to force unroll. Alternatively: use u16 intermediate for the 14-bit merge to steer the rlwimi field width. Also note iter1 pointer pattern (addi r9,r4,1 then r9+2 for byte3) is unusual - may need p=&data[1] for the first load of byte3.

