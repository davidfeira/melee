---
function: mnInfo_802522B8
tu: src/melee/mn/mninfo.c
headline: stack-offset + r30-r31-swap
tags: [stack-offset, r30-r31-swap]
---
## mnInfo_802522B8 (`src/melee/mn/mninfo.c`) — stack-offset + r30-r31-swap

- **Tags:** `stack-offset`, `r30-r31-swap`
- **Best fuzzy:** 98.9688%
- **Diagnosis:** child pointer spills to sp+0x18 instead of sp+0x14 (4 bytes off); with PAD_STACK(8) child lands at 0x14 but frame shrinks to 0x28 instead of 0x30; also addi r30,r31,0x0 vs li r30,0x0 for loop counter init
- **Tried:** 1) Changed PAD_STACK(12) to PAD_STACK(8): frame shrinks to 0x28, child at 0x14 but frame wrong. 2) Changed for-loop to do-while with i=count init: no change to either mismatch. 3) register keyword on data/jobj: no effect on stack layout. All remaining 7 mismatches are structural (stack offset) or init-order (addi vs li).
- **Likely fix:** Need to find variable declaration arrangement where PAD(12) gives frame=0x30 AND child at 0x14. The PAD array must start at sp+0x08 (12 bytes before child at 0x14) but our layout puts it 4 bytes higher. Try removing one of the 4-byte locals (count, i, data, jobj) if it can be inlined, or try a nested block scope for child to force different layout.

