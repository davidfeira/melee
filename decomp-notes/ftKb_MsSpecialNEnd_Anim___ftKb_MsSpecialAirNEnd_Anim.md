---
function: ftKb_MsSpecialNEnd_Anim & ftKb_MsSpecialAirNEnd_Anim
tu: src/melee/ft/chara/ftKirby/ftKb_SpecialNYs.c
headline: cross-tu-globals + paired-siblings
tags: [cross-tu-globals, reloc-symbol-false-positive, paired-siblings, regalloc]
---
## ftKb_MsSpecialNEnd_Anim & ftKb_MsSpecialAirNEnd_Anim (`src/melee/ft/chara/ftKirby/ftKb_SpecialNYs.c`) — cross-tu-globals + paired-siblings

- **Tags:** `cross-tu-globals`, `reloc-symbol-false-positive`, `paired-siblings`, `regalloc`
- **Best fuzzy:** 98.23% (N) / 98.87% (Air), 22/18 mismatches on 62-instr functions
- **Diagnosis:** Two paired siblings, identical body shape, single-call divergence (`ft_8008A2BC` vs `ftCo_Fall_Enter`). All 22 (N) / 18 (Air) mismatches root in one issue: target uses `lfd f31, ftKb_Init_804D9578@sda21` (named cross-TU sdata2 global = `S32_TO_F32` magic = `0x4330000080000000`); base emits anonymous `@623@sda21` local literal with the same value. The named symbol `ftKb_Init_804D9578` is registered in symbols.txt (sdata2:0x804D9578, double, scope global) and per splits.txt this address falls *inside* `ftKb_SpecialNYs.c`'s own sdata2 range (0x804D9554-0x804D95B0). All other mismatches are downstream regalloc cascade: r27↔r31 (fp), r25↔r31 (lis 0x8889 magic), r29↔r30 (ms_da pointer), driven by different literal-pool register pressure when mwcc anchors via @623 vs the named symbol.
- **Tried:** (1) `extern double const ftKb_Init_804D9578;` declaration — mwcc dropped the extern (it's referenced only via internal s32→double conversion magic, not source-named) and kept emitting `@623`. Symbol absent from obj. (2) `double const ftKb_Init_804D9578 = S32_TO_F32;` definition — symbol is now defined in obj at sdata2 offset 0x8, but mwcc *also* emits a separate @623 at sdata2 offset 0x10 with the same value, AND uses @623 (not the named symbol) for the s32→double conversion. Both literals coexist; the function still references @623. Dashboard fuzzy unchanged (still 98.23%/22 mismatches). (3) Source-shape rewrite of the loop: replaced `HitCapsule* hit = fp->x914; ... hit++;` with `u8* walker = (u8*) fp; ... (HitCapsule*)(walker + 0x914); walker += sizeof(HitCapsule);` — successfully forced mwcc to emit `r28 = fp` and `addi r3, r28, 0x914` at the use site (matching target's `addi r28, r27, 0x0` / `lwz 0x914(r28)` / `addi r3, r28, 0x914` pattern). Combined with `(s32)` casts on `ms_da->base_damage` and `additional_damage_per_iteration` to force the s32→double (xoris+magic) conversion path instead of u32→double. Together these dropped mismatches 25→22 and fuzzy 96.32%→98.23%.
- **Permuter contraindicated:** Per Permuter Boundary, false diffs from equivalent BSS/global base symbols should not be permuted. The 22 remaining mismatches are all reloc-name + downstream regalloc cascade — permuter would shuffle registers without bridging the literal-pool anchor.
- **Likely fix:** TU-data refactor for `ftKb_SpecialNYs.c`'s sdata2 (range 0x804D9554-0x804D95B0). Need to define the magic constant at the *exact* address 0x804D9578 (sdata2 offset 0x24 from TU base 0x804D9554) by declaring the right sequence of named sdata2 globals in source so mwcc lays them out byte-aligned with the target. This may require declaring the surrounding floats/doubles at offsets 0x00-0x20 first, then `double const ftKb_Init_804D9578 = S32_TO_F32;` at the right slot. Prior matched siblings in this TU (e.g. ftKb_SpecialNYs_8010929C, ftKb_SpecialN_800Exxx family) emit only @NNN literals so the address-stable layout has never been required. Same family as `fn_80122B54`, `grOnett_801E40E4`, `it_802B56E4`. Beyond single-function subagent scope.
- **Sibling family:** Both `ftKb_MsSpecialNEnd_Anim` (98.23%) and `ftKb_MsSpecialAirNEnd_Anim` (98.87%) share the exact same blocker; one TU-data fix should resolve both.
