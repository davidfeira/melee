---
function: fn_801656A8
tu: src/melee/gm/gm_1601.c
headline: header-prototype + permuter-blocked
tags: [header-prototype, permuter-blocked, struct-typing]
---
## fn_801656A8 (`src/melee/gm/gm_1601.c`) — header-prototype + permuter-blocked

- **Tags:** `header-prototype`, `permuter-blocked`, `struct-typing`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 96.57% / 6 mismatches, all extsb-related. Loop-form source over MatchPlayerData[0..6] with two passes (x20 unsigned >, x24 unsigned <) compiles to correct structure. Remaining diffs: mwcc inserts extsb r0,r4 before mulli (player_standings[arg1] index) and extsb r3,r4 before subfic (6-arg1). Target asm uses r4 directly without extsb both times. Strongly suggests true source signature is int arg1 (not s8 arg1 from header). Required u32 casts on x20/x24 fields (struct types them as s32/int, but compares are cmplw) — kept those casts.
- **Tried:** (1) loop-form with s8 arg1, signed compares: 90.6% / 18 mismatches (cmpw vs cmplw); (2) loop-form with s8 arg1 + (u32) casts on x20/x24: 96.57% / 6 mismatches — best. (Tried s32 idx=arg1 cache: regressed regalloc throughout.)
- **Likely fix:** Change header to int arg1 (header-edit, requires mama Claude follow-up). Function-local fix not possible within s8 signature — mwcc always extsb-extends s8 arg before integer arithmetic uses.

