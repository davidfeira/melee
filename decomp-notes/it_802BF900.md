---
function: it_802BF900
tu: src/melee/it/items/itnessyoyo.c
headline: stack-offset + frame-size
tags: [stack-offset, frame-size]
---
## it_802BF900 (`src/melee/it/items/itnessyoyo.c`) — stack-offset + frame-size

- **Tags:** `stack-offset`, `frame-size`
- **Best fuzzy:** 93.874%
- **Diagnosis:** All 24 remaining mismatches are stack-offset shifts. Target frame is 0xa0 (160 bytes); current source compiles to 0x90 (144 bytes). Locals occupy 108 bytes in both. Target layout: scale(0x18-0x47), offset(0x48-0x53), trans(0x54-0x83). Base layout: offset(0xc-0x17), scale(0x18-0x47), trans(0x48-0x77). Both have identical relative spacing — base is just shifted 0xc lower (smaller pre-locals padding) and missing 0x10 of overall frame size. Need source change that forces mwcc to (a) reorder offset between scale and trans, AND (b) consume an extra 16 bytes of stack.
- **Tried:** (1) reorder declarations to 'Vec3 offset; Mtx scale; Mtx trans' — dropped to 92.3% (worse, structural shuffle); (2) reorder declarations to 'Mtx trans; Vec3 offset; Mtx scale' with split init — 99.65%, gets relative ordering right (scale<offset<trans low-to-high) but frame still 0x90, locals just shifted by 0xc. Original 'Mtx trans; Mtx scale; ...; Vec3 offset' yields 99.811%.
- **Likely fix:** Need to either (a) introduce an extra hidden 16-byte auto / Vec3 padding that mwcc keeps, or (b) discover the original source uses a struct-by-value local or temporary that was lost when decompiled. Compare sibling it_802C33B8 (climbersstring) — it matches at 100% with simpler shape (one Mtx, no scale matrix). Look for a wrapper like Vec3 t = offset_global; offset = scale_vec(t, scl); or a local Vec3 scaled_offset that mwcc preserves.
