---
function: fn_800D8EC8
tu: src/melee/ft/chara/ftCommon/ftCo_Attack100.c
headline: permuter-queued + frame-size
tags: [permuter-queued, frame-size, sdata2-float, regalloc]
---
## fn_800D8EC8 (`src/melee/ft/chara/ftCommon/ftCo_Attack100.c`) — permuter-queued + frame-size

- **Tags:** `permuter-queued`, `frame-size`, `sdata2-float`, `regalloc`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 98.96% match (40 mismatches). Source structure correct: extracted Link/CLink tether-catch handler matching ftCo_AirCatch_Anim's body but with mv.co.catch.x0, dat_attrs offsets x84/x88/x8C/x90, and bare pos.x assignment (no pos_delta.x add) — this differs from AirCatch. Remaining diff is permuter territory: (1) stack frame 0x60 vs 0x58 — 8 bytes short; need additional local/PAD_STACK to widen; (2) sdata2 float labels named vs anonymous (ftCo_804D9048/9050/9058/9060 vs @789/@792/@793/@796) — sdata2-float false-positive class (post-link bytes match); (3) r5/r4/r6 register-allocation choices; (4) r4 vs r3 for tether_ip path (lwz r4, 0x2c(r3) vs lwz r3, 0x2c(r3)).
- **Tried:** (1) Modeled body on ftCo_AirCatch_Anim's Link/CLink branch substituting catch fields and offsets; (2) Added PAD_STACK(8) and inner block scope around HSD_JObjSetupMatrix path to coax extra stack space — moved frame from 0x50 to 0x58, still 8 bytes short of 0x60.
- **Likely fix:** Source likely correct; remaining is regalloc/scheduling/literal-pool ordering. Stack 8-byte gap needs another local (e.g., a Vec3 declared at outer scope rather than inside the if-block, or PAD_STACK(0x10) at top). Sdata2 float ordering depends on which order constants 1.0/8.0/0.0/4503601774854144.0 are emitted relative to fn_800D9228's identical literal-pool emission. Hand off to permuter (cluster) for register/scheduling sweep — but expect sdata2-float false-positive to require manual closing once frame size + register allocation hit.

