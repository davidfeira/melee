---
function: fn_8024BAF0
tu: src/melee/mn/mnsoundtest.c
headline: stack-offset + frame-size
tags: [stack-offset, frame-size, header-required]
---
## fn_8024BAF0 (`src/melee/mn/mnsoundtest.c`) — stack-offset + frame-size

- **Tags:** `stack-offset`, `frame-size`, `header-required`
- **Best fuzzy:** (unknown)
- **Diagnosis:** At 99.94% (6 mismatches) using AnimLoopSettings cast over &vec_0. All 6 remaining mismatches are uniform 4-byte stack-offset shifts: target body locals at 0x14/0x18/0x1C with frame=0x40, current source at 0x18/0x1C/0x20 with frame=0x44 (one extra 4-byte slot). Source uses 3 unused f32 padding locals (unused_a/b/c) to push body locals to higher offsets — needs exactly 2 unused slots, not 3. One trivial source tweak (delete one unused f32 declaration) should land 100%. Required header change: src/melee/mn/mnsoundtest.h fn_8024BAF0 prototype changed from UNK_RET/UNK_PARAMS to void(HSD_GObj*). Same TU as fn_8024B7E4 tu-data-osreport blocker but this function only depends on vec_0 base anchor (no cross-vec reach), so AnimLoopSettings* cast over &vec_0 works without TU-wide consolidation.
- **Tried:** 3 unused f32 locals (99.94%, 6 stack-offset mismatches); next attempt: try 2 unused f32 locals to drop frame from 0x44 to 0x40
- **Likely fix:** Remove one unused f32 from local decl list (keep 2 instead of 3) to shrink frame by 4 bytes — should match 100%.

