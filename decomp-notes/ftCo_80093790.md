---
function: ftCo_80093790
tu: src/melee/ft/chara/ftCommon/ftCo_Guard.c
headline: frame-size + regalloc
tags: [frame-size, regalloc]
---
## ftCo_80093790 (`src/melee/ft/chara/ftCommon/ftCo_Guard.c`) — frame-size + regalloc

- **Tags:** `frame-size`, `regalloc`
- **Best fuzzy:** 99.3958%
- **Diagnosis:** After fixing ftCo_80092158 (index *4, remove float vararg, drop 5th/6th efSync_Spawn args), function reaches 99.4% with 12 mismatches: frame size is -0x20 (base) vs -0x28 (target, 8 bytes larger) plus 3 temp-register differences in the inlined Fighter_804D650C pointer load (r0 vs r5) and slwi result (r3 vs r0). The r30/r31 swap was resolved by hoisting jobj declaration to function scope.
- **Tried:** (1) Changed ftCo_80092158: u8 temp_ret -> int, index Fighter_804D650C + (temp_ret<<2) instead of [temp_ret], removed 5th/6th args from efSync_Spawn (was passing temp_r7_2 and M2C_BITWISE(float,temp_ret)). Got 98.6%. (2) Hoisted jobj declaration before fp in ftCo_80093790 to fix r30/r31 swap. Got 99.4% with 12 remaining mismatches all frame/regalloc.
- **Likely fix:** Frame size: need -0x28 instead of -0x20; likely needs a 8-byte padding local (u8 _[8]) or a different local variable layout. Temp register choices (r0 vs r5 for Fighter_804D650C pointer, r3 vs r0 for slwi) are permuter-territory register allocation differences.

