---
function: fn_80164B48
tu: src/melee/gm/gm_1601.c
headline: permuter-territory + regalloc
tags: [permuter-territory, regalloc, frame-size, inlining]
---
## fn_80164B48 (`src/melee/gm/gm_1601.c`) — permuter-territory + regalloc

- **Tags:** `permuter-territory`, `regalloc`, `frame-size`, `inlining`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 6-block unrolled cascade testing 6 specific unlock bits via inlined fn_801605EC. Reaches 94.02%/55 mismatches with 'bool ok' local. Target uses scratch r6 for ok flag (per-block lifetime); mwcc instead allocates callee-saved r28 because the single 'ok' declared at function scope lives across all 6 blocks. Target frame is 0x28 (extra unused locals); base is 0x20. Remaining diffs are pure regalloc/scheduling within the 6 identical blocks.
- **Tried:** (1) if(!(idx==0xB||(*p&(1LL<<idx)))) return 0; -> 90.85% (no ok variable). (2) bool ok; ok=false; if(...) ok=true; if(!ok) return 0; per-block -> 93.96%. (3) ok=(idx==0xB)||(*p&(1LL<<idx)); for first block only -> 94.02%. All compile to inlined fn_801605EC body (matches gm_80164840 pattern) and __shl2i call.
- **Likely fix:** Permuter should land r6 vs r28 + scheduling. Source-shape candidates to explore: declare 'bool ok' INSIDE each block scope so lifetime is per-block; or use comma-expression to keep ok unspilled. Alternative: maybe target source is a tight do/while with array of offsets — but 6 separate bl gmMainLib_8015ED8C calls argues against a loop.

