---
function: it_80282DE4
tu: src/melee/it/items/itdosei.c
headline: permuter-false-positive + sdata2-anonymous-floats
tags: [permuter-false-positive, sdata2-anonymous-floats, tu-data-osreport]
---
## it_80282DE4 (`src/melee/it/items/itdosei.c`) — permuter-false-positive + sdata2-anonymous-floats

- **Tags:** `permuter-false-positive`, `sdata2-anonymous-floats`, `tu-data-osreport`
- **Best fuzzy:** 99.949%
- **Diagnosis:** 6 of 7 mismatches are sdata2 float reloc-symbol false positives: target uses named globals (it_804DC870/it_804DC878/it_804DC88C @sda21) while base uses anonymous compiler-generated literals (@209/@210/@778 @sda21). Post-link bytes are identical (same float values at same sdata2 offsets). The 7th mismatch is a single fmuls operand swap (target: fmuls f0,f2,f0 ; base: fmuls f0,f0,f2) for the expression facing_dir * -DDC inside the angle calc; sibling itDosei_UnkMotion9_Anim with the IDENTICAL source pattern matches because xDDC was just stored to memory above so the compiler reloads DDC first; in it_80282DE4 there's no prior store of xDDC, so the compiler picks a different load order which incidentally swaps the fmuls operands.
- **Tried:** 1) introduce f32 neg_ddc local: bumped stack frame by 8, 20 mismatches; 2) reorder factors: -ip->...DDC * facing_dir; no effect (compiler reorders), still 7 mismatches with fmuls swap unchanged. Reverted both.
- **Likely fix:** (a) accept as permuter-false-positive — the sdata2 float deduplication needs a TU-wide structural fix (cross-TU shared float pool / named const promotion). The fmuls swap is likely just incidental from one of those name-vs-anonymous forms triggering different scheduling. Worth re-checking once the TU's float-symbol situation is resolved. (b) DO NOT dispatch permuter — its scorer treats sdata2 reloc-symbol diffs as equivalent so it would report 100% while report.json fuzzy stays at 99.79%.
