---
function: fn_802B7E34
tu: src/melee/it/items/itsamusgrapple.c
headline: regalloc + permuter-plateau
tags: [regalloc, permuter-plateau]
---
## fn_802B7E34 (`src/melee/it/items/itsamusgrapple.c`) — regalloc + permuter-plateau

- **Tags:** `regalloc`, `permuter-plateau`
- **Best fuzzy:** 99.7368%
- **Diagnosis:** 5-instr regalloc-only mismatch in samus_grapple_anim inline link-walk: target uses r29, base uses r28. fp lives in r31 (target) vs r29 (base) — different callee-saved register pick for fp's lifetime spanning multiple calls causes link-walker to land in a different slot. mwcc allocates fp first (line 515 use), gobj copied to r30, ip in r4. Permuter cluster plateaued at score=425 across 24+ outputs; permuter outputs add bogus dummy if/blocks to consume registers but introduce structural noise. Diff is purely DIFF_ARG_MISMATCH on 5 instructions, all involving the same r29/r28 register choice.
- **Tried:** (1) hoisted fp->motion_id read into a local before the inline calls — no change in regalloc. (2) introduced gobj2 alias to extend gobj's live range past samus_grapple_anim — made it worse (+2 mismatches, mr r27,r30 spill).
- **Likely fix:** May need a structural change that biases mwcc to allocate fp into a higher register slot (r31) — e.g. introducing an additional local that takes the r29 slot, or restructuring samus_grapple_anim's inline to make link's live range start earlier. Or accept as cosmetic regalloc residue.
