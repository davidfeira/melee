---
function: ftCo_800D74A4
tu: src/melee/ft/chara/ftCommon/ftCo_Attack100.c
headline: permuter-queued + regalloc
tags: [permuter-queued, regalloc, instruction-scheduling]
---
## ftCo_800D74A4 (`src/melee/ft/chara/ftCommon/ftCo_Attack100.c`) — permuter-queued + regalloc

- **Tags:** `permuter-queued`, `regalloc`, `instruction-scheduling`
- **Best fuzzy:** 86.9322%
- **Diagnosis:** 86.9->92.0% (28 mismatches) after introducing 'off' temp var to break lwzx pattern. Remaining: pervasive r28<->r29 fp regalloc, r27<->r30 p regalloc, schedule of jumpsUsed vs off->x2C loads (commutative add operand order), and msid-=1 placement (early in target, late in base).
- **Tried:** (1) Introduced 'struct Fighter_x2D0_t* off' temp to materialize address arith separately - moved compiler from addi+lwzx to add+lwz pattern matching target (+5%). (2) Folded -1 into initial expression and reordered operand to off->x2C+jumpsUsed - regressed to 87% by changing register assignments.
- **Likely fix:** Permuter: pure regalloc + scheduling diff with no structural issues remaining. All semantic shapes (load patterns, address arith) match target.

