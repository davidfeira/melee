---
function: if_802F7E7C
tu: src/melee/if/if_2F72.c
headline: regalloc + mwcc-aliasing
tags: [regalloc, mwcc-aliasing]
---
## if_802F7E7C (`src/melee/if/if_2F72.c`) — regalloc + mwcc-aliasing

- **Tags:** `regalloc`, `mwcc-aliasing`
- **Best fuzzy:** 93.125%
- **Diagnosis:** 3-instr regalloc miss. Target: addi r31,r3,lbl@l; ...; addi r29,r31,0x0 (data and base directly chained). Base: addi r0,r3,lbl@l; mr r31,r0; mr r29,r0 (mwcc CSEs lbl_804A1340 address into r0 and copies to both r31/r29). 32-instr function; 3 mismatches concentrated in entry sequence assigning data/base both to lbl_804A1340. Permuter 183-min run plateaued at score=20 (60+ outputs across scores 20-240); best two output-20 variants used 'new_var=data' alias or '((HSD_GObj **) lbl_804A1340)[1]' inline cast, neither closes the gap. report.json shows 93.125% with 3 strict mismatches.
- **Tried:** (1) base = data (chain assignment) - identical 93.125%, no change; (2) declare-then-assign with data; base=data; - identical 93.125%. mwcc CSE on lbl_804A1340@ha+@l forces an intermediate r0 even when source explicitly writes base=data. Permuter explored 60+ shapes (output-20 best, output-25 next), couldn't break CSE either.
- **Likely fix:** permuter-territory at 3 mismatches but the cluster has already plateaued. Possible cross-TU influence: lbl_804A1340 is a static[13] in if_2F72.c (line 25). Could try (a) making the array volatile or non-static (likely breaks 9 other matched siblings using it), (b) inline asm to force register assignment, (c) different sibling pattern - sibling fn_802F77F8 uses 'void** base = lbl_804A1340;' (no cast) which compiled. Worth trying source shapes that suppress CSE between two distinct uses of the same global address - e.g. taking address of element 0 vs full-array decay. Also worth re-running permuter with a different seed/longer wall-clock; 3-instr gap is theoretically permuter-reachable.
