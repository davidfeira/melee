---
function: ftCo_800AB224
tu: src/melee/ft/chara/ftCommon/ftCo_0A01.c
headline: permuter-false-positive + sdata2-float
tags: [permuter-false-positive, sdata2-float, stack-offset]
---
## ftCo_800AB224 (`src/melee/ft/chara/ftCommon/ftCo_0A01.c`) — permuter-false-positive + sdata2-float

- **Tags:** `permuter-false-positive`, `sdata2-float`, `stack-offset`
- **Best fuzzy:** 99.9922%
- **Diagnosis:** Diff at fuzzy=99.99225%/strict=99.70155% with 34 mismatches. 31 are sdata2 float reloc-anchor mismatches (target uses named globals like ftCo_804D8828@sda21, base.o uses anonymous pool slots like @330@sda21). 3 are stack-offset diffs (target sp+0x44/0x48/0x4c, base sp+0xc/0x10/0x14) which likely reflect a different base.o stack frame layout rather than a real source bug — post-link addresses are equivalent. Permuter scorer treats sdata2 anchor renames as equivalent so it cannot make progress past plateau ~18655; report.json fuzzy still flags them. Per CLAUDE.md permuter-false-positive rules: do NOT dispatch permuter further.
- **Tried:** prep + diff inspection; classified all 34 mismatches against the documented permuter-false-positive layout classes
- **Likely fix:** Confirm post-link bytes match (build the .dol and check SHA1) — if main.dol is OK, this is fully matched in the link. Otherwise needs cross-TU pool-anchor inspection (other functions in ftCo_0A01.c may be allocating the sdata2 floats in a different order than base, or a sibling TU defines the named globals). Not a single-function-rewrite problem.
