---
function: ftCo_80093BC0
tu: src/melee/ft/chara/ftCommon/ftCo_Guard.c
headline: stack-offset + float-literal
tags: [stack-offset, float-literal, permuter-false-positive]
---
## ftCo_80093BC0 (`src/melee/ft/chara/ftCommon/ftCo_Guard.c`) — stack-offset + float-literal

- **Tags:** `stack-offset`, `float-literal`, `permuter-false-positive`
- **Best fuzzy:** 99.91% (Opus subagent — cluster permuter dispatched, score=0 outputs are linker-equivalent but objdiff still flags reloc-symbol mismatch)
- **Diagnosis:** 10 mismatches across two issues:
  1. AbsorbDesc stack offset shifted by +4 (target `r1+0x14`, ours `r1+0x10`) from inlined `ftCo_80092450`.
  2. Float constants `1.0f`/`0.0f` use named SDA2 globals `ftCo_804D8568`/`ftCo_804D8538` in target vs anonymous `@289`/`@212` literal pool in ours.
- **Tried:** `u8 _[4] = {0}` filler — mwcc optimized away, no change. Cluster permuter dispatched.
- **Likely fix:** declare named float globals matching `ftCo_804D8538/8568` and refer to them; permuter for the stack-offset side.
