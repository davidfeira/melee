---
function: ftCo_80093BC0
tu: src/melee/ft/chara/ftCommon/ftCo_Guard.c
headline: stack-offset + sdata2-float
tags: [stack-offset, sdata2-float, permuter-false-positive, inlining]
---
## ftCo_80093BC0 (`src/melee/ft/chara/ftCommon/ftCo_Guard.c`) — stack-offset + sdata2-float

- **Tags:** `stack-offset`, `sdata2-float`, `permuter-false-positive`, `inlining`
- **Best fuzzy:** 99.9118%
- **Diagnosis:** Frame is 0x28 (ours) vs 0x30 (target). The AbsorbDesc struct (from inlined ftCo_80092450) lands at sp+0x10 instead of sp+0x14. Attempt 1: open-coded ftCo_80092450 with struct AbsorbDesc + second fp2 variable to force r30 allocation -> r30+r31 both saved but frame grew only to 0x28 (struct at 0x0C, needs 0x14). The target's extra 8 bytes (0x08-0x13 gap) are unexplained by r30+r31 saves alone. Attempt 2: m2c-style separate s32/f32 locals (sp14..sp24) -> structural regression (81%). sdata2-float mismatches (ftCo_804D8538=0.0f, ftCo_804D8568=1.0f vs anonymous @212/@289) are reloc-symbol false-positives; post-link bytes identical.
- **Tried:** 1) AbsorbDesc struct + fp2=gobj->user_data (r30 forced): frame=0x28 vs 0x30, struct at sp+0x0C vs sp+0x14. 2) m2c separate f32/s32 locals (sp14..sp24): 81% structural regression. Prior: u8 _[4] filler tried by earlier agent, also failed.
- **Likely fix:** Need 8 more bytes in the frame. Target has sp+0x08..sp+0x13 unused (12-byte gap before struct). Try: ftCo_80092450 call + explicit PAD_STACK(8) before it, or declare a dummy s32 variable that forces 8 bytes of gap. Or define ftCo_804D8538 and ftCo_804D8568 as named globals (for the sdata2-float side) and see if that changes pool layout enough to shift the struct address.

