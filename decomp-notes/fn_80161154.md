---
function: fn_80161154
tu: src/melee/gm/gm_1601.c
headline: permuter-queued + frame-size
tags: [permuter-queued, frame-size, regalloc, permuter-territory]
---
## fn_80161154 (`src/melee/gm/gm_1601.c`) — permuter-queued + frame-size

- **Tags:** `permuter-queued`, `frame-size`, `regalloc`, `permuter-territory`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 5-stage tiebreak picker over MatchPlayerData[0..3], 216 mismatches at 95.5% (loop-form). Structural shape correct: stage 1 smallest x20, stage 2 largest x24, stage 3 smallest x44, stage 4 largest x50, then first-cand fallback. Cand bitmap stored on stack at sp+0xC (u8 cand[4]); target frame is 0x20, my loop-form is 0x18 (4 bytes shorter, target reserves more spill slack). Register allocation is shifted by 1 throughout (target r9/r10 for winner/any, mine r8/r9). Unrolled-with-pointer-walk attempt regressed to 73.8%. Decomp-permuter is offline per session override, would normally hand off.
- **Tried:** (1) loop form mirroring m2c structure: 95.5% / 216 mismatches; (2) hand-unrolled with u8* p pointer-walk to match target lbzu pattern: 73.8% / 519 mismatches (worse — defeated MWCC's hoisting of player_standings[i] address loads, and didn't fix frame size).
- **Likely fix:** Permuter on the loop-form source — remaining diffs look like register-allocation choice (which int gets r9 vs r10), instruction scheduling between stages, and small frame-size constant. Control flow and arithmetic match.

