---
function: ftCo_800B17D0
tu: src/melee/ft/chara/ftCommon/ftCo_0A01.c
headline: permuter-territory + r30-r31-swap
tags: [permuter-territory, r30-r31-swap, regalloc, paired-siblings, stack-offset]
---
## ftCo_800B17D0 (`src/melee/ft/chara/ftCommon/ftCo_0A01.c`) — permuter-territory + r30-r31-swap

- **Tags:** `permuter-territory`, `r30-r31-swap`, `regalloc`, `paired-siblings`, `stack-offset`
- **Best fuzzy:** (unknown)
- **Diagnosis:** Source structure matches target asm: ftCo_800A229C/AE7AC early-return, ftCo_800A5CE0 NULL/non-NULL split, identical flag-bit patterns, x4C+item_gobj guard around ftCo_800A866C vs ftCo_800A53DC/75DC. Sibling ftCo_800B1EF0 (same TU, same shape) is also unmatched with same regalloc pattern. Diff is 40% / 203 mismatches but every single mismatch is register allocation: target uses r30=fp,r31=&x1A88 with r28/r29 callee-saved before first bl for late literal-1/literal-0 holders; my source picks r28=fp,r29=&x1A88. sp28 stack offset differs by 4 (0x28 target vs 0x2c base). Switch on item kind (Heart/Tomato/Foods = 8/9/0x12) inlines as ftCo_800A5908(GET_ITEM(fp->item_gobj)) call -- same as 1EF0.
- **Tried:** Two source attempts: (1) mirror sibling ftCo_800B1EF0 layout (uses temp_r4 second pointer); (2) simplify to single temp_r31 + drop var_r0_3 + correct second-half guard to (x4C != NULL && item_gobj == NULL). Match% only nudged 39->40. All 203 mismatches are reg-allocation; control flow, calls, args, flag bits all correct.
- **Likely fix:** Permuter territory once decomp-permuter is online again. Cluster-dispatch this together with sibling ftCo_800B1EF0 -- both share the same structural blocker so a permuter win on one likely teaches the pattern. Consider also that mwcc may need a specific local-decl ordering or a temp-Fighter* pre-load to pin r30/r31 vs r28/r29.

