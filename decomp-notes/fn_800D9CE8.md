---
function: fn_800D9CE8
tu: src/melee/ft/chara/ftCommon/ftCo_Attack100.c
headline: permuter-queued + sdata2-float
tags: [permuter-queued, sdata2-float, permuter-false-positive, float-regalloc, permuter-territory]
---
## fn_800D9CE8 (`src/melee/ft/chara/ftCommon/ftCo_Attack100.c`) — permuter-queued + sdata2-float

- **Tags:** `permuter-queued`, `sdata2-float`, `permuter-false-positive`, `float-regalloc`, `permuter-territory`
- **Best fuzzy:** 98.2843%
- **Diagnosis:** 98.09% strict / 98.28% fuzzy with 9 mismatches. Source structure correct (Yoshi anim_rate special-case + LINK/CLINK/SAMUS/default item-jobj store + ChangeMotionState transition). Remaining mismatches are dominated by permuter false-positive sdata2-float class (4 of 9): @214/@270/@807 anonymous literal pool slots vs ftCo_804D9098/909C/90A0 named locals — same post-link addresses (0.0f, 1.0f, 4503599627370496.0 for int->float bias). The other 5 mismatches are real regalloc: target loads cur_anim_frame into f2 then 'fmr f31, f2' (uses f2 in early fcmpos, keeps f31 across function calls); my version loads directly to f31. fmr-after-load suggests CSE prefers volatile f2 first when expr only used pre-call, then promotes to f31 callee-saved for post-call use.
- **Tried:** V1: initial source-shape with f32 anim_frame + f32 start_frame locals, ftYs_CatchAttrs cast struct for dat_attrs (x124, x128, u8 x12C[]). 27 mismatches. V1+: added PAD_STACK(0x20) → 9 mismatches. V2: collapsed anim_frame, used fp->cur_anim_frame inline in comparisons → no change (still 9, same regalloc shape).
- **Likely fix:** Permuter on regalloc would resolve 5 instructions (target_f2 vs base_f31 swap pattern) — small enough to permute. The 4 sdata2-float mismatches are class-known false-positive (per Camera_8002FEEC.md, fn_802A3110.md): named local sdata2 floats (ftCo_804D9098 = 0.0f, 909C = 1.0f, 90A0 = double 4503599627370496.0) collide with mwcc's anonymous literal pool entries. Same final address; objdiff strict flags symbol-name mismatch. Likely needs paired-sibling matching or restructure of sdata2 pool. Currently queued for offline permuter.

