---
function: un_8031B328
tu: src/melee/ty/tydisplay.c
headline: tu-wide-data + permuter-false-positive
tags: [tu-wide-data, permuter-false-positive, string-pool]
---
## un_8031B328 (`src/melee/ty/tydisplay.c`) — tu-wide-data + permuter-false-positive

- **Tags:** `tu-wide-data`, `permuter-false-positive`, `string-pool`
- **Best fuzzy:** 99.8205%
- **Diagnosis:** Diff is dominated by data-anchor offset mismatches (target r30+0xc8/0x78/0x110/0x44 vs base r30+0x50/0x20/0xac/0xc8) all anchored on un_803FEFF0. Same blocker as un_8031B1FC (still 99.52%): strings live in tydisplay.c .data but the base TU is missing ~0x78 bytes of preceding .data items so all offsets relative to un_803FEFF0 shift by exactly 0x78. Also un_804D5AC0@sda21 (named) vs @661@sda21 (compiler-generated literal pool) for OSPanic third arg = '0'. The named static s8 un_804D5AC0 = 0 in base.c is forcing a separate symbol; original code likely passed a stringified '0' literal that mwcc pooled as @661. r29/r30 regalloc swap on 2nd HSD_ArchiveGetPublicAddress is downstream of the anchor symbol rename (changes liveness). All bytes link-equivalent; permuter scorer would say 100% but report.json fuzzy will not. Cannot fix in this function alone — needs tu-wide string layout work (likely matching un_803FEFF0 to be a longer multi-string array containing all of tydisplay.c's literal strings in the original order, plus replacing 'static s8 un_804D5AC0 = 0' with an in-source string literal '0' for the OSPanic call so mwcc emits @NNN pool entry).
- **Tried:** prep+diff inspection; cross-referenced un_8031B1FC sibling (same blocker, same offset delta of 0x78 across all string refs).
- **Likely fix:** TU-wide rewrite: collapse all .data string literals (asserts, OSReport messages, joint names) into a single un_803FEFF0[] anchor array in source order matching the original .data layout, OR (simpler) restructure source so mwcc lays out strings such that the relative offsets (+0xc8, +0x78, +0x110, +0x44) appear naturally. Replace named static un_804D5AC0 with inline '0' literal at OSPanic callsite. Verify by checking that un_8031B1FC's mismatches drop in lockstep — they share the anchor.
