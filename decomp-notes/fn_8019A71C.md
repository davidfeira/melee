---
function: fn_8019A71C
tu: src/melee/gm/gm_18A5.c
headline: branch-pattern + mwcc-aliasing
tags: [branch-pattern, mwcc-aliasing]
---
## fn_8019A71C (`src/melee/gm/gm_18A5.c`) — branch-pattern + mwcc-aliasing

- **Tags:** `branch-pattern`, `mwcc-aliasing`
- **Best fuzzy:** 98.3582%
- **Diagnosis:** 98.36%, 2 mismatches at FIRST if (*state == 0x13) call fn_8019B458. Canonical emits 1-branch form 'cmpwi 0x13; bne SKIP; bl fn_8019B458; SKIP:' but mwcc emits 2-branch 'cmpwi 0x13; beq DO; b SKIP; DO: bl fn_8019B458; SKIP:'. Pre-existing TODO comment in source confirms this exact issue. Inverted branch direction + extra unconditional jump-around. The other 4 ifs in the function (==0x1E, >=0x14, >=0x3C, >=0x46, >=0x5A, ==1) all match perfectly using the standard 1-branch form.
- **Tried:** V1: changed 'if(==1){return;} *state=0x1B' to 'if(==1){} else{}' at end of function -> same 2 mismatches (mwcc canonicalizes both forms). V2: removed wrapping '#pragma dont_inline on/reset' around the function -> same 2 mismatches (pragma has no effect on this function, it has no matching push/pop).
- **Likely fix:** The mismatch is at the entry-point if-statement that calls fn_8019B458. mwcc is generating an unusual jump-around-jump pattern only at function entry. Possible causes: (1) fn_8019B458 might have an attribute (it's the called function in same TU, may need decompiling to see if branch hint or alignment changes anything), (2) function ordering in TU - try moving fn_8019A71C above its callers, (3) compiler may be applying a setup-code prefix when entry condition triggers a non-inline call. Permuter is unlikely to fix this since it's about compiler-emitted prologue branching, not source-level register/expression choice. May need TU-level structural change or fn_8019B458 to be decompiled first.
