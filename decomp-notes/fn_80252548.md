---
function: fn_80252548
tu: src/melee/mn/mninfo.c
headline: regalloc + inlining
tags: [regalloc, inlining, header-prototype, permuter-territory]
---
## fn_80252548 (`src/melee/mn/mninfo.c`) — regalloc + inlining

- **Tags:** `regalloc`, `inlining`, `header-prototype`, `permuter-territory`
- **Best fuzzy:** 92.9661%
- **Diagnosis:** 92.92% / 59 mismatches after fixing two structural issues. Required #pragma dont_inline on around mnInfo_80251A08 AND mnInfo_80251F04 (mwcc -O4,p was auto-inlining both into fn_80252548 — without pragmas, score was 30.58% with the entire mnInfo_80251A08 switch + mnInfo_80251F04 HSD_SisLib_803A5ACC body inlined inline at the call sites). Also required header signature fixes: 'UNK_RET fn_80252548(UNK_PARAMS)' -> 'void fn_80252548(HSD_GObj*)' and 'UNK_RET mnInfo_80251D58(UNK_PARAMS)' -> 'void mnInfo_80251D58(HSD_GObj*, s32, u8, u32)'. Remaining 59 mismatches are pure register allocation: target uses 6 callee-saved GPRs (r26-r31) with stack frame 0x40, my version uses 5 (r27-r31) with frame 0x28. Specifically target holds two distinct zero-temp registers (r28, r29 from 'addi rN, r27, 0') for the two NULL stores, while mwcc folds mine to a single zero register. Target also gets gobj=r30/data=r31; mine gets gobj=r31/data=r29. Branch 1 walker: target does 'slwi r0, r27, 2; add r26, r3, r0' (dr = data + i*4 with i=0); mine does 'mr r27, r0' (dr = data direct copy). Sibling fn_802523D8 already matches with the SAME source structure (dw/dr pattern) but its else-branch is much shorter so register pressure differs.
- **Tried:** (1) dw/dr separate locals + dont_inline pragmas on mnInfo_80251A08 and mnInfo_80251F04 -> 92.92%. (2) Combined assignment 'dw = dr = gobj->user_data' -> 92.58% (slightly worse, reverted).
- **Likely fix:** Permuter territory: 59 mismatches are all DIFF_ARG_MISMATCH on register names + 2 DIFF_DELETE for missing zero-temp setups. Permuter should resolve via register-pressure perturbations (introduce/remove a small inline temp, reorder local declarations, force-extract gobj to a new local). Decomp-permuter currently offline per dispatch override; queued for later.

