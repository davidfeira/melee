---
function: fn_80394DF4
tu: src/sysdolphin/baselib/particle.c
headline: permuter-false-positive + bss-anchor
tags: [permuter-false-positive, bss-anchor, tu-wide-data]
---
## fn_80394DF4 (`src/sysdolphin/baselib/particle.c`) — permuter-false-positive + bss-anchor

- **Tags:** `permuter-false-positive`, `bss-anchor`, `tu-wide-data`
- **Best fuzzy:** 97.8684%
- **Diagnosis:** 11 mismatches: 10 BSS-anchor reloc-only false-positives (target uses hsd_804CF810@ha/@l with offsets 0xd0/0x0/0x0; base uses ...bss.0@ha/@l with offsets shifted +0x28: 0xf8/0x28/0x28 — same post-link bytes). 1 codegen mismatch is regalloc/expression-order (target: bne 0x3898 then blr; base: mr r4,r0 then beqlr — same effect, different shape). TU particle.c already has 4 logged stuck siblings (e.g. hsd_80395644) with identical hsd_804CF810 BSS-anchor pattern.
- **Tried:** prep + diff inspection. No source variants attempted: classification matches the documented permuter-false-positive class (BSS-anchor: hsd_804CF810 vs ...bss.0 with constant +0x28 delta). Sibling functions in same TU already logged with same blocker.
- **Likely fix:** TU-wide BSS layout fix on hsd_804CF810: split it from preceding ~0x28 of BSS objects so mwcc emits it as its own per-symbol BSS section. Same fix should unblock the 4 sibling stuck functions in particle.c. The single codegen difference (bne+blr vs mr+beqlr) might also resolve once the symbol is correct, since base regalloc may shift when not using anchored r4 chain.
