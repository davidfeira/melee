---
function: fn_80394DF4
tu: src/sysdolphin/baselib/particle.c
headline: permuter-false-positive + bss-anchor
tags: [permuter-false-positive, bss-anchor, tu-wide-data]
---
## fn_80394DF4 (`src/sysdolphin/baselib/particle.c`) — permuter-false-positive + bss-anchor

- **Tags:** `permuter-false-positive`, `bss-anchor`, `tu-wide-data`
- **Best fuzzy:** 97.8684%
- **Diagnosis:** 11 mismatches: 10 BSS-anchor reloc false-positives. Target uses hsd_804CF810@ha/@l with offsets 0xd0/0x0/0x0; base uses ...bss.0@ha/@l with offsets +0x12c shifted: 0x1fc/0x12c/0x12c. Offset delta is 0x12c (300 bytes), meaning BSS anchor is 300 bytes before hsd_804CF810. 1 codegen mismatch: target lwz r4 + bne + blr; base lwz r0 + cmplwi + mr r4,r0 + beqlr — same semantics, likely caused by r4 being held for BSS ha address in base forcing temp load to r0.
- **Tried:** prep + diff inspection. Updated offset delta from prior entry (was +0x28, now confirmed +0x12c from current diff output). No source variants attempted — BSS-anchor false-positive class confirmed.
- **Likely fix:** TU-wide BSS layout fix: adjust ordering of BSS objects so hsd_804CF810 is 0x12c bytes earlier in the section, or ensure mwcc emits a distinct per-symbol BSS section for it. The codegen mismatch (bne+blr vs mr+beqlr) will likely also resolve once the BSS symbol is correct and r4 is freed up earlier by the compiler.

