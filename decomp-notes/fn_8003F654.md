---
function: fn_8003F654
tu: src/melee/pl/plbonuslib.c
headline: permuter-queued + float-regalloc
tags: [permuter-queued, float-regalloc, fma-fusion, sdata2-anonymous-floats]
---
## fn_8003F654 (`src/melee/pl/plbonuslib.c`) — permuter-queued + float-regalloc

- **Tags:** `permuter-queued`, `float-regalloc`, `fma-fusion`, `sdata2-anonymous-floats`
- **Best fuzzy:** 82.8448%
- **Diagnosis:** Improved from 0% no-def to 82.63% match (121 mismatches). Remaining diff is dominated by float register allocation (compiler picks f29 for loop 0.0 constant instead of using f30; target reuses f30 for 0.0 and f31 for var_f30 accumulator, keeping only 2 float regs saved vs my 3), fmadds vs separate fmuls/fadds in dx*dx+dy*dy expression, fmr-vs-fneg ordering in abs() patterns (target preserves through f0 intermediate), and sdata2 anonymous-float anchor mismatches (lbl_804D7F58 vs @632 for 0.0f literal). All structural shape (control flow, loop body, assert sites, struct field accesses, sqrtf inline 3-iter) appears correct. Stack frame off by 8 bytes due to extra f29 save.
- **Tried:** (1) if (verify && index!=1) wrapping body; (2) split !verify and index==1 into separate ifs; (3) introduced dxsq/dysq intermediates to inhibit fmadds (gained match% but still produces fmadds in some cases); both attempts produced ~82% match.
- **Likely fix:** Permuter run: register allocation perm should find f30/f31 reuse of the 0.0 constant pattern. Constant scheduling perm should split the fmadds. The sdata2 anchor mismatch (named lbl_804D7F58 vs anonymous @632) may be permuter false-positive class — verify with --skip-symbols if remaining issues clear up.

