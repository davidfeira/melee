---
function: hsd_80394434
tu: src/sysdolphin/baselib/particle.c
headline: tu-data-osreport + reloc-symbol-false-positive
tags: [tu-data-osreport, cross-tu-globals, reloc-symbol-false-positive]
---
## hsd_80394434 (`src/sysdolphin/baselib/particle.c`) — tu-data-osreport + reloc-symbol-false-positive

- **Tags:** `tu-data-osreport`, `cross-tu-globals`, `reloc-symbol-false-positive`
- **Best fuzzy:** 94.10% (11 mismatches on 68-instr function)
- **Diagnosis:** All 11 mismatches are reloc-symbol false-positives at the BSS anchor. Target compile uses `lis r4, hsd_804CF810@ha; addi r31, r4, hsd_804CF810@l; addi r30, r31, 0x4` then loads via `0x0(r31), 0x0(r31), 0x4(r31), 0x8(r31), 0x4c(r31)` — i.e. anchors via the named struct symbol `hsd_804CF810`. Base compile emits `lis r4, ...bss.0@ha; addi r4, r4, ...bss.0@l; addi r31, r4, 0x28; addi r30, r4, 0x2c` then loads via `0x28(r4), 0x28(r4), 0x2c(r4), 0x30(r4), 0x74(r4)` — i.e. anchors on the bss-section start (`hsd_804CF7E8` is at `0x4CF7E8`, `hsd_804CF810` is at `0x4CF810`, delta = `0x28`). Linker resolves both forms to the same address; existing source comment confirms "linked code matches 100%". Same TU as fn_80393C14 (which used the *opposite* trick — adding pointer aliases to switch INTO bss.0 anchoring — to match). Function source is structurally and semantically correct; m2c output already matches the existing source.
- **Tried:** (1) Removed `s32* x4_ptr = (s32*) sp + 1;` and replaced `*x4_ptr` with `sp->x4` — regressed to 91.43% / 28 mismatches (lost the `addi r30, r31, 0x4`, plus regalloc cascade r25-r30). (2) Replaced `(s32*) sp + 1` with `&sp->x4` — regressed to 91.68% / 34 mismatches. Reverted both, back at baseline 94.10%.
- **Permuter contraindicated:** Per Permuter Boundary, false diffs from equivalent BSS/global base symbols should not be permuted. The 11 mismatches are 100% reloc-name divergence (lis/addi pair on `hsd_804CF810` vs `...bss.0+0x28`), which permuter cannot bridge.
- **Likely fix:** TU-wide BSS layout refactor for particle.c. The bss-section anchor needs to become `hsd_804CF7E8` (or moved earlier) so that mwcc anchors hsd_80394434's accesses via the named struct symbol rather than via a generic bss.0 + 0x28. Probably requires reordering or merging the BSS declarations in particle.static.h such that `hsd_804CF810` becomes the section start, OR introducing extra named statics so mwcc's anchor heuristic picks the named symbol over the section base. Same blocker family as Exception_ReportStackTrace, hsd_80394950 (both same TU). Sibling: this is the *inverse* problem from fn_80393C14 (matched by ADDING pointer aliases to switch INTO bss.0 anchor); here we'd need to switch OUT of bss.0. Multi-symbol cross-function refactor; beyond single-function scope.
