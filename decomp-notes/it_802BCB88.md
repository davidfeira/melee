---
function: it_802BCB88
tu: src/melee/it/items/itseakchain.c
headline: regalloc + scheduler
tags: [regalloc, scheduler]
---
## it_802BCB88 (`src/melee/it/items/itseakchain.c`) — regalloc + scheduler

- **Tags:** `regalloc`, `scheduler`
- **Best fuzzy:** 99.8872%
- **Diagnosis:** 2-instruction adjacent swap: target has 'addi r28,r28,0x1' (count++) before 'addi r3,r1,0x48' (&dir for lbVector_Normalize); base emits them reversed. Pure scheduler ordering with no source-shape sensitivity. Permuter explored hundreds of variants and converged at score 480 = same 2-swap diff (output-480-1's new_var hoist of (count%stride)==0 doesn't actually fix it). Tried: (a) moving count++ outside Vec3 dir block (above it); (b) interleaving count++ between dir.x= and dir.y=. Both produced identical diff. The asm window: lfs f1,0x60(r1) ; addi r28,r28,1 ; lfs f0,0x6c(r1) ; addi r3,r1,0x48 ; ... fsubs ... bl lbVector_Normalize. Compiler floats the &dir pointer setup right after the first lfs regardless of source order.
- **Tried:** moved count++ outside inner Vec3 dir scope (no change); placed count++ between dir.x= and dir.y= assignments (no change); permuter cluster plateau at 480 over many outputs incl. new_var hoist
- **Likely fix:** May need a different way to construct dir — e.g. use 3 floats x/y/z and only assemble &dir at call site, OR reorder pos0/pos1 evaluation so the first lfs after addi r28 changes register pressure. Permuter already exhausted obvious variants; consider whether lbVector_Normalize signature/inlining or a forced spill via volatile-like construct could shift the schedule. Cluster permuter is done — needs human-driven structural rewrite if pursued.
