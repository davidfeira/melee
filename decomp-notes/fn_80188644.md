---
function: fn_80188644
tu: src/melee/gm/gm_1832.c
headline: tu-data-osreport + cross-tu-globals
tags: [tu-data-osreport, cross-tu-globals, reloc-symbol-false-positive, paired-siblings]
---
## fn_80188644 (`src/melee/gm/gm_1832.c`) — tu-data-osreport + cross-tu-globals

- **Tags:** `tu-data-osreport`, `cross-tu-globals`, `reloc-symbol-false-positive`, `paired-siblings`
- **Best fuzzy:** 99.8197%
- **Diagnosis:** 14 ARG_MISMATCH at 99.82% fuzzy / 98.92% strict, all reloc-symbol false-positives. Target anchors via lbl_80473700@ha/@l directly with offsets 0/0x2f4/0x2f8/0x2fc/0x300/0x304/0x308/0x30c; base anchors via ...bss.0@ha/@l with offsets +0x1a4 (consistent across all 14 mismatches). The 0x1a4 delta means base merges lbl_80473700 into a combined bss.0 blob 0x1a4 below it. Plus 1 sdata2 anonymous-literal swap: lbl_804DA60C@sda21 (1.0f) vs @674@sda21. Function source is structurally correct (m2c output matches existing structure exactly: saved_count = lbl_80473700.count, then loop populating players[i].slot_type and resetting menu_values 0..7). No regalloc/scheduling/register diffs at all - every diff is a reloc symbol name.
- **Tried:** Manual diff inspection only. Per Permuter Boundary, false diffs from equivalent BSS/global base symbols should not be permuted; per CLAUDE.md, verify whether source really needs changing first. Permuter cannot bridge anchor-symbol divergence per prior fn_80186EFC (same TU!), gm_80173754, fn_80191154, soundtest, mp, it_2725 notes.
- **Likely fix:** TU-wide BSS layout fix in gm_1832.c: same family as sibling fn_80186EFC (already-noted same-TU blocker). Needs reordering / merging / explicit-sizing of BSS symbols so mwcc anchors via lbl_80473700@ha directly rather than picking an earlier symbol as the bss.0 anchor. Specifically lbl_80473700 needs to land 0x1a4 lower in the bss.0 blob, OR be split out so it gets its own anchor. Sibling fn_801884F8 in the same TU is already matched (same BSS-anchor family but different function shape) - per gm_1832.c TU notes the layout is partially-but-not-fully reconciled. The sdata2 @674 (1.0f) anchor also needs a named float static so mwcc stops emitting an anonymous local literal. Multi-symbol cross-function refactor; beyond single-function scope. When the TU-wide fix lands, this function should match for free.
