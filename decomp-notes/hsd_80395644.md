---
function: hsd_80395644
tu: src/sysdolphin/baselib/particle.c
headline: permuter-false-positive + bss-anchor
tags: [permuter-false-positive, bss-anchor, tu-wide-data]
---
## hsd_80395644 (`src/sysdolphin/baselib/particle.c`) — permuter-false-positive + bss-anchor

- **Tags:** `permuter-false-positive`, `bss-anchor`, `tu-wide-data`
- **Best fuzzy:** 99.7838%
- **Diagnosis:** 10 mismatches all BSS-anchor reloc-only: target uses hsd_804CF810@ha/@l with member offsets 0x50,0x4,0x20,0x1C,0x40,0x8 etc; base resolves through anonymous ...bss.0@ha/@l anchor with offsets shifted by +0x28 (0x78,0x2C,0x48,0x44,0x68,0x30). Same post-link address (permuter scorer would treat as match), but report.json fuzzy treats the symbol-pair as inequivalent. TU particle.c already has 3 logged stuck siblings -- structural blocker is BSS layout/split for hsd_804CF810, not C source.
- **Tried:** prep + diff inspection. No source variants attempted: classification is BSS-layout/reloc-symbol class, not codegen. C source is semantically straightforward (saved/restore, arithmetic on hsd_804CF810 fields, conditional call).
- **Likely fix:** Split hsd_804CF810 from preceding ~0x28 of BSS objects so mwcc emits it in its own per-symbol BSS section. Approaches: (a) reorder declarations in particle.c/particle.h so hsd_804CF810 isn't clustered, (b) introduce alignment/size attribute to break clustering, (c) use #pragma section split. Likely the same fix would unblock the 3 sibling stuck functions in particle.c.
