---
function: un_80317A60
tu: src/melee/ty/tyfigupon.c
headline: tu-data-osreport + cross-tu-globals
tags: [tu-data-osreport, cross-tu-globals, reloc-symbol-false-positive]
---
## un_80317A60 (`src/melee/ty/tyfigupon.c`) — tu-data-osreport + cross-tu-globals

- **Tags:** `tu-data-osreport`, `cross-tu-globals`, `reloc-symbol-false-positive`
- **Best fuzzy:** 99.97%
- **Diagnosis:** 7 mismatches, all reloc-symbol false-positives at 99.97% fuzzy. Target compile produces (cam_desc + sdtoy strings + nget strings) inside merged data object un_803FEEE8, with cam_desc at .data 0x4ec, sdtoy_jp at 0x524, sdtoy_data_jp at 0x530, sdtoy_us at 0x53c, sdtoy_data_us at 0x548. Base symbol layout is individualized: sdtoy_jp at 0x438, sdtoy_data_jp at 0x444, sdtoy_us at 0x450, sdtoy_data_us at 0x45c, nget_joint at 0x46c through nget_shapeanim at 0x4cc, then tyfigupon_cam_desc at 0x4f4. Float load at 80317BF8 uses un_804DDEB0@sda21 (target) vs @345@sda21 (base): base @345 is .sdata2:0x4 = 1.0f; target un_804DDEB0 is .sdata2:0x10 also 1.0f, indicating 12 extra bytes of sdata2 emitted before. mwcc is reordering: putting cam_desc struct BEFORE the sdtoy/nget string literals inside the merged un_803FEEE8 data block, even though source order has strings at lines 105-112 and cam_desc at line 114.
- **Tried:** Manual diagnosis only - no source-shape attempts. Permuter Boundary: data-layout/symbol-emission issue, not register allocation or scheduling. Function semantics already match (99.97% fuzzy).
- **Likely fix:** TU-wide data reorganization. mwcc emits per-function string pool order as cam_desc-then-strings inside merged un_803FEEE8 - but base wants strings-then-cam_desc. Possibly need to reference all sdtoy/nget strings BEFORE cam_desc somewhere upstream so mwcc emits them earlier, OR merge cam_desc + adjacent strings into a combined struct/array (cross-TU pattern from notes). For sdata2 anchor (un_804DDEB0 vs @345), need to declare all sdata2 float literals in the right order TU-wide so mwcc stops generating @NNN locals. Multi-symbol cross-function refactor, beyond single-function scope. Function source itself appears correct. Same blocker pattern as it_80274DAC, mpGetSpeed, Exception_ReportStackTrace.
