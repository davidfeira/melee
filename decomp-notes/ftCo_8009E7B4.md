---
function: ftCo_8009E7B4
tu: src/melee/ft/ftdynamics.c
headline: permuter-queued + regalloc
tags: [permuter-queued, regalloc, r30-r31-swap, branch-pattern]
---
## ftCo_8009E7B4 (`src/melee/ft/ftdynamics.c`) — permuter-queued + regalloc

- **Tags:** `permuter-queued`, `regalloc`, `r30-r31-swap`, `branch-pattern`
- **Best fuzzy:** (unknown)
- **Diagnosis:** Source structure converged: 90.7% (47 diffs, ~209 instr). Remaining diffs cluster around (1) byte-load reg: target keeps b6 in r4 across MARS/EMBLEM check, base puts in r0; (2) loop-counter reg: target uses r30, base r29 (arg1 binding flips r29/r30 priority); (3) >0->bool: target emits cmpwi/ble/li 0/li 1 branchy, base emits eqv/subfc/srwi/addze arithmetic. Used m2c-suggested pattern of 'Fighter* check = fp; check = NULL/(Fighter*)1' which matches the var_r3 trick (r3 still holds fp through the check).
- **Tried:** (1) explicit if/else for >0 cond, (2) local var u32 b6 to share x2227_b6 across uses, (3) Fighter* check sentinel using fp non-NULL as 'true' default, mirroring m2c var_r3=fp pattern.
- **Likely fix:** permuter for register allocation (r0 vs r4 byte load, r29 vs r30 loop counter). The branch-pattern for >0 may need a different idiom (e.g., explicit 'int x; if (...>0) x=1; else x=0;' wrapped differently, or a helper). Structure is correct per m2c.

