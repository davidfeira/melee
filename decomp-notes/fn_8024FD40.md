---
function: fn_8024FD40
tu: src/melee/mn/mndatadel.c
headline: regalloc + r30-r31-swap
tags: [regalloc, r30-r31-swap, permuter-territory]
---
## fn_8024FD40 (`src/melee/mn/mndatadel.c`) — regalloc + r30-r31-swap

- **Tags:** `regalloc`, `r30-r31-swap`, `permuter-territory`
- **Best fuzzy:** 94.1791%
- **Diagnosis:** 93.58% (21 mismatches, 67 instr). All mismatches are pure register allocation: target assigns gobj=r31, data=r30, user_data=r28, i=r27, loop_ptr=r29; base produces gobj=r27, user_data=r29, i=r28, data=r30, loop_ptr=r31. Additionally target materializes data addr as direct 'addi r30, r4, @l' before branch; base defers to inside else-branch as 'addi r0, r4, @l' + 'mr r30, r0'. Both are regalloc/scheduling artifacts. No structural diff.
- **Tried:** Attempt 1 (this session): moved data declaration into else-branch scope - made things worse (89.5%, 22 mismatches) as compiler materialized data even deeper with 3-instruction sequence. Attempt 2: restored top-level declaration, confirmed 93.58% is the best achievable without permuter.
- **Likely fix:** Permuter with regalloc shuffling. Try declaration reordering to make gobj first/most-prominent variable (pushing it toward r31) and user_data third (toward r28).

