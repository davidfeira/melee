---
function: fn_8024FD40
tu: src/melee/mn/mndatadel.c
headline: permuter-queued + regalloc
tags: [permuter-queued, regalloc, r30-r31-swap]
---
## fn_8024FD40 (`src/melee/mn/mndatadel.c`) — permuter-queued + regalloc

- **Tags:** `permuter-queued`, `regalloc`, `r30-r31-swap`
- **Best fuzzy:** 94%
- **Diagnosis:** 93.58% match (21 mismatches, 67 instr). Structural form is correct: dual-branch on mn_804A04F0.cur_menu == 0x18, else-branch loops 6x calling mn_8022EE84 with indexed user_data jobjs and compares frame == data->x0.end_frame. All ops, calls, control flow, and stack frame (0x40) match. Remaining mismatches are pure register allocation: target picks gobj=r31, user_data=r28, i=r27, loop_ptr=r29, data=r30; base picks gobj=r27, user_data=r29, i=r28, loop_ptr=r31, data via @ha/@l split materialized in the else-branch (mr r30, r0) instead of hoisted before the branch. Tried PAD_STACK(8) and PAD_STACK(16) and reordering local declarations; PAD_STACK(16) fixed frame size but couldn't budge regalloc. Pattern matches sibling fn_8024FC48 closely. Pure permuter territory.

