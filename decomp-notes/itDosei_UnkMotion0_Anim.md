---
function: itDosei_UnkMotion0_Anim
tu: src/melee/it/items/itdosei.c
headline: tu-wide-data + float-literal
tags: [tu-wide-data, float-literal, fuzzy-vs-strict]
---
## itDosei_UnkMotion0_Anim (`src/melee/it/items/itdosei.c`) — tu-wide-data + float-literal

- **Tags:** `tu-wide-data`, `float-literal`, `fuzzy-vs-strict`
- **Best fuzzy:** 99.9474%
- **Diagnosis:** Briefing assumed adding named sdata2 globals (it_804DC870..it_804DC8A0) would close 6+ dosei functions. Investigation shows the assumption is wrong. (1) Even with `volatile const f32 it_804DC878 = 0.0f` declared, mwcc emits ZERO references to it_804DC878 in compiled output — all 0.0f loads use anonymous @209 pool entry. The volatile qualifier does not survive const-folding for this case. (2) itlinkarrow.c uses the same named-global pattern (no volatile) and ALSO has zero refs to its named globals in our build, yet picker shows 100% fuzzy — proving the dashboard fuzzy metric ignores @sda21 anchor symbol differences. (3) The non-100% fuzzy functions in itdosei.c have OTHER blockers: itDosei_UnkMotion0_Anim 99.95% has 1 fmuls operand-order swap (f0,f2,f0 vs f0,f0,f2); it_3F14_Logic7_Dropped 99.7% has r28 vs r30 register allocation diffs; UnkMotion4/9_Anim are bigger structural mismatches. Adding named globals would only improve the strict-objdiff cosmetic score (99.85% -> closer to 100%) without moving the dashboard fuzzy score that commit-match gates on.
- **Tried:** Read git diff to see another agent already added ItemStateTable + made it_804DC878 volatile. Disasm of base build confirms zero it_804DC878 reloc entries (all references go to @209). Compared with itlinkarrow.o which has identical fuzzy/strict gap. Verified picker --mode all shows it_3F14_Logic7_PickedUp and Spawned at 100% fuzzy already; committing them via commit-match would also ship the ItemStateTable edits from the other agent which is out of scope. SHA1 OK confirmed via permute.py check.
- **Likely fix:** The named-global+literal-rewrite pattern (replace 0.0f with it_804DC878 in source) is what target itlinkarrow.o achieves but our build does not — that means mwcc treats `const f32 X = 0.0f; ... use(X);` as compile-time foldable. To force mwcc to emit a reference, declare without const (e.g. `f32 it_804DC878 = 0.0f;`) but then it goes to .sdata not .sdata2. Or use a cast/dereference trick (`*(volatile const f32*)&it_804DC878`). Even then, only the strict-objdiff score improves. The fuzzy score blockers are unrelated: fmuls operand order and register allocation — those are permuter territory or need a different decompilation approach. Recommend: dispatch permuter on itDosei_UnkMotion0_Anim for the fmuls swap; investigate UnkMotion4/9_Anim manually; itdosei.c needs the other agent finishes their ItemStateTable work before further investigation here.
