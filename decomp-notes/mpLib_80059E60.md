---
function: mpLib_80059E60
tu: src/melee/mp/mplib.c
headline: stack-offset + tu-wide-data
tags: [stack-offset, tu-wide-data]
---
## mpLib_80059E60 (`src/melee/mp/mplib.c`) — stack-offset + tu-wide-data

- **Tags:** `stack-offset`, `tu-wide-data`
- **Best fuzzy:** 99.875%
- **Diagnosis:** 12 mismatches all DIFF_ARG_MISMATCH and pure stack-offset class. Target keeps the FIRST pair of stack temp pointers high in the frame at r1+0xcc/0xd0 (stw r0, ...; addi r5, r1, ...) and the next 4 pairs low at r1+0x18/0x1c/0x20/0x24. Base puts all 6 low at r1+0x1c/0x18/0x14/0x10/0xc/0x8 (descending). Function has Mtx sp104 (0x30 bytes) plus PAD_STACK(0x3C) outer plus PAD_STACK(0x8) inside the 'if Camera_80030B50()' block; the 6 spill slots are pointer args used to call mpLib_DrawMatchingLines. The split (high pair vs low quartet) suggests the high pair belongs to the inner if block (terrain palette branch) while the low 4 belong to the outer else-if (platform/ledge) branch. Pure layout class, no opcode/regalloc diff.
- **Tried:** prep + diff inspection only; no source edits attempted (compact-brief recommended log-stuck-immediately based on classification 6 false-positive + 6 stack-offset; TU mp/mplib has 3 logged stuck siblings).
- **Likely fix:** Frame-layout fix likely requires reordering or repositioning the inner if-block's local declarations so the inner-scope spills go HIGH (above sp104) while keeping the else-if spills at the low 0x18-0x24 range. Try (a) moving 'mpIsland_Palette sp28' and 'mpIsland_PaletteEntry* var_r30' declarations to outer scope before the if (forces them into outer frame layout), (b) adding 1 dummy outer-scope local before sp104 to push it down, or (c) reviewing other mplib.c stuck siblings (3 logged) for shared layout pattern. May need TU-wide PAD_STACK or local-ordering convention discovered from a sibling fix.
