---
function: fn_8016588C
tu: src/melee/gm/gm_1601.c
headline: permuter-dispatched + frame-size
tags: [permuter-dispatched, frame-size, mwcc-branch-inversion, struct-typing]
---
## fn_8016588C (`src/melee/gm/gm_1601.c`) — permuter-dispatched + frame-size

- **Tags:** `permuter-dispatched`, `frame-size`, `mwcc-branch-inversion`, `struct-typing`
- **Best fuzzy:** (unknown)
- **Diagnosis:** mwcc -O4,p re-orders the if-else mode chain into signed-range comparisons (cmpwi+bge) instead of the target's cmplwi+bne sequence; also picks 0x18 stack frame vs target 0x30 because clamp tail folds out the dead 'if (max<0) max=-max' branch. Both attempts (switch, if-else chain, static-inline clamp helper) produce identical 55% match — frame size, switch lowering, and value-register choice (r5 in 0x1F branch only) all diverge from target.
- **Tried:** (1) switch on arg0->x5 with cases 2,1,3,default (same body shape); (2) explicit if/else chain on u8 mode with static-inline clamp helper. Both shapes ignored by mwcc and lowered identically.
- **Likely fix:** Source likely uses a non-trivial form of the clamp that prevents constant-folding the 'max < 0' check (forcing dead subic./bge prelude and 0x30 frame), e.g., a u8/s8 var that needs sign-extension, or limit derived from a runtime expression like (1<<bits)-1. The 5x inlined clamp suggests a macro, not a function. Switch ordering 2,1,3 with cmplwi suggests source uses a chain of equality tests on a u8 mode that is freshly loaded at each test, possibly via separate intermediate variable loads, OR the cases compute v through distinct typed locals (e.g. u8 v8 for the 0x1F branch only). Decomp-permuter is offline; queued for permuter once back online.

