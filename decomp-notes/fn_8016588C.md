---
function: fn_8016588C
tu: src/melee/gm/gm_1601.c
headline: permuter-blocked
tags: [permuter-blocked]
---
## fn_8016588C (`src/melee/gm/gm_1601.c`) — permuter-blocked

- **Tags:** `permuter-blocked`
- **Best fuzzy:** 60.9645%
- **Diagnosis:** Improved 0% -> 73.87% (79 mismatches remain). Identified function as MatchEnd-based: tests gm_801A4310()==0x1F (stamina mode), then if/else chain on arg0->x5 in {2,1,3,default}. Each branch computes a value (player_standings[arg1] fields) then clamps to [-MAX, MAX] where MAX=(1<<24)-1. Default branch: x20 - (x24 - self_destructs) + self_destructs * (s8)xC. Stuck on (a) target stack frame 0x30 vs my 0x20 (16 extra bytes unaccounted for), (b) target emits 'lis 0x100; subic. r0,r3,0x1; bge skip; lis 0xff00; addi r0,r4,1' dead-branch pattern that mwcc folds away no matter how I express (1<<24)-1; ABS macro folds it. The dead branch (negative case for positive 0xFFFFFF) appears to come from a non-folding construct (static? volatile? complex inline?). Header updated to 's32 fn_8016588C(MatchEnd*, s32)'. Permuter territory: register/order tweaks plus the dead-branch pattern.

