---
function: grVenom_80204428
tu: src/melee/gr/grvenom.c
headline: struct-split + header-required
tags: [struct-split, header-required]
---
## grVenom_80204428 (`src/melee/gr/grvenom.c`) — struct-split + header-required

- **Tags:** `struct-split`, `header-required`
- **Best fuzzy:** 99.7042%
- **Diagnosis:** Function needs grVenom_GroundVars.xE0 to be a u8 bitfield struct (b0-b7 single bits) instead of f32. Current source uses gp->gv.mutecity2.xC4_flags as workaround, but that resolves to offset 0xC4 in the gv union, while target asm accesses 0xE0 (offset 0x1C within venom struct = xE0). Also the mutecity2 path produces stack 0x18 vs target 0x20, and an anonymous float pool entry vs target named global grVe_804DB740. Sister function grVenom_80204B88 in same TU has same problem (treats xE0 as u16 bitfield) and uses local cast hack with own struct GroundVenom_ForLighting. Stack frame 0x20 indicates 8 bytes of unused padding/spill that the bitfield-correct source layout naturally produces.
- **Tried:** (1) Local pointer cast to anonymous struct with bitfield at xE0: introduces r29 spill, makes things worse (94%). (2) Inline *(u8*)&gp->gv.venom.xE0 &= ~mask: produces u8 access at 0xE0 and matches float, but uses rlwinm clear instead of rlwimi insert, and stack still 0x18 (87%).
- **Likely fix:** HEADER CHANGE to src/melee/gr/types.h: change grVenom_GroundVars to make xE0 a u8 bitfield struct (b0-b7 : 1) instead of f32. Then source can write gp->gv.venom.xE0.b0=0; etc which mwcc emits as rlwimi insert from r6=0 preloaded. xE4 should remain f32 but assigned from named global grVe_804DB740 (= 0.0f) to match anchor reference. The 80204B88 function uses xE0 as u16 bitfield via a local cast struct, may need similar handling or a union. Also worth checking sister function 80203B14 in case it also touches xE0.
