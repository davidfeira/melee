---
function: it_80279FF8
tu: src/melee/it/it_2725.c
headline: matched
tags: [mwcc-aliasing, permuter-false-positive, regalloc]
---
## it_80279FF8 (`src/melee/it/it_2725.c`) — matched

- **Tags:** `mwcc-aliasing`, `permuter-false-positive`, `regalloc`
- **Best fuzzy:** 100.0% in commit 8cf60df0e (`Match it_80279FF8`)
- **Diagnosis:** Definition-site `#pragma auto_inline off` around it_80272DE4/it_80272F7C prevents MWCC from auto-inlining those wrappers into later same-TU callers while preserving their own static-inline HSD_JObj* helper bodies. The `>` branch also needs to pass `item->xDD4_itemVar.it_279D.x48`; target keeps that value in f1 and loads `x60_scale` into f2 only for the compare / else-branch call. The final match required replacing the inlined `HSD_JObjGetScaleX` call with an explicit `__assert(it_804D51D0, 875, it_804D51D8)` plus direct `item_jobj->scale.x`, and writing the JObj load as assignment inside the null-check condition so MWCC binds the load/check to r30 instead of r0.
- **Tried:** Earlier caller-local pragmas (`dont_inline`, `inline_depth(0)`, `inline_depth(1)`, caller-site `auto_inline off`) could not block wrapper auto-inlining without also blocking desired static inlines. Codex TU attempt: helper-definition `#pragma dont_inline on` fixed the caller shape but regressed it_80272DE4 to 18.235294% because it blocked HSD_JObjAddScaleX/Y/Z inlines inside the helper. Definition-site `#pragma auto_inline off` plus the x48 branch argument landed in commit 69940be1a and reduced it_80279FF8 from 150 mismatches / 0% strict to 5 mismatches / 97.07317% strict. Direct `item_gobj->hsd_obj` for the JObj load regressed to 12 mismatches by shrinking the frame to 0x28, so keep `GET_JOBJ`.
- **Likely fix:** Done for this caller. The helper string-symbol class remains for it_80272DE4/it_80272F7C (`it_804D5188/5190` vs @406/@407), but this function no longer blocks on it.
