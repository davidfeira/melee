---
function: fn_800F9260
tu: src/melee/ft/chara/ftKirby/ftKb_SpecialN.c
headline: frame-size + stack-offset
tags: [frame-size, stack-offset, inlining]
---
## fn_800F9260 (`src/melee/ft/chara/ftKirby/ftKb_SpecialN.c`) — frame-size + stack-offset

- **Tags:** `frame-size`, `stack-offset`, `inlining`
- **Best fuzzy:** 6.86813%
- **Diagnosis:** Stack frame is 0x58 in all attempts but target is 0x60 (8 bytes short). The function inlines fn_800F9260_DrMario which contributes s32 candidates[9] to the stack. The inline's candidates land at sp+0x14 (base) vs sp+0x18 (target), and the Vec3 sp44 lands at sp+0x38 (base) vs sp+0x44 (target). The 8-byte frame size difference and 4-byte offset difference for candidates cannot be resolved by adding extra local variables — all s32/bool/int locals are optimized into registers with -O4. No permuter-solvable: stack frame size is structural.
- **Tried:** (1) Vec3 sp44 + bool flag + fn_800F9260_DrMario inline call → 107 mismatches, frame=0x58, candidates@sp+0x14, Vec3@sp+0x38. (2) Added s32 candidates[9] to outer scope as well → 125 mismatches, frame grew further due to double-allocation. (3) Wrote loop directly in fn_800F9260 with s32 candidates[9]/i/count/pick in outer scope → still frame=0x58 with Vec3 at sp+0x14 from outer candidates shadowing.
- **Likely fix:** The 8-byte gap between candidates end (sp+0x3C) and Vec3 (sp+0x44) in the target suggests a hidden local variable of 8 bytes (e.g., long long, double, or PAD_STACK(8)) declared BETWEEN the candidates loop block and the Vec3. The fn_800F9260_DrMario inline as currently written may need restructuring — possibly the inline should NOT have s32 candidates[9] and instead the outer function should pass a pointer to the candidates array, or the original source has an explicit local array at a specific scope with extra padding. Alternative: the static inline fn_800F9260_DrMario might be the wrong abstraction; the original fn_800F9260 may be self-contained without using that inline, with candidates at sp+0x18 coming from a different local layout driven by the order of variable declarations in the outer function.

