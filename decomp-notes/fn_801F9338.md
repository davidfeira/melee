---
function: fn_801F9338
tu: src/melee/gr/gricemt.c
headline: permuter-queued + frame-size
tags: [permuter-queued, frame-size, literal-pool, sdata2-float, paired-siblings]
---
## fn_801F9338 (`src/melee/gr/gricemt.c`) — permuter-queued + frame-size

- **Tags:** `permuter-queued`, `frame-size`, `literal-pool`, `sdata2-float`, `paired-siblings`
- **Best fuzzy:** 11.7206%
- **Diagnosis:** Reached 99.60% (11 mismatches each on fn_801F9338, fn_801F9448, fn_801F9558 - all clones). Body decoded: if (arg2->x34_flags.b1234 == 1) { check s16* p = (s16*)((u8*)gp + 0xOFFSET); if (p[0]==0) call Ground_801C2BA4(N), set p[0]=1, p[1]=0, then 3x grAnime calls on p[2], conditionally same on p[3] if != -1; tail call grIceMt_801FA7F0. Offsets: 0x100/N=2 (fn_801F9338), 0x10E/2, 0x108/4. Remaining diffs: (1) frame size 0x50 vs 0x48 - target has 8 extra bytes of padding/local; (2) sdata2 float pool: target references grIm_804DB5B4@sda21/grIm_804DB574@sda21 directly, base loads from local @598/@424 anonymous pool entries because the const float globals get inlined in this TU. Possibly fixable by removing 'const' from grIm_804DB574/grIm_804DB5B4 declarations but that's outside per-function scope and could regress other TU consumers. Decomp-permuter offline so cannot auto-permute.

