---
function: lb_80019628
tu: src/melee/lb/lb_0192.c
headline: sdata2-float + regalloc
tags: [sdata2-float, regalloc, mwcc-aliasing]
---
## lb_80019628 (`src/melee/lb/lb_0192.c`) — sdata2-float + regalloc

- **Tags:** `sdata2-float`, `regalloc`, `mwcc-aliasing`
- **Best fuzzy:** (unknown)
- **Diagnosis:** 86.85% (53 mismatches) after 2 attempts. Three blockers: (1) sdata2-float: lb_804D7CA8/lb_804D7CA0 named constants referenced as anonymous @138/@136@sda21 in compiled output; same TU (lb_0192.c) has lb_80019AAC which correctly uses the named constants — ordering issue requires TU-level pool reconstruction. (2) srawi-sign-extend: explicit (OSTime) cast on __cvt_dbl_usll u64 return causes compiler to emit srawi r29,r3,31 + addi r28,r3,0 instead of addi r28,r4,0 + addi r29,r3,0 — fix is to remove (OSTime) explicit cast and use implicit u64->s64 conversion. (3) addi-r7 encoding: compiler generates addi r7,r31,8 + indirect stw instead of direct stw to r31+0x8/0xC for x0[0].x8=0 zeroing — same bytes different encoding, permuter territory but depends on fixing srawi first.
- **Tried:** Attempt 1: explicit __cvt_dbl_usll calls with (OSTime) cast, direct struct field zeroing, __div2i for sampling rate division. Got 86.85%. Attempt 2: tried (OSTime)(f64)(u32) implicit path without __cvt_dbl_usll, dropped to 63.65% due to fsub vs fsubs (double vs single subtract) and extra __cvt_dbl_usll calls.
- **Likely fix:** Remove (OSTime) explicit cast from both __cvt_dbl_usll calls — use 'OSTime period = __cvt_dbl_usll(...)' without explicit cast. This fixes the srawi pattern. For sdata2-float: the named constants lb_804D7CA8 and lb_804D7CA0 need to be at same pool positions as lb_80019AAC's references — may need U32_TO_F32 macro from placeholder.h or same expression form as lb_80019AAC. After those two fixes expect <15 mismatches, permuter can close regalloc.

